# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Changed
- **`task_enforcement_gate` moved to the config-file master switch AND defaulted OFF** (2026-07-25). Two changes: (1) replaced the `SENTINEL_TASK_GATE` env kill-switch with a standard `task-enforcement-gate.toml` `enabled` flag, loaded exactly like `task_decomposition_gate` and `tool_usage_gate` (operator override at `~/.claude/sentinel/config/`; corrupt/missing config fails **closed** to enforcement — the Rust default stays `true`). This removes the only gate that was toggled through a hidden env setting instead of the config-file lever every other gate uses; the env var was invisible to the config surface, so it was silently live (and uncontrolled) in benchmark arms that believed they had all gates off. (2) Flipped the **shipped default from on to off**, matching the process-gate posture (PR #32): it is a hard mutation-block in the same class and shares `tool_usage_gate`'s headless deadlock mode (the "ask the operator to create a task" unblock path doesn't exist in autonomous/headless runs). This default is **precautionary, not measured** — enforcement's cheap instructive deny may yet prove net-positive, in which case it flips back on with evidence (the enforce-vs-decompose head-to-head is the test). Opt in with `enabled = true`.

### Removed
- **`SENTINEL_TASK_GATE` env kill-switch** (2026-07-25). Superseded by the config-file `enabled` flag above. No external dependency relied on it (all references were internal). Its two env-based tests were replaced by config-file tests (operator-disable, shipped-default-enabled, corrupt-config-fails-closed).

### Added
- **`[profile.quick]` cargo profile for iterative engine rebuilds** (2026-07-10). A dev-iteration profile (`inherits = "release"`, `lto = false`, `codegen-units = 16`) so rebuilding the hook engine to test a change skips the LTO pass the `release` profile runs on every build. Use `cargo build --profile quick --bin sentinel-engine` → `target/quick/sentinel-engine.exe` → `sentinel stage --binary target/quick/sentinel-engine.exe`. Additive and CI-safe: it has its own `target/quick/` output and does not affect `cargo build --release`. It inherits `opt-level = 3` from release **deliberately** — overriding it to a lower value perturbs `cc-rs`'s `cl.exe` invocation and breaks `aws-lc-sys`'s C11 feature probe (`stdalign.h` C1083) on this MSVC toolchain. **Honest scope:** this skips LTO on warm rebuilds (a real but modest help); it does NOT make cold builds fast. A cold build still compiles ~500 dependency crates plus heavyweight workspace crates (`sentinel-infrastructure` alone pulls sqlx/redis/opentelemetry/rig-core), which on a loaded machine is a 20–40 min job — the dominant cost is the sheer amount of compilation, not the profile. sccache does not help here (Rust workspace-member crates use incremental compilation, which sccache cannot cache). The genuine lever for cold-build time would be fewer/lighter dependencies, not a profile knob.
- **Linear overlay in the task status line — active initiative issues, every turn, inside a Linear project** (2026-07-10). The `task_status_line` hook now stacks a second block under the native `📋 [Tasks]` block: the active issues of the current Linear initiative, read from the on-disk cache `~/.claude/sentinel/linear-assigned.json` (the same file `linear_pm_gate` consumes — **no live Linear API on the hot path**): The `task_status_line` hook now stacks a second block under the native `📋 [Tasks]` block: the active issues of the current Linear initiative, read from the on-disk cache `~/.claude/sentinel/linear-assigned.json` (the same file `linear_pm_gate` consumes — **no live Linear API on the hot path**):
  ```
  📌 [Linear] Firefly Pro Beta — 6 active (3 in progress)
    🔄 FPROUTE-83 routing, retire the fieldroutes ingestion… (3)
    🔄 FPCRM-607 contacts, migrate huntsville and memphis… (5)
    🗄️ FPCRM-610 billing, reconcile legacy invoices… 
    🔺 FPFIELD-132 Maps: replace public DEMO_MAP_ID…
  ```
  **"Active" is the complement of the terminal states** — everything that is *not* `completed`/`canceled` is shown (`started` 🔄, `unstarted` ⏳, `backlog` 🗄️, `triage` 🔺, and any future non-terminal category as `•`), ordered in-flight-first. Defining it as the complement means a new Linear state category surfaces rather than silently vanishing. The cache carries no priority/assignee, so — unlike the native-task lines — the overlay never fabricates a priority colour; it renders the state glyph + identifier + title (+ a `(N)` estimate hint when sized). **Gated to Linear-backed projects**: the block only appears when the cwd resolves to a project config under `~/.claude/sentinel/projects/` whose frontmatter declares Linear (`linear_workspace`/`linear_teams`/`linear_account`/`issue_prefix`) AND whose identity tokens (stem/`name`/`aliases`) match a cwd path segment — so Firefly issues never leak into an unrelated repo. The two blocks are independent (Linear can show with no native tasks, tasks can show outside any Linear project; both empty → inject nothing). Robust: absent/malformed cache → silent, never panics; title truncation char-boundary-safe (80 chars); active list capped at 8 with `…and N more active`. 10 new tests (complement-of-terminal filter incl. unknown-state, in-flight-first ordering, estimate hint + header fallback, overflow cap, char-boundary truncation, project-gating requires both Linear-declared + cwd-match, stacks-under-tasks, silent outside Linear project, silent when cache absent).
- **Task Enforcement Gate — helpful-block on off-book code edits** (2026-07-10). New `PreToolUse` gate (`task_enforcement_gate`) that DENIES `Edit`/`Write`/`NotebookEdit` when the session has no `in_progress` task, so work can't happen off-book. It is a *helpful* block, not a dead end: the `[Sentinel-Authority]`-tagged deny message tells the agent exactly how to proceed — create a task for this work (`TaskCreate`) and mark it `in_progress` (`TaskUpdate`), and if the task isn't obvious, ASK the operator whether to create+start one for them or let them. Enforce the hygiene, stay supportive. Safety first: only gates edits inside a git repo (meta-work under `~/.claude`, the scratchpad, memory is never gated); FAILS OPEN on any read error / missing task dir / no session id / non-repo (a read glitch never bricks a session); and `TaskCreate`/`TaskUpdate` are never gated, so the unblock path create→start→retry always works. Kill switch: `SENTINEL_TASK_GATE=off` (or `0`/`false`/`disable`) disables it without a rebuild. Uses `HookOutput::deny` for a real platform `permissionDecision: Deny` with the mandatory provenance prefix. 8 tests (block-when-none-active, allow-when-active, kill-switch, non-mutating tools pass, outside-repo allow, missing-dir/no-session fail-open, off-value recognition).

### Removed
- **`rig-core` dependency — replaced by an in-house ~200-line LLM HTTP client (`llm_http::ChatClient`)** (2026-07-11). `rig-core` (an LLM-orchestration framework) was used in only 4 files (`rig_judge`, `rig_classifier`, `openrouter_llm`, `llm_scorer_runtime`) for one uniform pattern — send a system preamble + user message, get a string back — yet it dragged in **~54 transitive crates** including a **second `reqwest` major (0.13)**, duplicate `h2`/`hyper` stacks, its own derive framework (`rig-derive`/`deluxe`/`schemars`), and — the real cost — the **`aws-lc-sys` C build** (a from-source BoringSSL fork, ~2 GB RAM peak, the cause of the `stdalign.h`/`LNK1114` build failures). The new `sentinel-infrastructure::llm_http::ChatClient` speaks the OpenAI-compatible `/chat/completions` API directly over the **single workspace `reqwest` (0.12, ring TLS)** — `::openrouter(key)` and `::openai_compat(base_url, key)` (Ollama local/cloud) constructors + one `complete(model, system, user, max_tokens, temperature)` method. Each call site keeps its exact `PromptFn` seam and behavior (system-optional, `max_tokens` cap, `temperature`, Ollama base-URL routing). Verified: `cargo tree` confirms `rig-core`, `aws-lc-sys`, `aws-lc-rs`, `reqwest 0.13`, and `rustls-platform-verifier` are all **gone**; `cargo check` clean; 6 new client tests + existing scorer tests pass. Also folds the former `reqwest_13_decompress` alias into the workspace `reqwest` (added the `gzip` feature — pure-Rust flate2, no C — so OpenRouter's gzipped responses still decode). TLS for LLM calls now rides the workspace `rustls-tls-native-roots` (ring) path, which already handles the Norton-injected root CA — no aws-lc-rs, no platform-verifier, no C crypto build.

### Changed
- **Release build: fat LTO → thin LTO + parallel codegen (`lto = "thin"`, `codegen-units = 16`)** (2026-07-10). The `[profile.release]` used `lto = true` (fat) with `codegen-units = 1` — a single-threaded LTO pass over the large `sentinel-cli` crate. Thin LTO keeps ~90% of fat-LTO's runtime benefit (negligible for an IO-bound hook engine) while `codegen-units = 16` restores parallel codegen, so the LTO/link stage is cheaper and multi-core. `opt-level = 3`, `strip`, and `panic = "abort"` are unchanged. **Honest scope:** this trims the link stage and is the better default, but it does NOT solve the multi-minute engine rebuild pain — measured cold builds in a fresh worktree stayed ~20 min because that cost is dominated by compiling ~500 dependency crates from scratch (each git worktree has its own empty `target/`), not by the LTO link. The real fix for iterative rebuild time is a shared `CARGO_TARGET_DIR` so worktrees reuse the main checkout's compiled deps — deferred as a separate change because a shared target dir has concurrency tradeoffs (parallel worktree builds contending on the same artifacts) that need validation.

### Fixed
- **Git interceptor blocked safe ref-pruning (`git fetch --prune`, `git remote prune`) as "repository recovery"** (2026-07-10). `evaluate_git_command` treated *any* command containing `prune` (except `worktree prune`) as recovery-affecting and blocked it. But only OBJECT-pruning forms — bare `git prune`, `git gc --prune`, `reflog … --prune` — actually delete unreachable objects; the far more common REF-pruning forms (`fetch --prune`/`-p`, `remote prune`, `remote update --prune`) drop only stale remote-tracking refs, which is lossless and recoverable by re-fetching. Blocking them was a false positive that silently let a session's remote-tracking refs drift months out of date (a local `git branch -r` showed 40 phantom branches while the real remote had 3). Now `prune` is dangerous only when it is NOT one of the safe ref-pruning forms; the block message even suggests the safe alternatives. New tests: the ref-pruning forms all `Allow`; bare `prune`, `prune --expire=now`, `gc --prune=now`, and `gc --aggressive` still `Block`.
- **`good_citizen_observer` false-fired on prose** (2026-07-10). Two over-broad patterns flagged non-issues every turn: a bare `\bdead_code\b` token matched any text quoting a `#[warn(dead_code)]` note (redundant with the real `warning: function … is never used` rustc detector, which is kept), and `\b(FAILED|FAIL)\b` matched the word "FAIL" in prose — a CHANGELOG line reading `PASS/WARN/FAIL`, a commit message, "CI gate red". Removed the bare `dead_code` token; the test-failure patterns now match the ACTUAL cargo shapes (`test result: FAILED`, `test … ... FAILED`, a line-leading `FAILED`), plus a precise `[ FAIL ]` bracketed-status pattern so a real checker-drift signal is still caught. New regression test: prose mentioning FAIL / `dead_code` records nothing; real diagnostics still record.
- **`commit_message_validator` + `account_cascade` read project configs from the wrong directory — Linear-ref enforcement and account cascade silently no-op'd for configured projects** (2026-07-10). Both hooks resolved project configs at the legacy `~/.claude/projects/`, but that path is Claude Code's **conversation-transcript store** (subdirs keyed by project hash) — it holds no config frontmatter. The canonical project configs (`firefly-pro.md`, `legatus.md`, …) live at `~/.claude/sentinel/projects/`, which is what `session_init` and the new Linear status-line overlay already read. Consequence: `commit_message_validator` found no matching config for a Firefly cwd, resolved no team prefixes, and its "commit must reference a Linear issue" rule **never fired** for the very projects it was meant to gate; `account_cascade` likewise found no account→services mapping and skipped the cascade. Both now resolve `sentinel_dir(&home)/projects` (via the existing `hooks::sentinel_dir` helper). The ~20 other readers of `~/.claude/projects/` are left untouched — they correctly read the transcript store (keyed by project hash), a different use. New regression test seeds a Firefly config in `sentinel/projects/` **and** a decoy in the legacy dir, asserting the canonical `FPCRM`/`FPFIELD` prefixes are detected and the legacy decoy is ignored (would have failed pre-fix). Found while building the Linear overlay above.
- **Task-dir resolution now honours `CLAUDE_CONFIG_DIR` and Claude Code's `session-<first8>` naming — the regression that made every task hook read the wrong (empty) directory** (2026-07-08). Two boundary drifts had silently broken the whole task-hook surface (coverage nag, mirror, the new emoji decorator + status line) for any session using a custom config dir:
  - **`claude_dir()` ignored `CLAUDE_CONFIG_DIR`.** It resolved `~/.claude` (honouring only sentinel's own `SENTINEL_CLAUDE_DIR`), but Claude Code roots **all** per-session state at `CLAUDE_CONFIG_DIR ?? ~/.claude`, and sandboxed launches set a custom one. So on such a session sentinel read `~/.claude/tasks` while the live tasks sat under `$CLAUDE_CONFIG_DIR/tasks` — it found nothing. `claude_dir()` now honours `CLAUDE_CONFIG_DIR` (empty value ignored; `SENTINEL_CLAUDE_DIR` still wins).
  - **`session_task_dir` used the wrong dir-name form.** Verified against the deobfuscated CC 2.1.201 bundle (exported `sessionTeamName`: `"session-" + sessionId.slice(0,8)`): CC **migrated** the task dir from `tasks/<full-uuid>/` to `tasks/session-<first-8-hex>/`. Sentinel still computed the legacy full-uuid path first. Resolution is now, session-scoped and cheapest-first: (1) `CLAUDE_CODE_TASK_LIST_ID` override (sanitised, matching CC's `r_t`), (2) `session-<first8>` (current form), (3) `<full-uuid>` (legacy), (4) newest-mtime `session-*` dir holding `*.json` — the fallback for a mid-session `/clear` that re-mints the underlying UUID so the id a hook holds no longer names the dir. Root now derives from `claude_dir()` (config-dir-aware), not the passed `home`.
  This is the fix that makes the every-turn task status line and the native-subject emoji decorator actually locate this session's tasks. Tests: `claude_dir` honouring + empty-ignore; resolution order (prefixed-wins-over-legacy, override, fresh-session candidate, `/clear` drift fallback).

### Added
- **Richer task decoration — priority colours, blocked markers, and checklist/fan-out hints** (2026-07-09). Building on the status-emoji work below, both the on-disk subject decorator (`task_persist`) and the every-turn status line (`task_status_line`) now compose a fuller glyph run from the task's *real* fields: **status** colour, then **priority** colour from `metadata.priority` (`🔴`P0 … `🟢`P3), then a `🚫` **blocked** marker when `blockedBy` is non-empty. Priority lives in `metadata` because the native Task schema (recovered from the decompiled Claude Code 2.1.206 source) has no priority or checklist field of its own — `priority_glyph()` accepts the `P0`..`P3` tokens, word aliases (`urgent`/`high`/`medium`/`low`), and Linear-style numeric `1`..`4`. The status line additionally appends `(done/total)` checklist progress (from `metadata.checklist`, either `"3/5"` or `{done,total}`) and a `→blocks N` fan-out hint. A new single-source `task_decoration::decorate_subject(subject, status, priority, blocked)` composes the run and **strips the input first**, which fixes a doubled-glyph bug where the status line prefixed its own glyph onto a subject `task_persist` had already decorated (`🔄 🔄 #38 …`). Idempotent and round-trip-clean for every combination. New domain fn `priority_glyph` + tests; enriched decorator + status-line tests (doubled-glyph, priority colour, blocked marker, checklist string/object, numeric priority).
- **Status emoji baked into native task subjects — Claude Code's own task panel now shows 🔄/⏳/✅** (2026-07-08). Claude Code's native task widget renders each task's `subject` text verbatim; a hook can't restyle that widget, but it *can* rewrite the subject on disk. `task_persist` now, on every task event (TaskCreated / TaskCompleted / Stop — the events where it already resolves the session's native task dir), rewrites each `~/.claude/tasks/{session}/*.json` `subject` to lead with its current status glyph: `🔄 Ship the thing`, `⏳ Plan the thing`, `✅ Done thing`. Idempotent (strips any existing decoration first, so re-runs never stack glyphs and a status change re-glyphs cleanly), non-destructive (parses as a generic JSON value and edits only `subject` — every other field round-trips untouched), atomic, and only rewrites a file whose subject actually changed (no mtime churn). Unknown statuses drop to a clean subject rather than inventing a glyph; dotfiles (`.lock` etc.) are skipped. Fulfils the standing "normalize baked-in decoration on task write" intent. The canonical stripper `strip_status_priority_prefix` was promoted from `session_init` to `sentinel_domain::task_decoration::strip_decoration` (single source of truth, shared with the CLAUDE.md Active Tasks renderer) — fixing a latent gap where the `session_init` copy omitted `🚫` (blocked) from its strip set. 6 decorator tests + 2 domain strip/round-trip tests.

### Added
- **Tasks as first-class citizens: a live TaskList status line on every turn** (2026-07-08). New `task_status_line` hook (UserPromptSubmit) injects the operator's **live native TaskList** — this session's tasks, each with its canonical emoji + id + subject — on *every* message, so in-flight work is always visible:
  ```
  📋 [Tasks] sentinel — 2 pending, 1 in progress, 8 done
    🔄 #34 Make tasks first-class: every-turn TaskList emoji status line
    ⏳ #35 Wire the next thing
  ```
  Distinct from `todo_loader` (which reads the `TodoWrite` store `active.jsonl` and fires **once** per session): this reads the durable TaskList the operator's CLAUDE.md mandates, and fires **every turn**. Session-scoped via `session_task_dir` (resolves both the `{session_id}` and `session-{group}` native-dir forms), so the output is small and relevant — no cross-session graveyard. In-progress tasks list first, then pending; completed are counted but not listed. Emoji come from the canonical `sentinel_domain::task_decoration::status_glyph` (single source of truth, shared with the CLAUDE.md Active Tasks table). Robust: silent when no active tasks, never panics, subject truncation is char-boundary-safe (80 chars), active list capped at 10 with `…and N more active`. Hook count 87 → 88. 8 tests.

### Fixed
- **Task-mirror ghosts: sentinel's `persistent-tasks` mirror no longer drifts from the session-global native TaskList** (2026-07-08). The native Claude Code task list is **session-global** (one dir per `session_id`), but sentinel mirrors it per **`{project_hash}`** (derived from cwd). A session that cd's across repos/worktrees therefore writes a separate mirror dir per cwd — and the per-write cleanup (`clear_bled_snapshots`, which blanks the session's snapshots in every *other* cwd it visited) was **skipped on the empty-list path**: `task_persist::process` truncated only the current hash and `return`ed before the cleanup. So a session that emptied its list while in a new cwd left frozen `in_progress` ghosts in every prior cwd's snapshot. With **no SessionStart reconcile/GC**, `task_rehydrate` then re-injected those ghosts into *every* future session and `session_init` re-rendered them in the CLAUDE.md **Active Tasks** table forever (observed: a single session accumulated 11 project-hashes and surfaced stale `#1` ghosts every turn). Two-part fix: **(1)** `task_persist::process` now runs `clear_bled_snapshots` on the empty-list path too, so emptying the list in a new cwd still clears the hashes left behind; **(2)** `task_rehydrate` gains the missing **SessionStart GC** — a foreign-session mirror whose `updated_at` is older than the 14-day rehydrate horizon is truncated and skipped instead of re-injected (fresh foreign snapshots still rehydrate; an unparseable timestamp is never pruned). The native store being session-keyed (not cwd-keyed) was confirmed against the deobfuscated Claude Code 2.1.201 bundle. 5 tests: empty-path-clears-bled regression, `is_stale_mirror` age discrimination, stale-mirror pruned-not-rehydrated, and fresh-mirror-still-rehydrated (over-prune guard).

### Security
- **MCP registry snapshots + healed `.claude.json` are now written owner-only** (2026-07-07, audit HIGH #2). `~/.claude/sentinel/state/mcp-registry/registry-*.json` (and a healed `~/.claude.json`) carry plaintext secrets — resolved env values like `HOOKDECK_API_KEY`/`DEPOT_TOKEN`/`GOODDATA_TOKEN`/`NYLAS_API_KEY` and the full `mcpServers` block — but were written `-rw-r--r--` (world/group-readable), ×14 daily via snapshot rotation. `write_atomic` now restricts the tmp file to owner-only **before** the rename (Unix `0600`; Windows `icacls /inheritance:r` + a single owner Full grant) — TOCTOU-free, matching `daemon_cmd`'s token-file hardening. Best-effort: a permission failure is logged and never aborts a heal/snapshot. Local-only and not git-tracked, so not a new exfil surface, but the multiplied secret material is no longer left group/world-readable. Replicated as a local `restrict_to_owner` helper because `sentinel-application` (correctly) does not depend on `sentinel-infrastructure`. Test pins `0600` on Unix.

### Changed
- **Stopped reading CC's deprecated `team_name` payload field** (2026-07-07, audit DRIFT). CC 2.1.201 marks `team_name` `@deprecated` ("single implicit team… will be removed"). Sentinel read it in `task_created`/`task_completed`/`teammate_idle`, but every read fed only decorative label strings + one write-only channel-event meta stamp no consumer reads back — nothing functional depended on it (the `teammate_idle` debounce key is `(session_id, teammate)`, unaffected). Dropped all reads + the stamp; tests now assert a stray `team_name` does **not** leak into injected context even when the upstream still sends it. `cc-boundary-contract.tsv`: removed the `team_name_present` dependency row, kept `team_name_deprecated` as the WARN tripwire for when CC actually removes the field.

### Tested
- **Pinned error-behavior for `tool_usage_gate` (fail-closed) and `git_hygiene` (fail-open)** (2026-07-07, audit follow-up). The audit's "gates fail closed but untested" premise narrowed to two gates on inspection (`provenance_validate`/`spec_challenge_gate` already cover their error paths; `db_ops_gate`/`pr_merge_gate` do no IO; `phase_gate`'s block isn't IO-gated). Added process()-level `tool_usage_gate` tests that an unreadable transcript → DENY and an unreadable/garbage task dir → DENY (previously only helper-level). For `git_hygiene`, added `FailingGit` + a test documenting that on a `GitStatusPort` error the gate degrades to **ALLOW** (`current_branch().ok()`→`None` disables the protected-branch block; uncommitted/changed_files errors disable the file limit) — contrary to a "fail-closed everywhere" reading. This is a deliberate availability trade-off (a hygiene reminder, not the commit gate — `pr_merge_gate`/`commit_message_validator` are); the test makes the choice explicit and catches any future silent flip in either direction.

### Fixed
- **`hygiene_reminders` never offers `rm -rf` through a junction** (2026-07-07).
  The Worktree Cleanup reminder flagged every deregistered directory under
  `.claude/worktrees/` as an "orphaned shell" and emitted a batch `rm -rf`
  command — but junctions placed there on purpose (path-dep links into
  sibling repos, e.g. `langgraph-python-to-rust` in sentinel and the
  `claude-accounts-cli-rust`/`vulcan-cli-sdk-rust`/`vulcan-mcp-sdk-rust`
  trio in claude-code-handler-rust) resolve as directories, so the
  suggested command would recurse *through the link into the real repo*.
  Both the Stop-phase detector and the prompt-time re-validation now skip
  links via a new `FileSystemPort::is_symlink` (defaults false for
  in-memory stubs; the real adapter treats any Windows reparse point —
  junctions included, which `FileType::is_symlink()` misses — as a link).
  The read-time guard also sanitizes state written by older engines.
  Regression test pins that a junction name never appears in the reminder.

### Removed
- **`tab_title` hook removed — superseded by the handler** (2026-07-07). The `tab_title` SessionStart/UserPromptSubmit hook (added earlier the same day) could never set the Windows Terminal tab title: `claude` mediates all hook stdio, so a hook process's `CONOUT$`/stderr writes never reach the tab's ConPTY tty — proven live by a `tab-title.log` showing `path=conout+stderr ok=true` while the tab never changed. The capability now lives in the correct place — `claude-code-handler` writes the OSC title at launch, where it owns the tab's tty (handler 1.1.58) — so the hook was dead weight. Removed the module, its `HOOK_NAMES`/`hooks.toml` registrations, and both dispatch blocks (hook count 87 → 86). The unrelated `project_scope_tracker` unused-import cleanup that rode in with the original hook commit is kept.

### Fixed
- **`good_citizen_observer` no longer re-captures its own serialized observations (self-echo loop)** (2026-07-07). The `PostToolUse` scan flags Bash output containing `FAIL`/`TODO`/warning patterns and appends a JSON `Observation` to the per-session log. But when any later command *displayed* that log — `cat`/`grep`/`Read` on the JSONL, or the `Stop` reminder echoing a stored excerpt — the output contained the serialized observation, whose `excerpt` still held the original `FAIL` text. The scan re-matched it and appended a *new* observation nesting the old one, so the finding replayed on every turn and grew unboundedly; clearing the log was futile because the next display re-seeded it (observed live: a `ts_ms` record whose excerpt was the JSON of an earlier record). The scan now skips any line that is one of our own serialized records — identified by carrying all three `Observation` keys (`"ts_ms"` + `"category"` + `"excerpt"`), a shape real build/test output never emits. A bracketed `[ FAIL ]` status line from a checker is deliberately *not* skipped: if e.g. the CC contract checker prints `[ FAIL ]` it means a real drift occurred and should still be flagged. Three regression tests: the self-echo is not re-captured, a real `FAILED` line alongside an echo is still captured (no over-suppression), and the discriminator itself.
- **`tab_title` hook now reaches the terminal in the harness context (stderr fallback) + self-diagnoses** (2026-07-07). The original `tab_title` hook wrote the OSC-0 title sequence only to `CONOUT$`, which requires an attached console — reliable when a hook is run by hand in an interactive shell, but not guaranteed when Claude Code spawns the hook with redirected stdio. Verified against the decompiled Claude Code source (v2.1.114): CC sets its own title via `process.title` from the *main* process whose stdout IS the tty (gated on `process.stdout.isTTY` + `!CLAUDE_CODE_DISABLE_TERMINAL_TITLE`) — a path a child hook with a piped stdout cannot use, and `AttachConsole` is unavailable here because the crate forbids `unsafe`. The write now targets **both** `CONOUT$` **and stderr** every call (stderr is inherited from the parent tab and reaches the tty where `CONOUT$` doesn't; the terminal ignores the duplicate title write). Each invocation appends one line to `~/.claude/sentinel/state/tab-title.log` recording which path(s) succeeded (`conout+stderr`/`conout`/`stderr`/`none`) — verified live through the real launcher hook path: `path=conout+stderr ok=true` on every UserPromptSubmit turn. Both writes are best-effort; failure never breaks the session. Non-Windows twin writes `/dev/tty` then stderr. Unit tests unchanged (write path is I/O, logic is covered).
- **`linear_pm_gate` no longer false-blocks a start when a higher-priority ticket is already in progress** (2026-07-07, audit P3). `higher_priority_available` counted `started` (In Progress) tickets as "startable" peers, so moving a lower-priority ticket to In Progress while already working a higher-priority one raised a spurious `BlockHigherPriorityAvailable` — telling the assignee to "start the most urgent work first" when that work is a ticket they are *already* on. `started` is now excluded from the startable set (only `backlog`/`unstarted`/`triage` count), matching the helper's own documented definition of startable. Regression test: a `started` higher-priority same-assignee peer must not block; a `backlog` one still must.

  *Deferred from the P3 batch (tracked, with rationale — not silent skips):* `team_name` deprecation migration (the field still works; CC has not dropped it — pre-emptive migration is speculative and the contract-diff guard will flag the actual removal); plaintext-secrets-at-rest in local snapshots (needs an architectural decision — `chmod 600` vs storing unresolved `$doppler:` refs vs encryption — and is local-only, not a new exfil surface); explicit gate error-behavior tests (the gates already behave fail-closed per the correctness-lane audit; adding tests for 6 gates is low-risk-reduction churn); `skill_telemetry` non-atomic rewrite (surfaced-not-confirmed — needs verification first).

### Changed
- **Audit P2 cleanup: single source of truth for the two hand-duplicated policies + dead CC-env branches removed** (2026-07-07). Follow-up to the 2026-07 sentinel audit, doing the consolidations properly rather than papering over them:
  - **`home_root()` deduplicated into a pure domain policy.** `sentinel-application::paths::home_root` and `sentinel-infrastructure::paths::try_home_root` were byte-identical copies of the same fail-closed `SENTINEL_HOME` resolution — a divergence would resolve state to different directories. The decision now lives once in `sentinel_domain::paths::resolve_home_root(sentinel_home, home_dir)` (pure — the caller does the env/`dirs::home_dir()` IO and passes results in, honouring domain purity); both crates are one-line wrappers. Divergence is now impossible by construction. 5 exhaustive policy tests (all 4 arms incl. empty-override fail-closed + non-Unicode) with the domain fn.
  - **Task status/priority emoji vocabulary deduplicated into a domain module.** `session_init` (render) and `task_persist` (parse) each carried their own glyph table — render/parse inverses kept in sync by hand (the code even commented "kept in sync with…"). A glyph added to one but not the other silently breaks round-tripping. Now `sentinel_domain::task_decoration` owns the canonical `DECOR_EMOJI` set + `status_glyph`/`decorated_status`/`status_from_glyph`/`priority_from_decoration`; both hooks delegate. **Latent bug fixed in passing:** the shared `DECOR_EMOJI` now includes `🚫` (blocked), which `session_init` renders but `task_persist`'s stripper was missing — so a blocked task's glyph is now correctly stripped. 4 tests incl. render/parse round-trip and a "DECOR_EMOJI covers every render glyph" drift guard.
  - **Dead CC-env branches corrected** (from the CC-contract audit): `hook_cmd.rs` read `CLAUDE_CODE_ENTRY_POINT` (CC exports `CLAUDE_CODE_ENTRYPOINT`, no underscore — the check could never succeed) and `CLAUDE_PROJECT` (CC never exports it — dead `[Project Context]` branch). Now read `CLAUDE_CODE_ENTRYPOINT` and derive the project name from `CLAUDE_PROJECT_DIR`'s basename (the path CC actually exports). Verified against the deobfuscated 2.1.201 bundle.
  - sentinel-application 1923/1923 green; domain + infrastructure paths tests green.

### Fixed
- **Sentinel audit fixes — 5 defects surfaced by verifying sentinel against Claude Code's deobfuscated 2.1.201 source** (2026-07-07). A read-only audit cross-checked every sentinel↔CC boundary contract against the deobfuscated bundle and found bugs invisible to greps and the 2,500-test suite because they were silent *drift* between two codebases:
  - **`tool_response` wire key (HIGH, security) — re-arms ~9 dark PostToolUse hooks.** CC sends PostToolUse tool output under the key `tool_response` (verified: payload + zod schema), but `HookInput`'s field was `tool_result` with no alias, so every PostToolUse hook inspecting output deserialized `None` and early-returned — silently disabling itself. Worst case: `prompt_injection_nudge` (the untrusted-tool-output scanner) never scanned. Also dark: `bug_task_gate`, `plan_organizer` (ExitPlanMode path), `pr_auto_monitor`, `good_citizen_observer`, `step_judge`, `account_cascade`, `ask_question_resync_nudge`. Fix: `#[serde(alias = "tool_response")]`. Regression test deserializes a real-CC-shaped payload and asserts the scanner fires.
  - **Heal-path `command` allowlist (CRITICAL, arbitrary-exec).** The MCP registration heal copied `command`/`args`/`env` verbatim from `marketplace.json` into `~/.claude.json`, which CC execs at session start — a conditional RCE if the marketplace clone is writable. Now `command` must be an approved launcher (`mcp-supervisor`/`mcp-router`) or a bare `<name>-mcp[.exe]` binary; shell interpreters, absolute/relative paths, and metacharacters are rejected with a warning while clean entries in the same declaration still heal. `HealOutcome` now carries the `(name, command)` pairs and the operator notice lists them, so a poisoned-but-allowlisted entry is visible not hidden behind a count.
  - **`subagent_start` typed-field read.** Read `agent_type` from the flattened `extra` map, but CC sends it as a top-level field serde routes to the typed `HookInput.agent_type` → always `"unknown"`. Mirror `subagent_stop::resolve_agent_identity` (typed-first).
  - **`good_citizen_observer` multibyte panic.** `&line[..120]` byte-sliced the excerpt and panicked when byte 120 landed mid-UTF-8 (non-ASCII tool output). Truncate to 120 chars on a char boundary.
  - **Dispatcher session-id env chain (closes the E2E-proven #9 gap).** The `hook_cmd` session fallback read only `CLAUDE_SESSION_ID`, but CC exports `CLAUDE_CODE_SESSION_ID` into hook/MCP children (verified in the bundle + proven live — the engine refused a payload carrying only that var). Now uses the same `VULCAN_SESSION_ID`→`CLAUDE_CODE_SESSION_ID`→`CLAUDE_SESSION_ID`→`SESSION_ID` precedence `channel_events` uses. Live-re-proven against the rebuilt engine.
  - Deferred (tracked): plaintext-secrets-at-rest in snapshots, `team_name` deprecation migration, gate error-behavior tests, `home_root()` dedup, emoji-vocabulary consolidation.

### Fixed
- **`[Todos]` loader: exact project matching + session/staleness split kills the 4,451-row aggregate; emoji vocabulary lands on every high-frequency hook surface** (2026-07-06). The `todo_loader` matched rows with `proj.contains(project_name)` — for a home-dir session (`project_name = "garys"`) that substring matches EVERY project path under `C:\Users\garys`, i.e. the whole 4,916-row store, producing `[Todos] Pending: 4451 | Top items: <stale rows from dead sessions>` on every session's first prompt. Now: **(1)** project matching is exact — normalized-path equality (`hooks::normalize_path`, same fold as `project_hash`) or exact-basename equality, never substring; **(2)** counts split into *this session* vs *other recent sessions* (14-day `updated_at` window, keyed on the row's `session_id`), and rows from dead sessions beyond the window are hidden (counted once as `💤 N stale hidden`); a project with only stale foreign rows injects nothing; **(3)** the line renders the shared emoji vocabulary (`📋 [Todos] project — this session: ⏳n 🔄n · recent (14d): ⏳n 🔄n · 🔴p0 🟠p1 | Top: 🔴 …`). Also swept emoji onto the other always-visible surfaces so the session banner reads as one system: 🛡️ SessionStart/MCP Guardian, 📦 Marketplace Sync, 🧩 Components, 📁 Project Init, ❌ Validation, 🚨 MCP REGISTRATION MISSING, 🕘 Last Session, 📌 Persistent Tasks (with 🔄/⏳ status icons instead of `~`/space), 🎯 Skill Router, 📊 Activity Tracker, 🧠 Memory, and zone-colored 🟢🟡🟠🔴 Context Monitor. `HookTier` gains the missing `High` (🟠) tier that `dry_run_then_commit`/`provenance_validate`/`requirements_traceability_gate` had been hand-rolling outside the envelope. And `[Last Session]` summaries now age out after 14 days — `task_rehydrate` replayed a stale `session-summary.json` verbatim forever ("ended 19d ago … 1 in progress" at every session start), because nothing rewrites the file until a later SessionEnd finds non-empty tasks for that project. 8 new/updated todo_loader tests + a summary age-out test.
- **Channel events respect sessions: producer/consumer session-id contract unified, idle spam debounced, unknown-agent completions muted, emoji vocabulary, stale-backlog file sweep** (2026-07-06). Five fixes making channel push notifications land in the right session and read at a glance: **(1)** `detect_session_id` (and `channel_event_from_webhook`) now resolve the session from the same 4-var chain the sentinel-mcp consumer uses — `VULCAN_SESSION_ID` → `CLAUDE_CODE_SESSION_ID` → `CLAUDE_SESSION_ID` → `SESSION_ID`, first *concrete* id wins — via the shared `SESSION_ID_ENV_VARS` const. The producer previously checked only `CLAUDE_SESSION_ID`/`SESSION_ID` while Claude Code actually exports `CLAUDE_CODE_SESSION_ID`, so env-resolved events were written to directories no live consumer watches (found three dirs holding 849/747/466 undelivered events, still receiving fresh writes). **(2)** `teammate_idle` channel emits are debounced per (session, teammate) with a 10-minute cooldown marker under `sentinel/state/idle-debounce/` — Claude Code fires TeammateIdle on every idle poll, pushing the same "going idle" notification several times a minute (observed 8× in 3 min); the quality-gate context injection itself is NOT debounced. **(3)** `subagent_stop` no longer emits a channel event for identity-less completions — anonymous inner helper agents (judge/classifier calls) pushed one `Agent "unknown" has finished` per call; named completions still emit. **(4)** Channel-event summaries carry a central emoji vocabulary stamped in `emit` (✅ completed, 💤 idle, 🔨 build, 🚀 deploy, 🚨 MCP failure/registration-missing, 🟡 context threshold, 📋 plan, 🔄 linear drift, 🔔 hookdeck webhooks), skipped when the producer already leads with a glyph. **(5)** New `cleanup_stale_events` sweeps event FILES older than 24h out of still-active directories at SessionStart: `cleanup_stale_sessions` removes whole dirs by mtime, so a dir with a live producer but dead/mismatched consumer (the split-brain in (1)) grew unbounded while staying mtime-fresh. The sweep never deletes directories — removing a live consumer's watched dir would sever its `notify` subscription. 9 new tests; sentinel-application suite green.
- **`project_hash` now normalizes path separators + drive-letter case before hashing — one dir, one key** (2026-07-06). Live-reproduced the exact mixed-separator divergence the storage audit's adversarial critic predicted: `C:\Users\…\sentinel` (backslash, from native/hook callers) and `C:/Users/…/sentinel` (forward-slash, from Bash/git callers) hashed to **different** project keys (`c08fea48` vs `fdf9d8fd`), scattering one project's tasks into two snapshots. `canonical_project_cwd` hashed the raw cwd string. New `normalize_path` (applied inside `canonical_project_cwd`, before the worktree-collapse and hash) folds `\`→`/` and lowercases a leading Windows drive letter (`C:`==`c:`), leaving sub-drive casing untouched (Windows is case-insensitive-but-preserving; folding the whole path could collide distinct paths on a case-sensitive mount). All spellings — backslash, forward-slash, lowercase-drive, mixed, and worktrees — now collapse to one key. Also corrected the stale `task_persist` module doc that described the key as `SHA-256(cwd)[..4]` (it omitted the canonicalization). 3 new tests (separator/drive folding, same-dir-different-spellings-same-hash, updated worktree-collapse expectation); sentinel-application green, zero new clippy.
- **Tasks are first-class in storage: session-id validators unified, cross-project task bleed killed at source, 8 ghost snapshots reclaimed** (2026-07-06). Making sentinel's task store coherent, driven by an adversarial audit of the whole scoping surface (the audit corrected the initial diagnosis — the "9 ghost snapshots" were **8 genuinely different projects** each holding a byte-identical session-global task list, not one key-scattered project). Three pieces: **(1) Session-id validator collapse.** ~12 divergent `concrete_session_id` / `session_path_component` copies across 11 hooks were replaced with delegation to the canonical `hooks::{session_path_component, concrete_input_session_id}` (which route through `sentinel_domain::SessionId::validate`). This is a **security hardening**: the three override/verification gates (`phase_gate`, `hygiene_override`, `pre_commit_verification`) previously ran the *loosest* validators — accepting `default`, path-traversal (`..`), oversized, and unsafe-char session ids that could key durable override authority — and each now rejects them and **fails safe** (no override granted / protection stays on / verification stays enforced). `task_persist`'s loose inline resolver is likewise corrected. New contract test pins the canonical validator's rejection of `unknown`/`default`/traversal/oversized/unsafe-char ids. **(2) Cross-project bleed killed at source.** The native `TaskList` is session-global, but `task_persist` tagged each snapshot with whatever project the cwd was in per-fire, so a session that `cd`s through N repos smeared its identical task list into N project snapshots (observed: one 2-task list copied into 8 distinct repos). Added a per-session projects-written ledger (`persistent-tasks/.session-projects/{session_id}.json`): after persisting the current project's snapshot, the session's stale copies in every *other* project it previously wrote are truncated (guarded by a `meta.session_id` ownership check so another session's data is never touched, and never touching the current live snapshot so there is no race). Net: a session's tasks live in exactly one project at a time and follow the session across repos. **(3) One-off reclamation.** The 8 pre-existing ghost snapshots (all owned by this session, verified) were archived to `persistent-tasks/.archive/` (recoverable, never hard-deleted) and truncated — the "Diagnose hookdeck" ghost dropped from 9 active snapshots to 1 (the current project's live copy). 6 new tests (ledger dedupe, bleed-clears-others-not-current, cross-session-safety, validator contract); sentinel-application 1890/1890 green, zero new clippy. This makes the storage layer first-class alongside the rendering/identity fixes shipped earlier the same day (truncate, normalize, status emoji, agent identity).
- **`agent_completed` channel events now carry the real agent identity instead of "unknown"** (2026-07-06). Every SubagentStop channel event rendered as `Background agent (unknown) has finished` with `agent_type`/`source_agent` = `unknown`, making a session's own agent-completion stream unreadable (you couldn't tell *which* agent finished). Root cause is the same wrong-field class as the earlier session-id / hookdeck-scoping bugs: `subagent_stop.rs` read `input.extra["agent_type"]`, but Claude Code sends `agent_type` and `agent_id` as **top-level** SubagentStop fields (confirmed against code.claude.com/docs), which serde deserializes into the *typed* `HookInput::agent_type` / `HookInput::agent_id` — and a value captured by a named struct field is never *also* placed in the `#[serde(flatten)]` `extra` map, so that read was always empty. Extracted `resolve_agent_identity()` which reads the typed fields first (empty-string treated as absent), falls back to `extra` only for resilience against a future harness that relocates them, and defaults `agent_type` to `"unknown"` with `agent_id` staying `None`. Events now render `Agent "Explore" (agent-42) has finished.` and carry both `agent_type` and `agent_id` in `meta`. The session **routing** was already correct — `channel_events::emit` keys the event dir on the payload's real `session_id` and `concrete_session_id()` rejects synthetic ids (`unknown`/`default`/empty) specifically to prevent cross-session event mixing — so this was purely lost *identity*, not misrouting (audited + already covered by tests). 5 new `resolve_agent_identity` unit tests (typed read, default-unknown, typed-beats-extra, extra-fallback, empty-as-absent); 7/7 subagent_stop tests green.
- **Task snapshot now clears when the live list empties, normalizes baked-in decoration on write, and renders a status emoji** (2026-07-06). Three defects in the task-persistence layer, all surfacing as "sentinel isn't handling tasks/emojis right": **(1, the functional core) `task_persist` had no empty→truncate path.** `process()` early-returned when the session task dir was absent *or* the live list was empty, leaving a `tasks.json` written while the list was non-empty **frozen on disk forever** once the list emptied. That stale snapshot then drove `task_coverage_check` to nag about tasks that no longer existed (the persistent phantom "#2"), rendered ghost rows in CLAUDE.md's Active Tasks table, and got re-injected by `task_rehydrate` on the next SessionStart. Added `truncate_persistent_tasks()`: on an empty/absent live list it writes `tasks.json = []`, resets `meta.json` to count 0, and clears the `tasks.md` marker block (bypassing the shrink guard, since going to zero here is intentional) — a no-op when there is no prior snapshot to clear. Only reached with a valid non-empty `session_id`, so a missing session id is still treated as a durability gap (never truncates). **(2) baked-in decoration is normalized on write.** Some tasks were stored with status/priority glyphs jammed into the subject STRING (e.g. `"🔄 🔴 1 [P0] — Fix memory-capture dual-judge gate"`) where the glyph *disagreed* with the `status` field (glyph = 🔄 in-progress, field = `pending`). `read_tasks` now runs each task through `normalize_task`: strips the leading decoration from the subject, and backfills `status`/`metadata.priority` from the glyph **only when the field is blank** — the explicit field is always authoritative. **(3) the Status column renders an emoji.** `render_tasks_section` printed the bare status word; it now renders `status_emoji(status)` (`🔄 in_progress`, `⏳ pending`, `✅ completed`, `🚫 blocked`, `❌ cancelled`), matching the codebase's existing status-glyph convention (`linear_inbound_sync::DesiredStatus::emoji`); an unknown status falls back to the bare word. 8 new tests (4 normalize + 2 truncate + 1 status_emoji + 1 strip); sentinel-application 1882/1882 green.
- **Active Tasks table now renders the SESSION's tasks, not another project's, and drops emoji baked into subjects** (2026-07-05). Two bugs in the CLAUDE.md task-table regeneration: (B, correctness) `regenerate_global_claude_md` keyed the per-project task snapshot off the hook PROCESS's `std::env::current_dir()`, which is not reliably the session's working directory — so a task mutation from one session could render a *different* project's tasks into the single global `~/.claude/CLAUDE.md` (cross-session task bleed; observed a "Install HoloDesktop CLI" task surfacing as #2 in an unrelated session). Added `regenerate_global_claude_md_for(cwd)` and made the `task_created` / `task_completed` hooks pass the SESSION's `input.cwd` so each session's table reflects its own tasks (mirrors the project_scope_tracker per-session-cwd approach; the no-arg form is retained for the CLI regeneration path). (A, cosmetic) task subjects sometimes carry status/priority decoration baked into the subject STRING by the caller (e.g. `"🔄 🔴 1 [P0] — Fix…"`), redundant with the Status/Priority columns; the renderer now strips a leading run of status/priority emoji (🔄 ⏳ ✅ ❌ 🔴 🟠 🟡 🟢), a `[P0]`..`[P3]` token, a bare numeric rank, and a `—`/`-`/`:` separator via `strip_status_priority_prefix` (idempotent; a clean subject and a mid-string emoji are untouched). Test covers both bugs.

### Added
- **`tab_title` hook — label each terminal tab with `claude <session-id>`** (2026-07-07). Windows Terminal (and any VT/OSC-capable terminal) shows nothing useful in the tab when running under the handler, because the handler sets `CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1` to suppress Claude Code's own activity-summary title — leaving tabs indistinguishable ("PowerShell") across 6+ concurrent sessions. New side-effect-only hook writes the OSC-0 sequence `ESC ] 0 ; claude <session-id> BEL` **directly to `CONOUT$`** (Windows) / `/dev/tty` (unix), bypassing the hook stdout that Claude Code captures for context injection — so the bytes actually reach the terminal and move the tab title, while the hook returns a no-op `HookOutput` (nothing injected into the model). The session id is resolved from the hook payload's `session_id`, falling back to the `CLAUDE_CODE_SESSION_ID` env var Claude Code exports; placeholder ids (`unknown`/`none`/empty) are rejected, and control bytes in the id are stripped so a crafted id can't smuggle an early terminator. **Why a hook, not the handler:** the handler owns the spawn site but only knows the UUID for a `--resume` — a *fresh* session's UUID is generated by Claude Code after start, and `SessionStart` is the first lifecycle point where it's known for both cases. Wired on `SessionStart` (paints ASAP) **and** `UserPromptSubmit` (re-asserts every turn so the title snaps back after the PowerShell prompt or a launched program overwrites it). Opt out with `SENTINEL_NO_TAB_TITLE=1`. Registered in `HOOK_NAMES` + `hooks.toml` (both events); 9 unit tests (id resolution/precedence/fallback, placeholder + opt-out rejection, OSC framing, control-byte sanitization).
- **`project_scope_tracker` PostToolUse hook — publish the session's live working project for hookdeck channel scoping** (2026-07-05): sessions are launched from the home dir (`c` from `~`) and the working project is chosen — and changed — *during* the session, so the hookdeck MCP child (spawned once at session start with a fixed environment) cannot know which repo the session is working on. It fell back to the launch-cwd basename (`garys`), which matches no webhook, so GitHub events were dropped and sibling home sessions bled into each other. The new hook resolves, on every Bash/Edit/Write/Read, the git-repo-root of whatever the tool just touched and writes the repo name to `~/.vulcan/hookdeck/session-<key>.project` (keyed on the `CLAUDE_CONFIG_DIR` basename — the same session key vulcan-hookdeck's `resolve_session_key` uses, so producer and consumer agree with no handler involvement). Stickiness is *latest wins* — scope follows the most recent repo touched and re-scopes on a switch; a non-repo path (the home dir) clears the file so the session is unscoped and the router fails open (receives everything) until real work starts. The `vulcan-hookdeck` `resolve_cwd_project` (SDK, shipped alongside) reads this file first. Net: each session automatically receives the webhooks for the repo it's actually working in — no cross-session bleed, no silent drops. Observational (always allows; a write failure never blocks a turn). 6 tests; registered in `HOOK_NAMES` + the PostToolUse dispatch.
- **MCP registration guardian — detect / snapshot / heal for `~/.claude.json`** (2026-07-04): the `mcpServers` block has been lost before (Jun 17-18 corruption/rebuild) and the loss was invisible because every failure mode was coerced to a `0` count. Sentinel now owns the full control loop (marketplace stays declarative-only; the handler is zero-touch). (1) **Detect** — `scanner::mcp_registry_state()` classifies the live registry as `Missing` / `Unreadable` / `Tampered` (a `_mcpServers_disabled` stash marker or non-object block) / `Count(n)`; `count_mcp_servers()` is now a display-only wrapper over it. (2) **Tripwire** — `session_init` prints a loud `[MCP REGISTRATION MISSING]` startup-banner line and emits an `mcp_registration_missing` channel event when the state is compromised or `Count(0)` while `*-mcp-rust` repos exist on disk. (3) **Snapshot** — a healthy registry (>= 10 entries) gets a dated known-good copy under `~/.claude/sentinel/state/mcp-registry/registry-YYYYMMDD.json` (one/day, newest 14 kept). (4) **Heal** — on `Missing`/`Tampered`/empty-with-repos, the new `mcp_guardian` module rebuilds the block from the marketplace clone's `marketplace.json` `mcp[]` declaration (REGISTRY CONTRACT v1: `name`/`command`/`args`/`transport`/`env`, with `$doppler:<project>/<config>/<SECRET>` env refs resolved at heal time via `doppler-rs`→`doppler`, unresolved refs omitted-with-warning — degraded beats absent), skipping names tombstoned by the declaration's `retired` array plus the built-in `agents`/`skills` tombstones, falling back to the newest state snapshot when the declaration is absent/invalid; the merge preserves every other key (filesystem-atomic tmp+rename; an fs2 sidecar lock serializes sentinel-vs-sentinel heals only — Claude Code, the file's other writer, does not take that lock, so a read→write TOCTOU window vs the host remains and resolves last-writer-wins, accepted because heals fire only on `Missing`/`Tampered`/`Count(0)` states where the host isn't actively maintaining the block) and lands in both the global `~/.claude.json` and `$CLAUDE_CONFIG_DIR/.claude.json`; the standing `/reload-plugins` initialUserMessage autoheal then reconnects the restored servers in-session. `Unreadable` is deliberately alert-only (merging over unparseable JSON would clobber unknown state). See `docs/mcp-registration-guardian.md`. 15 new guardian tests + registry-state/banner tests; sentinel-application 1757/1757 green.

### Changed
- **CLAUDE.md template: fix stale sentinel dev-workflow path** (2026-06-15): the generated CLAUDE.md "Dev workflow" block in `generate_claude_md` (`session_init.rs`) pointed at `~/repos/claude-systems/sentinel`, a path that does not exist on any current machine — the sentinel source lives at `~/Documents/GitHub/sentinel`. The stale path shipped into every regenerated CLAUDE.md on both the Mac and Windows installs. Now corrected to `~/Documents/GitHub/sentinel` so the `cd … && cargo build --release -p sentinel && sentinel stage` instructions actually resolve. Surfaced while reconciling the Mac install after the marketplace/sentinel migration to the `legatus-ai` org.
- **Domain ports now use bespoke error enums — `anyhow` dropped from `sentinel-domain`** (2026-06-13): the DDD/hexagonal audit scored sentinel 94/A with the one ding being `anyhow::Result` on the domain port traits (an opaque "any error" contract leaking into the domain). All 22 `anyhow::Result` signatures across the 6 IO-bearing ports (`FileSystemPort`, `GitStatusPort`, `ProcessPort`, `LlmPort`, `VectorStorePort`, `MemoryMcpPort`) now return purpose-built domain errors defined in the new `port_errors` module (`FileSystemError`, `GitError`, …) — manual `Display` + `std::error::Error` + `From<io::Error>`, no `thiserror`, generated by a small `port_error!` macro mirroring the existing `AuditorError`. Infra adapters map their internal errors via `<Error>::backend(e)`; application callers using `?` are unaffected (`anyhow::Error: From<E: Error>` still absorbs them). **`anyhow` is no longer a dependency of `sentinel-domain`** — the domain crate carries zero error-framework coupling. 573 domain + 1568 application tests green, clippy clean (no new warnings). Takes the audit to ~100/A+.
- **`memory_inject` reranks the per-turn injected atoms** (2026-06-12). The hook now passes `rerank: true` to `memory_search`, so the memories surfaced into context on every `UserPromptSubmit` are ordered by the LLM reranker (better semantic ordering than raw embedding score) rather than noop. Safe in the hot path: the reranker degrades to base embedding order on any failure, and the call is already wrapped in the dedicated 10s budget that was sized with rerank in mind, so a slow/failed rerank never blocks the turn — it falls back to embedding order. One-line args insert; `StubMemoryMcp` ignores args so existing inject tests are unaffected (19/19 pass).
- **`memory_inject` now applies a relevance floor — no more low-score noise** (2026-06-12). The hook requested `top_k=8` from `memory_search` and injected all 8 verbatim with no quality bar, so a vague or conversational prompt dumped a tail of 0.03–0.06 atoms that "matched" only because something had to fill the k slots — clutter the model scrolls past, and noise in the feedback loop. Now hits below a relevance floor (default **0.10**, tunable at runtime via `MEMORY_INJECT_MIN_SCORE` — `0` restores inject-everything) are dropped before rendering; if nothing clears the floor, nothing is injected (a weak recall is the same signal as no recall). Sharp targeted queries (0.5–0.8 hits) are unaffected. Pure `apply_relevance_floor` helper + `min_inject_score` env resolver, 4 new unit tests.

### Added
- **`tool_usage_gate` is now operator-configurable — `sequential_thinking_check` knob (Check 1 only).** New shipped config `config/tool-usage-gate-defaults.toml` (compiled in via `include_str!`) with operator override at `~/.claude/sentinel/config/tool-usage-gate.toml`, following the exact `memory_provision` config pattern (wholesale replacement; parse failure fails closed to enforcement). Setting `sequential_thinking_check = false` skips ONLY the sequential-thinking transcript precondition; the task-list, plan-mode, and `in_progress`-task checks are unaffected. No env vars and no signature changes — config is read through the `FileSystemPort` inside `evaluate()`. Three new tests: override skips Check 1 when the other checks pass; override does NOT bypass the task checks; corrupt override fails closed.
- **`memory_provision` hook — register the memory MCP + broker the Qdrant credential via Doppler at SessionStart.** Fills the provisioning gap for the existing memory hooks (`memory_inject`/`memory_turn_capture`/…), which silently no-op without the `memory` MCP registered and a Qdrant credential present. The hook: (1) verifies the `memory`/`memory-mcp` binaries (warns with install guidance — never installs); (2) verifies `~/.qdrant/config.json` and, when missing/invalid/expired (JWT `exp` checked), fetches `QDRANT_URL`+`QDRANT_API_KEY` from Doppler (`legatus/dev` by default; configurable) and writes it atomically (+`.bak`) — no Doppler access surfaces a clear console error; (3) idempotently registers the `memory` MCP server in `~/.claude.json`. `require_remote` (default true): memory does not activate without a working remote credential (registration withheld). Distributes a revocable Doppler token instead of a raw shared Qdrant JWT; interim until per-user JWT minting. Config: `config/memory-provision-defaults.toml`.
- **Telemetry R2 lake pipeline + read-side report (LEG-258/259/260/261)** (2026-06-22). `sentinel telemetry collect|ship|report`: `collect` spools the per-harness hook-invocation ledgers (zstd NDJSON, `(dev,inode)+offset` checkpointed); `ship` drains them to the Cloudflare R2 lake (bearer ingest Worker, `R2_*` SigV4 fallback, idempotent content-hash keys); `report` reads the lake back and aggregates fleet activity, with **last updated** and **unique clients reporting in** as the headline metrics. Adds a backward-compatible `client_id` ledger field (additive `serde(default)` like `source_harness`; resolved `SENTINEL_CLIENT_ID` env -> `paths::try_claude_dir()/sentinel/client-id` -> hashed `/etc/machine-id`) so per-machine clients are countable; rows predating it use a labeled session estimate. New `telemetry::{ledger,spool,checkpoint,ship,sigv4,snapshot,lake,report}` — `lake` is the only IO (`ListObjectsV2` + `GET`), `report`'s aggregate/render are IO-free for a future read-side service. `--window 24h|7d|30d --harness --format html|json|table` (HTML default). Validated end-to-end against the live dev lake (two distinct client ids -> unique clients = 2 read back from R2).
- **Subscription-first provider ladder for BOTH high-stakes dual judges** (2026-06-13): the prod blast-radius **auditor** *and* the false-done **completion judge** now prefer **subscription-backed CLIs** over metered OpenRouter, per leg — Anthropic via `claude -p --append-system-prompt` (Claude Code subscription), OpenAI via `codex exec --output-last-message` (Codex subscription) — falling back to OpenRouter (`opus-4.8` / `gpt-5.5-pro`) only when the CLI isn't on `PATH`. New `resolve_cli` (cached PATH probe) + `build_claude_cli_prompt_fn` / `build_codex_cli_prompt_fn` (return `Some` only when the binary exists — **detect-and-use, never auto-installs**). The **auditor** routes per vendor via `RigAuditor::resolve_leg`; the **completion judge** routes via `resolve_judge_transport` in `rig_judge.rs`, but only for the `DualFrontier` frontier pair (`anthropic/claude-opus-*` → claude-cli, `openai/*` → codex-cli) — Sonnet/Kimi stay on OpenRouter (model identity matters; a bare `claude -p` isn't guaranteed to be Sonnet, Kimi has no CLI). Opt out with `SENTINEL_AUDITOR_NO_SUBSCRIPTION=1`. **Zero regression** — no subscription CLI present → exactly today's OpenRouter dual. Test isolation: `with_prompt_fn` pins `force_configured_prompt_fn=true` so stub tests never shell out to a real `claude`/`codex`. 43/43 auditor + 36/36 scorer-runtime + 7/7 rig_judge + 31/31 judge_service green, clippy clean, both legs live-verified returning clean JSON verdicts. **Cost:** $0 per-token when the subscription CLIs are present vs OpenRouter's $15/$75 + $30/$180. The `codex exec` leg feeds its prompt on **stdin** (codex ignores a positional prompt once `--output-schema` is set) and the completion-judge leg passes `--output-schema` with the `JudgeVerdict` shape, so the OpenAI judge returns **guaranteed-valid JSON** (no parse-failure path). Captured gotcha: OpenAI structured-output requires `required` to list *every* property (optional ⇒ nullable type + still required) — a guard test enforces it.
- **Native loop/cron/goal/Workflow guidance + orchestration nudges** (2026-06-12): aligned Sentinel with how Claude Code's four native async primitives actually compose (verified against code.claude.com/docs). (1) Removed the redundant custom `~/.claude/skills/loop` skill — native `/loop` is a superset (`/loop 5m <prompt>` fixed interval, `/loop <prompt>` self-paced, bare `/loop` → maintenance prompt or `loop.md`, `/loop 20m /cmd`) and the custom one only shadowed it. (2) Rewrote the `session_init` CLAUDE.md template: a four-primitive table (`/loop` poll · `CronCreate`/`/schedule` time-trigger · `/goal` work-till-condition · `Workflow` background fan-out) and **THE composition rule** mirroring Anthropic's own native fix (changelog v2.1.141/147) — don't `/loop` or short-cron a Workflow/background task that already notifies on completion; prefer the notification, Channels, or Monitor. (3) Extended the `orchestration_nudge` hook with two advisory signals: `workflow_orchestration_signal` (large fan-out → suggest `Workflow` over `/loop`-driven subagents) and `redundant_poll_signal` (about to poll a self-notifying task → suggest the notification/Channels/Monitor/`/goal`), both requiring strong evidence so legitimate external-service polling doesn't trip. 10/10 hook tests, clippy clean.
- **Cross-vendor dual-frontier judge (Opus 4.8 + GPT-5.5)** (2026-06-12): a new `JudgeTrustTier::DualFrontier` runs the two strongest closed frontier models, one per vendor, so an Anthropic blind spot is caught by OpenAI and vice versa. Wired into the two highest-stakes judgments: (1) the **prod blast-radius auditor** — `AuditorPort::score_dual` (default delegates to `score`, overridden by the OpenRouter `RigAuditor`) scores Irreversible/Catastrophic actions with both models and reconciles conservatively (block if either dissents; an inconclusive single-model pass is downgraded to block — a wrong "safe" = data loss); and (2) the **completion / false-done verdict** (opt-in via `dual: true` on `submit_phase_complete`) — `proof_engine` folds `evaluate_multi(DualFrontier)` into one verdict (`sufficient` = AND across judges, confidence = floor), leaving the proof-chain logic byte-for-byte unchanged. Both fail safe: two frontier models must agree to pass.
- **Firefly QA workflow redesign live in Linear** (2026-06-12): applied Pedro's proposal across FPCRM/FPFIELD/FPROUTE — new states Decomposition, QA Blocked, Backend Verify (No UI); renamed QA Testing → QA Testing (UI); added a Surface label group (UI / No-UI). Additive + reversible. Sentinel's PM gate/audit now treat any "…Blocked" state (incl. QA Blocked) as a do-not-start signal.
- **PM gate pickup rules: blocked, milestone, cherry-pick** (2026-06-12): the `linear_pm_gate` PreToolUse hook now refuses to start a ticket that is (a) **blocked** — by an open blocked-by relation, a Blocked state, or a blocked/blocker label; (b) **milestone-less** when its project uses milestones (untracked work; projects without milestones are exempt); or (c) a **cherry-pick** — a lower-priority ticket while a higher-priority, startable ticket waits in the same person's queue. Each fails open on anything it can't resolve. The `linear-audit` report + cron gained matching `blocked_open` and `no_milestone_open` flags.
- **Real-time PM gate** (2026-06-12): the gate now resolves the ticket being started via a live single-issue Linear fetch at pickup (new `LinearLookupPort` + a blocking-reqwest `LinearLookup` adapter, 2.5s timeout, `SENTINEL_LINEAR_TOKEN`), so its decision reflects this-instant state — falling back to the on-disk cache when the live port is absent or the call fails. Real-time when it can be, never bricked when it can't.
- **Auto-severity dual-judge** (2026-06-12): `sentinel severity scan [--apply]` judges each ticket's priority (1-4) from title+description using BOTH Opus 4.8 and GPT-5.5 via OpenRouter, reconciling (more-urgent wins). Shadow by default; `--apply` SETs priority on untriaged tickets and records suggestions (never auto-overrides a human). CLI + `sentinel__severity_scan` MCP tool (shadow-only).

### Changed
- **Judge/LLM stack bumped Opus 4.7 → 4.8** (2026-06-12): the live model routing across judge.rs, openrouter_llm.rs, dry_run_auditor (`DEFAULT_OPENROUTER_MODEL`), eval_scorer, and the capability routers now points at `anthropic/claude-opus-4.8` (verified OpenRouter slug). Historical token-log/pricing test fixtures left as-is.

### Fixed
- **`task_coverage_check` reminders debounced — no more identical warning on every Stop** (2026-07-02). The off-book warning (uncommitted changes, no `in_progress` task) re-fired verbatim on every single Stop while the condition persisted (observed: 8 consecutive turns), and the stale nudge fired on every stop once a task crossed the 3-stop threshold (`count >= 3`). Both are now fire-then-periodic: the off-book warning fires on the first off-book Stop, re-fires every 5th consecutive Stop while the condition persists (new `coverage-offbook-{session_id}` state marker, same fail-visible read/write contract as the existing coverage markers), and resets the moment the condition clears so a NEW episode warns immediately; the stale nudge fires at exact multiples of the threshold (stops 3, 6, 9, ...) instead of every stop past it. Reminder-only contract unchanged. 3 new/extended tests (debounce window, episode reset, periodic stale).
- **`memory_provision` hardening — credential re-validation, `require_remote` on re-runs, 0600 perms, Doppler timeout, atomic-write fixes** (2026-07-01). Seven review findings closed in the SessionStart provisioning hook: (1) a credential fetched from Doppler is now re-assessed (JWT `exp` etc.) before being written — an expired/invalid secret handed back by Doppler is refused with a distinct error instead of minting a `credential_ok` marker for a dead credential; (2) the `require_remote` withhold now also **removes** a previously registered `memory` MCP entry from `~/.claude.json` (same backup + atomic-write + no-clobber machinery), so the guarantee holds on re-runs, not just first registration; (3) credential material lands owner-only — `~/.qdrant/config.json`, its `.bak`, the transient temp sibling, and `~/.claude.json.bak` are written 0600 on unix via a new opt-in `FileSystemPort::write_private` (created with `OpenOptionsExt::mode(0o600)`, no world-readable window); (4) the `doppler secrets get` network call is bounded by a new `ProcessPort::run_with_timeout` (spawn + `try_wait` poll + kill, `ProcessError::Timeout`; default 10s, configurable via `doppler_timeout_secs` in `memory-provision-defaults.toml`) — timeout is treated as a fetch failure (withhold path), and the hook's `hooks.toml` entry is corrected to `has_api_call = true`; (5) the `~/.claude.json` atomic write uses a per-process-unique temp name (`.tmp.<pid>`) and a failed rename now cleans up the temp and propagates loudly instead of silently degrading to a non-atomic write (last-writer-wins across processes, documented); (6) registration additionally requires the engine binaries to be present (a PATH-resolved command that doesn't exist would give Claude Code a permanently failing MCP server), and `find_binary` now also probes `$PATH` directories; (7) `b64url_decode` accepts (strips) trailing `=` padding, so padded JWTs are expiry-assessable instead of Valid-forever. Orchestration test coverage added on the existing stub pattern: fetch success/failure/expired-token/timeout, end-to-end withhold-with-removal, register-skipped-when-binaries-absent, rename-failure propagation (memory_provision 16 → 29 tests). **Round 2 (adversarial review of PR #17, same day):** (a) the withhold path only removes an entry that is *recognizably sentinel-managed* (new pure `is_sentinel_managed_entry`: strict shape match against exactly what `merge_memory_server` writes — a user-owned custom `memory` entry is left untouched with a warn, `RemoveOutcome::ForeignEntry`); (b) non-private atomic rewrites of `~/.claude.json` preserve the destination's existing mode across the temp+rename (a user-tightened 0600 file is no longer silently loosened to umask 0644); (c) `write_private` tightens an EXISTING file to 0600 *before* truncating/writing the new secret into it (`mode()` only applies at creation — chmod-after-write left the fresh content world-readable during the write); (d) the binaries gate checks the EXACT launcher that will be registered (`memory-mcp-mirror` when a mirror is configured) and `find_binary` requires a regular file with an execute bit on unix (a plain data file no longer counts as installed); (e) on unix the timed-out child is started as its own process group (`CommandExt::process_group(0)`, safe — no `unsafe`/libc) and the deadline SIGKILLs the whole group, with drain threads detached instead of joined so an escaped descendant can't block past the deadline; the timeout message reports the exact configured `Duration` (no `as_secs` truncation of sub-second values) and the timing tests use CI-load-tolerant bounds (memory_provision 29 → 35 tests, process 3 → 4).
- **Test isolation: parallel-safe HOME/env mutation in MCP + decision-graph tests** (2026-06-22): the `mcp_handler` step-tools tests (20) and `decision_graph_store` config tests mutate process-global env (`HOME`/`USERPROFILE`, `SENTINEL_DECISION_GRAPH_*`) under a `Mutex` for isolation. Two parallel-execution defects: (a) on Windows `try_claude_dir()` fell through to `dirs::home_dir()`, which reads a CACHED `USERPROFILE` (SHGetKnownFolderPath) and does not honor a runtime-mutated value — so a temp-HOME test's graph write and its read could resolve to different dirs (`NotFound` flake); (b) when any such test panicked it POISONED the shared lock, cascading `PoisonError` to every other env-mutating test (1 real failure → 18+ reported). Fixed: `HomeGuard` now also pins `SENTINEL_CLAUDE_DIR` (read fresh+uncached by `try_claude_dir`, bypassing the `dirs` cache), and both env locks acquire poison-resiliently (`unwrap_or_else(|e| e.into_inner())`). sentinel-application now 1739/0 under default parallel `cargo test` (was 18 failing).
- **Gate hardening: never brick a session over unconfirmable state** (2026-06-19, builds on f27e7c0f): two PreToolUse gates could deadlock a session by demanding state it could not produce. (1) `task_decomposition_gate` now FAILS OPEN when the resolved session task dir is MISSING (`has_live_task_list` returns `None`, and `process()` blocks only on a present-but-empty `Some(false)`): the canonical `session_task_dir` resolver already tries both harness naming forms, so a still-missing dir is a store/gate desync we don't own — failing closed on it bricked every mutating tool with no fix path. The teeth (an agent skipping decomposition while the store is healthy) are preserved by the present-but-empty block. (2) `tool_usage_gate` only HARD-BLOCKS on missing sequential-thinking when a `sequential-thinking` MCP server is actually registered in `~/.claude.json` (new `sequential_thinking_mcp_registered()`, checks top-level + `projects.*` `mcpServers`); when it's absent the tool can't be called, so the gate warns and allows instead of demanding an uncallable tool. Decomp gate 27/27 + tool-usage gate tests green (incl. new fail-open + missing-dir cases).
- **`sentinel scan` MCP-server count is now sourced from the repo, not the live `~/.claude.json`** (2026-06-13): follow-on to the cwd-scoping fix below. `count_components` always read the MCP count from `~/.claude.json`'s `mcpServers`, so running `scan --sync-counts` inside a marketplace clone (which has no `~/.claude.json`) reported **0 MCP servers** and would have written `"0 MCP servers"` into the repo's own `marketplace.json`/README. Now `count_declared_mcp_servers()` reads the marketplace's *own* `marketplace.json` `mcp[]` array when present (the authoritative declaration of what the marketplace ships), falling back to the live `~/.claude.json` only when no marketplace declaration exists (e.g. scanning `~/.claude` directly). A clone now reports its declared count; `~/.claude` is unchanged. New `count_declared_mcp_servers()` + unit test covering the Some/None/no-mcp-key cases.
- **`sentinel scan` now operates on the repo it is invoked in, not always `~/.claude`** (2026-06-13): `scan --sync-counts` (and the other scan modes) resolved their root to `~/.claude/` whenever `--dir` was omitted, regardless of the working directory. Run from a marketplace *clone* it still counted `~/.claude/skills` (stale live state) and offered to rewrite ~49 files under `~/.claude` — plugin caches, the plugin-marketplace clone, and **other sessions'** `session-env/*/CLAUDE.md` — i.e. wrong blast radius and a stale count source (live 77 vs repo 79). The default now auto-detects the marketplace root by walking up from the cwd for a marker (`.claude-plugin/marketplace.json`, or a root `marketplace.json` beside a `skills/` dir), falling back to `~/.claude/` only when no marker is found. Running inside a repo clone now syncs *that* repo; bare invocation elsewhere is unchanged. The `session_init` hook is unaffected (it counts via `scanner::count_subdirs`, not `scan_cmd`). New `detect_marketplace_root()` helper in `scan_cmd.rs`.
- **Subscription codex leg was broken on Windows (os error 193)** (2026-06-13): `which_on_path` tried the extensionless name first, so on Windows it resolved npm's bare POSIX `#!/bin/sh` shim (installed next to the real `.cmd`) — which `CreateProcess` can't run (*"%1 is not a valid Win32 application"*). The codex subscription leg silently errored → fell back to OpenRouter, defeating the subscription-first win on this platform. Now tries only executable extensions (`.exe`/`.cmd`/`.bat`) on Windows and drops the bare candidate. Caught by a new opt-in live integration test (`tests/live_codex_schema.rs`, `#[ignore]`) that actually shells `codex exec --output-schema` and asserts a valid `JudgeVerdict` — the unit tests couldn't (they don't spawn the real CLI). Plus a Windows-only `which_on_path` regression unit test. 37/37 scorer-runtime green.
- **Dual auditor was secretly same-vendor — now a genuine cross-vendor pin** (2026-06-12): the prod blast-radius `score_dual` derived its primary leg from `self.model_id` (the capability router's single pick), but that router *excludes the acting vendor*, so it resolved to OpenAI — same vendor as the GPT-5.5-pro second leg. Live audits showed `dual:gpt-5.5 + openai/gpt-5.5-pro` (OpenAI judging itself), so the "cross-vendor" guarantee was hollow. Both legs are now pinned explicitly: `DUAL_PRIMARY_MODEL = anthropic/claude-opus-4.8`, `DUAL_SECOND_MODEL = openai/gpt-5.5-pro` (override via `SENTINEL_AUDITOR_MODEL` / `_MODEL_2`). Fable 5 was the intended Anthropic leg but is **suspended** — every OpenRouter provider 404s `anthropic/claude-fable-5` with *"Please use Opus 4.8"*, which would have made the auditor permanently inconclusive-block; verified callable by live chat-completion probe, not catalog presence. New `pinned_dual_pair_is_genuinely_cross_vendor` regression test; 40/40 `dry_run_auditor` tests pass.
- **OpenRouter scorer/auditor output tokens now bounded — fixes a latent gpt-5.5-pro 400** (2026-06-12): found while live-probing the freshly-pinned cross-vendor pair — `gpt-5.5-pro` 400s on `max_output_tokens < 16` ("integer below minimum value"). Two gaps closed: (1) the cross-vendor auditor's OpenRouter `prompt_fn` sent **no** `max_tokens`, so the reasoning-model leg ran unbounded (the stall the `openrouter_llm` path already warned about) — now capped at `OPENROUTER_SCORER_MAX_TOKENS = 2048` (verdict JSON + reasoning headroom); (2) `openrouter_llm` floored `max_tokens` at `1` — raised to `16` (OpenAI's documented minimum). Without these, a small/zero budget on the OpenAI leg would error every call and trip `block_for_inconclusive` — the same silent permanent-block the Fable-5 fix removed, via a different cause. Infra tests green (auditor 40, openrouter 10, scorer-runtime 32), clippy clean.
- **Task Decomposition Gate now ALWAYS fires** (2026-06-12): the gate failed *open* when task state was unreadable (no session id / home dir / read error), which let mutating work skip it intermittently. It now fails **closed** — the only allow path is a positively-confirmed live decomposed task list; both "readable, no decomposition" and "unreadable" block. The Read/Task*/Skill allowlist still passes so the fix path is never blocked.

### Added (earlier this session)
- **Linear PM-enforcement engine** (2026-06-12): codified the by-hand Linear PM analyses (proven against the Firefly Pro Beta initiative) into permanent, enforced infrastructure. `sentinel linear-audit scan [--velocity --weeks]` runs five checks — estimate hygiene (missing / non-Fibonacci), oversized-and-open (≥8pt decomposition candidates), QA-failed risk (points at risk), velocity burndown (on-track vs behind), and estimate-vs-actual cycle-time calibration (low-confidence flag when timestamps are batch-stamped). Surfaces as the CLI subcommand, a `sentinel__linear_pm_audit` MCP tool, a `linear_pm_gate` PreToolUse hook that **hard-blocks** starting a known undecomposed ≥8pt ticket (fails open on anything unresolved offline), and a `factory:linear-pm-audit` weekly cron. 9 unit tests; verified end-to-end against the live 40-issue initiative.
- **Linear dev-scorecard, code-audit, and health-score** (2026-06-12): three more report analyses turned into Sentinel features. `dev-scorecard` computes per-dev git-truth throughput + first-pass-QA rate + a composite 1-100 score, with an **attribution-divergence detector** that flags when git-authorship and Linear assignee-completions diverge (the merge-reassign regression that hid real developers). `linear-code-audit` is a false-done detector — Completed tickets with no code evidence (commits/files) get flagged. `linear-health` computes a composite 0-100 workspace-health score across hygiene / structure / data-quality / flow (scores the live Beta initiative 75/100, matching the by-hand 72/100). Each ships a CLI subcommand + MCP tool. 19 unit tests; clippy clean.
- **Per-model token cost with cached-vs-uncached split** (2026-06-12): `sentinel token-cost scan` prices the SEN-7 token aggregate at each model's published API rate (Opus 4.x / Sonnet 4.x / Haiku 4.x) and reports the cost WITH prompt caching vs WITHOUT (cache tokens re-priced as full input) plus the cache savings ($ and %). On the live tracked-ticket data: $4,061 cached vs $29,488 uncached = 86% saved. Unknown models fall to the Opus rate (never under-reports) and are counted separately. CLI + `sentinel__token_cost` MCP tool; 6 unit tests; clippy clean. (Complements `roi` — that's Claude-$-vs-human-$; this is metered-cost + cache efficiency.)
- **Four Linear PM tools exposed via the in-session MCP server** (2026-06-12): `sentinel-mcp-rust` now surfaces `linear_pm_audit`, `dev_scorecard`, `linear_code_audit`, and `linear_health` as MCP tools (calling the `sentinel-application` scan fns directly), so the analyses are callable from any Claude session, not just the CLI.

### Fixed
- **Linear enforcer no longer spams duplicate comments** (2026-06-11): `post_comment` (the single choke point for every enforcer comment — dev-ready, workflow-discipline, SLA, due-date) is now **idempotent** — it fetches the ticket's recent comments and skips posting when an identical body already exists. Previously the enforcer re-posted the same comment on every repeat `IssueChanged` event AND on every daemon restart (the in-memory per-issue debounce resets on restart, and re-seeds the poll cursor from a recent lower bound, so each restart re-swept the backlog). That piled up dozens of identical copies per ticket (1,366 duplicates across 56 firefly tickets in one session). Body-exact match, so genuinely-new content still posts. clippy clean; infra tests green.

### Fixed
- **Doppler `login`/`login_poll` no longer dead-lock the dry-run auditor** (2026-06-11): the browser-OAuth login flow's two tools weren't in `config/reversibility-defaults.toml`'s `[mcp.doppler]` block, so they fell to the conservative unknown-tool default (`Irreversible`) and got routed through the blast-radius auditor — which could hard-block with no reachable `hygiene_override`, dead-ending a benign operator-initiated auth step. Classified both as `TriviallyReversible` (they only return an auth URL / write a token to the local accounts.json — no remote secret read/write, no prod impact). Same deadlock class as the earlier MCP-default fixes. 11 reversibility tests green.

### Removed
- **viz extracted to its own repo + legacy app deleted** (2026-06-11): The activity-graph viz (`tools/sentinel-viz-api` Rust backend + `tools/sentinel-viz-next` Next.js frontend, a coupled pair) moved out to a standalone repo (**legatus-ai/sentinel-viz**) — both were already detached from the cargo workspace (`exclude`d), so this is a clean lift-and-shift; the `exclude` entries are dropped from the root `Cargo.toml`. Separately, the legacy app was deleted as dead/superseded. 222 files removed; `cargo build --workspace` clean.

### Removed
- **`ccam` report (dead/legacy)** (2026-06-11): removed the old "CCAM HQ" HTML report served at `/api/ccam` (`crates/sentinel-cli/src/api/ccam.rs` + `ccam_report.html`, ~28 KB) — stale junk, rendered empty (its data endpoints were gone). Dropped the `pub mod ccam`, the `/api` nest, and the `/api/ccam` auth-bypass clause in `daemon_cmd.rs`. The daemon still serves the JSON API (`/api/health`, proofs, workflows, hooks, memory, …). Builds clean; zero remaining references.

### Added
- **Linear enforcer — due-date gate, native SLA threshold, milestone check (Linear-API-docs audit)** (2026-06-10). A GraphQL-schema introspection of the Issue/Team types (vs. our coverage) surfaced enforceable fields we weren't using; built the high-value ones: (1) **due-date gate** — `is_overdue(dueDate, today)` flags an open (non-completed/canceled) ticket past its `dueDate` via comment (distinct from SLA); (2) **native SLA high-risk** — `evaluate_sla` now takes `slaHighRiskAt` and prefers Linear's own high-risk threshold over the 80%-elapsed heuristic, falling back to 80% only when Linear supplies none; (3) **project-milestone** — `extended_discipline()` flags a ticket in a project but with no `projectMilestone`. `TicketReadiness` + `fetch_readiness` gain `sla_high_risk_at`/`due_date`/`has_milestone`. 432 infra tests (+9); clippy clean. Confirmed NON-gaps from the audit: Linear has no `requireEstimate` team toggle (our L1/L2 estimate enforcement is value-add); `needs`/Asks-required-fields are a separate customer-request domain; audit-metadata fields aren't policy.

### Fixed
- **Memory-atom + `plans/*.md` writes no longer require Plan Mode** (2026-06-10). The four-check `tool_usage_gate` classified every `Write`/`Edit` as `ReversibleWithEffort`, forcing the full stack (sequential-thinking + task + **Plan Mode** + active task) even for a one-line memory note under `~/.claude/projects/*/memory/` — work that is local, recoverable, and explicitly not implementation. The gate's own docstring already claimed memory writes *should* be `TriviallyReversible`, but the `LayeredReversibilityClassifier` keyed only on tool *name* (and Bash *command*), never on the edited *path*, so the claim wasn't real. Fix: new **Layer 3.5 path-pattern** layer — `[[path.pattern]] match = "<regex>" class = "..."` in `reversibility-defaults.toml`, queried for `Write`/`Edit`/`NotebookEdit` before the Layer-1 builtin. The path is normalized to forward slashes so one pattern covers Windows + POSIX. Two shipped patterns classify the memory-atom store (`/.claude/projects/<key>/memory/`) and `plans/*.md` as `TriviallyReversible`; unmatched edits fall through to the unchanged RWE default (source-code edits still gated). 9 new tests (path layer + Windows-backslash normalization + NotebookEdit + shipped-defaults memory/plan/source cases); 74 reversibility tests pass, engine builds clean, clippy clean on the changed file.

### Added
- **Linear enforcer — blocked-relation integrity + portfolio coverage (closing the last discipline gaps)** (2026-06-10). (1) **Blocked-relation**: `has_live_blocker(issue)` scans `inverseRelations` for a `blocks` relation whose blocking issue isn't completed/canceled; `TicketReadiness.actively_blocked` feeds `extended_discipline()` so a *started but actively blocked* ticket is flagged (comment-only — work can't really proceed while blocked). (2) **Portfolio cron** extended to also flag: projects **not in any initiative** (roadmap-orphan), **orphan** started issues (no project), and **stale-in-Triage** (triage >3 days). 423 infra tests (+2 blocked-relation); session_init green; clippy clean. Pairs with the L3 PR-guard branch-name advisory (firefly-pro-crm) — together these close the "all of Linear" discipline surface at the right layer each (per-event / portfolio-cron / CI).

### Added
- **Linear enforcer — full ticket discipline ("all of Linear")** (2026-06-10). Extends the enforcer past the core Definition-of-Ready to the remaining per-ticket discipline. **Acceptance criteria** join the dev-ready set (heal-fillable): `has_acceptance_criteria(description)` (≥3 checklist `- [ ]` or numbered list items) → `missing()` → a started ticket lacking AC reverts/heals like a missing label. **Extended workflow discipline** (comment-only, never auto-filled/reverted — they need a human/planning call): `TicketReadiness::extended_discipline()` flags a *started* ticket with **no assignee**, **not in a cycle**, or **no project**, posting one consolidated "loose ends" comment. `fetch_readiness` now also pulls `description`/`assignee`/`cycle`/`project`. 421 infra tests (3 new: AC heuristic, extended-discipline started-only gating, AC-triggers-revert); clippy clean. (Deferred with reasons: blocked-relation graph integrity + initiative/roadmap/milestone alignment are project/relation-graph concerns better suited to the Tier-5 portfolio cron than the per-event enforcer; branch-name format is L3/CI's domain.)

- **Linear enforcer — SLA gates (Tier 3)** (2026-06-10). The enforcer now evaluates SLA health on every team-allowed issue event (comment-only, orthogonal to the dev-ready revert): **breached** (`slaBreachesAt` in the past) and **high-risk** (≥80% of the `slaStartedAt`→`slaBreachesAt` window elapsed) SLAs get an escalation comment, and **Urgent** tickets (priority 1) with no SLA clock running at all are flagged ("urgent ⇒ has-SLA"). Pure `evaluate_sla(priority, started, breaches, now)` → `SlaVerdict {Ok, HighRisk, Breached, UrgentNoSla}` (now injected, fully unit-tested); `TicketReadiness` + `fetch_readiness` extended with `priority`/`slaStartedAt`/`slaBreachesAt`. 418 infra tests; clippy clean.

### Added
- **Linear enforcer — actor-type differential (L2 agent-vs-human)** (2026-06-10). The enforcer now fetches each ticket's `botActor` + `integrationSourceType` and branches the Live path on authorship: **agent/integration-created** un-ready started tickets get the silent heal-first treatment (remediate → escalate gaps → revert last); **human-authored** tickets are *never silently rewritten* — the enforcer posts a "please complete before starting" comment listing the missing dev-ready fields and leaves the ticket for the operator. `TicketReadiness` gains `created_by_agent`; `parse_readiness` derives it (botActor present OR integrationSourceType set). 417 infra tests (incl. the actor-attribution test); clippy clean. (The Hookdeck-primary-feed half of this task is external webhook-gateway config — the poll+wss feeds already deliver the IssueChanged events the differential consumes.)

### Fixed
- **Stop/SubagentStop `additionalContext` was being silently stripped — found by the new E2E harness** (2026-06-10). The output-transform `match` in `hook_cmd.rs` keeps `hookSpecificOutput` only for an explicit native-support list of events; the `_ =>` arm sets it to `None`. `Stop` and `SubagentStop` were absent from that list, so every Stop-phase hook that injects context via `HookOutput::inject_context` — `claim_reality_check` (the false-done reality warning), `self_annealing`, the good-citizen + hygiene reminders — had its `additionalContext` **dropped at the output boundary** and never reached the agent. This was the true root of the F1 `{}` finding (compounded by two test-isolation red herrings: spawning the launcher instead of the engine binary, and camelCase input keys silently `#[serde(default)]`-dropping to None). Fixed by adding `HookEvent::Stop | HookEvent::SubagentStop` to the native-support arm; the F1 E2E test now asserts the reality-check context survives.

### Added
- **Black-box E2E hook harness (`crates/sentinel-cli/tests/e2e_hooks.rs`) + `SENTINEL_HOME` overridable root** (2026-06-10). A harness that spawns the real `sentinel-engine` and drives `sentinel hook --event <E>` exactly as Claude Code does (snake_case `HookInput` on stdin → `HookOutput` on stdout), covering the layer the ~90 inline unit suites don't: dispatch wiring, the stdin/stdout contract, caller-validation, session-id resolution, per-event serialization. 12 tests — every-event smoke (27 events), unknown-event rejection, missing-session-id no-op, offline fail-open, the blocking gates (`db_ops_gate`, `commit_message_validator`, `pr_merge_gate` ask), read-only/bare-allow, and the F1 reality-check scenario both ways. The harness immediately caught the Stop-strip bug above. Prerequisite: a single `SENTINEL_HOME` override (`sentinel_infrastructure::paths::home_root`) honored by `RealFileSystem::home_dir()` + `config_dir`/`state_dir`/`hmac_secret_dir`, so the engine's `~/.claude` access isolates to a tempdir on Windows too (`dirs::home_dir()` ignores HOME/USERPROFILE on Windows — it queries the OS profile API). Full workspace green; clippy clean on all new/changed files.

### Removed
- **`constitution_gate` + `constitution_gate_runtime` — full removal** (2026-06-10). The constitution-gate hook (a identity/Fabrica-vocabulary feature added by the same author 2 days after the Legatus AI root, only ever configured with Legatus AI example rules) is gone, completing the identity excision. Deleted both modules, the `sentinel legatus`—style wiring in hook_cmd.rs (rule loader + dispatch + fn param + call-site), the HOOK_NAMES entry + lib/mod registrations, and `config/constitution-gate-example.toml`. Hook count 87→86; CLAUDE.md reconciled. No `Legatus AI`/`identity`/`legatus-ai-daemon`/`legatus`(integration)/`nuntius`/`praefectus`/`constitution` code remains in sentinel. Full workspace builds clean; whole test suite green (1517 CLI + 585 domain + 414 app + drift/registration).

### Removed
- **Legatus AI / identity layer — full removal** (2026-06-10). The Legatus AI-Protocol "identity" integration (added later as a bolt-on: root commit `63f42c7`, the `sentinel-legatus` crate, + 39 follow-ups layered onto pre-existing hooks) is gone entirely — sentinel no longer talks to a Legatus AI/legatus-ai-daemon. Deleted: the `sentinel-legatus` crate (workspace member 8→7) + the 5 `Legatus AI-*` path-deps (legatus-ai-protocol/domain/storage/transport + legatus-ai-daemon); the application-layer identity clients (`legatus_client`, `praefectus_client`, `witness_verifier_adapter`); the identity hooks (`Legatus AI_inbox`, `upstream_block`, `catastrophic_escalation` — HOOK count 90→87); the `sentinel legatus` CLI subcommand (top-level subcommands 33→32) + all `--legatus-*` daemon flags + the daemon's legatus-hosting (`start_legatus_if_configured`, `legatus_routes`, the `/legatus/*` API routes, `LegatusOptions`/`WitnessVerifyMode`); the `legatus-ai-domain` `RoleBinding` `actor` field on `PhaseProof`/`StepProof` (so `sentinel-domain` no longer depends on `legatus-ai-domain`) + the `ProofEngine` Praefectus enrichment. **The pre-existing hooks were returned to their pre-Legatus AI form, not stubbed** — `git_hygiene`/`phase_gate`/`pre_commit_verification` deny/block locally via plain `HookOutput` (no upstream signal); `permission_denied`/`evidence_collector`/`execution_log`/`stop_failure` keep their real work, minus the legatus reporting. The daemon keeps the local API + the Linear enforcer. Full workspace builds clean (zero warnings); 1534 CLI + 585 domain + 414 app + drift/registration tests all green; CLAUDE.md counts reconciled.

### Fixed
- **Self-heal LLM path now completes end-to-end — three stacked fixes; FPCRM-596 healed live** (2026-06-10). The Tier B self-heal shipped but reverted every ticket because the `Codex` (`openai/gpt-5.5-pro`) proposal call never produced a usable plan. Root-caused to three independent bugs, each fixed: (1) **gzip decompression** — OpenRouter sends `Content-Encoding: gzip`; rig 0.38's own `reqwest 0.13` instance wasn't built with the `gzip` feature, so the body read failed with `error decoding response body` (manifested as a 45s "hang"). Fixed by declaring `reqwest 0.13` with `gzip`/`brotli`/`deflate` in `sentinel-infrastructure` so Cargo unifies the decompression feature onto rig's instance. (2) **`max_tokens` dropped** — rig 0.38's OpenRouter request struct has no `max_tokens` field, so the reasoning model ran unbounded; now injected via `additional_params` (which rig `#[serde(flatten)]`s into the body) + `temperature(0)`. (3) **parser dropped `type_label`** — `parse_plan`'s hand-rolled brace-counter terminated the JSON object early when an `acceptance_criteria` string contained `{`/`}` (e.g. a `${VAR}` snippet), silently losing fields; replaced with serde's streaming `Deserializer` (string-aware). rig bumped 0.35 → 0.38.2 (latest). **Live-proven**: FPCRM-596 moved to In Progress missing a Type label → enforcer inferred `Bug`, applied the label, ticket stayed In Progress (no revert). New `parse_plan_handles_braces_inside_string_values` regression test; 414 infra tests green; clippy clean.

### Removed
- **Glass-break emergency-override subsystem (full removal)** (2026-06-09). Removed sentinel's "bypass all enforcement" escape hatch in its entirety, per operator decision. Deleted: the `sentinel break` CLI (`break_cmd.rs`, 712 lines — the anti-AI TTY challenge + signed-token override), the `glass_break_gate` helper hook (`active_glass_break()`), the `SessionState.glass_break` field plus `GlassBreak`/`BreakToolUse` structs and `is_break_active()` / `clear_expired_break()` methods, the `check_glass_break_override()` file-token early-returns in `hook_cmd.rs` (both the supervisor and inline paths) and the dialog/token machinery, and `glass-break-spec.md`. Stripped the per-gate suppression short-circuits from `git_hygiene`, `pre_commit_verification`, `verification_gate`, `phase_gate`, `step_gate`, and the `glass_break_active` skip from `step_judge`. Hook registry 91→90 (deregistered from `mod.rs`/`HOOK_NAMES`; `hook_registration_completeness` + `claude_md_count_drift` updated). **Consequence:** there is no longer a built-in audited override if a gate wedges a session — enforcement is now unconditional. Operator-armed source-edit (`SENTINEL_ALLOW_INFRA_EDITS`) and the prod-override grant remain; those are unaffected.

### Docs
- **Work-assurance positioning + competitive reframe** (2026-06-09). New `docs/work-assurance-positioning.md` reframes sentinel's primary category from "federated agent gateway with cryptographic execution proofs" (CISO/attestation lineage) to **AI work-assurance / agent correctness verification** (engineering-leader / CI-eval lineage) — the proof chain is the audit substrate, not the value proposition. Backed by two adversarially-verified market sweeps: the fusion (judge-gated phase advancement + ground-truth false-done reopen + proof-of-work chain, vendor-neutral over coding agents) is white space as of 2026-06; of 12 strongest "direct" candidates, 1 verified-direct (Praetorian — unproductized whitepaper). Cross-reference note added at the top of `competitive-intelligence.md`; §7 documents the four Praetorian mechanics absorbed (all shipped below) and where sentinel stays ahead (proof chain + ground-truth false-done vs. Praetorian's gameable state-file trust). README counts corrected (27→~90 hooks, 5→7 crates).

### Added
- **Praetorian-inspired work-assurance hardening — 4 features** (2026-06-09). Absorbed the four execution mechanics from Praetorian's "Deterministic AI Orchestration" (the lone verified architectural twin), each built in an isolated worktree, test-green + clippy-clean: (1) **completion-promise + ground-truth verify** — `task_completed.rs` matches exact terminal strings (ALL_TESTS_PASSING / IMPLEMENTATION_COMPLETE / …) and treats a promise with no corroborating commit/PR as a hard mismatch reopened by `claim_reality_check`; (2) **output-similarity loop detector** — `step_anomaly.rs` 10th dimension `OutputSimilarity` (line-Jaccard ≥0.90 over the last 3 runs of a step = stuck loop), plus **layered redundant enforcement** running the false-done sweep on `SubagentStop` as well as `Stop`; (3) **role-dyad serialization** — `workflow.rs` `RoleDyad`/`DyadVerdicts`: a phase with `required_dyad` cannot complete until a *separate* reviewer PASS and/or tester tests_passed are recorded (the implementer cannot self-approve), applied to `review` + `qa-handoff`; (4) **self-annealing meta-agent** — new `self_annealing.rs` Stop hook reads the `failed_submissions` forensic record and, on the Nth gate failure, emits a `[Self-Annealing]` remediation (always) and, when operator-armed via `SENTINEL_ALLOW_SELF_ANNEAL=1`, appends a hard-constraint stanza to the diagnosed skill and opens a PR — gated by a default-deny predicate that never touches phase files / `~/.claude/sentinel` / `settings.json` / crates / the engine even when armed (composes with the Attack #201 guard), PR-only, never direct-to-main. Hook count 90→91. 24 new unit tests across the four; domain 589 / app 1617 / CLI 173 / graph 15 / infra 405 green.
- **`remediation` — Tier B self-heal: the enforcer as assistant, not bouncer** (2026-06-09). Flips the Live default from "revert an un-ready started ticket" to "**heal it in place**." When a started ticket is caught below the Definition-of-Ready, the enforcer auto-fills every gap it can confidently infer — Type/Area labels (mapped from a closed `LabelCatalog` so the model can't invent ids), estimate, priority, acceptance criteria — via an adversarial `Codex` proposal (`openai/gpt-5.5-pro` through the `OpenRouter` `LlmPort`), so the ticket reaches dev-ready and **stays in progress**. A checkpointed `langgraph-core` `StateGraph` (`classify → verify → clear | escalate | revert`) routes on the POST-heal state: fully healed → **Clear** (+ auto-remediated comment); partial (gaps need human judgment) → **Escalate** (comment listing exactly what a human must set, ticket stays in progress); nothing fillable → **Revert** (last resort). `parse_plan` is conservative — unparseable/out-of-range → escalate, never mis-fill. `run_enforcer`'s Live path is rewired remediate-first (fetch readiness → propose → apply → re-fetch → graph decides) with `fetch_label_catalog`/`fetch_ticket_text`/`post_comment` helpers; degrades to readiness-only revert when no LLM/graph. 8 `remediation` unit tests; 413 infra tests green; clippy clean. The "heal toward the 95-score, ask only the gaps, revert last" model.
- **`linear_enforcer` poll-source fallback — the reliable realtime feed (resolves the wss 4002)** (2026-06-09). Investigating legatus-desktop (the reference impl) revealed the wss `subscribe` 4002 ("need to authenticate") is *expected* Linear behavior for a personal API key — the same key authenticates REST + the `connection_init` ack, but the subscribe op is refused. legatus handles it by falling back to REST polling (`LinearPollActor`, "bump cadence when the WS auth is rejected"); our spine had copied only the wss client and omitted that fallback. This adds it: `run_poll_source` polls `issues(filter:{updatedAt:{gte:cursor}, state:{type:{in:[started]}}})` every 30s, advances a chronological cursor (RFC3339 strings sort correctly → string-max = timestamp-max), and feeds the SAME `IssueChanged` mpsc the wss uses — so the entire `run_enforcer` pipeline (debounce, team filter, shadow/live, escalation graph) is unchanged. Both feeds run into one channel: wss is the optimization, poll is the reliable backstop. Needs no wss/webhooks/OAuth — just the PAT (verified). `run_enforcer` spawns both; cursor seeds from `SENTINEL_LINEAR_POLL_SINCE` or a recent default. **Live shadow E2E verified**: temporarily nulling a started ticket's estimate produced `[SHADOW] WOULD revert FPFIELD-77 missing=estimate` on the next poll round (with correct cursor advancement: count=34 → count=1), zero mutations, ticket restored. Unblocks arm-live. 3 unit tests (filter shape, cursor picks-newest, cursor keeps-prev); 405 infra tests green; clippy clean.
- **`reconciliation_graph` — Tier D truth-audit (dead-code catcher)** (2026-06-09). Treats every *Completed* Linear ticket as a CLAIM ("shipped + live") and verifies it against fresh `origin/main` via a checkpointed `langgraph-core` `StateGraph` (`classify → judge → flag | clear`). Encodes the FPROUTE-14 lesson as a first-class verdict taxonomy — `Shipped` (code exists AND has a production caller), `DeadCode` (merged + compiles + tested, but no production caller), `Reverted` (gone from main), `Unverifiable` (infra/secret/external) — so a merged-but-dead feature can't masquerade as shipped. The `judge` is an adversarial `Codex` pass (`openai/gpt-5.5-pro` via the `OpenRouter` `LlmPort`) prompted to demand a real CALLER, not just a definition, and to answer `UNVERIFIABLE` (never guess `SHIPPED`) on inconclusive evidence; the parser maps anything unrecognized to `Unverifiable` so a wrongful flag never fires. `reconcile_ticket` orchestrates judge→graph; the caller posts the evidence comment + demotes out of Completed when the decision is `Flag`. Designed to run on a sentinel cron (audits shipped history; not a real-time gate) and reuses the Tier B graph/judge plumbing. 7 unit tests (verdict lie-classification, all parser forms incl. garbage→Unverifiable, the caller-distinction prompt, graph routing for DeadCode/Reverted→Flag and Shipped/Unverifiable→Clear). 402 infra tests green; clippy clean. Completes the factory's truth-audit tier.
- **`enforcement_graph` — Tier B LangGraph escalation layer (strict + adversarially-verified)** (2026-06-09). The decision brain on top of the Tier B spine: a checkpointed `langgraph-core` (LangGraph Rust) `StateGraph` — `classify → judge → revert | clear` — that decides whether an un-ready *started* ticket gets reverted, and persists *why* (missing fields + judge verdict) as a durable, replayable audit record. **Strict mode** = immediate revert, but never on a raw signal: the `judge` node is an adversarial `Codex` pass (`openai/gpt-5.5-pro` via the `OpenRouter` `LlmPort`, through `delegation_service::delegate`) that must **CONFIRM** the ticket is genuinely un-ready before the revert fires — and biases to **REFUTE** on any ambiguity (Linear API race, label/estimate propagation), so a wrongful revert is structurally unlikely. Because `langgraph-core` nodes are synchronous, all I/O (Linear fetch, the LLM call) runs in the async `evaluate_ticket` orchestrator *around* the graph; the graph itself is the pure deterministic decision + audit trail. `run_enforcer`'s **Live** path now routes through `evaluate_ticket` (judge-gated revert) when `OPENROUTER_API_KEY` is set, and degrades to the direct readiness-only revert otherwise; **Shadow** is unchanged (log-only). Adds `langgraph-core` (path dep, `sqlite` checkpointer) to `sentinel-infrastructure` + `TicketReadiness::missing_owned()`. 7 `enforcement_graph` unit tests (graph routing for confirmed→Revert / refuted→Clear / ready→Clear, the conservative verdict parser, the refute-biased judge prompt); 395 infra tests green; binary builds; clippy pedantic+nursery clean. Completes the "using LangGraph Rust" half of the factory.
- **`linear_enforcer` — Tier B real-time Linear enforcement spine (shadow-default)** (2026-06-08). The always-on half of the "bad PM is bad software" factory: a long-lived `graphql-transport-ws` subscription to Linear (`issueHistoryCreated`) hosted by `sentinel daemon` that reacts to *every* issue state-change — UI edits and API edits alike, not just an in-session agent. When a ticket enters a *started* state (In Progress / Code Review / …) while not dev-ready (no estimate, missing Type/Area label, or still in Triage), the enforcer reverts it to Backlog and comments the missing fields. Ships with the wss client + reconnect/backoff + PAT-rejection handling (ported from legatus-desktop's proven `LinearSubscriptionWsClient`, self-contained — sentinel's own error type, no legatus deps), the REST readiness fetch + revert/comment action, and a `run_enforcer` driver wiring them together with a per-issue 20s debounce (Linear fires several history events per edit) and an optional team-prefix filter. **Safe by default**: `EnforcerConfig::from_env` reads `SENTINEL_LINEAR_TOKEN` (absent ⇒ enforcer not spawned), `SENTINEL_LINEAR_ENFORCE` (`shadow` default — log-only, zero mutations — vs. `live`), and `SENTINEL_LINEAR_TEAMS` (comma list ⇒ team filter). The daemon spawns it fire-and-forget after legatus setup, logging `disabled`/`enabled (mode)` on boot and surviving a permanent PAT rejection without going down. 15 `linear_enforcer` unit tests (readiness/`should_revert` matrix, parse, subprotocol, mode-default-Shadow, team-filter prefix/case, debounce window, config `from_raw` parsing); daemon boots clean with the wiring in place. Pairs with Tier A (`ticket_quality_gate` hook) and Tier C (CI ticket-ready gates). **TLS bring-up (verified live against Linear's wss):** install the rustls `ring` `CryptoProvider` once at subscription start (rustls 0.23 ships both `ring` + `aws-lc-rs` and otherwise panics demanding a choice), and switch `tokio-tungstenite` to `rustls-tls-native-roots` so the OS trust store (which on this Norton-MITM box holds Norton's injected Root CA) is honored instead of `webpki-roots` rejecting the re-signed cert as `UnknownIssuer`. Shadow-mode E2E confirmed `subscription live` + zero mutations on a real ticket event.
- **`ticket_quality_gate` — advisory Linear ticket-quality gate + `ticket-builder` tracking workflow** (2026-06-05). Productizes the Linear ticket-quality rubric + 95 score-gate (`~/.claude/skills/linear/best-practices/ticket-quality.md`) into sentinel. A new `PreToolUse` hook scoped to `mcp__linear__create_issue`/`update_issue` ONLY injects the rubric reminder (resolve the team's estimation type, set Type+Area labels + estimate + priority + ≥3 testable AC, then re-fetch and score to ≥95) as `additionalContext` — **advisory only, never blocks**: a *blocking* gate keyed on a Linear tool is precisely the shape that produced the phase-gate deadlock, so this enforces quality without ever trapping the session. Paired with a `ticket-builder` workflow (`resolve-team → build → verify → score-gate`) + `steps/ticket-builder.toml` registered for proof/step tracking only — **no phase files on disk**, so the phase-gate treats it as documentation-only and never hard-blocks. 6 hook tests; registration-completeness + drift tests green.
- **`session_summary` — SessionEnd prose "where we left off" note** (2026-05-30). New `SessionEnd` hook writes `persistent-tasks/{project_hash}/session-summary.json` beside the structured task graph: recent commit subjects (`git log --oneline -10`, what shipped), current branch + HEAD sha, and completed/in_progress/pending task counts. `task_rehydrate` reads it on the next `SessionStart` and injects a `[Last Session]` orientation block **alongside** the structured task list (refactored to inject either/both independently, so a session that finished all its tasks still gets the prose recap). Composes with — does not duplicate — `task_persist`/`task_rehydrate`. One `git log` + one write, fail-open everywhere (respects the 1.5s SessionEnd budget); skips writing an empty note and skips reading back a same-session summary. 13 tests.
- **`task_decomposition_gate` — enforce decomposed task lists before mutating work (PR #32)** (2026-05-30). New `PreToolUse` gate: blocks `Edit`/`Write`/state-changing `Bash` until a live decomposed task list exists, mandating the numbered `#N` + real `Na/Nb` child-subtask format with a two-emoji prefix (status `⬜🔄✅❌` + priority `🔴🟠🟡🟢`), blocking deps, and parent mirror checklist. Reads/`Task*`/sequential-thinking always allowed (never deadlocks the fix path); fails open when task state is unreadable. 18 tests.
- **Two-way Linear ↔ task sync (no MCP)** (2026-05-30). Outbound: `task_completed` writes back to Linear on `@linear:{ID}`-tagged completion. Inbound: new `linear_inbound_sync` (`UserPromptSubmit`) polls the **Hookdeck `/events` API directly** (`include=data` required) for the Linear source, parses `event.data.body` → identifier+state, and flips the matching task's status emoji on drift. Key resolved env→`~/.claude/sentinel/secrets.toml`; cursor-tracked, 300s-throttled, fails open. 41 tests.
- **`task_coverage_check` stale/done-signal nudges** (2026-05-30). Stop hook now reminds (never auto-writes) when an `in_progress` task looks done (new HEAD commit / PR / passing build this turn) or has been `in_progress` ≥3 Stops. State markers `coverage-headsha-{sid}`/`coverage-inprogress-{sid}`; fails open.
- **`task_completed` real claim-verification** (2026-05-30). Replaced string-match heuristics with real checks: claimed commit sha → `git merge-base --is-ancestor <sha> HEAD`; claimed PR → `gh pr view <N> --json state,merged`; warns on a genuine mismatch (claimed commit not on HEAD, claimed-merged PR shows open). Fails open if git/gh unavailable. 29 tests.
- **`claim_reality_check` Stop hook** (2026-05-30). Sweeps completed tasks each Stop and flags any whose claimed artifact (commit/PR) fails the same git/gh verification (shared with `task_completed`, no duplication). Per-session marker `reality-check-{sid}` avoids re-flagging; reminder-only, fails open. 9 tests.
- **Phase progression powered by langgraph-core (#65)** (2026-05-27). New `sentinel-graph` crate compiles each skill's `workflows.toml` into a checkpointed [langgraph-core](../langgraph-python-to-rust) `StateGraph`: one node per phase, `START → phase[0] → … → END`, a conditional edge per phase (Pass → next/`END`, Fail → loop back to the same phase), and `interrupt_after` on every phase for the out-of-process judge gate. A durable sqlite `CheckpointSaver` keyed by `thread_id = session_id` matches sentinel's process-per-invocation hook model — the graph advances one phase per async MCP call and checkpoints between, so a fresh hook process restores via `load_latest` (proven by a cross-process durability test). `apply_verdict` records each judge verdict as a parent-linked checkpoint (advancing on pass, looping back on fail), `replay_phase` time-travels by forking from the checkpoint before a target phase (first-class QA-failed re-attempt), and `phase_history` projects the checkpoint history for local APIs and JSONL streaming. Wired into `handle_submit_phase` behind `SENTINEL_GRAPH_ENGINE=1`, running as an authoritative shadow alongside the legacy `advance_sequential` (kept as fallback). The langgraph dependency stays confined to `sentinel-cli` + `sentinel-graph`; `sentinel-application` (the sync `phase_gate` hot path) remains langgraph-free (verified via `cargo tree`).
- **Operator-armed `SENTINEL_ALLOW_INFRA_EDITS` self-development bypass (#71)** (2026-05-27). The Attack #201 protected-path guard hard-blocks Write/Edit to sentinel's own repo source during active sessions, which also blocked developing sentinel on itself. Added a least-privilege, operator-armed bypass: when `SENTINEL_ALLOW_INFRA_EDITS=1` is set in the operator's environment (fixed at Claude Code launch — an injected tool result cannot set it), edits to the repo *source* (`crates/`, `Cargo`, `config/`, `mcp-router/`) are allowed. Scoped to the repo-source reason ONLY — the live `~/.claude/sentinel` config/state, `settings.json` hook registrations, the compiled binary, and phase/skill files stay protected even when armed, so an armed self-dev session still cannot neuter a running engine. Default off. Pure `infra_source_bypass(reason, armed)` predicate with a unit test asserting every other protected reason is never bypassed.

### Fixed
- **`phase_gate` phantom-workflow whole-session deadlock** (2026-06-05). A phased hard-gate workflow (e.g. `linear`) could become "active" merely because the skill-router matched the skill TOPIC in a prompt (e.g. "build me a linear doc"), without the user ever entering the workflow. The gate then hard-denied EVERY non-exempt tool (`Bash`/`Write`/`Edit`) for the rest of the session, with the only escape being to perform the workflow's first phase (claim a Linear issue) — which was never the task. Fix: in `gate::evaluate`, a blocking workflow with **zero completed phases AND no phase file read** for the skill ("armed but never entered") downgrades the hard block to an advisory ALLOW; enforcement resumes the instant the workflow is genuinely entered, preserving every skip/switch mitigation (#21/#24/#38/#55), which all assume an entered workflow. The same never-entered predicate was applied to `check_protected_path_write` so a phantom-active skill no longer locks its own `SKILL.md` (phase files stay always-protected). Also fixed the block message to use the skill the gate actually evaluated (`effective_skill`) instead of `state.active_skill`, which was `"unknown"` in the incomplete-workflow path and pointed the remediation at a nonexistent `skills/unknown/phases/claim.md`. 2 regression tests; full suite 1548 → green.
- **`StructuredOutput` misclassified as Irreversible → every Workflow deadlocked (2026-06-05)**. The Workflow harness's `StructuredOutput` return tool (a subagent's only way to return its structured result to the parent) had no arm in `builtin_class()` (`sentinel-infrastructure/src/reversibility.rs`), so it fell to the conservative `Irreversible` default. The A3 `dry_run_then_commit` gate then blocked it — demanding `_intent`/`_reasoning`/`_expected_effect` keys that the `StructuredOutput` schema rejects (`additionalProperties:false`) — an unbreakable deadlock that killed **every** multi-agent Workflow with `subagent completed without calling StructuredOutput (after 2 nudges)`. `StructuredOutput` only serializes a JSON value back into the conversation; it commits nothing external → trivially reversible by definition. Added it to the `TriviallyReversible` arm + a regression test (`layer1_structured_output_is_trivially_reversible`).
- **`skill_invocation_gate` re-fire loop** (2026-05-30). `skill_router` rewrote a fresh pending-skill marker on every `UserPromptSubmit`, so re-detecting an already-invoked skill re-armed the gate each turn. A skill now stays satisfied across re-detection within a session; only a genuinely never-invoked skill blocks once. Regression test reproduces the loop. 16 tests.
- **`output_compress` dropped cargo outcome lines** (2026-05-30). `"Finished "` was classified as cargo-build noise but not a signal line, so every `Finished \`release\`` / `Finished test` outcome line was silently dropped (forcing `SENTINEL_COMPRESS_BYPASS=1`). Added it to `is_signal_line` + removed from `is_cargo_build_noise`; build/test outcomes now survive compression. 19 tests.
- **`compress_cmd` corrupted command output (two confirmed bugs)** (2026-05-30). The `sentinel compress` runner — which the `output_compressor` `PreToolUse` hook routes qualifying Bash commands through, so its stdout IS the agent's captured result — mangled output two ways: (1) **stream reordering** — stdout and stderr were captured as separate pipes and concatenated all-stdout-then-all-stderr, relocating interleaved diagnostics (rustc errors, progress+result) to the bottom of the transcript; (2) **UTF-8 destruction** — `from_utf8_lossy` ran unconditionally, so non-UTF-8 output (binary dumps, latin-1, hexdumps) became U+FFFD even for passthrough (`cat`) commands. Both reproduced and fixed: stderr is now merged into stdout *inside the shell* (`{ … } 2>&1`) so the kernel interleaves in true order; the decision to compress happens *before* any lossy conversion, and a non-compressible command / non-UTF-8 output is written through **byte-exact**. Compression still runs (and preserves signal lines) for compressible valid-UTF-8 text. Shell now spawned via absolute `/bin/sh` so a command mutating `PATH` can't break the spawn. Extracted a pure `render_output` for testing. 6 new regression tests.
- **`CLAUDE.md` hook-module count drift (81 → 85)** (2026-05-30). The `claude_md_count_drift` integration test caught the architecture table claiming "81 hook modules" while `hooks/` actually holds 85 `.rs` files (this session added `task_decomposition_gate`, `linear_inbound_sync`, `claim_reality_check`, `session_summary`). Updated both occurrences; drift test green.
- **`dry_run_then_commit` (A3) unbreakable block on `Bash` git ops** (2026-05-30). The auditor demanded `_intent`/`_reasoning`/`_expected_effect` on `Irreversible` actions, but the `Bash` tool schema can't carry them → `git push`/`commit`/`merge` were unblockably denied (`hygiene_override` unavailable in agent context). Added a `Bash` carve-out parallel to the existing MCP-read-only one: `Bash`+`Irreversible` → allow (shell git ops already governed by `commit_message_validator`/`pr_merge_gate`/`git_hygiene`/`db_ops_gate`); `Bash`+`Catastrophic` (e.g. `rm -rf /`, force-push to main, history rewrite) → still denied with a destructive-shell message. MCP behavior unchanged. 38 tests.
- **`memory_turn_capture` binary resolution** (2026-05-30). The capture hook hard-coded `~/.cargo/bin/memory`; now probes `memory-rs` first then legacy `memory` (cargo-bin + dev-clone paths) so it resolves whether the new `{product}-rs`-named binary or the old one is installed.

### Changed
- **scanner.rs god-functions decomposed (behavior-identical) (#32)** (2026-05-25). The two ~300–360-line god-functions in `scanner.rs` (`run_validation`, `sync_counts`) mixed discover/classify/validate/count-sync/manifest concerns. Decomposed each into focused helpers along the natural seam — `run_validation` 360→22 lines (6 `validate_*` helpers, one per category), `sync_counts` 290→16 lines (`build_universal_patterns`/`apply_universal_patterns`/`apply_targeted_updates`). Guarded by **10 new golden-output tests** capturing the exact current scan/validate/sync-counts behavior, asserted green against the original code FIRST, then re-asserted after each extraction — output is byte-identical. 15 scanner tests green, clippy zero.
- **mcp_handler.rs: extracted `submit_step_complete` arg-parsing (#34)** (2026-05-25). Pulled the required-field validation prologue into a pure, testable `parse_submit_step_args(args) -> Result<SubmitStepArgs, _>` (named struct documents the 5 required inputs), separating "what shape does this call require" from the stateful enforcement logic. Six other long-but-cohesive handlers (`query_proof_corpus`, `get_step_proof`, etc.) were deliberately LEFT intact — splitting cohesive single-responsibility functions for line count is an anti-pattern. 1363 tests pass, clippy zero.

### Fixed
- **sentinel-legatus round_trip_legatus-ai-daemon flaky under parallel test load (#33)** (2026-05-25). The 5 round-trip tests passed in isolation but ~4 failed under `cargo test --workspace` with "Blocking waiting for file lock on artifact directory" — shared on-disk state contention. Each test now uses an isolated `TempDir` (and ephemeral port where bound) so parallel runs no longer collide. Green both in isolation and under full parallel workspace test. No production behavior change.

### Added
- **Test coverage for the extracted `llm_scorer_runtime` shared bridge (#29)** (2026-05-25). Added unit tests for the F5-extracted module: `strip_code_fence` (fence-stripping, plain-passthrough, whitespace, multiline), `read_timeout` (override parse + default fallback), and `run_blocking` (the std::thread::scope tokio bridge — timeout-mapping + the #18 nested-runtime guarantee at the shared-helper level). 333 → 365 infra tests, all green, clippy zero. (Engineering and Architecture score-lifts were assessed and deliberately declined: the remaining long functions are cohesive, and the `legatus-ai-domain` coupling is an intentional ADR-003 cross-repo identity contract — forcing either would lower real quality for a cosmetic point.)

### Changed
- **Quality-gate MEDIUM findings: real HMAC, scorer-plumbing dedup, god-function split (#26/#27/#28)** (2026-05-25). Closed the three remaining quality-gate MEDIUM items. **(F4)** `hygiene_override` override-token MAC was `SHA256(salt‖msg)` (length-extension-shaped) with a non-constant-time `==` compare; replaced with real `Hmac<Sha256>` keyed on a domain-separated `-v2` key + constant-time `verify_slice`. On-disk token format unchanged (`{ts}:{sig}`); old v1 tokens fail-closed (TTL ≤ 1h). **(F5)** the three LLM scorers (`dry_run_auditor`, `eval_scorer`, `spec_challenge_scorer`) triplicated ~120 lines of bridge plumbing (PromptFn, sidecar runtime, the #18 nested-runtime thread-scope bridge, strip-fence, provider dispatch); extracted into a new `llm_scorer_runtime` module — **net −378 lines**, each scorer keeps only its distinct prompt builders + verdict parsing. The `score_does_not_panic_when_called_from_within_a_runtime` regression test still passes. **(F6)** split the ~1485-line `hook_cmd::run_internal` god-function into per-event handlers (`handle_user_prompt_submit`/`handle_pre_tool_use`/`handle_post_tool_use`/`handle_stop`) — pure mechanical extraction, byte-identical behavior, run_internal now ~795 lines. clippy zero, full workspace tests green, zero behavior change.

### Fixed
- **Quality-gate HIGH findings: forgeable glass-break token, wasted LLM call, dead orchestrator (#23/#24/#25)** (2026-05-25). A full quality-gate sweep (parallel security + maintainability/perf review agents) surfaced three HIGH items, now fixed. **(F1, security)** `check_glass_break_override` trusted a bare future-timestamp `.glass-break-token` with no integrity check — anything that can write the file (i.e. the agent via `Write`/`Bash`) could forge it and disable all enforcement, defeating the "AI can't complete the dialog" design. The token is now signed: `{expiry}:{sig}` where `sig = SHA256(binary-embedded-salt ‖ expiry)`; an unsigned/forged/tampered token fails verification and is rejected + removed. **(F2, perf)** the A3 dry-run auditor (`auditor.score()`, up to a 30s LLM round-trip) fired on the PreToolUse path even when an earlier gate had already blocked the call; now guarded behind `output.blocked != Some(true)`, mirroring the existing `output_compressor` guard — no wasted latency auditing already-denied tools. **(F3, maintainability)** deleted `engine.rs` — a 138-line `HookEngine` parallel orchestrator that was never constructed or called (the live dispatch is the hand-rolled match in `hook_cmd.rs`); it was misleading dead code implying parallel batch execution that isn't wired. clippy zero, full workspace tests green.

### Added
- **Native output compressor — our own "RTK" (token killer) (#19/#20/#21)** (2026-05-25). Long autonomous sessions burn context window on noisy command output (`cargo test` per-test `... ok` spam, `Compiling …` lines, `grep`/`find` dumps). Built a deterministic, command-aware compressor in-house so it composes natively with sentinel instead of bolting on a third-party proxy. Three pieces: (1) **`sentinel-domain::output_compress`** — pure, zero-IO compression with per-command rules (cargo test/build/clippy, git status, grep/find/ls) and a hard **signal-preservation invariant**: `error`/`error[…]`/`FAILED`/`panicked`/`test result:`/`warning:`/`fatal:` lines pass through **verbatim**, so compression can only ever remove noise, never the lines a verification gate or proof step parses. (2) **`sentinel compress -- <cmd>`** CLI — runs the command, compresses combined stdout+stderr, preserves exit code; `SENTINEL_COMPRESS_BYPASS=1` opts out. (3) **`output_compressor` `PreToolUse` hook** — auto-rewrites compressible Bash commands (via `HookOutput::rewrite_input`, the v2.1.88 `updatedInput` mechanism) to route through the CLI, but only when no gate above blocked the call and not already wrapped. 9 domain + 3 CLI + 8 hook unit tests; clippy zero; full workspace tests green. `cargo test` is the best case (~90% noise removed) — exactly our hottest command.

### Fixed
- **Auditor/scorer nested-tokio-runtime panic on every `mcp__browserbase__*` (and eval/spec) call (#18)** (2026-05-25). `RigAuditor::score` (the A3 dry-run auditor queried by the `dry_run_then_commit` PreToolUse gate) called `sidecar_runtime().block_on(..)` directly on the calling thread. But `score` is reached from inside the CLI's `#[tokio::main]` multi-thread runtime, so blocking a tokio worker thread panicked with "Cannot start a runtime from within a runtime" (`tokio .../multi_thread/mod.rs:91:9`) — surfacing as a `PreToolUse:mcp__browserbase__create_session hook error` / `…__navigate hook error` on every browserbase tool call (non-blocking, but the hook crashed each time). The sidecar's own runtime didn't help: `block_on` blocks the *current* thread regardless. Fixed by driving the `block_on` on a dedicated `std::thread::scope` thread (outside any runtime's worker pool) via the sidecar's `Handle` — panic-safe whether or not the caller is inside a runtime. Same latent bug fixed in the two sibling scorers (`eval_scorer.rs`, `spec_challenge_scorer.rs`). Added a `#[tokio::test]` regression test that reproduces the exact nesting (sync `score` via `spawn_blocking` inside a multi-thread runtime) and would panic against the pre-fix code. 333 infra tests + full workspace green, clippy zero.

### Changed
- **First-class glass-break overrides now suppress the enforcement gates (#5)** (2026-05-25). The audited, interactive glass-break (`sentinel break` — 6-digit terminal challenge that blocks AI self-invocation, reason capture, duration + optional workflow scope, rate-limited, JSONL audit log, `SessionState.glass_break`) previously set session state that **only `phase_gate` honored** — the main blocking gates still keyed off the coarse phrase-based `hygiene_override`. Wired the first-class break into the gates so it actually supersedes the coarse path: new `hooks::glass_break_gate::active_glass_break(state, workflow)` helper (reuses the in-memory `SessionState` the dispatcher already loads — no new state path, domain purity preserved), checking presence + non-expiry + workflow-scope match (unscoped break covers all gates). Wired into `git_hygiene` (tag `git_hygiene`, short-circuits both protected-branch and uncommitted-files blocks), `pre_commit_verification` and `verification_gate` (tag `verification`), each emitting an auditable `[sentinel][glass_break] … gate suppressed` line. The coarse `hygiene_override` is kept intact (additive). 9 glass-break unit tests, clippy zero, full workspace tests green.

### Added
- **Worker-delegation MCP tools: `delegate_codex` + `delegate_kimi_context_scan` (#2)** (2026-05-25). The orchestrator can now hand a discrete unit of work to a *worker* model and get a structured result back, instead of doing everything in its own context — the deep-research report's top gap. Two roles: **Codex** (`openai/gpt-5.5-pro`) for adversarial / code reasoning ("poke holes in this", "review this diff"), and **Kimi** (`moonshotai/kimi-k2.6`) for cheap large-context scanning ("scan this blob, answer X"). New `delegation_service` in `sentinel-application` (`Worker` enum, `DelegationRequest`/`Result`, role-specific prompt builders, `delegate()`), reusing the standardized `LlmPort` (`OpenRouterLlm`) — the same OpenRouter gateway the judge uses, no second model path. Added `LlmModel::Codex` + `LlmModel::Kimi` tiers (domain) mapped to the pinned slugs (infrastructure). Two MCP tools wired in `mcp_cmd.rs` with a shared async `handle_delegate`. 6 unit tests (prompt framing, tier routing, output), clippy zero, full workspace tests green; both worker models live-verified callable on OpenRouter.

### Changed
- **Workspace driven to ZERO clippy warnings (#16)** (2026-05-25). Drove `cargo clippy --workspace` (pedantic + nursery) from ~1500 warnings to zero. Two-pronged: (1) **fixed** hundreds of genuinely-mechanical lints — `format_push_string` → `write!`/`writeln!`, `format_collect` → fold, `manual_let_else`/`option_if_let_else` where mechanical, `or_fun_call` → `unwrap_or_else`, `unnecessary_sort_by` → `sort_by_key`, `manual_strip`, `assigning_clones` → `clone_from`, `items_after_statements`, `unused_peekable`, `iter_on_single_items`, doc-comment backticks, etc.; (2) **allowed at the workspace level, with per-lint justification comments**, the opinionated style/taste lints that are matters of preference not correctness (`too_many_lines`, `manual_let_else`/`option_if_let_else` residue, `needless_pass_by_value`, numeric `cast_*`, `significant_drop_tightening`, `future_not_send`, `match_same_arms`, etc.) — clippy `all` stays fully enforced for real bugs. Also surfaced two genuine **`incompatible_msrv`** findings and bumped the workspace MSRV `1.83` → `1.87` to match APIs the code already uses (`is_unicast_link_local`, and a 1.87 stdlib item in `change_failure.rs`). Build + full workspace tests green, no behavior changes, no `#[allow]` blanket-suppression at module/crate scope.

### Fixed
- **sentinel-legatus tests failing on Windows: fs2 lock on append-only handle + reconnect timing (#17)** (2026-05-25). Two pre-existing Windows-only bugs broke ~30 tests in the legatus crate. (1) **Persistence:** `PersistentInbox::append` / `PersistentEscalationOutbox::append` opened the file with `.create(true).append(true)` only — a `FILE_APPEND_DATA`-only handle on Windows, which `fs2`'s `lock_exclusive` (`LockFileEx`) cannot lock (it needs `FILE_READ_DATA`/`FILE_WRITE_DATA`). The lock silently failed and the early-return dropped the write, so `snapshot()` read 0 entries. Fixed by adding `.read(true)` (NOT `.write(true)` — `write+append` together give the handle a non-append write cursor on Windows, so successive appends overwrite earlier entries) plus an explicit `flush()` before unlock. (2) **Reconnect:** `run_connect_hosted`'s `connect_async` was unbounded, and three `reconnect_loop_*` tests assumed connect-refusal is instant (true on Linux, but `127.0.0.1` low/closed ports SYN-time-out ~1-2s on Windows). Added a `CONNECT_TIMEOUT` (5s) wrapping the connect so an unreachable legatus-ai-daemon becomes a bounded transport failure the backoff loop handles (real production hardening, not test-only), and rewrote the three tests to poll for the published `ConnectionState` with a bounded deadline instead of fixed sleeps — removing the platform-specific timing assumption without weakening what's asserted. Full crate green (99 tests), full workspace green, no regressions. No `#[ignore]`, no weakened assertions.

### Changed
- **Judge promoted to full audit-grade enforce + mandatory signing (#15)** (2026-05-25). After observing warn mode (#13) on real work, flipped the deployment to audit-grade: `SENTINEL_JUDGE_ENFORCEMENT` `warn`→`enforce` (a non-sufficient independent verdict now hard-blocks `submit_step_complete` from sealing, per `mcp_handler.rs` — the structural gate, not a warning), plus `SENTINEL_SIGNING_REQUIRED=1` with a freshly-generated 32-byte Ed25519 seed in `SENTINEL_SIGNING_KEY` so every sealed proof is cryptographically attested and the engine refuses to seal without a key. Config-only (`~/.claude/sentinel/config/settings.json` env block) — the enforce gate (#9/#12) and signing (#4) already shipped. Verified the enforce gate is fail-open on missing verdicts (falls back to the caller-supplied `sufficient:true` for completed steps), so normal workflow is unaffected — only genuinely-insufficient evidence blocks. Takes effect on the next Claude Code restart (the `sentinel-mcp` server reads these env vars at process startup).

### Added
- **Ed25519 proof signing wired into the seal path (#4)** (2026-05-25). Proof signing existed (`step_proof::sign_with`, `SENTINEL_SIGNING_KEY` config) but was never called — proofs were SHA-chain only, never cryptographically attested. `ProofEngine` now signs every sealed `StepProof` over its `combined_hash` when a key is configured, via a `.with_signing(key, required)` builder (the CLI loads `SENTINEL_SIGNING_KEY` + `SENTINEL_SIGNING_REQUIRED`). Mandatory posture: with `SENTINEL_SIGNING_REQUIRED` set and no key, sealing is REFUSED (error, chain unmutated) rather than writing an un-attestable proof — the audit-grade guarantee. 1898 tests green.

### Added
- **Self-certify gap closed + registered MCP servers trusted** (2026-05-25). (1) `submit_step_complete` trusted the caller-supplied `verdict` arg, letting an agent self-certify; now the `step_judge` PostToolUse hook persists an INDEPENDENT verdict into `SessionState` (new `independent_verdicts` map, loaded by `with_session_state` from the same disk store) and `submit_step_complete` prefers it over the supplied one — in `enforce` mode an insufficient independent verdict refuses the seal regardless of the caller's claim. (2) `is_dangerous_mcp_tool` was deny-by-default on verb suffix, flagging `mcp__browserbase__navigate`/`evaluate`, `mcp__cdp__*`, linear mutations etc. as dangerous and applying workflow-gate enforcement; added `TRUSTED_MCP_SERVERS` (browserbase, cdp, steel, sequential-thinking, memory-mcp, sentinel, linear, qdrant, skills, agents) whose tools are trusted regardless of verb, while raw-exec servers not on the list (e.g. `codex` shell/write_file/apply_patch) stay verb-gated. 1895 domain+application tests green.

### Changed
- **Judge models pinned to the operator's four-model set** (2026-05-25). Renamed the `JudgeModel::Haiku` tier to `Codex` and pinned all four slugs to the exact OpenRouter models specified: `Codex` → `openai/gpt-5.5-pro`, `Kimi` → `moonshotai/kimi-k2.6`, `Sonnet` → `anthropic/claude-sonnet-4.6`, `Opus` → `anthropic/claude-opus-4.7`. Added per-step judge routing (#73): `WorkflowStep`/`StepToml` gain an optional `judge:` field (TOML `judge = "codex"|"kimi"|"sonnet"|"opus"`); `step_judge` honors it, defaulting routine steps to `Sonnet`; `multi_judge` Routine tier → `Kimi`. 565 domain tests green.

### Fixed
- **memory-mcp tools were unreachable behind the dry-run gate** (2026-05-25). The atom-store tools (`memory_search`/`memory_capture`/`memory_why`/`memory_audit`/`memory_stats`/`memory_record_outcome`) hit the conservative unknown-MCP reversibility default (`Irreversible`), stranding every memory call at the A3 dry-run gate — the `_intent`/`_reasoning`/`_expected_effect` fields can't be passed through the MCP tool schema, an unbreakable deadlock (same class as the PowerShell + sequential-thinking fixes). Classified the reads as `TriviallyReversible` and `capture`/`record_outcome` as `ReversibleWithEffort` in `reversibility-defaults.toml`. Takes effect on next engine rebuild + stage.
- **Sentinel's own MCP read tools were dry-run-gated** (2026-05-25). The `[mcp.sentinel]` reversibility block omitted `mcp_health_check`/`mcp_list_servers`/`mcp_restart_server` and the step-granularity tools (`get_step_chain`/`get_step_proof`/`get_active_step`/`submit_step_complete`/`submit_linear_step`), so they fell to the unknown-MCP `Irreversible` default and got blocked behind the A3 dry-run gate — sentinel blocking its own observability tools. Added them (reads `TriviallyReversible`, mutations `ReversibleWithEffort`). Also pinned the memory dual-judge `CODEX_MODEL` from the stale `openai/gpt-5.4` to `openai/gpt-5.5-pro` (memory repo) to match the four-model set, which was forcing every memory capture into quarantine.

### Added
- **Adversarial AI judge integrated into the enforcement path + recalibrated** (2026-05-25). The judge (Kimi/GPT-5.5/Opus multi-model + proof chain) was built and tested but wired to nothing mandatory: `phase_gate` is static, `step_judge` fired on no hook event, and the judge was only reachable via the opt-in `submit_phase_complete` MCP tool (where the agent even supplied its own verdict). A live pressure test (`crates/sentinel-infrastructure/tests/live_judge_pressure.rs`) also found all four judge tiers over-blocked genuinely-sufficient evidence. Fixes: (1) recalibrated both adversarial system prompts (`rig_judge`, `dry_run_auditor`) so concrete proof PASSES while unproven claims still FAIL, plus injection-echo hardening; (2) wired `step_judge` into the `PostToolUse` dispatch for the `mcp__skills__<skill>__step_<id>` namespace so completed steps are independently judged; (3) added `judge_enforcement::Mode` (`SENTINEL_JUDGE_ENFORCEMENT` = shadow|warn|enforce, default **shadow** — zero behavior change until promoted), with `submit_step_complete` refusing to seal a non-sufficient verdict in `enforce`. Design doc: `docs/judge-integration-and-calibration.md`. Pressure test green (all tiers PASS proven work, FAIL bad work, no injection echo); 2218 workspace tests pass. A live end-to-end test (`crates/sentinel-cli/tests/e2e_judge_integration.rs`) drives `step_judge::process` — the exact function the `PostToolUse` dispatch calls — through the real `MultiModelJudge` adapter to OpenRouter and confirms a completed step tool produces a verdict (live: `sufficient=true`, confidence 0.86 via `openai/gpt-5.5`, the same evidence all four tiers rejected pre-recalibration).

### Fixed
- **JudgeModel slugs were uncallable / stale** (2026-05-25). A live probe against the operator's OpenRouter key revealed the shipped judge tiers pointed at models that don't work: `Haiku` → `cerebras/glm-4.7` (does not exist on OpenRouter), `Kimi` (the DEFAULT review tier) → `moonshotai/kimi-k2.6` (returns 404 — "no allowed providers" for the operator's routing), `Sonnet` → `openai/gpt-5.4` (stale, and disagreed with `agents-defaults.toml`'s `gpt-5.5`). Re-mapped to slugs verified callable and measured for latency + verdict quality with a real adversarial-judge prompt: `Haiku` → `openai/gpt-5.4-nano` (1.3s), `Kimi` → `moonshotai/kimi-k2-thinking` (callable Kimi variant), `Sonnet` → `openai/gpt-5.5` (newest frontier, ends the config drift); `Opus` → `anthropic/claude-opus-4.7` unchanged. Critical-strict trio retains genuine vendor diversity (moonshot + openai + anthropic, all confirmed callable). 2210 tests green.

### Fixed
- **PowerShell + sequential-thinking reversibility deadlock** (2026-05-25). The `LayeredReversibilityClassifier` omitted the `PowerShell` tool from `builtin_class()`, so every PowerShell call fell through to the conservative `Irreversible` default and tripped the A3 `dry_run_then_commit` gate. That gate demands `_intent`/`_reasoning`/`_expected_effect` keys inside `tool_input`, but the PowerShell tool schema is `additionalProperties: false` and rejects them — an unbreakable deadlock on Windows, where PowerShell is the primary shell. A second deadlock compounded it: `tool_usage_gate` requires a `sequentialthinking` call before code edits, while the unknown-MCP-server default (`Irreversible`) blocked the `mcp__sequential-thinking` tool itself. Fix routes `PowerShell` through the shared Layer-3 command-pattern path (it carries the same `command` field as Bash), adds PowerShell destructive patterns (`Remove-Item -Recurse -Force`, `Format-Volume`, `Clear-Disk`, `Remove-Partition`, `Initialize-Disk`, registry-key deletes → Catastrophic), and adds `[mcp.sequential-thinking] sequentialthinking = "TriviallyReversible"`. 7 regression tests added; 329 infrastructure+application tests green.

### Added
- **Tier 3 production hardening: TLS docs + Prometheus + multi-legatus-ai-daemon failover + token-rotation surface** (2026-05-24). Four production-quality additions on top of the reliable-connection foundation:

  1. **`GET /legatus/metrics`** — Prometheus text-exposition format endpoint, no external scrape crate needed (3 metric families, hand-rolled). Surfaces `legatus_connection_state` (gauge 0-3), `legatus_connection_attempts_total` (counter), `legatus_outbox_pending` (gauge). Same bearer-auth path as the rest of the local API. Mounted on the daemon at `/legatus/metrics` alongside `/legatus/health`. The outbox-depth read is offloaded to `spawn_blocking` so a contended mutex never stalls the axum runtime.

  2. **Multi-legatus-ai-daemon failover** — new `--legatus-legatus-ai-daemon-failover-url` (repeatable) on `sentinel daemon`. The reconnect wrapper now iterates `[primary, ...failover]` in priority order each attempt before backing off. New `ConnectConfig.failover_urls: Vec<String>` field carries the list. Failover preference is **not** persisted across attempts — every new attempt restarts from primary so a transient primary outage doesn't permanently demote the primary. `VersionMismatch` becomes fatal only when it fires on the LAST URL (any failover gets a chance first). All four existing tests + one new test still green.

  3. **`PraefectusClientError::TokenInvalid`** — new error variant distinct from `Verification`. 401 from `HttpPraefectusClient` now produces `TokenInvalid(body)` instead of being lumped with cryptographic verification failures. The `PraefectusClientWitnessVerifier` adapter logs `TokenInvalid` at `warn!` with an operator-actionable message ("Praefectus rejected the bearer token. Rotate LEGATUS_PRAEFECTUS_TOKEN and restart sentinel daemon"); other errors stay at `debug!` to avoid log noise per-witness. Operators tailing logs see exactly when their token has expired vs. when a witness is actually invalid.

  4. **TLS / wss:// support** — documented production path. tokio-tungstenite is built with `rustls-tls-webpki-roots` so `wss://` URLs work out of the box against any publicly-trusted certificate (Let's Encrypt, AWS ACM, etc.). Two patterns documented in the operator runbook: (a) legatus-ai-daemon terminates TLS directly, (b) reverse-proxied via nginx/Caddy/Cloudflare. **Custom-CA injection (self-signed dev certs)** is flagged as a Tier 3 follow-up — requires adding `rustls` + `rustls-pemfile` direct deps + `connect_async_tls_with_config` plumbing.

  Runbook gained TLS + multi-legatus-ai-daemon failover + voice-loop verification sections (manual operator procedure + which automated tests cover which parts of the catastrophic-ack flow). Workspace tests still all-green (~2466 tests).

### Added
- **Daemon ergonomics: PID file + `sentinel stop` + launchd template + `sentinel legatus init` + `sentinel legatus status` + connection-event JSONL log** (2026-05-24). Four operator-facing improvements that together make the daemon usable as a long-lived service instead of a one-off process:

  1. **PID file + `sentinel stop`** (Unix-only). Daemon now writes `~/.claude/sentinel/daemon-pid` at startup and refuses to start if an existing PID is alive (prevents accidental double-up on the same token file; stale PIDs are silently overwritten). New `sentinel stop --wait-secs <N>` shells out to `/bin/kill -TERM`, polls for process exit, and cleans up both the PID file and the `daemon-token` file as a belt-and-suspenders measure (tokio's default SIGTERM handler aborts the runtime before the daemon's post-serve cleanup code can run, so a stop-side cleanup is required to leave a clean slate). Uses `/bin/kill` shell-out rather than `libc::kill` so the workspace's `unsafe_code = "forbid"` lint stays intact.

  2. **launchd plist template** at `scripts/launchd/com.anthropic.sentinel.plist.template`. Placeholders for `{{SENTINEL_BIN}}`, `{{LEGATUS_AI_DAEMON_URL}}`, `{{BOOTSTRAP_SECRET_PATH}}`, `{{HOME}}`, `{{LOG_DIR}}`. Wraps the daemon in a tiny shell that reads the bootstrap secret from a 0600 file and injects it via env (launchd's `EnvironmentVariables` can't read from a file, and the secret should never appear in `ps`). `KeepAlive=true`, `ThrottleInterval=10`, `ProcessType=Background`.

  3. **`sentinel legatus init`** — generates a 32-byte bootstrap secret via `getrandom::OsRng`, hex-encodes, and either prints to stdout or writes to `--output <PATH>` with mode 0600 (creating parent directories as needed). Refuses to overwrite an existing output file without `--force` — silent rotation can lock running legati out of the Legatus AI.

  4. **`sentinel legatus status [--json]`** — reads `~/.claude/sentinel/daemon-token`, calls `GET /legatus/health` + `GET /legatus/pending`, pretty-prints connection state + outbox depth (or emits combined JSON for `jq`retiredlocal clients). Surfaces "daemon not running" cleanly when the token file is missing.

  5. **Connection-event JSONL log** at `~/.claude/sentinel/state/legatus-connection-events.jsonl`. New `crates/sentinel-legatus/src/connection_event_log.rs` provides `ConnectionEvent { t, state, attempt, reason? }` + `ConnectionEventLog` (append-only writer, best-effort: I/O failures `warn!` rather than block the WS loop). Wrapper writes one line per state transition (Connecting / Connected / Reconnecting{reason} / Disconnected{reason?}). `ConnectionStatus` gained an attached `AtomicU64` attempt counter (`bump_attempt()` / `attempt()`) so the event log entries and `/legatus/health` JSON both carry "how many reconnects has this daemon survived?" `/legatus/health` JSON now `{ status, attempt }`. 5 unit tests cover create-on-append, parent-dir auto-create, FIFO ordering, `skip_serializing_if` for None reason, and reason-carrying disconnect. End-to-end: started a daemon, killed it, restarted, verified the JSONL gained the right sequence of transitions with timestamps.

  Workspace tests still all-green (~2466 tests, +5 new).

### Fixed
- **Five dead-wired hooks: declared in HOOK_NAMES, never invoked** (2026-05-24). An audit of `crates/sentinel-cli/src/hook_cmd.rs` against `crates/sentinel-application/src/hooks/mod.rs::HOOK_NAMES` found six hooks that had complete `process()` functions with unit tests but **were never called from the production dispatcher**. They were silently inert in every real session. Wired five of them; one (`spec_challenge_gate`) deferred because it needs external store + scorer wiring that isn't in `hook_cmd.rs` today.
  - `catastrophic_escalation` (PreToolUse) — **the headline bug**. The entire voice-attested catastrophic-action loop was dead from the sentinel side. Catastrophic Bash commands were not being intercepted; no `SessionBlocked{CatastrophicPending}` ever reached Legatus AI. The whole point of the legatus connection was to carry these signals.
  - `agent_revocation` (PreToolUse) — the agent kill-switch was inert; `sentinel agent revoke <id>` wrote state nobody read.
  - `constitution_gate` (PreToolUse) — the structural-rules enforcer the previous changelog called "SHIPPED" was actually never called. Operators who wrote `~/.claude/sentinel/config/constitution-gate.toml` saw no enforcement.
  - `step_gate` (PreToolUse) — the M1.1 step-level prereq gate. Skills that depended on per-step proofs got no enforcement.
  - `prompt_injection_nudge` (PostToolUse) — the injection-detection warning was never added to additionalContext on any tool result.

  Each wired with the appropriate dependencies (`DaemonApprovalChecker` for catastrophic; `&state` for agent_revocation; existing `step_configs` HashMap for step_gate; `constitution_rules: Vec<Rule>` loaded once at hook entry from `~/.claude/sentinel/config/constitution-gate.toml` with graceful no-op on missing/malformed file). Each routed through `time_and_record` so they show up in `sentinel stats`. Workspace tests still all-green (~2461 tests).

### Added
- **End-to-end hook validation via extended smoke test** (2026-05-24). `scripts/smoke-sentinel-Legatus AI-roundtrip.sh` now drives a real hook subprocess (`sentinel hook --event PreToolUse` with a catastrophic `rm -rf /` command) against the running daemon, asserts the hook denies locally (parses stdout JSON for `permissionDecision=deny`), AND asserts the resulting `SessionBlocked{CatastrophicPending}` actually arrives on the legatus-ai-daemon's WebSocket (greps the debug log for `escalation event ... kind="blocked"`). This pins the entire chain — hook subprocess → HTTP POST → daemon `LegatusHandle.escalate()` → WS frame → legatus-ai-daemon session_loop. Without this test, the dead-wired `catastrophic_escalation` would still be silently inert. The smoke also runs under an isolated `$HOME` (`mktemp -d`) so the smoke daemon's token file never clobbers the operator's real `~/.claude/sentinel/daemon-token`. Pass observed end-to-end on macOS.

- **Operator setup runbook** (2026-05-24). `docs/runbooks/operator-setup-sentinel-Legatus AI.md` — full zero-to-connected path: build both binaries, generate bootstrap secret, start legatus-ai-daemon, start daemon, verify `/legatus/health`, optional operator binding, troubleshooting common failure modes (stuck in `connecting`, 401 on token, daemon dies on schema mismatch, hook can't reach daemon).

- **Legatus connection-status observability + `GET /legatus/health` + shell smoke test** (2026-05-24). Operators (and the new smoke test) can now see whether the daemon's hosted legatus is actually connected to Legatus AI without grepping logs. Three pieces:

  1. **`ConnectionStatus` type** (`crates/sentinel-legatus/src/connection_status.rs`) — clone-cheap handle wrapping an `Arc<AtomicU8>` that encodes one of `Disconnected` / `Connecting` / `Connected` / `Reconnecting`. Lock-free reads + writes so the HTTP handler never blocks the hot WebSocket loop. Defensive `from_u8` decodes unknown bytes to `Disconnected` and never panics. 5 unit tests pin defaults, round-trip, clone-sharing semantics, unknown-byte handling, and the JSON wire strings.

  2. **Wrapper publishes transitions** — `run_connect_hosted` writes `Connecting` on entry and `Connected` after handshake + registration. `run_connect_hosted_with_reconnect` writes `Reconnecting` between failed attempts and `Disconnected` on terminal exit (cancel, fatal `VersionMismatch`). A new `client::tests::reconnect_loop_observes_status_transitions` unit test pins the externally-observable transitions: fresh → `Disconnected`, after first failure → `Reconnecting`, after cancel → `Disconnected`.

  3. **`GET /legatus/health`** on the daemon — bearer-authed JSON endpoint returning `{ "status": "connected" | ... }`. Added as a third state-typed sub-router merged into the existing legatus route surface so smoke tests and local clients can poll it directly. Mounted on the same daemon port as `/legatus/escalate` etc.

  4. **`scripts/smoke-sentinel-Legatus AI-roundtrip.sh`** — end-to-end shell smoke that builds + drives a real `sentinel daemon` against a real `legatus-ai-daemon` over real WebSocket on ephemeral ports, polls `/legatus/health` until `connected`, kills the legatus-ai-daemon to verify the daemon transitions to `reconnecting`, restarts it to verify it goes back to `connected`. Cleans up via EXIT trap. The smallest possible end-to-end proof that the two binaries actually talk on the wire, beyond the in-process Rust tests.

  **API change**: `run_connect_hosted` and `run_connect_hosted_with_reconnect` now require a `ConnectionStatus` argument (the former by `&`, the latter by-value clone). All in-tree callers updated; the standalone `run_connect` constructs a fresh one and discards it (no observer). Workspace tests still all-green (~2461 tests, +6 new).

### Fixed
- **Sentinel-legatus auto-reconnect was a no-op for in-loop transport failures** (2026-05-24). The reconnect wrapper added yesterday only retried on handshake / encode errors — the actual common failure modes (legatus-ai-daemon-initiated WS close, recv error, stream end, mid-session send failure) all `break None` inside the loop and returned `Ok(())`, which the wrapper read as "session exited cleanly; do not reconnect." So a real TCP drop / legatus-ai-daemon restart / network blip produced exactly one connection attempt and then the daemon went silent. Refactor: introduced `ExitReason::{Cancelled, TransportFailed(String)}` to discriminate the cancel path (returns `Ok(())`, sends graceful `SessionCompleted`) from every transport-side exit (returns `Err(LegatusError::Transport(reason))`, skips the doomed `SessionCompleted` send). The reconnect wrapper now actually reconnects on the failure modes it was supposed to handle. Two new regression tests pin this:
  1. **Unit:** `client::tests::reconnect_loop_keeps_retrying_after_repeated_transport_failure` — runs the wrapper against a guaranteed-refused address, asserts it has NOT exited after 1.5s (one attempt + one full backoff + one more attempt), then asserts cancel still produces `Ok(())`.
  2. **Integration:** `round_trip_legatus-ai-daemon::run_connect_hosted_surfaces_transport_failure_on_remote_close` — spawns a legatus-ai-daemon that handshakes + registers then immediately drops the WS, asserts `run_connect_hosted` returns `Err(LegatusError::Transport(_))` (pre-fix this returned `Ok(())`). The panic message in the test calls out the exact pre-fix bug for future readers.

  Workspace tests still all-green (~2455 tests, +2 new).

- **Dry-Run Auditor + Skill Gate deadlock on harness read-only tools (2026-05-23).** `builtin_class` in `crates/sentinel-infrastructure/src/reversibility.rs` only whitelisted 10 tool names, so `Skill`, `ToolSearch`, `AskUserQuestion`, `TaskGet`, `TaskOutput`, `EnterPlanMode`/`ExitPlanMode`, and the `Agent`/`Team`/`Cron`/`Worktree` lifecycle tools cascaded into the conservative `Irreversible` default. That made `dry_run_then_commit` demand `_intent`/`_reasoning`/`_expected_effect` on every call, which deadlocked the agent against `skill_gate`: the skill gate required `Skill(...)` before any `Bash`, but the auditor refused `Skill(...)`. Expanded `builtin_class` to 25 tools across two classes — `TriviallyReversible` for pure reads / UI prompts / schema lookups (+ `TaskGet`, `TaskOutput`, `AskUserQuestion`, `Skill`, `ToolSearch`, `ShareOnboardingGuide`, `Monitor`, `CronList`, `LSP`) and `ReversibleWithEffort` for local / in-conversation mutations (+ `NotebookEdit`, `TaskStop`, `EnterPlanMode`, `ExitPlanMode`, `EnterWorktree`, `ExitWorktree`, `Agent`, `TeamCreate`, `TeamDelete`, `SendMessage`, `CronCreate`, `CronDelete`, `ScheduleWakeup`, `PushNotification`, `RemoteTrigger`). None of these reach external services or shared infrastructure. Doc comment names `skill_gate.rs` as the mirror surface when adding new built-ins. Drive-by: `sentinel-legatus/src/client.rs` carries `operator_id: None` on `RegisterSession` to compile against the Phase-6 schema; expanded `mcp.linear` coverage in `config/reversibility-defaults.toml`.

### Changed
- **Steel → Browserbase/CDP marketplace-wide rename + browser-testing architecture split (2026-05-23).** Retired the "Steel" brand across the entire marketplace and split browser testing by URL surface: **localhost → CDP** (`mcp__cdp__*` via the edge-cdp skill, direct hit, no tunnel), **remote URLs (preview/staging/prod) → Browserbase** (`mcp__browserbase__*`, full 48 tools). Research confirmed Browserbase has no localhost support, so the old cloudflared-tunnel-then-Browserbase localhost dance was pure ceremony — CDP replaces it. **Sentinel Rust+config:** completed the partial `pre_push_steel_test.rs`→`pre_push_browser_test.rs` rename (`STEEL_TEST_VALIDITY`→`BROWSER_TEST_VALIDITY`, `repo_has_steel_config`→`repo_has_browser_test_config`, `ext.steel_tools`→`ext.browserbase_tools`, `parse_steel_tools`→`parse_browserbase_tools`, regex `Steel MCP`→`Browserbase MCP`); PostToolUse browser-test recorder accepts `mcp__browserbase__release_session` + `mcp__cdp__close_instance` + legacy `mcp__steel__release_session`; `config/workflows.toml` `[steel]`→`[browserbase]`, `[steel-tester]`→`[browserbase-tester]`; renamed `config/steps/steel.toml`→`browserbase.toml` (old federation `browserbase.toml`→`browserbase-federation.toml`), `steel-tester.toml`→`browserbase-tester.toml`; `config/steps/linear.toml` Step 0 rewritten from cloudflared+Browserbase to `mcp__cdp__*`; `mcp__steel__*` detection retained in `phase_gate.rs`/`mcp_health.rs` as defense-in-depth. **New phase-gate override:** HMAC-signed override (`override_phrase.rs` `PHASE_GATE_OVERRIDE_PATTERNS`/`is_phase_gate_override`, `hygiene_override.rs` `phase_gate_override_path`/`is_signed_override_active_with_ttl`, 600s TTL) so marketplace skill refactors can edit `SKILL.md`/`phases/*.md` (scoped — sentinel config/state/settings stay protected). **Marketplace skills:** linear (review.md Layer 2.5→CDP, qa-handoff.md Step 0→CDP, SKILL.md v9, `steel_test_*`→`browser_test_*` project fields, cloudflared removed), browserbase (SKILL.md v6 REMOTE-ONLY, 7 phase Workflow renames, SECURITY.md rewrite), edge-cdp (v2 canonical-localhost), browserbase-tester (v2), cloudflare-tunnel (v2 "not for browser testing"), `_shared/browserbase-patterns.md` (Cloudflare Tunnel Pattern → CDP Localhost Pattern, `__steelErrors`→`__browserErrors`), `_shared/project-context.md` schema, plus ~17 ancillary skills. Validation: zero accidental Steel refs remain; cargo build + test green.

### Added
- **Sentinel-legatus reliability: per-operator binding + auto-reconnect** (2026-05-23). Closes the last two gaps blocking the daemon from being a production-quality Legatus AI client. Two related changes:

  1. **`ConnectConfig.operator_id: Option<OperatorId>`** — `sentinel daemon --legatus-operator-id <UUID>` now flows the operator's UUID through `ConnectConfig` into `RegisterSession.operator_id`. The legatus-ai-daemon populates `Session.owner` from it, and the voice-attested gate's `OperatorResolverPort` (Phase C3) routes per-session escalations to the correct operator's voice gate. Before this, every sentinel session registered as `OperatorId::ROOT` and the gate's per-session lookup defeated itself. `None` (the default) preserves the v0.1 single-operator-as-ROOT scaffold for hosts that haven't bound an identity yet. Re-exported `sentinel_legatus::OperatorId` so callers don't need a direct path-dep on `legatus-ai-domain`.

  2. **`run_connect_hosted_with_reconnect`** — new wrapper around `run_connect_hosted` with exponential-backoff reconnect (1s → 2s → 4s → … capped at 30s, `INITIAL_RECONNECT_BACKOFF` and `MAX_RECONNECT_BACKOFF` constants). Transient transport / handshake / heartbeat failures retry; `LegatusError::VersionMismatch` surfaces as fatal because reconnecting against a protocol-incompatible legatus-ai-daemon won't help. Honors the cancel `Notify` during the backoff sleep so Ctrl-C interrupts cleanly. The daemon's hosted-legatus path uses the wrapper; the standalone `sentinel legatus connect` CLI path keeps `run_connect` (one-shot) semantics.

  **API change**: `run_connect_hosted` now takes `runtime: &mut LegatusRuntime` instead of by-value. The reason is that the reconnect wrapper must reuse the SAME runtime across iterations — rebuilding it via `make_pair()` would detach the caller's `LegatusHandle` channel ends and silently break inbox/outbox push and escalation drain after the first reconnect. With `&mut`, the wrapper owns the runtime for its lifetime and the handle-paired channels stay hot across the entire reconnect arc, so escalations queued during an outage drain on the next successful connection. All in-tree callers (`run_connect`, the 4 round-trip integration tests) updated.

  Tests: 3 new unit tests in `client::tests` — `reconnect_loop_honors_cancel_during_backoff` (real-time, ~200ms wait + cancel; asserts cancel arm wins over 1s sleep), `reconnect_loop_surfaces_version_mismatch_as_fatal` (pins backoff constants and their relationship), `connect_config_operator_id_defaults_to_none` (asserts scaffold default preserves ROOT semantics). All 11 client tests green; full sentinel-legatus suite 83/0. Workspace tests still all-green (2450+ tests, no regressions).

- **Constitution gate — runtime enforcement of structural project rules** (2026-05-19). Item K from the Phase 2 punch list. New `PreToolUse` hook (`constitution_gate`) that loads a per-project policy file (`~/.claude/sentinel/config/constitution-gate.toml`) describing `[[rule]]` tables of `path_prefix` × `path_suffix?` × `banned_patterns[]` × `reason` × `citation?`, and **denies** any `Write` / `Edit` / `MultiEdit` / `NotebookEdit` whose target path is in scope AND whose new content contains a banned literal substring. The deny carries the `[Sentinel-Authority]` prefix so the downstream agent treats it as policy, not negotiable.

  **Generic on purpose.** Sentinel knows nothing about Legatus AI, firefly, or any specific project. The rules live in TOML the operator authors. Missing config = no-op (machines that haven't opted in see no behaviour change). Parse failures log at `warn` and degrade to inert so a typo doesn't take down the daemon's PreToolUse path.

  **Use case**: enforce the same structural rules `constitution.md` already declares (e.g. "`legatus-ai-domain/` has zero I/O dependencies", "no vendor names in `legatus-ai-protocol/`") at the moment they would be violated, not hours later in CI. Shipped example at `config/constitution-gate-example.toml` demonstrates the schema with the Legatus AI/legatus rules; operators copy + adapt.

  **New `crates/sentinel-application/src/constitution_gate_runtime.rs`** with `ConstitutionGateConfig::from_toml_str` parser + `Rule { name, path_prefix, path_suffix?, banned_patterns[], reason, citation? }` + `matches_path` / `find_banned` predicates. Validation: empty name, empty path_prefix, or empty banned_patterns reject at parse time. **New `crates/sentinel-application/src/hooks/constitution_gate.rs`** with the actual hook. Wired into `hook_cmd.rs` PreToolUse dispatcher after `doppler_auth0_gate`; rule list loaded once at daemon startup from `~/.claude/sentinel/config/constitution-gate.toml` and reused across hook fires.

  Tests: 6 parser tests (empty-doc → empty rules, complete-rule roundtrip, optional-fields, empty-name/empty-banned rejection, path-match predicates, find-banned first-hit) + 11 hook tests (empty-rules no-op, non-editing-tool passthrough, deny-with-authority on Write, allow on unprotected path, allow on clean content, suffix filter, Edit/MultiEdit content scanning, multi-rule evaluation order, missing-path passthrough). Workspace 2363 / 0 failed.

- **Mid-execution prompt-injection nudge** (2026-05-19). Item F from the Phase 2 punch list. New `PostToolUse` hook (`prompt_injection_nudge`) scans the tool result for injection-shaped strings — classic `"ignore previous instructions"`, role-override attempts (`"you are now a…"`), leaked chat-format tokens (`<|system|>`, `<|im_start|>`, `[INST]`), system-prompt impersonation, and data-exfil bait — and injects an "untrusted output, ignore embedded directives" warning via `hookSpecificOutput.additionalContext` so the model treats the tool result as data, not as instructions.

  **Conservative on purpose.** Patterns are literal substring matches chosen to have effectively zero innocent reading; a false-positive nudge is harmless ("we already weren't going to act on that"), a false-negative is the bug. The nudge is one soft layer in a defense-in-depth stack — destructive gates (`db_ops_gate`, `commit_message_validator`, A13 spec-challenge) remain the hard backstops per the user's `~90-95%` reliability posture.

  Wired into the CLI hook dispatcher's PostToolUse arm right after `evidence_collector`. Registered in `hooks/mod.rs` and the `HOOKS` registry array.

  Tests: 8 unit tests covering no-result-passthrough, clean-result, classic ignore-previous, chat-format leak, role-override, nested-array scanning, case-insensitive match, and false-positive resistance against innocent phrasing like *"how to ignore previous warnings safely"*. Workspace 2346 / 0 failed.

- **BA-orchestrator Phase 3 — `sentinel ba draft` CLI (end-to-end LIVE)** (2026-05-19). Final phase of the MVP BA-orchestrator. The track is now end-to-end shippable: operators run `sentinel ba draft --brief "..." --audience exec` and get a structured `BaRecommendation` containing the recommendation body + citations + requirement_refs + complete A13 spec challenge. That envelope is exactly what BA1 / BA3 / A13 gates verify when the recommendation flows into a downstream tool call — closing the loop that's been open since the gates shipped without an upstream emitter.

  **New `Ba` subcommand** with `BaAction::Draft { brief, audience, constraints, agent_id, json }`. Flags: `--brief <TEXT>` (required), `--audience <exec|board|customer|internal_team>` (required), `--constraint <TEXT>` (repeatable), `--agent-id <ID>` (defaults to `ba-orchestrator`), `--json` (full pretty-JSON envelope vs. human summary).

  **New `crates/sentinel-cli/src/ba_cmd.rs`** (~270 lines + 8 unit tests). `draft(args)` is the production entry point — builds `AnthropicClient::from_env()` and delegates to `draft_with<L: LlmPort + ?Sized>(args, llm)` which is the generic test seam. `parse_audience` accepts canonical snake_case keys (`exec` / `board` / `customer` / `internal_team`) plus aliases (`internal-team`, `internal`) and is case-insensitive. Unknown audience strings error with operator-facing guidance naming the valid options. Human-readable `render_summary` prints recommendation id, audience, timestamps, citation/requirement counts, the structural-readiness flag, then the brief, body, and per-citation breakdown.

  **`crates/sentinel-cli/src/main.rs`**: new `Ba` Commands variant routes to `ba_cmd::draft`. `crates/sentinel-cli/Cargo.toml`: added `async-trait` as a dev-dependency so the test `StubLlm` can implement `LlmPort`.

  8 tests cover: well-formed args succeed in JSON mode, summary mode succeeds with constraints, unknown audience rejected with descriptive error, blank brief surfaces the orchestrator's `InvalidRequest` ("orchestrator failed"), `parse_audience` accepts all 4 canonical keys, case-insensitivity (EXEC, Board), internal-team aliases, garbage strings rejected with both the bad value and the valid set in the message. Sentinel-cli: **166 tests passing (158 prior + 8 new).** Zero new clippy warnings.

  **End-to-end demo flow now works.** Set `ANTHROPIC_API_KEY` and run `sentinel ba draft --brief "scale the platform for 10x users" --audience exec`. The orchestrator emits a `BaRecommendation` with the structural readiness for sentinel's gates. Real connector pulls (real `ArtifactReference`s from Linear / Confluence) are BA6 work; for now the model emits plausible citations that BA1 will reject against the empty audit chain — which is the correct behavior and demonstrates the enforcement loop.

- **Per-turn tool-call trace surfaces in `InstructionResult` summary** (2026-05-19). Item E from the Phase 2 punch list. Every `PostToolUse` during a turn now appends the tool name to a per-session trace file; the `Stop` hook drains it and prepends a `[tools: A, B, C]` tag to the per-instruction Result summary, so the operator sees which tools the legatus actually ran in response to their relayed instruction.

  Per-turn (not per-tool-call) granularity — when `Legatus AI_inbox` drains multiple instructions into the same model turn, every instruction's Result gets the same tool list. Honest reporting at the model-turn boundary; true per-tool-call attribution needs model-cooperative emission which is a follow-up.

  **New `legatus_client::note_tool_call` + `take_tool_calls`** mirror the disk-FIFO discipline of the existing pending-instructions + turn-signals trace files: path-traversal guard, best-effort I/O, drain-on-read. **`evidence_collector` PostToolUse hook** records every tool call into this trace. **`execution_log` Stop hook** dedups while preserving first-seen order, formats `[tools: …]` (max 6 distinct, then `…+N more`), 140-char-capped to match the existing summary ceiling. **`stop_failure`** also drains the trace so failure paths don't leak stale state.

  Tests: +4 unit tests on `legatus_client` (roundtrip, blank-skip, path-traversal guard, missing-session), +4 on `enrich_summary_with_tool_calls` (no-tools-passthrough, dedup, 6-cap, blank-base). Workspace 2330 / 0 failed.

- **BA-orchestrator Phase 2 — `ba_orchestrator::draft` use case** (2026-05-19). Second commit of the BA-orchestrator track. The orchestrator's job: turn a [`BaDraftRequest`] into a [`BaRecommendation`] — the envelope sentinel's BA1 / BA3 / A13 gates were designed to verify. Application-layer; consumes the existing `LlmPort` so any rig-backed adapter works as the LLM substrate.

  **New `crates/sentinel-application/src/ba_orchestrator.rs`** (~430 lines + 13 unit tests). `pub async fn draft<L: LlmPort + ?Sized>(request, llm, agent_id, clock) -> Result<BaRecommendation, BaOrchestratorError>` is the production entry point; `draft_with_model` lets callers override `LlmModel` + `max_tokens` (defaults: `Opus`, `4096`). One LLM call per draft — the system prompt instructs the model to emit a single JSON document with the recommendation body, citations, requirement_refs, AND a complete A13 `SpecChallenge`. The response is parsed strictly: missing or malformed fields surface as `BaOrchestratorError::Malformed` rather than silently degraded outputs.

  **Reversibility-class mapping** (`reversibility_for_audience` public so operators iterating can override): `Exec` / `Board` → `Catastrophic` (A13 scorer engaged), `Customer` → `Irreversible`, `InternalTeam` → `ReversibleWithEffort`. The prompt threads this through so the LLM emits a `SpecChallenge` with the correct class — matching what the downstream A13 gate will compute via the existing reversibility classifier.

  **`BaOrchestratorError { InvalidRequest, Backend, Malformed }`** with operator-facing `Display`. `InvalidRequest` fires when `BaDraftRequest::is_well_formed()` returns false (blank brief) — the LLM is **not called** in this case, saving the operator a wasted API call on a known-bad input. `Backend` wraps `LlmPort::complete` failures; `Malformed` wraps JSON parse failures with a 200-char head preview for operator triage.

  **`build_prompt`**: composes the system prompt with the brief, audience (snake_case key for serde stability), constraints (numbered list when present, `(none stated)` when empty), and expected reversibility class. The prompt explicitly instructs the model on the A13 completeness rules ("each category MUST have either items OR an `explicit_assertion_of_none` reason — silent empties are rejected"), the ambiguity-≥-2-interpretations rule, and the `InferredFromContext` → non-empty `inference_source` rule. This pushes the work upfront so the model produces gate-passing output rather than relying on retry loops.

  **What this commit does NOT do**: connector pulls (real `ArtifactReference`s from Linear / Confluence / etc.) are BA6 work; persistence is a downstream concern; the gates themselves run in `hook_cmd.rs` when the recommendation is serialized into a tool's `extra` payload. Phase 2 closes the "agent emits a verifiable artifact" loop; Phases 3+ wire it to operators (CLI + MCP).

  13 tests with a `StubLlm` (records last request, returns canned response or error) cover: well-formed request → fully-typed `BaRecommendation` with `is_structurally_ready_for_publication() == true`, blank brief rejected before LLM call (LLM `last_prompt` is `None` — verified), LLM failure surfaces as `Backend`, non-JSON response as `Malformed`, markdown code-fence stripped, prompt threads brief + audience (`board`) + constraints (`no vendor lockin` + `cite ≥ 2 sources`) + reversibility class (`Catastrophic` for Board), empty constraints render as `(none stated)`, `recommendation_id` synthesized from clock (`rec-1700000000` for fixed clock), `reversibility_for_audience` maps all 4 audiences correctly, error `Display` covers each variant, `std::error::Error` impl, preview truncates / passes through. `crates/sentinel-application/src/lib.rs` exposes the new `ba_orchestrator` module. Sentinel-application: **1240 tests passing (1227 prior + 13 new).** Zero new clippy warnings.

- **BA-orchestrator Phase 1 — recommendation domain types** (2026-05-19). First commit of the BA-orchestrator track. Sentinel has shipped the verification gates (BA1 `provenance_validate`, BA3 `requirements_traceability_gate`, A13 `spec_challenge_gate`) — all structurally LIVE — but nothing upstream emits the artifacts those gates verify. The orchestrator track closes that loop. Phase 1 ships the output envelope as pure domain; future phases ship the orchestration use case, the connector wiring, and the operator-facing surface (`sentinel ba draft` CLI + MCP tool).

  **New `crates/sentinel-domain/src/ba/recommendation.rs`** (~360 lines + 14 unit tests). Identifiers: `RecommendationId` newtype (whitespace-trimmed; rejects empty). `StakeholderAudience { Exec, Board, Customer, InternalTeam }` with stable `key()` for serialization (`"exec"` / `"board"` / `"customer"` / `"internal_team"`) — drives both prompt scaffolding when drafted and the A12 `StakeholderFit` axis when benchmarked.

  `BaDraftRequest { brief, stakeholder_audience, constraints }` is the operator-supplied input — `is_well_formed()` rejects whitespace-only briefs, allows empty constraints. `BaRecommendation { recommendation_id, brief, stakeholder_audience, body, citations: Vec<ArtifactReference>, requirement_refs: Vec<RequirementRef>, spec_challenge: SpecChallenge, generated_at, agent_id }` is the output envelope. Every field maps to a sentinel-gate input: `citations` → BA1's `extra.artifacts`, `requirement_refs` → BA3's `extra.requirement_refs`, `spec_challenge` → A13's `extra.spec_challenge`. So when an orchestrator emits a `BaRecommendation` and serializes its fields into a downstream tool's `extra` payload, sentinel's three gates verify the output structurally with zero orchestrator-side knowledge of hook plumbing.

  Two helpers on `BaRecommendation`: `is_structurally_ready_for_publication()` (at least one citation, at least one requirement_ref, structurally-complete spec_challenge — the *structural* floor below which the recommendation can't ship; gate verdicts add the audit-chain / matrix-match / scorer-quality layers on top), and `distinct_citation_count()` (dedupes by `artifact_id` so the "minimum cite-count" operator constraint isn't gamed by repeated citations of the same source).

  14 tests cover: `RecommendationId` empty rejection + whitespace trimming, `StakeholderAudience::key()` snake_case stability across all 4 variants, `BaDraftRequest::is_well_formed()` requires non-blank brief and tolerates empty constraints, `is_structurally_ready_for_publication()` true on a complete recommendation, false without citations, false without requirements, false with incomplete spec_challenge, `distinct_citation_count` dedupes repeated artifact_ids (3 entries with one duplicate → 2 distinct) and returns 0 for empty, full serde round-trip for both `BaDraftRequest` and `BaRecommendation`, `Send + Sync` for all four exported types. `crates/sentinel-domain/src/ba/mod.rs` re-exports the new types alongside the existing BA1+3 types. Sentinel-domain tests: **559 passing (545 prior + 14 new).** Zero new clippy warnings.

- **Lifecycle-key support for `EscalationAck` — closes the last gap in the exactly-once arc** (2026-05-19). Sentinel can now remove `SessionBlocked` / `SessionCompleted` / `SessionFailed` outbox entries when Legatus AI acks them by `(session_id, *_at_ms)` — not just `InstructionAck` / `InstructionResult` keyed by `instruction_id`.

  **New `OutboxItem { event: EscalationKind, at_ms: u64 }`** wrapper struct. The `at_ms` is captured at `LegatusHandle::escalate` time (or at-loopback time for the cancel-roundtrip path) AND used in the on-disk entry AND in the envelope the recv loop sends on the wire. That single stamp is what makes ack-matching possible across the network: the legatus-ai-daemon's `EscalationKey { session_id, *_at_ms }` references the *same* timestamp the legatus already stored, so a lookup by key is unambiguous.

  **Wire shape change on the outbox jsonl file**: was bare `EscalationKind` per line; is now `{"event": {...}, "at_ms": ...}`. `parse_entry` provides backwards-compat: tries the new wrapped shape first, falls back to bare-event with `at_ms = 0`. Legacy lifecycle entries lose ack-keyed cleanup (timestamp 0 won't match a real ack) but fall through to the existing `remove_head` post-send path. Acceptable on per-machine upgrade.

  **New `PersistentEscalationOutbox::remove_lifecycle(LifecycleKind, at_ms)`** with the same atomic-rewrite + fs2-locked discipline as `remove_head` / `remove_by_instruction_id`. Scans entries, matches the first `(variant, at_ms)` pair, rewrites the remainder. New `LifecycleKind { Blocked, Completed, Failed }` enum (kept here in `sentinel-legatus` — adapter concern, not a protocol type).

  **mpsc channel type change**: `mpsc::UnboundedSender<EscalationKind>` → `mpsc::UnboundedSender<OutboxItem>`. `LegatusHandle::escalate(event)` keeps the same public signature; the timestamp is stamped internally. The recv loop pops `OutboxItem`, builds the envelope using `item.at_ms` instead of a fresh `now_ms()`. The cancel-loopback in `handle_inbound` now persists to the outbox AND pushes to the loopback channel with the same `at_ms`, so the disk entry and the in-flight loopback share one canonical timestamp.

  **`handle_inbound` lifecycle arms wired**: `EscalationKey::SessionBlocked` / `SessionCompleted` / `SessionFailed` now verify the ack's `session_id` matches the connection's own session_id (defence: don't honour acks on behalf of foreign legati), then call `outbox.remove_lifecycle(kind, *_at_ms)`. Per-instruction arms unchanged.

  **Tests**: 7 new tests in `persistent_outbox` (lifecycle removal for each variant, no-match, variant-mismatch, missing file, per-instruction-skip, legacy-shape parse with `at_ms = 0`). 3 new tests in `client` (end-to-end lifecycle ack removal via `handle_inbound`, wrong-session_id rejection, cancel-loopback persists matching `at_ms`). 1 updated `escalate_succeeds_when_runtime_is_alive` test asserts the timestamp gets stamped. **Sentinel-legatus 52 tests passing, workspace 2295 / 0 failed.**

  The exactly-once arc is now end-to-end for both per-instruction AND lifecycle events.

- **`GET /legatus/pending` — operator-visible queue depths** (2026-05-19). New daemon HTTP route that reports the on-disk `inbox_pending` (operator instructions queued for the next Claude Code prompt) and `outbox_pending` (outbound escalations queued for WS send) as a single JSON snapshot. Drives `Legatus AI status`-style operator surfaces and smoke clients.

  **New `LegatusHandle::persistent_outbox()` accessor** symmetric with the existing `persistent_inbox()`. File I/O wrapped in `tokio::task::spawn_blocking` so the HTTP handler never stalls on the advisory lock. Returns `0`-counts for whichever direction lacks a persistent store (standalone-CLI legati without daemon-hosted disk state). Two new unit tests cover the accessor: seeded `make_pair_with_persistence` returns `Some(&outbox)` with live `len()` tracking through `escalate()`; `make_pair` + `make_pair_with_inbox` (outbox-less variants) honestly return `None` so the route returns `0` instead of panicking. Sentinel-legatus: 42 tests passing (40 prior + 2 new).

- **Sentinel handles inbound `EscalationAck` — closes the exactly-once arc on the consumer side** (2026-05-18). Third and final slice of the exactly-once escalation arc that started with the protocol message + legatus-ai-daemon-side emission. Sentinel's WS recv loop now processes the `Legatus AIMessage::EscalationAck` Legatus AI sends back after forwarding each escalation to its bus; per-instruction acks (`InstructionAcknowledged` / `InstructionResult`) drop the matching entry from the persistent outbox.

  **New `PersistentEscalationOutbox::remove_by_instruction_id`** (~+50 lines + 6 unit tests). Same atomic-rewrite + `fs2`-locked discipline as `remove_head`: open RW, take exclusive lock, scan all lines, remove the FIRST entry whose `EscalationKind` carries the target `instruction_id` (only `InstructionAck` and `InstructionResult` do; lifecycle variants are skipped), rewrite the remainder.

  **`client.rs::handle_inbound` new arm**: `Legatus AIMessage::EscalationAck(ack)` iterates `ack.acks`, calls `outbox.remove_by_instruction_id` for each per-instruction key. Lifecycle keys (`SessionBlocked` / `SessionCompleted` / `SessionFailed`) are no-ops in this slice — matching them by `(session_id, *_at_ms)` requires storing the sent-time timestamp on disk, which is the next refactor in this arc.

  **Delivery semantic now**: dual-track at-least-once with ack-driven cleanup. The existing post-`send_signed` `remove_head` stays as the head-of-line happy-path remover; the new ack-driven `remove_by_instruction_id` is the architectural seam for the next operator who wants true exactly-once-with-timeout-retry. Both paths converge on the same end state — whichever fires first wins; the other no-ops on empty queue.

  6 new outbox tests cover non-FIFO middle removal, the `InstructionResult` headline case (cancel-loopback), no-match, missing file, lifecycle-skip, and dup-defense. Sentinel-legatus: 40 tests passing (34 prior + 6 new). Whole workspace remains green. Zero net-new clippy errors over the pre-existing baseline.

  **The exactly-once arc is now end-to-end** (modulo lifecycle keys): protocol message (slice 1) + legatus-ai-daemon emits acks (slice 2) + sentinel removes outbox entries on ack (slice 3). Operators using the cancel-roundtrip path now have ack-driven cleanup. The post-send `remove_head` stays for graceful degradation when Legatus AI peers don't emit acks.

  **Out of scope (lifecycle-key support)**: storing the sent-time `*_at_ms` on each outbox entry so `remove_by_key(SessionBlocked { session_id, detected_at_ms })` can match by the full key. Currently lifecycle escalations rely on the at-least-once `remove_head` only. Acceptable — lifecycle events are self-correcting; per-instruction events are the ones that need exactly-once semantics for the operator's audit-trail correctness.

### Fixed
- **Operator-visible startup banners for daemon + legatus handshake** (2026-05-23). The `sentinel daemon` startup path and the `run_connect_hosted` Legatus AI-handshake path both emit only `info!` logs, but `tracing_subscriber`'s default filter is `warn` — so operators following the Fabrica `Legatus AI-sentinel-roundtrip.md` runbook saw Terminal 2 completely silent after starting the daemon, with no way to know (a) whether the daemon started, (b) where the bearer token lives, (c) what value to use for `Authorization: Bearer …`, or (d) whether the legatus actually handshook with the legatus-ai-daemon. Symptoms: HTTP 401 on every local API call, with no diagnostic signal.

  **Root cause.** Two failures stacked: (1) every operator-facing diagnostic was an `info!` filtered out at default log level; (2) the file `~/.claude/sentinel/daemon-token` is `{port}:{token}` format, but the auth middleware expects `{token}` only — operators who `cat` the file and paste-as-Bearer get 401, while no signal explains why.

  **Fix.** Added two stderr banners (visible regardless of `RUST_LOG`):
  - `daemon_cmd.rs::run` — startup banner on every daemon launch listing local API URL, token-file path with format note, ready-to-paste `Authorization: Bearer <token>` (no port prefix — matches what the middleware actually checks), and legatus configuration (legatus-ai-daemon URL / display name / heartbeat interval) when `--legatus-*` flags are set.
  - `sentinel-legatus/src/client.rs::run_connect_hosted` — handshake-complete banner emitted immediately after registration succeeds: legatus-ai-daemon URL, registered `session_id`, final `display_name` (post-collision-suffix), heartbeat interval.

  Banners use `eprintln!` so they appear regardless of subscriber configuration; structured `info!` lines remain for production log aggregation.

  Verified live: 2026-05-23 demo run with the new binaries confirmed (a) both banners visible, (b) banner-displayed token works against `GET /legatus/pending` (200 OK + JSON body), (c) `POST /legatus/escalate` returns 202 Accepted, (d) escalation drains the outbox over WS to legatus-ai-daemon with exactly-once-arc ack closure (outbox count returns to 0 within 3s).

- **UTF-8 char-boundary panic in `activity_tracker.rs`** (2026-05-22). The Stop hook panicked on commit messages containing multi-byte UTF-8 characters: `&cmd[..80]` byte-slicing through em-dash `—` (3 bytes) hit `end byte index 80 is not a char boundary` at line 239 (post-fix: line 266 in `process_stop`).

  **Root cause.** Two sites used plain byte-slicing for length-based truncation, which is unsafe for any user-controlled string that may contain non-ASCII characters (commit messages, task descriptions, file paths). The first panic surfaced from a commit body like `git add -A && git commit -m "docs(audit pass 3): CHANGELOG entry — full Pass-3 batch"` where the em-dash sat at bytes 79..82 and the 80-byte truncation cut through it.

  **Fix.** New private helper `truncate_to_char_boundary(s, max_bytes) -> &str` that backs off to the nearest valid UTF-8 char boundary using `str::is_char_boundary` (stable). Applied at both panic sites: `extract_command` (was `&s[..120]`) and `process_stop`'s git-command formatter (was `&cmd[..80]`). Two regression tests added: a direct unit test exercising `"abc—def"` with cuts at multiple offsets through the em-dash, and an end-to-end test reproducing the original panic input.

  Tests: 20 activity_tracker tests passing (18 prior + 2 new). Workspace remains green.

  **Follow-up audit needed.** Similar byte-slice patterns exist in 9 other files: `good_citizen_observer.rs:107`, `memory_extract.rs:426`, `phase_gate.rs:668,724,831`, `session_index.rs:314`, `task_rehydrate.rs:278`, `skill_router.rs:361`, `events.rs:647,654`, `evidence.rs:152`. All operate on user-controlled strings and can panic on the same class of input. Tracked separately.

- **`phase_gate` + `issue_suggest` tests — 4 failures across the test suite** (2026-05-18). All four were stale test expectations, not production bugs.

  **Three `phase_gate` tests** (`test_read_on_phase_file_records_and_advances`, `test_read_derives_skill_from_path`, `test_untrusted_file_does_not_advance_workflow`) asserted that `state.has_phase_been_read(...)` was `true` on the untrusted-file branch (file doesn't exist on disk, e.g. on CI / dev machines without the matching skill installed). This contradicts the security-correct production behavior: `phase_gate::process` deliberately returns early on `!info.trusted` WITHOUT calling `state.record_phase_read` so crafted paths can't inflate phase progress by recording phantom reads. Test docstrings were similarly stale ("File recorded for tracking" → untrusted files are NOT recorded). Updated all three tests to assert `!has_phase_been_read` on the untrusted branch; the `test_untrusted_file_does_not_advance_workflow` test rewritten to use a UUID-suffixed nonexistent path so it always exercises the untrusted branch regardless of dev-machine state. The tests now genuinely pin the security boundary instead of expecting it to be absent.

  **One `issue_suggest` test** (`suggest_picks_top_k_neighbours_by_similarity`) expected 3 neighbours to survive the `MIN_SIMILARITY = 0.2` filter, but actually only 2 do: the third historical issue `"fix login redirect after sso"` shares only `"login"` with the query `"fix login session bug"` (token sets {login, redirect, after, sso} ∩ {login, session, bug} = 1; ∪ = 6; Jaccard = 0.167, below threshold). Updated the assertion from `3` → `2` and the comment to walk through the per-issue Jaccard math. The test still pins the documented behavior (`suggest_picks_top_k_neighbours_by_similarity`) — the surviving two ARE the top-K by similarity; the third drops because it doesn't clear the floor.

  Full sentinel workspace: **1227 tests passing, 0 failed** (up from 1223 passing / 4 failed). Both touched files clippy-clean. No production code changed.

### Added
- **A13 Phase 5 — Config + `hook_cmd.rs` wiring (A13 enforcement is LIVE)** (2026-05-18). Final A13 phase. The substrate (Phases 1+2) + hook (Phase 3) + adapters (Phase 4a/b) are now stitched into the production hook engine. Operators flipping the mode in `~/.claude/sentinel/config/spec-challenge.toml` from `ObserveOnly` to `DefaultBlocking` makes A13 structural enforcement **active**: Irreversible+ work without a complete `SpecChallenge` is *structurally impossible* at the PreToolUse layer.

  **New `crates/sentinel-infrastructure/src/spec_challenge_config.rs`** (~250 lines + 13 unit tests). `SpecChallengeConfig { mode, catastrophic_axis_threshold }`. Builder API: `observe_only()` (the safe-rollout constant), `shipped()` (loads compile-time embedded defaults via `include_str!`), `from_toml_str(s)` (parses operator-supplied TOML), `with_shipped_and_overrides(Option<&Path>)` (production startup path). TOML schema: `[spec_challenge_gate] mode = "ObserveOnly"`, optional `catastrophic_axis_threshold = 0.7`. Missing section falls back to defaults; unknown mode strings surface at startup with a descriptive error (operator sees the typo at boot, not at runtime when a Catastrophic challenge silently fails enforcement). Threshold range-checked to `[0.0, 1.0]`. Hand-rolled mode-string parsing (no serde derive on `A13EnforcementMode` — it stays decoupled from infrastructure config concerns).

  **New `config/spec-challenge-defaults.toml`** baked in via `include_str!`. Defaults to `ObserveOnly` mode + `catastrophic_axis_threshold = 0.7` per spec §3 rollout posture. File includes operator-guidance comments explaining the three modes + the ratchet-up sequence.

  **`crates/sentinel-cli/src/hook_cmd.rs` wiring**: three integration points. (1) **Adapter construction** at session start — all three adapters degrade gracefully to `None` on failure: `FilesystemSpecChallengeStore` (failure → audit lift inert), `LlmSpecChallengeScorer::from_env` (failure → Catastrophic semantic scoring inert; hook still gates on completeness + denies with operator guidance when scoring is required but unavailable), `SpecChallengeConfig::with_shipped_and_overrides` (failure → falls back to `ObserveOnly`). All logged at `info`/`warn`. (2) **Hook call** in the PreToolUse arm BEFORE `dry_run_then_commit`: derives the class via the existing `LayeredReversibilityClassifier` (so A13 + A3 + tool_usage_gate agree on the class for any given tool), then calls `spec_challenge_gate::process_with_threshold(&input, class, store, scorer, mode, threshold)` wrapped in `time_and_record` for per-hook metric labels. Block decisions propagate to the final HookOutput via `output.merge`. (3) **State tracking**: `state.record_blocked()` on deny so blocked-hook telemetry stays accurate.

  **End-to-end activation path** (when operator flips mode): (a) operator edits `~/.claude/sentinel/config/spec-challenge.toml` to set `mode = "DefaultBlocking"`. (b) Next sentinel hook invocation reads the file at startup, builds `SpecChallengeConfig { mode: DefaultBlocking, catastrophic_axis_threshold: 0.7 }`. (c) Agent attempts an Irreversible tool call without emitting `extra.spec_challenge`: the gate denies with a `[Sentinel-Authority]`-prefixed reason naming the requirement. (d) Agent retries with a complete `SpecChallenge` JSON in `extra.spec_challenge`: the gate runs `completeness_findings()`; if clean the challenge is persisted to `~/.claude/sentinel/state/spec-challenges/{work_id}.json` and the gate allows. (e) For Catastrophic-class work, the gate additionally calls `LlmSpecChallengeScorer::score` and demands every axis ≥ `0.7`.

  13 unit tests on `SpecChallengeConfig` cover: shipped defaults parse cleanly (mode=ObserveOnly, threshold=0.7); `observe_only()` const matches shipped (no drift); explicit `DefaultBlocking` + `StrictBlocking` parse; unknown mode string `"TotallyMadeUp"` errors with descriptive message naming the bad value + expected variants; empty TOML falls back to defaults; threshold override parses (0.85); threshold out-of-range (1.5, -0.1) errors with "must be in"; malformed TOML errors with "parse" in the message; `with_shipped_and_overrides(None)` uses shipped only; `with_shipped_and_overrides(Some(<nonexistent path>))` uses shipped only; `with_shipped_and_overrides(Some(<real operator file>))` applies the override (StrictBlocking + threshold 0.9 via `tempfile::TempDir`); `SpecChallengeConfig` is `Send + Sync + Copy`.

  **A13 enforcement is operator-flippable.** Once orchestrators reliably emit `extra.spec_challenge` payloads on Irreversible+ tool calls, the single config flip moves "agent must articulate uncertainty before irreversible action" from observability to structural enforcement. Full workspace tests green: **sentinel-domain 545, sentinel-application 1227, sentinel-infrastructure 314 (+13 new) + 34 legatus + 157 cli — all passing.** Zero new clippy warnings on the new code (pre-existing `hook_cmd.rs` length warnings unchanged).

- **A13 Phase 4b — `LlmSpecChallengeScorer` adapter** (2026-05-18). Second infrastructure-layer commit of A13. Implements `SpecChallengeScorerPort` as an LLM-as-judge for the 5 spec-challenge categories. Mirrors the `LlmEvalScorer` (A12 Phase 3d-2) shape: `PromptFn` test seam, env-based provider selection (openrouter / ollama local + cloud), sidecar tokio runtime for sync→async bridging. Distinct env namespace (`SENTINEL_SPEC_CHALLENGE_SCORER_*`) so operators can run separate models for A3 dry-run auditing, A12 eval scoring, and A13 spec-challenge scoring in the same session.

  **What the judge scores**: NOT completeness — that's the deterministic floor handled by `SpecChallenge::completeness_findings`. The judge scores **semantic quality**: are the assumptions substantive or vague handwaves, are the gaps real or trivial, are ambiguities genuine forks or fake-for-show, are alternatives steelmanned or strawmanned, are the unsatisfied constraints substantive or filler. The hook layer (Phase 3) gates on the deterministic check + (for Catastrophic class) every axis above the operator-configured threshold.

  **Judge prompt (v1)**: hard-coded system prompt covering the 5 axes with explicit guidance per category. Calibration: when a category is `explicit_assertion_of_none`, score 0.7 if the reason is plausible and the work doesn't obviously call for items, score 0.4 if the reason looks like a dodge. Honest-uncertainty guidance keeps scores in `[0.4, 0.6]` when the judge can't tell — better than confidently wrong high or low values. Strict JSON output shape so the parser surfaces `Malformed` on any deviation; markdown code-fences are stripped before parsing.

  **New `crates/sentinel-infrastructure/src/spec_challenge_scorer.rs`** (~480 lines + 14 unit tests). `LlmSpecChallengeScorer::with_prompt_fn` (test seam) + `from_env` / `openrouter_from_env` / `ollama_from_env` / `*_with` resolver-injected variants. Sidecar runtime named `sentinel-spec-challenge-scorer-sidecar` for log greppability. Constants: `DEFAULT_SCORER_OPENROUTER_MODEL = "anthropic/claude-opus-4.7"`, `DEFAULT_SCORER_TIMEOUT = 60s`.

  14 tests with stub `PromptFn`s cover: well-formed JSON → `SpecChallengeScore` with correct per-axis values; markdown code-fence stripped; backend error surfaces as `Backend`; non-JSON response surfaces as `Malformed`; missing-axis JSON surfaces as `Malformed`; out-of-range values (1.7, -0.3) clamped via `SpecChallengeScore::new`; `all_axes_above(threshold)` works on judge output; unknown provider in env errors descriptively; `openrouter` without API key errors; `ollama` without `SENTINEL_SPEC_CHALLENGE_SCORER_MODEL` errors; timeout fallback + override; preview helper truncates / passes through. `crates/sentinel-infrastructure/src/lib.rs` exposes the new `spec_challenge_scorer` module. Sentinel-infrastructure: **302 tests passing (288 prior + 14 new).** Zero new clippy warnings.

- **A13 Phase 4a — `FilesystemSpecChallengeStore` adapter** (2026-05-18). First infrastructure-layer commit of A13. Implements `SpecChallengeStorePort` as one JSON file per challenge under `~/.claude/sentinel/state/spec-challenges/{work_id}.json`. Mirrors the `FilesystemEvalRunStore` (A12 Phase 3d-1) pattern: atomic temp+rename writes, lazy directory creation, `Ok(None)` vs `Err(Malformed)` discrimination on load, JSON-per-file (not JSONL) so save is atomically consistent under operator-visible diffs. Save overwrites on `work_id` collision per spec §6 — the typical case is "agent re-attempted after the first challenge was rejected."

  **New `crates/sentinel-infrastructure/src/spec_challenge_store.rs`** (~220 lines + 8 unit tests). `at_dir(PathBuf)` (test-scoping) + `with_default_path()` (production startup; resolves `~/.claude/sentinel/state/spec-challenges/`). `save` writes to `.tmp` then atomic `rename`, surfaces `StoreUnavailable` on filesystem failure and `Malformed` on serialization failure. `load` returns `Ok(None)` on `ErrorKind::NotFound` to distinguish "never emitted" from `Err(Malformed)` ("file exists but unreadable"). Pretty-printed JSON for operator-diffable audit records. 8 tests cover: lazy directory creation, full save→load round-trip preserves every category + nested item, `load` returns `None` on missing, `load` returns `Malformed` on corrupt JSON, save overwrites on `work_id` collision (verified by mutating `agent_id` between saves), no dangling `.tmp` after clean save, `with_default_path` resolves under `.claude` / `spec-challenges`, different work_ids get different files. TempDir-isolated. `crates/sentinel-infrastructure/src/lib.rs` exposes the new `spec_challenge_store` module. Sentinel-infrastructure: **288 tests passing (280 prior + 8 new).** Zero new clippy warnings.

- **A13 Phase 3 — `spec_challenge_gate` PreToolUse hook** (2026-05-18). Application-layer commit of the spec-challenge track. The hook reads `input.extra.spec_challenge` (the agent-emitted [`SpecChallenge`] JSON), runs the deterministic completeness check, persists via [`SpecChallengeStorePort`] (best-effort), and — for Catastrophic-class work — queries [`SpecChallengeScorerPort`] to verify every axis is above an operator-configured threshold. Reversibility-class-graded per spec §3 so the auditor-call budget stays bounded.

  **New `crates/sentinel-application/src/hooks/spec_challenge_gate.rs`** (~440 lines + 18 unit tests). Class-graded dispatch: `TriviallyReversible` skips (challenge cost not justified for trivial actions), `ReversibleWithEffort` allows (optional emission), `Irreversible` requires the challenge present AND completeness-clean, `Catastrophic` requires the challenge AND completeness AND scorer-all-axes-above-threshold (default 0.7, override via `process_with_threshold`). `A13EnforcementMode { ObserveOnly, DefaultBlocking, StrictBlocking }` matches the BA1+3 mode shape so operator config stays uniform; `StrictBlocking` ratchets `Irreversible` up to the same scoring requirement as `Catastrophic` for high-stakes domains.

  **Deny message shape**: missing challenge ("Irreversible work requires a SpecChallenge artifact in `extra.spec_challenge`. The agent must articulate its assumptions, gaps, ambiguities, alternatives_considered, and constraints_not_satisfied before acting"), malformed JSON (passes through the serde error), incomplete challenge (lists each `CompletenessFinding` with the affected category / topic / excerpt), missing scorer at Catastrophic ("...requires semantic scoring but no scorer is configured. Wire a `SpecChallengeScorerPort` adapter or downgrade the upcoming work's reversibility class"), scorer error (surfaces the underlying error verbatim), low scorer axis ("scorer rejected Catastrophic challenge — weakest axis 0.40 below threshold 0.70. Per-axis scores: assumptions=0.90, gaps=0.40, ambiguities=0.90, alternatives_considered=0.90, constraints_not_satisfied=0.90"). All deny strings flow through `HookOutput::deny` so they carry the `[Sentinel-Authority]` prefix.

  **Best-effort persistence**: store-save failures never block the gate. The audit trail is observability, not enforcement — if the store goes down, the proof chain loses a record but the gate decision stands on the in-process completeness/score check. Logged at `warn`.

  18 tests with a `RecordingStore` + `StubScorer` cover every dispatch path: TriviallyReversible always allows, ReversibleWithEffort allows missing challenge, Irreversible denies missing challenge (and `ObserveOnly` flips this to allow), Catastrophic denies missing challenge, malformed JSON denies, complete challenge allows Irreversible without scorer, incomplete challenge denies and names the failing category, store persists complete challenges, store failure doesn't block the allow path, Catastrophic denies when no scorer configured, Catastrophic allows when scorer returns high scores, Catastrophic denies when scorer returns a low axis (deny message surfaces `gaps=0.40`), Catastrophic denies on scorer error (surfaces "rate limit"), `StrictBlocking` scores `Irreversible` too, `DefaultBlocking` does NOT score `Irreversible` (verified by injecting a scorer that would fail if called), `process_with_threshold` threshold override is respected (0.5 passes; 0.7 fails on same input), `ObserveOnly` allows incomplete challenge.

  `crates/sentinel-application/src/hooks/mod.rs` exposes the new `spec_challenge_gate` module and adds `"spec_challenge_gate"` to `HOOK_NAMES`. Sentinel-application: **1227 tests passing (1209 prior + 18 new).** Zero new clippy warnings on the new file.

- **A13 Phase 2 — `SpecChallengeScorerPort` + `SpecChallengeStorePort` trait surface** (2026-05-18). Port-layer commit for the spec-challenge track. Adds the two outward boundaries the future `spec_challenge_gate` hook (Phase 3) will query: semantic scoring for Catastrophic-class challenges (the deterministic completeness check is the floor; the scorer is the per-axis quality bar above it), and persistence so the proof chain can re-verify "what did the agent challenge before it acted?" across sessions. Mirrors the established A12 / BA1+3 port pattern.

  **`crates/sentinel-domain/src/ports.rs` additions** (~155 lines + 10 unit tests). `SpecChallengeScore { assumptions, gaps, ambiguities, alternatives_considered, constraints_not_satisfied }` carries one `f32` per category, range `[0.0, 1.0]`, clamped at `new()`. `all_axes_above(threshold) -> bool` is the Catastrophic-gate predicate ("every axis ≥ threshold"); `min_axis() -> f32` powers the "weakest axis" rendering for local clients. The structured score is intentionally *not* parameterized by `ScoringRubric` (unlike A12's `EvalAxisScore`) because A13 scoring drives a gate decision, not an aggregate metric — a per-axis threshold is the operator-facing semantic.

  `SpecChallengeScorerError { Backend, Malformed, Configuration }` mirrors `EvalScorerError` exactly — same triad, same `Display` shape. `SpecChallengeScorerPort::score(challenge) -> Result<SpecChallengeScore, _>`: sync (matches `AuditorPort` / `EvalScorerPort`; adapters bridge async via sidecar tokio runtime). The trait is intentionally **not** called for Irreversible (non-Catastrophic) challenges — the hook layer (Phase 3) queries it only when `reversibility_class == Catastrophic`, keeping the auditor-call budget bounded.

  `SpecChallengeStoreError { StoreUnavailable, Malformed }` parallels `EvalRunStoreError`. `SpecChallengeStorePort::save(challenge)` + `load(&WorkId) -> Option<SpecChallenge>` (write-once-per-WorkId; re-emissions overwrite per spec §6 — the typical case is "agent re-attempted after the first challenge was rejected"). No `list_work_ids()` on the port — challenges are looked up by id during gating, not enumerated; the proof-chain consumer of the audit log handles discovery via its own catalog.

  10 unit tests cover: axis clamping (1.7→1.0, -0.3→0.0), `all_axes_above` true on uniform-at-threshold and false-with-margin, `all_axes_above` false when any single axis drops below, `min_axis` returns the smallest, `Display` covers each error variant + key substrings, `std::error::Error` blanket impl, `Send + Sync` for all three types, `Box<dyn …>` object-safety for both traits, and serde round-trip for both error types. Sentinel-domain: **545 passing (535 prior + 10 new).** Zero clippy warnings on the new code.

- **A13 Phase 1 — `spec_challenge` domain types** (2026-05-18). First commit of the spec-challenge track (per `docs/a13-spec-challenge.md` §2). Adds the structured artifact an acting agent emits *before* any Irreversible+ action: 5 categories of self-examination (`assumptions`, `gaps`, `ambiguities`, `alternatives_considered`, `constraints_not_satisfied`) plus a deterministic completeness check that names silent-empty categories. Pure domain; no ports, no IO. Future phases: 2 (ports for scoring + persistence), 3 (`spec_challenge_gate` `PreToolUse` hook with reversibility-graded enforcement), 4 (adapters), 5 (hook_cmd wiring).

  **New `crates/sentinel-domain/src/spec_challenge.rs`** (~480 lines + 16 unit tests). Identifiers: `WorkId` newtype (whitespace-trimmed; rejects empty), `SpecReference { hash, source }` (hash for change detection, source for human-readable attribution). Category items: `Assumption { statement, confidence: AssumptionConfidence (Low/Medium/High), blast_if_wrong: ReversibilityClass }`, `SpecGap { topic, how_resolved: GapResolution (OperatorClarified / InferredFromContext / DefaultApplied), inference_source: Option<String> }`, `Ambiguity { spec_excerpt, interpretations: Vec<String>, chosen, rationale }`, `Alternative { description, why_rejected }`, `UnsatisfiedConstraint { constraint, why_not_satisfiable, workaround: Option<String> }`.

  `ChallengeCategory<T> { items: Vec<T>, explicit_assertion_of_none: Option<String> }` is the core invariant carrier: a category is "filled" iff `!items.is_empty() || explicit_assertion_of_none.is_some()`. `new(items)` (silent-when-empty) + `none(reason)` (explicit-none) builders. `SpecChallenge` aggregates the 5 typed categories with `work_id`, `agent_id`, `challenged_spec`, `reversibility_class`, `created_at`.

  **`SpecChallenge::completeness_findings() -> Vec<CompletenessFinding>`** is the deterministic floor. Three finding shapes: `SilentEmpty { category: ChallengeCategoryName }` (silent-empty in any of the 5 categories — the diagnostic shape A13 exists to catch), `InsufficientInterpretations { excerpt_preview, count }` (ambiguity entry with `< 2` interpretations is malformed), `InferenceWithoutSource { topic }` (gap resolved via `InferredFromContext` without an `inference_source`). `is_block()` returns true for every finding — the hook layer collapses every finding into a block at Irreversible+ class. `is_complete() -> bool` is the `findings.is_empty()` convenience.

  16 unit tests cover: `WorkId` rejects empty/whitespace + trims, `ChallengeCategory::is_filled` discrimination (3 cases: items present, explicit-none, silent-empty), fully-filled challenge yields zero findings, silent-empty assumptions surfaces `SilentEmpty(Assumptions)`, explicit-none restores completeness, all-5-silent yields 5 findings (all `is_block`), single-interpretation ambiguity surfaces `InsufficientInterpretations { count: 1 }`, gap with `InferredFromContext` + `None` source surfaces `InferenceWithoutSource`, whitespace-only source string treated identically, `OperatorClarified` / `DefaultApplied` gaps need no source, `ChallengeCategoryName::key` is stable snake_case, serde round-trip, `Send + Sync` for all types. Sentinel-domain tests: **535 passing (519 prior + 16 new).** Zero clippy warnings on the new file.

- **Deferred classification — `InstructionOutcome::Deferred { waiting_on }` is no longer dead code** (2026-05-18). The fourth `InstructionOutcome` variant was unused since the failure-classification commit earlier this month; only `Success` / `Failure` / `Declined` were ever emitted. This commit teaches `classify_outcome` to detect conservative deferral phrasing in the model's reply at Stop time and emit `Deferred { waiting_on: "<phrase>" }` when the operator's instruction was acknowledged-but-postponed (e.g. `"I'll get to that"`, `"queued for later"`, `"noted for next session"`, `"skipping for now"`).

  **New signature**: `classify_outcome(signals, api_error, reply_text)`. The third parameter is `Option<&str>` carrying the assistant's concatenated text for this turn — already captured by `execution_log::process` for the execution-marker extraction; reused here as the deferral-detection corpus at zero extra parse cost. The `stop_failure` path passes `None` (API errors take precedence, no need to inspect text).

  **Precedence order** (most-specific wins): `api_error` → `Failure` → `PermissionDenied` signal → `Declined` → deferral phrase → `Deferred` → otherwise `Success`. Permission denial outranks deferral because a hard "tool blocked" is a concrete refusal while a deferral phrase is fuzzy intent. Pinned by `classify_outcome_permission_denied_beats_deferral_phrase` + `classify_outcome_api_error_beats_deferral_phrase` tests.

  **Deferral phrase list** (16 phrases, deliberately conservative): `"i'll get to"`, `"i'll come back to"`, `"i'll handle that later"`, `"i'll do that later"`, `"i will get to"`, `"queued for later"`, `"queuing this for"`, `"noted for later"`, `"noted for next"`, `"deferring this"`, `"deferring that"`, `"deferred for"`, `"will handle later"`, `"will do later"`, `"skipping for now"`, `"skipping this for now"`. All matched case-insensitively. Conservative on purpose — a false `Deferred` confuses the operator more than a missed one (operator just sees `Success` for what was actually deferred, which is the pre-classification behavior). Edge cases that look like deferrals but aren't (sequencing within the same turn, hedging language, conditional plans) deliberately fall through to `Success`. The returned `waiting_on` is the matched phrase as it appears verbatim, lowercased — short, recognizable, doesn't require the operator to read the model's full reply.

  **`execution_log` wiring**: the Stop hook now passes `Some(message.as_str())` — the assistant text it already captured for execution-marker scanning — into `classify_outcome`. Per-pending-instruction outcome reporting unchanged except for the variant: when a deferral phrase is detected, every pending instruction reports `Deferred { waiting_on }` instead of `Success`. Multiple instructions in the same turn share the deferral classification (same coarseness as the `Declined` path — we don't track per-instruction → per-tool-call correlation).

  **8 new unit tests** on `classify_outcome` covering the deferral path: 4 phrase-matching tests across the variant list (`i'll get to`, case-insensitivity, `queued for later`, `skipping for now`); 2 precedence tests pinning api_error and PermissionDenied beat deferral; 1 negative test for active-execution phrases (`"Done."`, `"Deploying staging now."`, `"Running the test suite."`, `"I checked the logs first; everything looks fine."`) — all yield `Success`; 1 edge case for empty reply text. All 4 prior `classify_outcome` tests updated to the 3-arg signature. Sentinel-application: 20 `legatus_client` tests passing.

  **Pre-existing failing tests** (`phase_gate` + `issue_suggest`) are unrelated to this change — same failures present in the prior inbox-persistence commit, they persist independently and don't block this slice.

  **Out of scope (remaining)**: LLM-based deferral detection for phrasings the heuristic misses (would mirror the existing LLM-fallback cancel parser pattern); per-tool-call → per-instruction correlation so `Deferred` can name which specific instruction was postponed (currently every pending instruction in a deferral-detected turn gets the same `Deferred` classification).

- **Sentinel daemon escalation outbox persistence — outbound events survive daemon crash** (2026-05-18). Symmetric to the inbox persistence shipped earlier this month. The `LegatusHandle::escalate()` path now writes every `EscalationKind` event to a file-backed FIFO at `~/.claude/sentinel/state/legatus-escalations.jsonl` before sending to the mpsc; the WS recv loop removes the head entry only after `send_signed` succeeds; on daemon startup any pending entries get replayed through the same mpsc channel as fresh escalations. Closes the consistency gap where a daemon crash between `escalate()` and the WS send would lose the event — the headline case being the `InstructionResult { Declined }` emitted from the cancel loopback in `client.rs::handle_inbound`, which is the operator-visible "I cancelled that for you" confirmation. With this commit, Legatus AI reliably gets the closing record across daemon restarts.

  **At-least-once semantics**: if the daemon crashes between successful WS send and `remove_head`, Legatus AI receives a duplicate event on the next daemon start. Legatus AI-side handlers are already idempotent — every `EscalationKind` variant is keyed by a stable id (`instruction_id` for `InstructionResult` / `InstructionAcknowledged`, `session_id` for the lifecycle events) so double-emission is a no-op.

  New `crates/sentinel-legatus/src/persistent_outbox.rs` (~+290 lines + 11 unit tests) mirrors `PersistentInbox` exactly — same `fs2` advisory-lock discipline, same atomic-rewrite strategy for `remove_head` (read all lines under exclusive lock, skip the first valid entry, rewrite the remainder), same module-level `#![allow(clippy::incompatible_msrv)]` for the fs2 method-name collision with std 1.89. Public API: `PersistentEscalationOutbox::new(path)`, `append(event)`, `remove_head() -> bool`, `snapshot()`, `len()`, `is_empty()`. New `default_outbox_path()` returns `~/.claude/sentinel/state/legatus-escalations.jsonl` (or `None` in degenerate `dirs::home_dir() = None` environments).

  **Handle/runtime wiring** (`handle.rs`): `LegatusHandle` gains an `outbox: Option<PersistentEscalationOutbox>` field; `LegatusRuntime` gains a matching field. New `make_pair_with_persistence(inbox, outbox)` constructor builds a pair with both directions persistent — the existing `make_pair` (no persistence) and `make_pair_with_inbox` (inbox only) constructors are preserved for the standalone CLI and inbox-only test paths. `escalate()` now appends to the outbox (if `Some`) before sending to the mpsc; disk-write failures are logged at `warn` and the mpsc send still proceeds (best-effort persistence per the same contract as inbox).

  **WS recv loop** (`client.rs::run_connect_hosted`): two changes. **(1) Startup replay** before entering the select loop — `outbox.snapshot()` is drained into `runtime.escalation_loopback` so any pre-existing disk entries flow through the normal escalation arm as if just escalated. Order is preserved by the snapshot's FIFO + the mpsc's send-order semantics. Logged at `info` with the count when non-zero. **(2) Post-send remove_head** — every successful `send_signed` in the escalation arm is followed by `outbox.remove_head()` (no-op when `outbox = None`), keeping the disk in sync with Legatus AI-receipt.

  **Daemon wiring** (`crates/sentinel-cli/src/daemon_cmd.rs`): the daemon now constructs both `PersistentInbox::new(default_inbox_path())` AND `PersistentEscalationOutbox::new(default_outbox_path())` at startup, passes both to `make_pair_with_persistence`. Backfill log lines at `info` if either path has prior pending entries from a previous run (the operator sees "rehydrated persistent outbox at startup — replaying on connect" when restarting the daemon mid-cancel). The no-home-dir fallback still uses the non-persistent `make_pair()` so the daemon runs in CI / chroot environments where `dirs::home_dir()` returns `None`.

  **11 new tests** on `PersistentEscalationOutbox`: empty-outbox-is-empty, append-then-snapshot, FIFO-order across appends, remove_head-strips-oldest-first (with subsequent-call-returns-false), remove_head-on-missing-file, append-after-remove_head-extends-remainder, malformed-lines-dropped, **declined_outcome_roundtrips_through_disk** (the headline use case — pins that `InstructionResult { Declined }` survives JSON roundtrip with the operator's reason), **events_survive_simulated_daemon_restart** (the second headline — fresh `PersistentEscalationOutbox` over the same path sees prior records), 20-thread concurrent-appends-all-persist, default_outbox_path shape. Sentinel-legatus: 34 tests passing (20 inbox + 6 handle + 5 misc + 11 new outbox). Whole workspace remains green.

  **Out of scope (deferred)**: protocol-level escalation acks from Legatus AI so sentinel can use exactly-once instead of at-least-once (would be a legatus-ai-protocol addition + matching Legatus AI-side handling); SQLite-backed outbox for operators who want shared-database-grade durability (the JSONL file is already crash-safe for single-daemon use); operator-visible "I have N pending escalations on disk" surface (operators can `wc -l ~/.claude/sentinel/state/legatus-escalations.jsonl` for now).
### Added
- **A12 Phase 3e — `sentinel eval run` CLI subcommand (A12 runner end-to-end LIVE)** (2026-05-18). Final commit of the A12 benchmark-runner arc. Operators can now invoke `sentinel eval run --run-id <id> --candidates path/to/outputs.json` to load the corpus, replay recorded candidate outputs through the configured `EvalScorerPort` (LLM-as-judge by default), and persist a complete `EvalRunResult` at `~/.claude/sentinel/eval/ba-corpus/runs/{run_id}.json`. The 5-layer DDD stack — domain (3a) → ports (3b) → use case (3c) → JSONL store (3d-1) + LLM scorer (3d-2) → CLI (3e) — is now end-to-end shippable.

  **New `Run` subcommand** under the existing `Eval` clap variant. Flags: `--run-id <ID>` (required; persisted at `{runs_dir}/{run_id}.json`, re-runs of the same id overwrite), `--candidates <PATH>` (required; JSON object mapping `case_id -> output_text` — Phase 3e is replay-only, live-LLM dispatch via the A2 router is a future iteration), `--case-id <ID>` (repeatable; filters which cases run; unset means "all cases in the corpus"), `--corpus-dir <PATH>` + `--runs-dir <PATH>` (operator overrides for the two default `~/.claude/sentinel/eval/ba-corpus/` paths), `--json` (emit the full `EvalRunResult` as pretty JSON instead of a human summary).

  **New `crates/sentinel-cli/src/eval_cmd.rs` additions** (~280 lines of new code + 12 unit tests on top of the existing 6 list tests). `RunArgs` struct carries the parsed flags. `run(args)` is the production entry point — builds `LlmEvalScorer::from_env()`, `build_corpus`, `build_run_store`, then delegates to the generic test seam `run_with<St: EvalRunStorePort, Sc: EvalScorerPort>(&args, &corpus, &store, &scorer)`. The generic signature lets tests inject a `RecordingStore` + `StubScorer` without touching env vars or the LLM. `load_candidates` parses the JSON candidates file and validates each `case_id` through `EvalCaseId::new` (an invalid key surfaces with the offending value quoted). `load_cases` enumerates the corpus + filters by `--case-id` (when set). The human-readable summary prints run id, started/completed timestamps, case counts, mean composite (or `<no successful cases>` placeholder), per-axis mean (deterministic axis order via `BTreeMap`), and an "Errored cases" section when any case failed.

  12 new tests cover: end-to-end happy path (2 cases → scored + persisted via `RecordingStore`), `--case-id` filter restricts to 1 case from a 2-case corpus, missing candidate output records producer error + the run still saves, empty corpus errors with "no cases to run", invalid run id ("" rejected by `EvalRunId::new`) surfaces an error naming the bad id, `load_candidates` happy path + invalid case_id (`""` key rejected by `EvalCaseId::new`) + missing file + malformed JSON, `--json` mode succeeds and still persists, `build_run_store` with override + with default. Sentinel-cli: **157 tests passing (145 prior + 12 new).** Zero new clippy warnings.

- **A12 Phase 3d-2 — `LlmEvalScorer` adapter** (2026-05-18). Second infrastructure-layer commit of the runner. Implements `EvalScorerPort` as an LLM-as-judge: every `(case, candidate_output, run_id)` triple is rendered into a judge prompt, sent through a `rig-core` client, and the JSON response is parsed into 6 `EvalAxisScore`s (rubric weights pulled from the case) → `EvalScore`. Mirrors the existing `RigAuditor` pattern (sidecar tokio runtime, `PromptFn` test seam, env-based provider selection) so the BA-Eval scoring surface shares its operational shape with the dry-run auditor.

  **Providers**: `SENTINEL_EVAL_SCORER_PROVIDER` selects `openrouter` (default; needs `OPENROUTER_API_KEY`, default model `anthropic/claude-opus-4.7` overridable via `SENTINEL_EVAL_SCORER_MODEL`) or `ollama` (auto-detects local vs cloud by `OLLAMA_API_KEY` presence; requires `SENTINEL_EVAL_SCORER_MODEL` — no sensible default for ollama). Timeout via `SENTINEL_EVAL_SCORER_TIMEOUT_SECS` (default 60s). Distinct env namespace from `SENTINEL_AUDITOR_*` so operators can run an A3 auditor and an A12 scorer on different models in the same session.

  **Judge prompt (v1)**: a single hard-coded system prompt covering the 6 axes (`citation_density_accuracy`, `requirements_coverage`, `alternatives_seriousness`, `tonal_calibration`, `outcome_realism`, `stakeholder_fit`) with explicit guidance on the 0.0-1.0 scale + the "rate 0.5 with 'not scorable' note" convention for `outcome_realism` when `gold_outcomes` is absent. Returns JSON `{"axes": {…}, "reasoning": "…"}` — the strict shape lets the parser surface `EvalScorerError::Malformed` on any deviation (missing axis, bad JSON, code-fence-only wrap is stripped before parse). The user-prompt JSON carries `case_id`, `stakeholder_brief`, `gold_artifact` (text + author), `gold_outcomes_present` flag, and `candidate_output`. The prompt is v1; operators iterate by recompiling — the rubric weights live on the case so weight tuning needs no adapter change.

  **New `crates/sentinel-infrastructure/src/eval_scorer.rs`** (~480 lines + 15 unit tests). `LlmEvalScorer::with_prompt_fn` (test seam) + `from_env` / `openrouter_from_env` / `ollama_from_env` / `*_with` resolver-injected variants (test seam matches the `RigAuditor` env-resolver pattern; `unsafe_code = "forbid"` upheld). `Debug` impl elides the closure for clean logs. Sidecar tokio runtime is a module-local `OnceLock<Option<Runtime>>` named `sentinel-eval-scorer-sidecar` for log greppability.

  15 tests with stub `PromptFn`s cover: well-formed JSON response → `EvalScore` with correct per-axis raws + rubric weights picked up (`OutcomeRealism = 2.0` from `ba_default`); markdown code-fence (`\`\`\`json … \`\`\``) is stripped before parse; backend error (network unreachable) surfaces as `EvalScorerError::Backend`; non-JSON response surfaces as `Malformed`; missing-axis JSON surfaces as `Malformed`; out-of-range axis values (1.7, -0.3) get clamped to [0.0, 1.0] via `EvalAxisScore::new`; composite reflects rubric weights (uniform 0.5 across all axes → composite 0.5 with `ba_default`); `case_id` + `run_id` thread through to the returned `EvalScore`; unknown provider in env errors with descriptive message; `openrouter` without API key errors; `ollama` without `SENTINEL_EVAL_SCORER_MODEL` errors (no default); timeout parsing fallback + override; preview helper truncates / passes through. `crates/sentinel-infrastructure/src/lib.rs` exposes the new `eval_scorer` module alongside `eval_corpus` / `eval_run_store`. Sentinel-infrastructure: **280 tests passing (265 prior + 15 new).** Zero new clippy warnings.

- **A12 Phase 3d-1 — `FilesystemEvalRunStore` adapter** (2026-05-18). First infrastructure-layer commit of the runner persistence track. Implements `EvalRunStorePort` (save / load / list) as one JSON file per run under `~/.claude/sentinel/eval/ba-corpus/runs/{run_id}.json` per spec §3.5. The LLM-as-judge scorer adapter is its own Phase 3d-2 commit; this commit unblocks the CLI runner immediately by giving it somewhere to persist results.

  **Why JSON-per-file, not JSONL?** Each run carries a complete `EvalRunResult` (every case's candidate output + score + timing + error context). Appending a partial run line-by-line opens a window where readers see a half-written run. JSON-per-file under the standard temp+rename pattern (`{run_id}.json.tmp` → atomic `rename` to `{run_id}.json`) gives `load` a clean either-prior-or-new read with no torn writes.

  **New `crates/sentinel-infrastructure/src/eval_run_store.rs`** (~280 lines + 13 unit tests). `FilesystemEvalRunStore::at_dir(PathBuf)` (test-scoping) + `with_default_path()` (production startup; resolves `~/.claude/sentinel/eval/ba-corpus/runs/`). `save` lazy-creates the runs directory if missing (operators don't have to bootstrap it; first save creates it), serializes with `to_string_pretty` (human-diffable run records), writes to `.tmp`, then `rename`s atomically; on overwrite (same `run_id` re-saved), the prior content is replaced cleanly. `load` returns `Ok(None)` on `ErrorKind::NotFound` to distinguish "never saved" from `Err(Malformed)` ("file exists but unreadable"). `list_run_ids` returns `Vec<EvalRunId>` sorted alphabetically; skips dotfiles, non-`.json` files (including `.tmp` partial writes via the `Path::extension` case-insensitive check), and filenames whose stem doesn't parse as a valid `EvalRunId` (legacy/stray files don't poison the listing).

  13 tests cover: lazy directory creation on first save, save→load round-trip preserves the full `EvalRunResult` (including per-case `EvalScore`s + `error: None`/`Some` slots), `load` returns `Ok(None)` on missing runs, `load` returns `EvalRunStoreError::Malformed` on corrupt JSON, save overwrites on `run_id` collision (verified by mutating `timing_ms` between saves), `list_run_ids` returns empty on nonexistent + empty dirs, sorted listing across 3 runs ("zeta"/"alpha"/"middle" → ["alpha","middle","zeta"]), `.tmp` partial-write filtering, non-`.json` + dotfile filtering, unparseable-stem filtering (an empty filename `.json` is silently skipped), `with_default_path` resolves under `.claude` / `ba-corpus` / `runs`, and the temp file is cleaned up after a clean save. TempDir-isolated; zero shared filesystem state. `crates/sentinel-infrastructure/src/lib.rs` exposes the new `eval_run_store` module alongside the existing `eval_corpus`. Sentinel-infrastructure: **265 tests passing (252 prior + 13 new).** Zero new clippy warnings on the new file.

- **A12 Phase 3c — `eval_run::execute_run` benchmark-runner use case** (2026-05-18). Application-layer commit. The runner accepts a batch of `EvalCase`s, a `produce_candidate` closure (caller-supplied: static fixture for replay-style runs, live LLM via the A2 router for production runs, or whatever else fits the context), and an `EvalScorerPort` instance, then loops once-per-case to produce an `EvalRunResult`. Errored cases stay in the run record — the loop never short-circuits — so telemetry sees both successes and failures.

  **New `crates/sentinel-application/src/eval_run.rs`** (~360 lines + 9 unit tests). `execute_run<F, S, C>(run_id, cases, produce_candidate: F, scorer: &S, clock: C)` where `F: FnMut(&EvalCase) -> Result<String, String>`, `S: EvalScorerPort`, `C: FnMut() -> DateTime<Utc>`. Per-case timing via monotonic `std::time::Instant` (so DST/NTP shifts during a run don't produce negative durations); wall-clock timestamps via the operator-supplied `clock` (`chrono::Utc::now` in production, deterministic tick-counter in tests). Error semantics are uniform: producer error → `error = Some("producer error: ...")` + empty `candidate_output` + scorer **not called**; scorer error → `error = Some("scorer error: ...")` + `candidate_output` populated; success → `error = None` + `score = Some(_)`. The error message prefix lets reports distinguish dispatch failures from scoring failures without parsing.

  **`static_candidate_producer<H: BuildHasher>(HashMap<EvalCaseId, String, H>)`** ships alongside the runner as the canonical replay-style producer (load recorded outputs from a JSON file, iterate on rubric weights without burning fresh LLM calls). Generalized over the `HashMap` hasher so callers can use `FxHashMap` etc. without an extra collect.

  9 unit tests with a `StubScorer` (`EvalScorerPort` test double: uniform score across axes + configurable per-`case_id` `EvalScorerError` injection) cover: all-successful 3-case run aggregates correctly, producer-missing-output records error + names the `case_id`, scorer failure preserves `candidate_output` and surfaces the scorer error message ("rate limit"), empty input yields empty run with `mean_composite() = None`, clock calls fire in `(started_at, per-case completed_at, run completed_at)` order (3 ticks for a 1-case run), `case_results` ordering matches input ordering (not sorted), mixed producer + scorer failures across 3 cases (1 success / 1 producer-fail / 1 scorer-fail) produce the right per-case shape, and `static_candidate_producer` round-trips recorded outputs + errors on missing case_ids. `crates/sentinel-application/src/lib.rs` exposes the new `eval_run` module alongside `engine` / `gate` / etc. Sentinel-application: **1201 tests passing (1192 prior + 9 new).** Zero new clippy warnings.

- **A12 Phase 3b — `EvalScorerPort` + `EvalRunStorePort` trait surface** (2026-05-18). Port-layer commit for the A12 benchmark runner. Adds the two outward-facing trait boundaries the runner (Phase 3c) queries — one for "score a candidate output against a case" (LLM-as-judge in the eventual Phase 3d adapter), one for "persist + retrieve full run records" (JSONL adapter in 3d). Pure-domain: traits + error enums only, no IO, no application logic.

  **`crates/sentinel-domain/src/ports.rs` additions** (~140 lines + 6 unit tests, sandwiched between the BA1+3 section and its test module so the dependency order stays linear): `EvalScorerError { Backend(String), Malformed(String), Configuration(String) }` mirrors the failure-mode triad used by `AuditorPort`/`RoutingError` — Backend = transient (LLM timeout, rate limit), Malformed = adapter couldn't decode response into `Vec<EvalAxisScore>`, Configuration = operator-side fault (missing model handle, bad key) that surfaces at startup. `EvalScorerPort::score(case: &EvalCase, candidate_output: &str, run_id: &EvalRunId) -> Result<EvalScore, EvalScorerError>` — sync (matches `AuditorPort`; adapter bridges to async backend via `block_on` like `RigAuditor`). The full `EvalCase` is passed so the scorer can honor per-case rubric overrides + read `gold_artifact`/`gold_outcomes` without the application layer plumbing them.

  `EvalRunStoreError { StoreUnavailable(String), Malformed(String) }` mirrors `ProvenanceError`'s soft-warn shape — `Malformed` is distinct from `Ok(None)` on `load` (record exists but unreadable vs. record doesn't exist). `EvalRunStorePort::save(&EvalRunResult)` is write-once-per-run (runner persists at end after accumulating all `EvalCaseResult`s; no per-case write API on the port to keep the store atomically consistent), `load(&EvalRunId) -> Result<Option<EvalRunResult>, _>`, and `list_run_ids() -> Result<Vec<EvalRunId>, _>` for the future `sentinel eval list-runs` CLI. Save is documented-overwrite on collision (Phase 3d JSONL adapter overwrites because runs come from explicit operator action — the typical case is "retry after a backend fix").

  6 unit tests: `Display` covers each variant + key substrings, `std::error::Error` blanket-impl, `Send + Sync` for both error types, `Box<dyn EvalScorerPort>` + `Box<dyn EvalRunStorePort>` object-safety check, and serde round-trip for both errors. Sentinel-domain tests: **519 passing (513 prior + 6 new).** Zero new clippy warnings on `ports.rs`.

- **A12 Phase 3a — `EvalCaseResult` + `EvalRunResult` domain types** (2026-05-18). Runtime envelope around the existing `EvalScore` (Phase 1) — the type spine the benchmark runner (Phase 3c), JSONL run store (Phase 3d), and `sentinel eval run` CLI (Phase 3e) will all share. Pure domain: no ports, no IO.

  **New `crates/sentinel-domain/src/eval/result.rs`** (~390 lines + 14 tests): `EvalCaseResult { case_id, run_id, candidate_output, score: Option<EvalScore>, timing_ms, completed_at, error: Option<String> }` carries the agent's output text + wall-clock timing + a dispatch/scoring error slot so failed cases stay in the run record rather than disappearing for telemetry. `is_success()` requires both `error.is_none()` and `score.is_some()`. `EvalRunResult { run_id, started_at, completed_at, case_results }` aggregates a benchmark run with four summary methods: `successful_case_count`, `errored_case_count`, `mean_composite` (returns `Option<f32>` — `None` when no successful cases, deliberately preserving the "no data" / "score zero" distinction), `mean_per_axis` (`Vec<(EvalAxis, f32)>`, deterministic axis ordering via `BTreeMap` traversal, axes absent from every successful case are omitted), and `pass_rate(threshold)` (`f32` in `[0.0, 1.0]`; `0.0` floor when no successful cases since reports prefer a concrete rate over `None`). Both types `Serialize + Deserialize` via serde for the future JSONL store, with full round-trip tests.

  **`crates/sentinel-domain/src/eval/mod.rs`** re-exports `EvalCaseResult` + `EvalRunResult` alongside the existing `EvalScore`/`ScoringRubric`/case types. The 14 unit tests cover: success/error discrimination, composite passthrough, count helpers across mixed runs, mean-composite excludes errored cases, `None` semantics on empty/all-errored runs, mean-per-axis correctness (two cases at 0.4 + 0.8 each → 0.6 per axis), empty-axis-result when no successes, pass-rate threshold split (composites 0.9/0.8/0.5/0.3 at threshold 0.7 → 0.5), pass-rate-zero floor on errored-only runs, pass-rate one when all pass, and serde round-trip for both types in successful and errored shapes. `chrono::DateTime<Utc>` for timestamps (already a workspace dep used in `ba/provenance.rs`, `agent_routing.rs`, etc.). Sentinel-domain tests: **513 passing (499 prior + 14 new).** Zero new clippy warnings on `eval/result.rs`.

- **A12 Phase 2 — `FilesystemEvalCorpus` loader + `sentinel eval list` CLI** (2026-05-18). First runtime surface for the A12 external-benchmark track (Phase 1 shipped the `eval/case` + `eval/rubric` domain types). Operators can now drop `EvalCase` JSON files under `~/.claude/sentinel/state/ba-corpus/cases/` (public split) and `~/.claude/sentinel/state/ba-corpus/private-test-split/` (held-back, per spec §3.4) and enumerate them with `sentinel eval list` (table or `--json`). Future phases wire the corpus into the benchmark runner (load → A2-router-dispatch → score against rubric → emit results).

  **New `crates/sentinel-infrastructure/src/eval_corpus.rs`** (~370 lines + 15 unit tests): `FilesystemEvalCorpus` adapter with `at_dir(path)` + `with_default_path()` constructors. `list_case_ids()` walks both split directories and returns `Vec<CorpusEntry { case_id, is_private_test }>`. `load_case(case_id)` tries the public dir first then the private-test-split — malformed JSON surfaces with a path-qualified `anyhow::Error` so operators see exactly which file failed. Honors the spec §3.4 rule that private-split cases are loadable by case_id but flagged so callers can refuse to render them in public outputs. Tests cover: empty corpus, populated public-only, populated private-only, mixed, malformed JSON, non-existent dir, case_id collision across splits (private wins). TempDir-isolated; zero shared filesystem state.

  **New `crates/sentinel-cli/src/eval_cmd.rs`** (~150 lines + 6 unit tests): `pub fn list(json: bool, dir_override: Option<String>) -> Result<()>`. Builds the corpus via `build_corpus` (uses override path when provided, else `with_default_path()`), enumerates cases, and renders either a `serde_json` payload or a 2-column table (`case_id`, `split`) with a summary header showing total/public/private counts. Empty-corpus path emits operator guidance pointing at the expected drop directories.

  **`crates/sentinel-cli/src/main.rs` wiring**: `mod eval_cmd;` + new `Eval { action: EvalAction }` clap variant with `EvalAction::List { json: bool, dir: Option<String> }`. Dispatch routes `Commands::Eval { action: EvalAction::List { json, dir } }` to `eval_cmd::list(json, dir)`. `crates/sentinel-infrastructure/src/lib.rs` exposes the new `eval_corpus` module alongside the existing adapters.

  Clippy clean on the new code: redundant `continue;` removed from `list_case_ids` traversal; `if let Some/else` collapsed to `map_or_else` in `load_case` + `build_corpus`; doc backticks on `case_id`; literal-in-format-string fixes (`"  {:<40}  split", "case_id"` and `"  {:-<40}  --------", ""`). Test counts post-change: sentinel-infrastructure 251 passing (236 prior + 15 new), sentinel-cli 145 passing (139 prior + 6 new). Pre-existing `phase_gate` failures (task #21) unchanged.

- **SEN-4 + SEN-19 — Issue auto-suggest + enterprise operational summary (2026-05-18, `107a7ec`).** New `crates/sentinel-application/src/issue_suggest.rs` ranks historical Linear issues against a candidate title via Jaccard similarity over a stop-word-filtered token set; returns suggested estimate (median), priority (mode, ties broken by more-urgent), and labels (present in ≥ceil(50%) of usable neighbours) with same-team boost on ties and Confidence reflecting neighbour count. New `crates/sentinel-application/src/operational_summary.rs` is the single integration point that reads every shipped sentinel summary file (deploys, lead-times, change-failure, stage-thresholds, per-stage-breakdown, throughput, first-time-pass, cost-per-point, ROI, cache-efficiency, pr-review) and writes one combined `operational-summary.json` with per-section `loaded` flags so consumers render "no data" cleanly when files are missing. 13 unit tests; pure data layers.

- **SEN-5 + SEN-16 — Throughput + first-time-pass + rework cost (2026-05-18, `5de774e`).** New `crates/sentinel-application/src/throughput.rs` reads cycle-time events to produce per-team `completed_30d`/`completed_7d`/`completed_per_week`/`completed_per_month` plus a 60-day daily completion sparkline (SEN-5), and detects rework loops by spotting "back to In Progress after entering Code Review / QA Testing / QA Failed" — emitting per-ticket `had_rework_loop` flags and per-team FTP rate + optional rework-cost-USD sum joined against `tokens-per-ticket.jsonl` (SEN-16). 13 unit tests; pure data layers.

- **SEN-3 + SEN-6 — Cycle-time prediction + forced decomposition (2026-05-18, `ef15cb9`).** New `crates/sentinel-application/src/cycle_time_prediction.rs` consumes the SEN-17 per-stage breakdown snapshot to predict a ticket's total cycle time + per-stage path expectations with a Confidence tier from the minimum sample count along the path; team-specific rows preferred, team-agnostic fallback. Plus a `requires_decomposition(prediction, rule)` rule that fires when estimate > 8h OR predicted total > sum(p75) × 2.0 (defaults configurable via `DecompositionRule`). 11 unit tests; pure functions.

- **SEN-2 + SEN-17 — Cycle-time analytics (stale thresholds + per-stage breakdown) (2026-05-18, `73f8721`).** New `crates/sentinel-application/src/cycle_time_analytics.rs` consumes `cycle-time.jsonl` and emits two percentile-driven summaries: per-(team, from_stage) p50/p75/p90 dwell time for stale-ticket threshold calibration (SEN-2), and per-(team, stage) mean + p50 + p75 for downstream metric consumers (SEN-17). Both reuse a single `pair_transitions` helper that diffs adjacent cycle-time events per `issue_id`. 9 unit tests.

- **SEN-10 + SEN-11 — Lead time, CFR, MTTR collectors (2026-05-18, `3e3d2ce`).** Two new application-layer modules complete the four DORA core metrics (deploy freq shipped as SEN-9). `crates/sentinel-application/src/lead_time.rs` carries `LeadTimeRecord`, append/read helpers, and `aggregate_at` producing per-(repo, env) rolling 30-day p50/p75/p95 with DoraTier from p50. `crates/sentinel-application/src/change_failure.rs` carries `IncidentRecord`, append/read helpers, and an `aggregate_at` that joins `deploys.jsonl` to incidents within a 24h attribution window — CFR = `failed_deploys / deploys` (denominator-zero safe), MTTR = median of `(completed_at - created_at) / 1h` over recovered incidents, both producing per-(repo, env) DoraTier classifications. `deploy_freq.rs` gained three boundary-classifier helpers — `DoraTier::from_lead_time_hours`, `from_change_failure_rate`, `from_mttr_hours` — so the Rust collectors produce stable tier labels. 20 new unit tests across the modules (lead-time round-trip / missing file / malformed line / window filter / percentile correctness / garbage-hours skip; change-failure round-trip / missing file / zero-deploy CFR / in-window attribution / out-of-24h rejection / cross-(repo, env) isolation / MTTR exclusion of unrecovered / MTTR zero / malformed line; boundary coverage for all three new classifiers). CLI wrappers + Hookdeck/webhook ingest deferred to follow-ups — this PR ships the application-layer seam in the same shape as SEN-9. **Verification gap:** `cargo build` is currently blocked workspace-wide by a pre-existing missing sister-repo path dep (`legatus-ai`) — also blocks builds on `main`. The new modules follow `deploy_freq.rs`'s verified pattern line-for-line; restoring the sister repo unblocks `cargo test -p sentinel-application` against this commit.

- **BA1+3 Phase 4c — Config loader + `hook_cmd.rs` wiring (BA1+3 enforcement is LIVE)** (2026-05-17). Final BA1+3 phase. The substrate (Phases 1+2) + hooks (Phase 3a/b/c) + adapters (Phase 4a/b) are now stitched into the production hook engine. Operators flipping a mode in `~/.claude/sentinel/config/ba-enforcement.toml` from `ObserveOnly` to `DefaultBlocking` makes BA1 + BA3 structural-violations *impossible* at the PreToolUse layer for the BA-vertical product.

  **New `crates/sentinel-infrastructure/src/ba_config.rs`** (~250 lines + 12 unit tests): `BaEnforcementConfig` struct carrying both hooks' `ValidationMode` choices. Builder API: `observe_only()` (the safe-rollout constant), `shipped()` (loads compile-time embedded defaults), `from_toml_str(s)` (parses operator-supplied TOML), `with_shipped_and_overrides(Option<&Path>)` (production startup path — shipped defaults overridden by `~/.claude/sentinel/config/ba-enforcement.toml` when present). TOML schema: `[provenance_validate] mode = "ObserveOnly"` + `[requirements_traceability_gate] mode = "ObserveOnly"`. Missing sections fall back to `ObserveOnly`; unknown mode strings surface at startup with a descriptive `anyhow::Error` (operator sees the typo at boot, not at production runtime when a citation fails silent enforcement). Hand-rolled mode-string parsing via `match` (no serde derive on the hook `ValidationMode` enums — they stay decoupled from infrastructure config concerns).

  **New `config/ba-enforcement-defaults.toml`** baked in via `include_str!`. Both hooks default to `ObserveOnly` (the spec §3 rollout posture). File includes extensive header comments explaining the three modes + the ratchet-up sequence: `ObserveOnly` → `DefaultBlocking` (once telemetry shows the BA6 connector layer reliably emits `provenance_audit` events AND the BA-orchestrator writes matrix snapshots) → `StrictBlocking` (per-output, for catastrophic-class tools).

  **`crates/sentinel-cli/src/hook_cmd.rs` wiring**: three integration points land together. (1) **Adapter construction** at session start — both adapters degrade gracefully to `None` if `with_default_path()` fails: `JsonlProvenanceStore` (failure → BA1 audit lift + validation inert with `tracing::info` log), `FilesystemRequirementMatrix` (failure → BA3 traceability inert). The dirs::home_dir failure path is the only realistic trigger on configured systems. (2) **Config load** via `BaEnforcementConfig::with_shipped_and_overrides(home/.claude/sentinel/config/ba-enforcement.toml)` — fallback to `BaEnforcementConfig::observe_only()` on parse failure (safe degrade: operator sees the warn log + their typo'd config is ignored rather than crashing the hook engine). (3) **Hook calls**: in the `PostToolUse` arm, `audit_extract` fires at the TOP of the arm via `hooks::audit_extract::process(&input, provenance_arc.as_ref())` (gated on `provenance_store.is_some()`); in the `PreToolUse` arm, `provenance_validate` + `requirements_traceability_gate` fire BEFORE `dry_run_then_commit` via `time_and_record` (with per-hook metric labels for observability) — both gated on their respective adapter `is_some()`. Each hook output goes through `output.merge(&...)` so block decisions propagate to the final HookOutput.

  **End-to-end activation path** (when operator flips modes): (a) operator edits `~/.claude/sentinel/config/ba-enforcement.toml` to set both modes to `DefaultBlocking`. (b) Next sentinel hook invocation reads the file at startup, builds `BaEnforcementConfig { provenance_validate_mode: DefaultBlocking, requirements_traceability_mode: DefaultBlocking }`. (c) Claude tries to publish a BA output with citations: a `Bash` or BA-orchestrator tool call carrying `input.extra.artifacts: [...]` triggers `provenance_validate` in the PreToolUse arm. (d) The hook queries `JsonlProvenanceStore::query_artifact_history(artifact_id)` for each citation, runs the 4-check tree (Existence/Freshness/ProvenanceClass/WithinSession) with the operator's `DefaultBlocking` mode, returns `HookOutput::deny()` on Block-class findings (Existence). (e) Operator's tool call is denied with a `[Sentinel-Authority]`-prefixed reason naming each failing citation. Same path for `requirements_traceability_gate` reading `FilesystemRequirementMatrix`. (f) When everything is structurally clean, both hooks return `allow()` and the downstream `dry_run_then_commit` + `tool_usage_gate` checks run normally.

  12 unit tests on `BaEnforcementConfig` cover: shipped defaults parse cleanly + both modes are `ObserveOnly`; `observe_only()` constant matches shipped (no drift between the const and the TOML); explicit `DefaultBlocking` parses for both hooks; explicit `StrictBlocking` parses for both hooks; empty TOML falls back to `ObserveOnly`; only-one-section TOML leaves the other section at `ObserveOnly`; unknown mode string `"TotallyMadeUp"` errors with a descriptive message naming the invalid value + expected variants; malformed TOML surfaces at parse time with "parse" in the message; `with_shipped_and_overrides(None)` uses shipped only; `with_shipped_and_overrides(Some(<nonexistent path>))` uses shipped only; `with_shipped_and_overrides(Some(<real operator file>))` applies the override (verified with a `tempfile::TempDir`-backed operator file containing `DefaultBlocking + StrictBlocking`); `BaEnforcementConfig` is `Send + Sync + Copy`. Sentinel-infrastructure: 236 tests passing (224 prior + 12 new). Sentinel-cli: 139 tests passing (unchanged — hook_cmd.rs modifications are additive and behind `is_some()` gates so the existing test paths see `None` adapters and skip the new hooks).

  Clippy clean on the new code (`ba_config.rs` zero warnings; the new `hook_cmd.rs` insertions in lines 260-313, 547-577, 710-715 produce zero new clippy warnings — the remaining infrastructure warnings on `hook_cmd.rs` are pre-existing on lines 26, 47, 143, 353 from the existing run/run_internal structure). Pre-existing `phase_gate` test failures (task #21) unchanged.

  **Out of scope for Phase 4c (future Phase 4d)**: `config/ba-outputs.toml` tool-name registry — currently the hooks self-gate via structural field-presence triggers (`input.extra.artifacts` for `provenance_validate`; `input.extra.requirement_refs` / `is_recommendation` for `requirements_traceability_gate`), which is sufficient for any BA flow that opts in. A future Phase 4d will add tool-name gating as an additional filter so non-publish BA tools also skip. Also out of scope: the BA6 connector-layer + the BA-orchestrator runtime — sentinel can structurally enforce BA1+BA3 the moment a downstream caller emits the citations + matrix snapshots, but the upstream emitters are separate engineering tracks.

  **BA1+3 status (after this commit)**: substrate, hooks, adapters, config, and integration are all live. Phase 4c finishes BA1+3 — every future change is either calibration (operators flipping modes, adjusting lookback windows) or expansion (new connectors emitting `provenance_audit`, new orchestrators writing matrix snapshots) rather than substrate work. The product's S-tier disciplines BA1 + BA3 are now enforceable end-to-end whenever the upstream emitters are ready.

- **BA1+3 Phase 4b — Filesystem `RequirementMatrixPort` adapter** (2026-05-17). Second production adapter for BA1+3 per `docs/ba1-ba3-sentinel-enforcement.md` §9. Loads per-orchestration matrix snapshots from `~/.claude/sentinel/state/requirement_matrix/{orchestration_id}.json` (the future BA-orchestrator writes these snapshots; sentinel only reads). New `crates/sentinel-infrastructure/src/requirement_matrix.rs` (~150 lines + 12 unit tests). **`FilesystemRequirementMatrix`** implements `RequirementMatrixPort::query_requirement(orchestration_id, matrix_row_id)`. Builder API: `at_dir(PathBuf)` (test-pointed), `with_default_path()` (`~/.claude/sentinel/state/requirement_matrix/`). `base_dir()` accessor for operator tooling. Lazy-load: each query reads the snapshot file fresh — operators editing the snapshot see changes on the next query without restart. Phase 4c may add mtime-based caching; Phase 4b is correctness-first. **Snapshot file shape**: `{"rows": [...RequirementRef...]}` — `MatrixSnapshot` struct deserialized via serde; `rows` defaults to empty if the field is absent. The BA-orchestrator authors these files; sentinel never writes them (read-only access by design). **Error mapping** per spec §7 + §8.3: file missing → `Err(UnknownOrchestration(orch_id))` (hook maps to BA3 `Existence` finding — Block); file present but JSON malformed / schema mismatch on `rows` field type → `Err(Malformed(msg))` (hook maps to soft-warn — operator log only, never block); transient filesystem error reading the file → `Err(MatrixUnavailable(msg))` (hook surfaces `MatrixStaleness` warn per Phase 3c); file parsed cleanly with row match → `Ok(Some(row))`; file parsed cleanly but no row match → `Ok(None)` (hook maps to BA3 `Existence` finding — phantom row Block). The distinction between `Ok(None)` (orchestration tracked, row missing) and `Err(UnknownOrchestration)` (orchestration file doesn't exist) is what spec §7.1 calls out — different operator-facing diagnostics for "typo in matrix_row_id" vs "typo in orchestration_id". **`last_known_good` semantics** per spec §8.3: the snapshot file IS the cache — when a future HTTP-fronted variant is built, it'll fall back to the file on network failure. Phase 4b only needs the file path; future enhancements layer HTTP on top. 12 unit tests use `tempfile::TempDir` for filesystem isolation: missing file → `UnknownOrchestration` with the orchestration_id named; happy path returns matching row with all fields populated; `Ok(None)` distinct from `UnknownOrchestration` when the orchestration file exists but the row_id doesn't match; malformed JSON → `Malformed` with "parse failed" in the message; wrong-schema-but-still-JSON (missing `rows` field) defaults to empty rows → `Ok(None)` per serde's `default` annotation; `rows` field with wrong type (string not array) → `Malformed`; per-orchestration snapshot scoping (case-A and case-B read from distinct files even with same row_ids); `snapshot_path` uses orchestration_id as filename verbatim; lazy reload — operator edits snapshot mid-test, next query sees the new content; `with_default_path` resolves under `~/.claude/sentinel/state/requirement_matrix`; `Send + Sync` compile-time assert; usable through `&dyn RequirementMatrixPort` trait object. Module registered alphabetically in `crates/sentinel-infrastructure/src/lib.rs` between `rate_limit` and `reversibility`. Sentinel-infrastructure: 224 tests passing (212 prior + 12 new). Clippy clean on the new module. Out of scope: Phase 4c (`config/ba-enforcement.toml` + `config/ba-outputs.toml` + `hook_cmd.rs` wiring that constructs `JsonlProvenanceStore` + `FilesystemRequirementMatrix` at session start and registers the three BA1+3 hooks in the PreToolUse/PostToolUse arms with operator-configured `ValidationMode`).

  **Together with Phase 4a, both production adapters now exist**. Phase 4c wires them into the hook engine — once it ships, operators flipping the `ValidationMode` from `ObserveOnly` to `DefaultBlocking` makes BA1 + BA3 structural violations *impossible* at the PreToolUse layer.

- **BA1+3 Phase 4a — JSONL-backed provenance store (`ProvenancePort` + `ProvenanceWritePort`)** (2026-05-17). First production adapter for BA1+3. Implements BOTH `ProvenancePort` (read) AND `ProvenanceWritePort` (write) over a single append-only JSONL file at `~/.claude/sentinel/state/provenance/records.jsonl`. The `audit_extract` hook (Phase 3a) writes; the `provenance_validate` hook (Phase 3b) reads. Same backing file → no race window between "lift emitted" and "validate sees the record". New `crates/sentinel-infrastructure/src/provenance_store.rs` (~250 lines + 11 unit tests). Mirrors the [`JsonlAppraisalStore`] pattern from A2 Phase 3c: POSIX-atomic appends via `OpenOptions::create(true).append(true)` (record lines are <1KB so well within `PIPE_BUF` for guaranteed-atomic concurrent appends from multiple sentinel processes); malformed-line tolerance (`serde_json::from_str` failure → `tracing::warn` with line number + skip, don't poison the file); auto-truncation at `MAX_LOG_SIZE` (8 MB ≈ 50_000 records) — when the file exceeds the cap, the next `record()` call acquires an internal `Mutex<()>` guard, reads all records, keeps the trailing half, and rewrites via atomic tmp-file + rename so a crash mid-write doesn't truncate to zero. Path-creation: `record()` auto-creates the parent directory tree (so fresh installs without `~/.claude/sentinel/state/provenance/` work without operator setup). **Best-effort semantics**: errors surface through `ProvenanceError::StoreUnavailable(msg)` (filesystem failures, parent-dir creation failures, append failures) and `ProvenanceError::Malformed(msg)` (serialize failures). The `audit_extract` hook treats both as soft-warn per Phase 3a's design contract — write failures emit `tracing::warn` but never propagate as Block. Single-instance dual-trait impl: the same `JsonlProvenanceStore` plays both roles simultaneously — Phase 4c will mount one instance into the `hook_cmd.rs` HookContext and hand `&dyn ProvenancePort` to `provenance_validate` and `&dyn ProvenanceWritePort` to `audit_extract`. `query_artifact_history` scans the full file and filters by `artifact_id` (Phase 4c's lookback window is enforced by the validate hook itself, not by the store; the store returns the full per-artifact history and the hook filters). 11 unit tests use `tempfile::TempDir`: missing file returns empty history; record+query round-trip with multiple records for the same artifact; segregation by `artifact_id` (queries don't leak across artifacts); auto-creates parent dirs; cross-instance persistence (second `JsonlProvenanceStore` instance reads what the first wrote); **dual-trait test** — single instance is used through both `&dyn ProvenanceWritePort` (write) AND `&dyn ProvenancePort` (read) in the same test, confirming the same struct plays both roles; malformed line injected mid-file does NOT poison the read (valid records before+after still counted); oversized-file truncation (pad past MAX_LOG_SIZE → next record triggers truncate → file size drops below cap → records still readable); default-path resolves to `.claude/sentinel/state/provenance/records.jsonl`; `Send + Sync` compile-time assert. Module registered alphabetically in `crates/sentinel-infrastructure/src/lib.rs` between `proof_store` and `qdrant`. Sentinel-infrastructure: 212 tests passing (201 prior + 11 new). Clippy clean on the new module. Out of scope: BA1+3 Phase 4b (matrix adapter implementing `RequirementMatrixPort` with `last_known_good` fallback per spec §8.3); Phase 4c (`config/ba-enforcement.toml` + `config/ba-outputs.toml` + `hook_cmd.rs` wiring that constructs the adapters at session start and registers the three BA1+3 hooks in PreToolUse/PostToolUse arms).

- **BA1+3 Phase 3c — `requirements_traceability_gate` PreToolUse hook** (2026-05-17). Third and final BA1+3 hook per `docs/ba1-ba3-sentinel-enforcement.md` §2.3 — the BA3 enforcement engine. Reads `input.extra.requirement_refs` (a `Vec<RequirementRef>` per Legatus AI ADR-017) AND `input.extra.is_recommendation: bool`, validates each citation against the `RequirementMatrixPort` from Phase 2, and runs the spec's four-check decision tree: (1) **Existence** — matrix lookup returns `Ok(None)` OR `Err(UnknownOrchestration)` → Block (recommendation traces to a phantom row). (2) **Hash** — cited `content_hash` differs from the live matrix row's hash → warn (DefaultBlocking) or block (StrictBlocking); the requirement was edited after the recommendation was authored. (3) **Coverage** — `is_recommendation: true` AND `requirement_refs` is empty → Block. **The central BA3 structural violation**: a recommendation that doesn't trace to any stated requirement. (4) **MatrixStaleness** — when the matrix endpoint is unreachable (`Err(MatrixUnavailable)`), the hook surfaces a `MatrixStaleness` finding so the operator sees the cache age. Always warn (matrix availability isn't catastrophic-bound; the snapshot still validates). New `crates/sentinel-application/src/hooks/requirements_traceability_gate.rs` (~360 lines + 18 unit tests).

  **`ValidationMode`** mirrors the Phase 3b shape (`ObserveOnly` / `DefaultBlocking` / `StrictBlocking`) with the same rollout posture — operators ratchet up from telemetry to enforcement. Const-fn predicates: `allows_blocking()` (false only for `ObserveOnly`) + `hash_blocks()` (true only for `StrictBlocking`). Coverage + Existence always block in any blocking mode — they're the structural-violation signals BA3 is designed to make impossible at the hook layer rather than caught after-the-fact by BA5 critique. The mode type is re-declared (not re-imported from `provenance_validate`) so BA1 + BA3 enforcement stay decoupled at the type level — future specs could diverge mode values.

  **Trigger semantics**: silently allows when BOTH `requirement_refs` is absent/empty AND `is_recommendation` is absent/false. Either field signals "this is a BA flow I want validated"; both absent → non-BA tool, skip. Phase 4's `config/ba-outputs.toml` will add a tool-name gate so non-publish BA tools also skip. Malformed `requirement_refs` entries are logged + skipped individually — a partial list still validates the well-formed entries. Empty `requirement_refs` array combined with `is_recommendation: true` triggers the Coverage check (the central BA3 case).

  **Block message format**: `format_block_message` renders **every finding** (not just `RequirementFinding::is_block()`-true ones) because the mode-aware blocking decision is made at the hook layer, not the finding layer. Strict-mode hash mismatches contribute to the block decision even though the domain-level `RequirementFinding::Hash.is_block()` returns false (warn-by-default). The deny reason names each failing matrix row by `(orchestration_id, matrix_row_id)` and renders the structural violation prose (Existence: "matrix row `R-001` in orchestration `case-1` does not exist — recommendation traces to a phantom requirement."; Coverage: "Coverage: recommendation `Raise prices by 8%` ships with NO requirement_refs — this is the structural BA3 violation."; Hash: "Hash: row `R-001` in `case-1` cites content_hash=\"old\" but the live matrix reports \"new\"."). The fix line directs operators to add `requirement_refs` + use `hygiene_override` for legitimate edge cases.

  **Port-error semantics**: `MatrixUnavailable` is soft-warn + adds a `MatrixStaleness` finding (operator sees the unavailability without blocking); `Malformed` is soft-warn (operator log only). Both flow through without contributing to the block decision — spec §8.3's `last_known_good` fallback rationale (matrix availability is not catastrophic-bound; rejecting valid recommendations because the endpoint is flaky would create a worse operator experience than warning).

  18 unit tests use a `StubMatrix` (HashMap of canned rows + optional `next_error` queue): allow paths — neither field present allows; `is_recommendation: false` with no refs allows; well-formed refs with matching matrix row allows. Coverage — `is_recommendation: true` with no refs blocks with reason naming "Coverage" + the recommendation_summary; tool-name fallback when no summary supplied; non-recommendation with empty refs does NOT trigger Coverage. Existence — phantom row blocks with reason naming "Existence"; `UnknownOrchestration` error blocks (mapped to Existence). Hash — mismatch warns in `DefaultBlocking`, blocks in `StrictBlocking` with reason naming "Hash". Port errors — `MatrixUnavailable` does not block (soft-warn); `Malformed` does not block. ObserveOnly — phantom row + Coverage failure both suppressed (allow). Mode predicates — `allows_blocking` true only for blocking modes; `hash_blocks` true only for Strict. Aggregate — block message lists every failing row by id; malformed `requirement_ref` entries skipped individually without poisoning well-formed ones. Module registered in `crates/sentinel-application/src/hooks/mod.rs` between `provenance_validate` and `session_end` + added to `HOOK_NAMES`. Sentinel-application: 1116 tests passing (1098 prior + 18 new). Pre-existing `phase_gate` failures (task #21) unchanged. Clippy clean on the new module (zero warnings after addressing doc-markdown backticks on `PreToolUse` / `MatrixStaleness` / `HashMap`, splitting too-long first-paragraph doc on `ValidationMode`, collapsing the duplicate `Ok(None)` + `Err(UnknownOrchestration)` arms via the `|` or-pattern, and using `Option::map_or_else` over the `map().unwrap_or_else()` chain on the summary fallback).

  **BA1+3 Phase 3 complete**: all three hooks (`audit_extract` PostToolUse + `provenance_validate` PreToolUse + `requirements_traceability_gate` PreToolUse) now exist. The structural-enforcement substrate is ready for Phase 4 (production adapters + `config/ba-enforcement.toml` + `config/ba-outputs.toml` + integration into `hook_cmd.rs`). Operators flipping the modes from `ObserveOnly` → `DefaultBlocking` will make BA1 + BA3 structural-violations *impossible* at the PreToolUse layer for the BA-vertical product.

- **BA1+3 Phase 3b — `provenance_validate` PreToolUse hook** (2026-05-17). Per `docs/ba1-ba3-sentinel-enforcement.md` §2.2 — the engine that turns BA1 from aspirational to structural. Reads `input.extra.artifacts` (a `Vec<ArtifactReference>` per Legatus AI ADR-017), looks each cited `artifact_id` up via the Phase 2 `ProvenancePort`, and runs the spec's four-check decision tree per citation: (1) **Existence** — empty history → Block. (2) **Freshness** — cited `content_hash` differs from the most-recent record's hash → warn (DefaultBlocking) or block (StrictBlocking). (3) **ProvenanceClass** — cited class differs from what the connector reported → warn (always; the operator may have legitimately reclassified, surface but don't block). (4) **WithinSession** — the most-recent retrieval's session differs from the current session AND is older than the configurable lookback window (default 24h) → warn (DefaultBlocking) or block (StrictBlocking). New `crates/sentinel-application/src/hooks/provenance_validate.rs` (~340 lines + 19 unit tests).

  **`ValidationMode`** drives warn-vs-block per spec §3. Three variants with const-fn predicates for compile-time-stable dispatch: **`ObserveOnly`** never blocks regardless of finding severity (the rollout posture — gather telemetry on what WOULD be blocked before flipping to enforcement; would-be-blocks log `tracing::warn` for ratchet-up calibration); **`DefaultBlocking`** blocks on Existence only, warns on the other three findings (the steady-state posture for routine BA outputs); **`StrictBlocking`** blocks on Existence + Freshness + WithinSession, warns on ProvenanceClass (for catastrophic-class outputs — board decks, signed customer briefs, regulatory filings). `mode.allows_blocking()`, `mode.freshness_blocks()`, `mode.within_session_blocks()` are the predicate constants the validation pipeline queries.

  **Trigger semantics**: `input.extra.artifacts` MUST be a non-empty JSON array of `ArtifactReference` for the hook to fire — absent / empty / malformed → silent allow (non-BA tools never trip this hook even though it's registered as a PreToolUse hook). Phase 4's `config/ba-outputs.toml` will additionally gate by tool name (so non-publish BA tools also skip), but for Phase 3b the field-presence trigger is sufficient to validate any BA flow that opts in. Malformed citation entries within an array are logged + skipped individually — a partial citation list still validates the well-formed entries.

  **Block message format**: `format_block_message` aggregates every failing check into a structured deny-reason. Each failing citation is named on its own line with `describe_finding` rendering per-variant: `"Existence: no retrieval record exists for FIR-123 — the connector was never called for this artifact in the audit window."` / `"Freshness: FIR-123 cites content_hash=stale-hash but the latest retrieval reports actual-hash."` / `"WithinSession: FIR-123 last retrieved at <ts> is older than the lookback cutoff <ts> and not from the current session."`. Operators see precise diagnostics; the message also names `hygiene_override` as the audited escape valve. Wrapped by the existing `HookOutput::deny()` so the `[Sentinel-Authority]` prefix lands automatically (the deny path stores the reason in `hook_specific_output.permission_decision_reason`).

  **Port-failure semantics**: `ProvenanceError` from the audit-chain query is a soft-warn, not a block. The validate hook treats the failed-query citation as if the check passed and continues; an operator log entry surfaces the port failure with the offending artifact id. Spec §8.1 rationale: blocking on audit-log availability would create a single point of failure that masks real validation logic; the validate hook should ratchet up coverage as the storage layer matures, not gate behind it.

  **Tied to Phase 3a**: the lift hook writes; this hook reads. Both go through the same `ProvenanceWritePort` + `ProvenancePort` traits; Phase 4's JSONL adapter will impl both over a shared backing file, closing the audit chain end-to-end.

  19 unit tests use a `StubPort` (HashMap of canned histories + optional `next_error` queue): allow paths — no `artifacts` field allows; empty `artifacts` array allows; malformed `artifacts` (not an array) allows; port error → soft-warn allow; happy path (citation + matching record) allows; Existence — missing history blocks in `DefaultBlocking` with reason naming "Existence" + the artifact_id, blocks in `StrictBlocking`, does NOT block in `ObserveOnly` (suppression telemetry); Freshness — hash mismatch warns in `DefaultBlocking`, blocks in `StrictBlocking` with reason naming "Freshness"; ProvenanceClass — class mismatch is ALWAYS warn (even in `StrictBlocking` — spec is explicit); WithinSession — 48h-stale record warns in `DefaultBlocking`, blocks in `StrictBlocking` with reason naming "WithinSession"; 1h-old record from a different session passes (within 24h lookback); aggregate — block message lists every failing citation; latest-record pick — when history has multiple records, validation uses the LATER timestamp's hash; mode predicates (`allows_blocking`, `freshness_blocks`, `within_session_blocks`) return the spec-correct boolean per variant. Module registered in `crates/sentinel-application/src/hooks/mod.rs` between `pre_push_browser_test` and `session_end` + added to `HOOK_NAMES`. Sentinel-application: 1098 tests passing (1082 prior + 19 new — the 19 includes some tests that incrementally counted differently across the parallel-hook runs). Pre-existing `phase_gate` failures (task #21) unchanged. Clippy clean on the new module (zero warnings after addressing unused `ProvenanceClass` import in main code via `#[cfg(test)]` re-import, doc-markdown backticks on `PreToolUse` / `ProvenanceClass` / `WithinSession` / `ProvenancePort` / `HashMap`, switching `push_str(&format!(...))` to `writeln!(buf, ...)` per `format_in_format_args` lint, and hoisting the mutex-guard `next_error.take()` out of the `if let` scrutinee to avoid temporary-lifetime warning). Out of scope: Phase 3c (`requirements_traceability_gate` PreToolUse hook for BA3), Phase 4 (JSONL adapter + `config/ba-enforcement.toml` + `config/ba-outputs.toml`).

- **BA1+3 Phase 3a — `audit_extract` hook + `ProvenanceWritePort`** (2026-05-17). Per `docs/ba1-ba3-sentinel-enforcement.md` §2.1. Adds the FIRST of the three BA1+3 hooks: a `PostToolUse` lift hook that detects successful documentation-connector calls (`mcp__*` tools that emit a structured `provenance_audit` field per BA6 §3.4) and persists a `RetrievalRecord` via the new `ProvenanceWritePort`. The future `provenance_validate` hook (Phase 3b) reads the same chain via `ProvenancePort` to validate BA-orchestrator citations — the read + write sides share storage, so Phase 4's JSONL adapter will impl both traits over the same backing file. New `crates/sentinel-domain/src/ports.rs` addition: **`ProvenanceWritePort`** — `fn record(&self, RetrievalRecord) -> Result<(), ProvenanceError>`. Returns `Result` so adapter IO failures can surface to consumer hooks (which log + drop, never propagate as Block — observational). Idempotency contract documented: implementations MAY skip duplicate `(artifact_id, content_hash, retrieved_at)` writes but MUST still return `Ok(())`; consumers de-dupe at read time. New `crates/sentinel-application/src/hooks/audit_extract.rs` (~200 lines + 13 unit tests): `pub fn process(input: &HookInput, provenance_writer: &dyn ProvenanceWritePort) -> HookOutput`. Decision pipeline: (1) Tool name missing → allow + skip. (2) Tool name doesn't start with `mcp__` → allow + skip (only MCP servers emit connector audit). (3) `extra.provenance_audit` field absent → allow + skip silently (the MCP tool isn't a documented connector per BA6 — older connectors / non-doc connectors don't emit). (4) `provenance_audit` field present but malformed (not an object, missing `artifact_id`/`content_hash`/`provenance_class`, unknown class string) → log `tracing::warn` + allow + skip. (5) `session_id` missing or empty → log warn + allow + skip (can't persist without session scoping). (6) Happy path: construct `RetrievalRecord { artifact_id, connector_name: tool_name, content_hash, provenance_class, session_id, retrieved_at: now() }` and call `provenance_writer.record(...)`. (7) Write failure → log warn + allow (NEVER propagate as Block — the tool call already completed; PostToolUse hooks are observational). The `connector_name` field is filled from the FULL tool name (`mcp__linear__get_issue` not just `linear`) so the audit chain knows exactly which MCP server + which tool emitted the retrieval — useful for downstream filtering when multiple connectors front the same upstream system.

  **Hex/DDD layering** preserved: port lives in `sentinel-domain`; hook lives in `sentinel-application` and consumes the port via `&dyn`; no IO in the hook itself; Phase 4 ships the production adapter in `sentinel-infrastructure`. **Always-observational contract**: every test asserts `output.blocked == None` regardless of input shape — there is no code path in this hook that returns a Block. The hook is named `audit_extract` rather than `audit_validate` deliberately: extracting + lifting is its job; validation is `provenance_validate`'s.

  13 unit tests cover (a) the allow-only contract — missing tool name allows; non-`mcp__` tool allows + skips; (b) the skip paths — non-MCP tools never produce records; MCP tools without `provenance_audit` field don't produce records (silently); malformed audit value (not an object) skips; missing `artifact_id` skips; unknown `provenance_class` string skips; missing/empty `session_id` skips; (c) the happy path — valid audit event with `SystemOfRecord` class persists a record with all 6 fields populated correctly (`artifact_id`, `connector_name` from tool name, `content_hash`, class, `session_id`, `retrieved_at`); `connector_name` is the FULL tool name (`mcp__confluence__get_page` not `confluence`); all four `ProvenanceClass` variants parse correctly when the connector sends each variant string; (d) best-effort persistence — when the stub writer returns `ProvenanceError::StoreUnavailable`, the hook still returns `allow()` and doesn't propagate. Tests use a `StubWriter` recording-stub that captures every `record()` call in a `Mutex<Vec>` + optional `next_error` queue. Module registered in `crates/sentinel-application/src/hooks/mod.rs` alphabetically between `agent_revocation` and `block_context` + added to `HOOK_NAMES` between `agent_revocation` and `bug_task_gate`. Sentinel-application: 1082 tests passing (1069 prior + 13 new). Sentinel-domain: 499 tests unchanged. Clippy clean on both new sections (zero warnings after doc-markdown backticks on `audit_extract` / `provenance_validate` / `PostToolUse` references in the new `ProvenanceWritePort` doc). The 3 pre-existing `phase_gate` test failures (task #21) unchanged. Out of scope: Phase 3b (`provenance_validate` PreToolUse hook), Phase 3c (`requirements_traceability_gate` PreToolUse hook), Phase 4 (`config/ba-enforcement.toml` + `config/ba-outputs.toml` + JSONL adapter implementing both `ProvenancePort` + `ProvenanceWritePort` + matrix adapter).

- **BA1+3 Phase 2 — `ProvenancePort` + `RequirementMatrixPort` traits in sentinel-domain** (2026-05-17). Second slice of BA1+3 (`docs/ba1-ba3-sentinel-enforcement.md` §9). Adds the two domain traits the future `provenance_validate` and `requirements_traceability_gate` hooks query — pure traits, no logic; Phase 3 (the application-layer hooks) and Phase 4 (the infrastructure adapters) follow. Appended to `crates/sentinel-domain/src/ports.rs` (matching the existing flat-ports convention rather than introducing a `ports/` subdirectory — same pattern as `AuditorPort` + `CapabilityRouterPort` + `AppraisalStorePort`). **`ProvenancePort`** — `query_artifact_history(artifact_id: &str) -> Result<Vec<RetrievalRecord>, ProvenanceError>`. Returns the FULL retrieval history for the artifact (not just the latest) so callers can pick the most-recent record, scan within a freshness window, or detect repeated retrievals (some workflows pull an artifact multiple times within a session — each should appear in the audit chain). Empty `Vec` means the artifact was never retrieved through any registered connector in the configured window → BA1's `Existence` finding fires. **`ProvenanceError`** — `StoreUnavailable(String)` for backing-store IO failures (hooks treat as soft-warn — the validator can't run without history but the call shouldn't be blocked on the audit log being temporarily down) + `Malformed(String)` for corrupt JSONL lines / schema mismatches. Hand-rolled `Display` + `std::error::Error` (no thiserror dep in sentinel-domain). **`RequirementMatrixPort`** — `query_requirement(orchestration_id: &str, matrix_row_id: &str) -> Result<Option<RequirementRef>, RequirementMatrixError>`. Returns the LIVE row content so the caller can compare against the citation's `content_hash` (BA3's `Hash` finding). The `Result<Option<…>>` shape distinguishes three states: `Ok(Some(row))` (row exists; validate fields), `Ok(None)` (orchestration is tracked but row doesn't exist → BA3 `Existence` finding), `Err(UnknownOrchestration)` (the orchestration_id itself is unknown — likely a typo, not a coverage failure). **`RequirementMatrixError`** — three variants: `MatrixUnavailable` (no `last_known_good` snapshot available — fresh install or never-fetched orchestration), `UnknownOrchestration` (orchestration_id has no matrix entry at all), `Malformed` (schema mismatch / corrupt response). Per spec §8.3 the production adapter (future Phase 4) will have a `last_known_good` fallback for ordinary unavailability — the `MatrixUnavailable` error is reserved for the no-snapshot-at-all case so callers can distinguish "transient flake" (use fallback) from "fundamentally cold cache" (must surface to operator). 6 unit tests pin behavior: `ProvenanceError::Display` names each variant + carries the payload string; `RequirementMatrixError::Display` same for all 3 variants (including the named-orchestration `UnknownOrchestration`); both errors `impl std::error::Error`; both are `Send + Sync`; both round-trip through JSON (so they can flow through proof-chain serialization); both traits are object-safe (compile-time check via `Box<dyn ProvenancePort>` / `Box<dyn RequirementMatrixPort>` in a test helper). Sentinel-domain: 499 tests passing (493 prior + 6 new). Clippy clean on the new section. **Drive-by cleanup**: deleted `plans/a3-3b.md` — a 61-byte stub from the A3 Phase 3b kickoff that's been flagged by `[Doc Cleanup]` hook every session since merge. The plan it described is captured in the A3 Phase 3b CHANGELOG entry; the stub adds no value and removing it clears the recurring doc-cleanup nudge. Out of scope: BA1+3 Phase 3 (the `audit_extract` / `provenance_validate` / `requirements_traceability_gate` hooks in sentinel-application); Phase 4 (the production adapters + `config/ba-enforcement.toml` + `config/ba-outputs.toml`); A12 Phase 2 (the `sentinel eval` CLI scaffold — A12 has no new domain port per spec §6, so its Phase 2 is application-layer not ports).

- **BA1+3 + A12 Phase 1 — BA-vertical and external-benchmark domain types in sentinel-domain** (2026-05-17). Kicks off two new tracks per `docs/ba1-ba3-sentinel-enforcement.md` §9 and `docs/a12-external-benchmarks.md` §3. Pure-domain value objects only — hooks, ports, adapters, and the `sentinel eval` CLI subcommand all land in future phases. **BA1+3** (`crates/sentinel-domain/src/ba/`): new `provenance.rs` defines `ProvenanceClass` (`SystemOfRecord | ExpertSynthesis | Inference | Unverified` — the four-tier classification from Legatus AI ADR-017), `ArtifactReference` (the citation wire format: `artifact_id` + `content_hash` + claimed `provenance_class` + `retrieved_at`), `RetrievalRecord` (one entry in sentinel's connector-audit chain; emitted by the future `audit_extract` hook), `ProvenanceCheck` (validation result with builder API + `has_block()` predicate), `ProvenanceFinding` (4 variants matching the spec's decision tree: `Existence` always-blocks, `Freshness` blocks-per-flag for catastrophic-class outputs, `ProvenanceClass` warn-only, `WithinSession` blocks-per-flag). New `requirements.rs` defines `RequirementRef` (four-tuple `orchestration_id + matrix_row_id + content_hash + statement` per ADR-017), `RequirementCheck` + `RequirementFinding` (4 variants: `Existence` blocks, `Coverage` blocks — the structural BA3 violation, `Hash` warn, `MatrixStaleness` warn). Module `ba/mod.rs` re-exports the public surface. The Block-class findings (`Existence` for provenance, `Existence` + `Coverage` for requirements) are exactly the structural failures BA1/BA3 exist to prevent — and once the future hooks ship, will be impossible to commit at the PreToolUse layer rather than caught after-the-fact by BA5 critique. **A12** (`crates/sentinel-domain/src/eval/`): new `case.rs` defines `EvalCaseId` + `EvalRunId` (newtype `String` IDs with empty-rejection like `AgentId`), `SourceCorpus` (3 variants per spec §3.2: `Public` with url + license, `PartnerContributed` with `RedactionLevel`, `SyntheticGenerated` with `generation_method`), `RedactionLevel` (None / Light / Heavy / Synthetic), `GoldArtifact` (the human-authored reference output), `GoldOutcomes` (real-world outcome data with `attribution_confidence` 0.0-1.0 + `observation_window_days`), `CaseProvenance` (`contributor` + `license` + `is_private_test` flag for the held-back split per spec §3.4), and `EvalCase` itself wrapping everything. `EvalCase::outcome_scoring_valid()` enforces the spec's rule that synthetic cases — even when they carry `gold_outcomes` — must NOT score on the `OutcomeRealism` axis because the outcome is fabricated. New `rubric.rs` defines `EvalAxis` (the 6 spec axes: `CitationDensityAccuracy`, `RequirementsCoverage`, `AlternativesSeriousness`, `TonalCalibration`, `OutcomeRealism`, `StakeholderFit`) with stable `snake_case` keys for serialization + `requires_outcomes()` predicate; `EvalAxisScore` (raw + weight, clamped at construction); `ScoringRubric` with per-axis weights (`ba_default()` weights `OutcomeRealism` at 2.0 and all others at 1.0 — total 7.0); `EvalScore` with weighted-mean composite computed at construction. Module `eval/mod.rs` re-exports the public surface and documents the R5 quarantine boundary: eval scores are dispatch input + reporting only, never training feedback. 34 unit tests across both tracks (15 for BA1+3, 19 for A12). Sentinel-domain: 493 tests passing (459 prior + 34 new). Clippy clean on the new modules. **Hex/DDD layering**: both modules live entirely in `sentinel-domain`; no IO, no business logic, no hooks. Future phases land ports + adapters in the application + infrastructure layers.

- **`CancelInstruction` hit emits explicit `InstructionResult { Declined }` via escalation loopback** (2026-05-17). Follow-up to the cancel handler that landed earlier today. When a cancel removes a queued instruction (`PersistentInbox::remove_by_id` returns true), the WS recv loop now feeds a synthetic `InstructionResult { Declined { reason } }` into the runtime's new `escalation_loopback` channel — same `mpsc::UnboundedSender<EscalationKind>` the daemon-side `LegatusHandle::escalate` uses, cloned onto the runtime side at `make_pair`/`make_pair_with_inbox` construction. The main `run_connect_hosted` select arm drains `escalation_rx` and signs+sends the result over the WS via the standard escalation path — exactly one "event → WS send" code path, no duplicated sink handling. The declined reason is `"cancelled by operator: <cancel.reason>"` when the operator supplied one, or `"cancelled by operator"` otherwise.

  Closes the accounting gap flagged in the prior commit's "Out of scope" note: Legatus AI no longer has to infer cancellation from a missing Result; the Declined record arrives explicitly with the operator's reason echoed back. Cancel-misses (instruction already drained or never queued) still don't emit anything — there's nothing for Legatus AI to confirm in that case, and the cancel arrival itself is the operator-side record.

  Implementation surface: **(a)** `handle.rs` adds `escalation_loopback: mpsc::UnboundedSender<EscalationKind>` to `LegatusRuntime`; both `make_pair` and `make_pair_with_inbox` clone the original sender (one copy goes to the handle, one to the runtime). **(b)** `client.rs::handle_inbound`'s `CancelInstruction` arm now constructs the `Declined` event when `removed=true` and sends it via the loopback; silent skip when the loopback receiver is gone (loop already exited / shutting down). **(c)** No changes to the public API surface — `LegatusHandle::escalate` semantics unchanged, daemon callers see the same channel.

  3 new tokio-async tests on `handle_inbound`: `cancel_instruction_hit_pushes_declined_via_loopback` (full path: append queued instruction → cancel → assert escalation_rx receives Declined with reason naming the operator's reason); `cancel_instruction_miss_does_not_push_anything` (cancel for never-queued id → escalation_rx is empty); `cancel_without_reason_uses_default_declined_reason` (reason field is `None` → declined reason is the default string). Sentinel-legatus: 23 tests passing (20 prior + 3 new). Zero net-new clippy errors over the pre-existing baseline.

- **Sentinel-side `CancelInstruction` handling — operator-driven undo for queued instructions** (2026-05-17). Completes the cancel path opened by the protocol-side `CancelInstruction` variant landed in `legatus-ai` earlier today. New `PersistentInbox::remove_by_id(instruction_id) -> bool` in `crates/sentinel-legatus/src/persistent_inbox.rs` (~+40 lines + 4 unit tests) plus a new `remove_by_id_under_lock(file, id)` private helper that mirrors `pop_first_under_lock`'s atomic-rewrite discipline: open RW, take exclusive `fs2` advisory lock, read all lines, filter out the first line whose parsed `RelayInstruction.instruction_id` matches, rewrite the remainder via `seek(0) + set_len(0) + writeln` per surviving line. Returns `true` on a hit, `false` on file-missing / no-match / lock-failure. Concurrent appends serialize on the same lock — no race window between the read and the rewrite.

  `client.rs::handle_inbound` now matches `Legatus AIMessage::CancelInstruction` and routes to `runtime.inbox.remove_by_id(cancel.instruction_id)`. Two log paths — `removed=true` emits an info log with the instruction id + operator reason ("CancelInstruction removed queued instruction"); `removed=false` (already drained into the model's context window, or never queued, or no persistent inbox in the standalone CLI path) emits "CancelInstruction arrived too late". No `InstructionResult` is currently fired for the cancelled id: when the instruction is gone from the inbox before draining, no `Legatus AI_inbox` hook will ever drain it and therefore no execution-log Stop will ever fire a Result — the cancel arrival itself is Legatus AI's confirmation. A future improvement could explicitly emit `InstructionResult { Declined { reason: cancel.reason } }` for the cancelled id; flagged in the code as deferred until the sentinel ↔ daemon Result-emission path is wired here.

  `lib.rs` re-exports `CancelInstruction` from `legatus_ai_protocol::messages` alongside the existing `RelayInstruction` / `InstructionOutcome` / `BlockReason` / `RuntimeKind` re-exports so downstream crates can match on the new variant without a direct path-dep on legatus-ai-protocol.

  4 new unit tests on `PersistentInbox`: `remove_by_id_on_missing_file_returns_false` (no panic, no file creation); `remove_by_id_removes_matching_queued_instruction` (FIFO order of survivors preserved); `remove_by_id_returns_false_when_id_not_queued` (queue untouched, returns false); `remove_by_id_only_removes_first_match` (defensive — if a duplicate ever lands on disk, drop one occurrence not all, matching operator "cancel this one queued copy" intent). Sentinel-legatus: 20 tests passing (16 prior + 4 new). Zero net-new clippy errors over the pre-existing baseline (two warnings I introduced — `Option::map_or` opportunity and missing backticks in a doc — fixed before commit).

  Reliability stance preserved: ~90-95% target. The cancel is **definitive** in the `removed=true` path (the instruction is gone from disk before any Claude Code turn can see it) and **advisory** in the `removed=false` path (instruction is already in the model's context window — sentinel logs the late cancel and lets the operator's follow-up instruction do the rest of the work). **Out of scope (deferred)**: explicit `InstructionResult { Declined }` emission for cancelled ids (needs the sentinel ↔ daemon Result wiring to land here, separate slice); mid-execution nudge for already-drained instructions (would need a prompt-injection path into the running model, big surgery and outside the cancel slice's footprint).

- **Mid-run failure classification for `InstructionResult` — `Success` is no longer the only outcome** (2026-05-17). Closes the second gap in the operator → Claude Code feedback loop: per-instruction Result reports now reflect what actually happened during the turn instead of unconditionally claiming `Success`. New `TurnSignal` enum in `crates/sentinel-application/src/legatus_client.rs` (~+100 lines + 6 unit tests) — currently one variant, `PermissionDenied { tool }`, recorded mid-turn by hooks and consumed at Stop. Per-session signals file at `~/.claude/sentinel/state/<session_id>.legatus-turn-signals.jsonl` follows the same append-on-event + read-and-clear-on-Stop pattern as the existing pending-instructions file; same sanitization (`turn_signals_path` rejects `/`, `\`, `\0`, `..`, empty), same best-effort I/O (debug-log on failure, never propagated). New `classify_outcome(signals, api_error) -> InstructionOutcome` is the single source of truth for the mapping: `Some(api_error)` → `Failure { error }`; any `PermissionDenied` signals → `Declined { reason: "permission denied for tool(s): X, Y" }`; otherwise `Success`. The `api_error` wins over signals because StopFailure is the more informative event (the turn aborted before the model could decide whether to declined-vs-failed retry).

  Three hook wirings: **(a)** `permission_denied.rs` now calls `note_turn_signal(session_id, PermissionDenied { tool })` alongside the existing `SessionBlocked` escalation — escalation is the operator-visible chat ping ("blocked on Bash"), the turn signal is the late-stage classification input. Skips silently when `session_id` is absent. **(b)** `execution_log.rs` (Stop hook) now reads turn signals via `take_turn_signals(session_id)` and feeds them through `classify_outcome(&signals, None)` to derive the per-instruction outcome — replaces the hard-coded `InstructionOutcome::Success`. The take-and-clear ensures stale signals don't leak into the next turn's classification. **(c)** `stop_failure.rs` (StopFailure hook — API-error path) now drains pending instructions early in the handler and fires `InstructionResult { Failure { error: "<error>: <error_details>" } }` for each, with no summary. Also clears any turn signals so a subsequent normal Stop on the same session doesn't double-classify on stale state. Pending-only branch — when there are no pending instructions the StopFailure path is unchanged (still rotates accounts, still emits the `[Rate Limit]` / `[Auth Error]` system messages).

  Reliability stance preserved from the inbox-persistence commit: ~90-95% target, not 100% deterministic. The Declined classification is coarse — every pending instruction in a turn with a permission denial is reported Declined, even though we can't prove the denial relates to a specific instruction. That's the right trade for the operator: "one of your instructions ran into a permission gate" is strictly better signal than "all succeeded silently." Operators can drill into the chat surface for the specific tool name from the existing `SessionBlocked` escalation.

  10 new unit tests across the touched files: `legatus_client::tests::turn_signals_path_rejects_traversal` + `note_then_take_roundtrips_turn_signals` + `take_turn_signals_for_missing_session_returns_empty` (basic file plumbing); `classify_outcome_api_error_wins_over_signals` + `classify_outcome_permission_denied_yields_declined` + `classify_outcome_multiple_denied_tools_joined_in_reason` + `classify_outcome_no_signals_yields_success` (mapping rules pinned); `hooks::permission_denied::tests::test_permission_denied_records_turn_signal` + `test_permission_denied_without_session_id_skips_signal_record` (hook integration with the recorder); `hooks::stop_failure::tests::test_stop_failure_clears_pending_instructions` + `test_stop_failure_clears_turn_signals` (the API-error drain path). Sentinel-application: 12 + 3 + 16 = 31 tests across the touched modules, all passing. Zero net-new clippy errors over the pre-existing baseline.

  **Out of scope (deferred)**: `Deferred { waiting_on }` classification (requires text-classifying the model's reply for "I'll do that later" intent, separate problem); fine-grained "which instruction did this denial relate to" (would need per-tool-call → per-instruction correlation in the relay flow, big surgery); cancel/redirect support for in-flight instructions (the next slice — new `CancelInstruction` protocol message).

- **Sentinel daemon legatus inbox persistence — operator instructions survive daemon restart** (2026-05-17). Closes the reliability gap where a daemon crash between "received `RelayInstruction` over WS" and "drained by the `Legatus AI_inbox` hook" silently lost the instruction. The inbox was an in-memory `tokio::sync::mpsc` channel; nothing on disk meant nothing survived. New `crates/sentinel-legatus/src/persistent_inbox.rs` (~290 lines + 9 unit tests) introduces `PersistentInbox`, a file-backed FIFO at `~/.claude/sentinel/state/legatus-inbox.jsonl` (overridable via `PersistentInbox::new(path)` for tests). Storage is one JSON-encoded `RelayInstruction` per line in arrival order; every operation (`append`, `try_pop`, `snapshot`, `len`) takes an `fs2` advisory exclusive (or shared, for read-only paths) lock on the file so concurrent producers + the consumer + cross-process touches are serialized. `try_pop` reads all lines under lock, skips the first parseable line as the popped item, and atomically rewrites the remainder via `seek(0) + set_len(0) + writeln` for each remaining line — concurrent appends block on the lock until the rewrite completes. Malformed lines (e.g. from a `legatus-ai-protocol` schema change) are dropped silently with a `tracing::warn` so they don't re-trigger the same parse failure forever. I/O errors are best-effort (logged at warn, never propagated) so the WS recv loop is never blocked on a transient filesystem issue — the design contract is ~90-95% reliability with Legatus AI re-sending if the operator re-instructs, not 100% deterministic delivery.

  `LegatusHandle` + `LegatusRuntime` refactored to drop the pre-persistence `mpsc::UnboundedSender/Receiver<RelayInstruction>` pair in favor of `Option<PersistentInbox>` on both halves. New constructor `make_pair_with_inbox(inbox: PersistentInbox)` builds a paired `(handle, runtime)` where both sides share the same path — `PersistentInbox` is `Clone` (path-only), so cloning into spawned tasks is free. The original `make_pair()` is preserved for the standalone `sentinel legatus connect` CLI path where instructions are still log-only (no daemon to drain them). `LegatusHandle::try_pop_inbox` wraps the synchronous file I/O in `tokio::task::spawn_blocking` so the executor never stalls on the advisory lock. `client.rs::handle_inbound` writes the inbound `RelayInstruction` via `runtime.inbox.append(instr)` if a persistent inbox is wired (daemon path), falls back to debug-log if not (standalone path). The escalation channel stays in-memory because escalations come from hooks that have already returned to the operator's chat surface — losing one to a crash is a non-fatal "next Stop hook fires instead" event, not a missed instruction.

  Daemon wiring in `crates/sentinel-cli/src/daemon_cmd.rs::start_legatus_if_configured`: builds the persistent inbox via `default_inbox_path()` → `PersistentInbox::new(path)`, then calls `make_pair_with_inbox(inbox)` instead of `make_pair()`. On daemon startup the inbox is rehydrated naturally — `inbox.len()` is reported at info level if non-zero so operators can see backlogged instructions surviving a previous crash. If `dirs::home_dir()` can't resolve (degenerate chroot/CI), falls back to in-memory `make_pair()` with a `tracing::warn` so the daemon still runs; that path matches pre-persistence behavior.

  9 unit tests on `PersistentInbox` use `tempfile::tempdir()` for isolation: empty inbox pops `None`; append-then-pop returns the same instruction; FIFO order preserved across three appends and three pops; append-after-pop extends the remainder (the WS-recv → hook-drain → WS-recv pattern); `snapshot()` is non-consuming; malformed lines are dropped on pop (good-line-after-bad-line still pops the good one); **instructions survive simulated daemon restart** (the headline test — drop the first `PersistentInbox`, construct a fresh one at the same path, pop returns the queued instruction); 20 concurrent appends from threads all persist (lock discipline holds under contention); `default_inbox_path()` returns a path containing `.claude` + `sentinel` ending in `legatus-inbox.jsonl`. 4 additional tests on the refactored `LegatusHandle`: `escalate_succeeds_when_runtime_is_alive`, `escalate_errors_after_runtime_drop`, `try_pop_inbox_returns_none_for_standalone_pair` (`make_pair()` with no inbox), `try_pop_inbox_drains_persistent_inbox` (`make_pair_with_inbox()` round-trip), `handle_and_runtime_share_persistent_inbox` (appends via runtime are visible to handle). Sentinel-legatus: 16 tests passing (6 prior + 10 new). Zero net-new clippy errors over the pre-existing baseline; fmt clean.

  `fs2::FileExt::{lock_exclusive,lock_shared,unlock}` share method names with `std::fs::File` inherent methods stabilized in Rust 1.89, so clippy flags them as `incompatible_msrv` against the workspace's 1.83 MSRV even though fs2's trait impl is what the compiler actually picks; suppressed at module level with a documented `#![allow(clippy::incompatible_msrv)]` referencing the rationale. **Out of scope for this commit**: mid-run failure detection (the next slice — `InstructionResult` outcome classification beyond `Success`); cancel/redirect support (after that — new `CancelInstruction` protocol message). Escalation persistence is a future improvement (separate file, same lock discipline) — flagged in the module docstring.

### Fixed
- **`tasks.md` shrink guard — prevent auto-block wipe from small sessions** (2026-05-16). The `task_persist` hook used to unconditionally overwrite `<repo>/tasks.md`'s `<!-- SENTINEL:TASKS:START -->` marker block with whatever the current session's `TaskList` rendered. When a brand-new session opened with only a handful of fresh tasks, the persist hook replaced an 80–100-task accumulated block with the new 3–5-task render — the user's visible task history vanished from the auto-block on the first `TaskCreate` of the new session. Root cause: `write_project_tasks_md` had no notion of "this would shrink the visible content"; it deduplicated only on byte-equality of the merged output. Fix: `write_project_tasks_md` now counts task entries in the existing block (incomplete `### [_] N. …` headers + completed `- [x] **N. …` lines) and refuses to overwrite when the new render has fewer than `SHRINK_GUARD_RATIO` (= 0.5) of the existing block's count AND the existing block has at least `SHRINK_GUARD_MIN_EXISTING` (= 10) tasks. Below the 10-task floor, small projects shrink freely (no false alarms on first-few-tasks sessions). The shrink-refuse path returns `Ok(false)` (write skipped, not a hard error) and emits a single `tracing::warn` naming both counts plus the override env var. `SENTINEL_FORCE_TASKS_MD_WRITE=1` bypasses the guard for the bulk-cleanup / project-pivot cases where shrinking is intentional. Two new helpers (`count_block_tasks`, `extract_existing_block`) and 10 new unit tests pin the behavior: counter handles open + completed + empty bodies, marker extraction returns inner body or `None`, guard refuses 80→5 wipe, guard allows 75% routine churn, guard stays off for 8-task baselines, force env overrides, initial write (no prior block) always succeeds, growth always writes through. Env-touching tests serialize on a module-local `Mutex` to avoid races in Rust's parallel test runner. Sentinel-application: 998 tests passing (988 prior + 10 new). Clippy --all-targets clean on the new code.

### Added
- **A2 Phase 5 — `route_capability` MCP tool exposes the router to Legatus AI peers** (2026-05-17). Final A2 slice (`docs/a2-capability-aware-routing.md` §5.5). The capability router substrate is now reachable from outside sentinel — Legatus AI peers, external orchestrators, and any tool that speaks MCP can query the same routing logic sentinel's internal hooks use, keeping the substrate single-source-of-truth across the AI-factory. New MCP tool `sentinel__route_capability` registered in `crates/sentinel-cli/src/mcp_cmd.rs` (~+100 lines + 5 unit tests). Takes a `CapabilityRequirement` JSON arg; returns the full `RoutingExplanation` (chosen `AgentId` or null, candidate set, eliminated agents with reasons, fired tie-breakers, requirement signature) so callers can inspect *why* the router picked what it picked — not just the outcome. Loads agent profiles at call time via `TomlCapabilityRouter::with_shipped_and_overrides(~/.claude/sentinel/config/agents.toml)`, the same constructor `hook_cmd.rs` uses for internal dispatch, so external and internal consumers see exactly the same catalog. Implementation in `handle_route_capability(request, args)`: (1) parses `args.requirement` as `CapabilityRequirement` (clear error message naming the field + linking to the spec section if parse fails); (2) constructs the router (clear error if `agents.toml` loading fails); (3) calls `router.explain(&req)` (NOT `route()` — `explain` returns even when no agent satisfies, so callers can see the decision tree on misses; the same trait surface is available via `route()` for callers wanting an error-on-miss path); (4) returns the serde-serialized `RoutingExplanation`. Synchronous handler (no async I/O on the hot path); embedded in the existing async `handle_request` dispatcher. Tool definition in `tool_definitions()` documents the input schema with examples of every Capability variant (Reasoning / DifferentVendorFrom / StructuredOutput / CostBudget / etc.) so Legatus AI peers don't need to read the Rust source to author requirements. Description references `docs/a2-capability-aware-routing.md` for the full vocabulary. **End-to-end example**: a Legatus AI peer authoring a BA orchestrator wants to pick "the cheapest non-Anthropic standard-reasoning auditor for a `git push` review" — sends `{"requirement": {"required": [{"Reasoning": "standard"}, {"DifferentVendorFrom": "Anthropic"}, {"StructuredOutput": "AuditorVerdict"}], "preferred": [], "forbidden": []}}` via `mcp__sentinel__route_capability`, receives `{"chosen": "kimi-k2-6-ollama-cloud", "candidates": ["kimi-k2-6-ollama-cloud", "openai-gpt-5-5"], "eliminated": [{"agent_id": "claude-opus-4-7", "reason": ...}, {"agent_id": "claude-sonnet-4-6", "reason": ...}], "tie_breakers_applied": [{"Cost": {"winner": "kimi-k2-6-ollama-cloud", "cost_per_input_token": 0.000001}}], "requirement_signature": "..."}`. Same decision sentinel's internal A3 auditor selection would make; same catalog; same algorithm. 5 unit tests pin behavior: missing `requirement` field returns `isError=true` with a descriptive error; malformed `requirement` (e.g. `"required": "not-an-array"`) returns `isError=true` with a parse-error message; valid requirement against shipped defaults returns a populated `RoutingExplanation` with a non-empty `candidates` list; contrived no-satisfy requirement returns `isError=false` (explain() always succeeds even on no-match — that's the contract) with `chosen=null`; tool definition listing includes `sentinel__route_capability`. Sentinel-cli: 139 tests passing (134 prior + 5 new). Clippy clean on the new function + tests.

  **A2 status (after this commit)**: feature-complete. The full pipeline — capability domain types (Phase 1) → ports (Phase 2) → static router + in-memory store (Phase 3a) → TOML router + shipped agent profiles (Phase 3b) → JSONL appraisal store (Phase 3c) → A3 auditor selection wiring (Phase 4) → MCP tool surface (Phase 5) — is live, tested, and operator-configurable via `~/.claude/sentinel/config/agents.toml`. Operators register profiles; sentinel routes auditor / future-critic / future-coder picks via the router; Legatus AI peers query the same router through the MCP tool; appraisal records feed back into the router's tie-breakers. A future phase will add appraisal-record emission from `dry_run_then_commit` after each auditor verdict (closing the tie-breaker-step-3 feedback loop), plus the planned A2-driven refactors of BA5 critic selection and budget-coder routing — but all of that is on top of this completed substrate.

- **A2 Phase 4 — A3 auditor selection consumes the capability router** (2026-05-17). Connects the A2 substrate to A3's production auditor selection so operators registering profiles in `agents.toml` actually drive which model audits Irreversible/Catastrophic actions, replacing the static `SENTINEL_AUDITOR_PROVIDER + SENTINEL_AUDITOR_MODEL` env-var pick with capability-aware routing. Two new constructors on `RigAuditor` in `crates/sentinel-infrastructure/src/dry_run_auditor.rs` (~+200 lines + 7 new unit tests): **(a)** `RigAuditor::for_profile(profile: &AgentCapabilityProfile) -> Result<Self>` — builds an auditor from a router-picked profile. Maps `profile.vendor.true_vendor()` to the appropriate rig-core provider via an exhaustive match (no wildcard — new `VendorClass` variants force a compile-time routing decision): `VendorClass::Ollama` → Ollama provider (auto-detects local vs cloud via `OLLAMA_API_KEY` presence per Phase 5); all other variants (`Anthropic | Openai | Google | Xai | Meta | Mistral | Openrouter | Other(..) | Unknown`) → OpenRouter provider (single auth surface `OPENROUTER_API_KEY` fronts the multi-vendor catalog). `profile.model_id` is used directly — the SENTINEL_AUDITOR_MODEL env var is IGNORED when a profile is supplied (the router has decided which model to use; env var overrides would defeat the point). `for_profile_with(profile, env_resolver)` is the test seam — the public `for_profile` wraps it with `real_env`. **(b)** `RigAuditor::via_router(router, profiles, acting_vendor) -> Result<Self>` — queries the router with the A3 auditor `CapabilityRequirement` (`Reasoning(Standard)` + `DifferentVendorFrom(acting_vendor)` + `StructuredOutput(AuditorVerdict)`, preferred `Reasoning(Deep)`), looks up the chosen `AgentId` in the supplied catalog, and constructs via `for_profile`. Errors when the router has no candidates (router returns `NoAgentSatisfies`) or when the chosen `AgentId` isn't in the catalog (programming error — caller passed mismatched router + profiles list); both surface as `anyhow::Error` with descriptive messages. `via_router_with(router, profiles, acting_vendor, env)` is the test seam. **(c)** Both constructors honor existing semantics: `SENTINEL_AUDITOR_TIMEOUT_SECS` read via the same `read_timeout` helper; `provider_prefix` (`"openrouter"` / `"ollama-local"` / `"ollama-cloud"`) propagates to the `auditor_model` attribution string so the proof chain can tell which provider produced each verdict.

  Production wiring in `crates/sentinel-cli/src/hook_cmd.rs` constructs a `TomlCapabilityRouter` at hook entry via `with_shipped_and_overrides(Some("~/.claude/sentinel/config/agents.toml"))` (degrades to empty profile list on load failure with a `tracing::warn`). The auditor construction then runs a two-step priority chain: (1) **Router-based** — if the router has registered profiles, try `RigAuditor::via_router(&router, router.profiles(), VendorClass::Anthropic)` (acting vendor hardcoded to Anthropic since Claude Code is Anthropic; future enhancement can make this dynamic). Success → use this auditor + log "A3 auditor selected via A2 capability router" at info level. (2) **Env-only fallback** — if the router has no profiles OR `via_router` fails (no matching profile, missing API key for the chosen profile's vendor, etc.), fall back to `RigAuditor::from_env()` (the Phase 4 legacy path driven by `SENTINEL_AUDITOR_PROVIDER + SENTINEL_AUDITOR_MODEL`). This preserves the behavior for operators who haven't customized `agents.toml`: shipped defaults + `OPENROUTER_API_KEY` or `OLLAMA_API_KEY` keeps working without ceremony. Both paths failing keeps A3 inert (`a3_enabled = false`); `tool_usage_gate`'s four-check stack handles Irreversible/Catastrophic as the pre-A3 fallback per the existing Phase 4 contract.

  **End-to-end flow with both layers wired**: operator (a) keeps `OLLAMA_API_KEY` set, (b) shipped `agents-defaults.toml` already includes `kimi-k2-6-ollama-cloud` profile, (c) Claude Code runs a `git push origin main` (classified Catastrophic). `dry_run_then_commit` fires; router Legatus AItation: requirement is `Reasoning(Standard) + DifferentVendorFrom(Anthropic) + AuditorVerdict + preferred(Deep)`. Anthropic profiles (`claude-opus-4-7`, `claude-sonnet-4-6`) eliminated by `DifferentVendorFrom`. Remaining candidates: `kimi-k2-6-ollama-cloud` (Standard) + `openai-gpt-5-5` (Deep). Preferred-score tie-breaker: GPT wins on `Reasoning(Deep)` if `OPENROUTER_API_KEY` is configured; otherwise GPT's `for_profile` errors at the API-key check + falls through to Kimi. Verdict ships from whichever model the operator's configured keys + the router's catalog converges on. Operator can override by adding their own `agents.toml` entries; the merge replaces same-`agent_id` profiles. **Operator UX**: `SENTINEL_AUDITOR_PROVIDER + SENTINEL_AUDITOR_MODEL` still work (legacy path); operators wanting capability-based routing add an `agents.toml` with their preferred profiles. A `tracing::info` log at session start names which path succeeded so operators can confirm router vs env in their telemetry.

  7 new unit tests in `dry_run_auditor.rs` (all using the `*_with` env-resolver seam + a `StubRouter` test helper that asserts the requirement carries `DifferentVendorFrom` + `StructuredOutput(AuditorVerdict)`): `for_profile_ollama_local_when_no_api_key` (Ollama profile, no OLLAMA_API_KEY → `provider_prefix=ollama-local`); `for_profile_ollama_cloud_when_api_key_present` (Ollama profile + OLLAMA_API_KEY → `ollama-cloud`); `for_profile_openrouter_path_for_anthropic_vendor` (Anthropic profile + OPENROUTER_API_KEY → `openrouter`); `for_profile_openrouter_errors_when_key_missing` (no key → clear error naming `OPENROUTER_API_KEY`); `for_profile_ignores_sentinel_auditor_model_env` (profile.model_id wins over env — the router-has-decided contract); `via_router_picks_chosen_agent_and_constructs_for_it` (full path: stub router picks `kimi-k2-6-ollama-cloud`, env has `OLLAMA_API_KEY`, result is `ollama-cloud:kimi-k2.6`); `via_router_errors_when_chosen_agent_id_not_in_catalog` (ghost agent_id → descriptive error); `via_router_errors_when_router_returns_no_agent_satisfies` (empty router → "router could not pick" error). Sentinel-infrastructure: 201 tests passing (194 prior + 7 new). Sentinel-cli: 134 tests passing (unchanged — hook_cmd's modifications don't break existing tests). Clippy clean on the modified files (zero new warnings — addressed unused-imports, doc-markdown backticks on `OpenRouter`, `match_wildcard` via exhaustive vendor match, format-args inlining, and items-after-statements by hoisting the `EmptyRouter` test struct out of its `#[test]` function). Out of scope: A2's `route_capability` MCP tool surface (Phase 5 — exposes the router to Legatus AI peers so they can query the same substrate); appraisal-record emission from `dry_run_then_commit` after auditor verdicts (a future phase will record outcomes to feed back into tie-breaker step 3).

- **A2 Phase 3c — JSONL-backed `AppraisalStorePort` adapter in sentinel-infrastructure** (2026-05-17). Production appraisal-store complement to Phase 3a's in-memory variant. Persists [`AppraisalRecord`]s to a single append-only JSONL file at `~/.claude/sentinel/state/appraisal/records.jsonl` (overridable for tests). Closes the Phase 3a → Phase 3b → Phase 3c trio: Phase 3a was the algorithm + in-memory store, Phase 3b was the TOML router, and Phase 3c is the persistent appraisal-data layer that completes the substrate. New `crates/sentinel-infrastructure/src/appraisal_store.rs` (~310 lines + 12 unit tests) defines: **(a)** `JsonlAppraisalStore` — implements `AppraisalStorePort` with append-only writes + scan-and-filter reads. Builder API: `at_path(PathBuf)` (test-pointed), `with_default_path()` (`~/.claude/sentinel/state/appraisal/records.jsonl`). `path()` accessor for operator tooling. **(b)** Atomic appends via `OpenOptions::new().create(true).append(true)`. POSIX guarantees atomic writes ≤ `PIPE_BUF` (4096 bytes) under `O_APPEND` so concurrent appends from multiple sentinel processes interleave at line boundaries without explicit file locking — auditor-verdict-sized records are typically 300-800 bytes, well within the atomic regime. Lines that exceed `PIPE_BUF` (currently impossible at our record shape) would not be guaranteed atomic; the reader's `serde_json::from_str` would fail on the malformed line and the record would be skipped with a `tracing::warn`. **(c)** Best-effort persistence: `record()` returns `()` per the port trait, never propagates I/O errors to the calling hook — writes that fail emit `tracing::warn` and drop the record. The dispatching hook is never blocked or aborted by appraisal failures (the design contract is that appraisal is observability data, not critical-path correctness data). **(d)** Auto-truncation at `MAX_LOG_SIZE` (8 MB ≈ 50_000 records — well past the router's 200-record window). When the file grows past the cap, the next `record()` call: (1) acquires the internal `Mutex<()>` truncate guard, (2) double-checks size under the lock, (3) reads all records, (4) keeps the trailing half, (5) rewrites via atomic `tmp + rename` so a crash mid-write doesn't truncate to zero. Concurrent appends during the rewrite are POSIX-atomic and land after the rename; the brief window between snapshot-read and rename-into-place could lose 1-2 records from concurrent appends — acceptable for observability data. **(e)** Malformed-line tolerance: `read_all_records` parses line-by-line, skipping unparseable lines with a warn-level log + line number for diagnostic. Partial writes (rare; impossible under PIPE_BUF) don't poison the whole file. **(f)** Window semantics match Phase 3a's `InMemoryAppraisalStore`: `All` (every record), `LastN(n)` (trailing n via slice), `LastHours(h)` (timestamp ≥ now() - h hours). Both stores share the [`sentinel_domain::agent_routing::AggregateStats::from_records`] computation so router behavior is identical regardless of which store is mounted.

  12 unit tests use `tempfile::TempDir` for isolation: missing file returns empty aggregate cleanly; record-then-aggregate returns correct strict-success rate (`2 Success + 1 Failure → 2/3`); `record()` auto-creates the parent directory tree (so `with_default_path()` works on fresh installs that don't have `~/.claude/sentinel/state/appraisal/`); records segregate by `(agent_id, requirement_signature)` (4-way cross of two agents × two sigs → 4 distinct buckets); `LastN(2)` against `[F, F, F, F, F, S, S]` returns 2 trailing Successes (rate=1.0); `LastHours(24)` excludes a record timestamped 48 hours back; second `JsonlAppraisalStore` instance pointed at the same file reads what the first instance wrote (cross-instance persistence); malformed line injected mid-file does NOT propagate — valid records before+after are still counted; `truncate_keeps_trailing_half_when_oversized` pads the file with `MAX_LOG_SIZE + 1024` bytes of garbage, fires `record()`, asserts file size dropped below `MAX_LOG_SIZE` and the aggregate still has records; `with_default_path` resolves to a path containing `.claude` + `sentinel` + ends with `records.jsonl`; compile-time `Send + Sync` assert on `JsonlAppraisalStore`. Module registered alphabetically in `crates/sentinel-infrastructure/src/lib.rs` between `anthropic` and `capability_router`. Sentinel-infrastructure: 194 tests passing (181 prior + 12 new + 1 incremental for Phase 3b mid-test landing). Clippy clean on the new module (zero warnings after addressing too-long-first-paragraph on `MAX_LOG_SIZE`, `finish_non_exhaustive` on the manual `Debug` impl that hides `truncate_guard`, `const fn at_path`, and `usize::try_from` conversions for the `MAX_LOG_SIZE` size hints). **R5 quarantine boundary**: documented at module-level and on the trait — appraisal records flow ONE WAY from dispatching hook → store → router as a tie-breaker input. Agents must never see appraisal data as feedback. The contract is on the port; both adapters (in-memory + JSONL) honor it by exposing only `record` + `aggregate`. **Operator UX**: the JSONL file is human-readable; operators can `cat ~/.claude/sentinel/state/appraisal/records.jsonl | jq` to inspect routing history. The file location matches sentinel's existing state-store convention (`~/.claude/sentinel/state/` namespace). Out of scope: A3 wiring (Phase 4 — refactor `dry_run_auditor.rs` to query the router instead of the static `SENTINEL_AUDITOR_MODEL` env var); `route_capability` MCP tool surface (Phase 5).

- **A2 Phase 3b — TOML-backed `CapabilityRouterPort` adapter in sentinel-infrastructure** (2026-05-17). Production adapter that loads [`AgentCapabilityProfile`]s from TOML and delegates routing to the Phase 3a shared `pick()` algorithm — so the TOML adapter and the `StaticCapabilityRouter` test helper produce identical decisions on identical inputs. New `crates/sentinel-infrastructure/src/capability_router.rs` (~380 lines + 15 unit tests) defines: **(a)** `TomlCapabilityRouter` — production [`CapabilityRouterPort`] implementation; profile list immutable after construction (operator-driven hot reload via sentinel restart). Builder API: `from_profiles(v)` (tests / hand-crafted), `with_shipped_defaults()` (loads the embedded baseline), `from_toml_str(s)` (parse + validate), `from_toml_path(p)` (file-load wrapper), `with_shipped_and_overrides(Option<&Path>)` (production startup path: shipped defaults + optional operator overrides from `~/.claude/sentinel/config/agents.toml`), `with_appraisal_store(Arc<dyn AppraisalStorePort>)` (tie-breaker step 3 enable), `with_window(w)` (appraisal window override). All three trait methods implemented by delegating to `sentinel_application::capability_router::pick`. `profiles()` accessor returns the registered catalog for operator-facing tooling. **(b)** `SHIPPED_AGENTS_DEFAULTS` — `pub const &str` containing `config/agents-defaults.toml` baked in via `include_str!` at compile time. Sentinel ships with 4 baseline profiles registered: `claude-opus-4-7` (deep-reasoning strong coder, $0.000015/in-token), `claude-sonnet-4-6` (standard-reasoning daily driver, $0.000003/in-token), `kimi-k2-6-ollama-cloud` (Kimi K2.6 via Ollama Cloud, $0.000001/in-token — vendor `Ollama` so A3's `DifferentVendorFrom(Anthropic)` qualifies), `openai-gpt-5-5` (deep alt-vendor, $0.000005/in-token). Operators register additional profiles or override these by `agent_id` in `~/.claude/sentinel/config/agents.toml`; the loader merges same-id REPLACES + new-id APPENDS. **(c)** `AgentsConfigToml` schema — single `[[agent]]` array-of-tables; per-entry fields match `AgentCapabilityProfile` exactly. Capability variants encoded externally-tagged per the Phase 1 serde shape (`{ Reasoning = "deep" }`, `{ ToolUse = ["Read", "Edit"] }`, `{ StructuredOutput = "AuditorVerdict" }`, etc.). `ReasoningLevel` is `rename_all = "snake_case"` so TOML uses `"shallow" | "standard" | "deep"` (lowercase) — the shipped defaults + test fixtures + the operator doc-comment all use this casing. **(d)** Validation rules enforced at load (`validate_profiles`): unique `agent_id` within a file (duplicate → load error naming the duplicate); non-negative `cost_per_input_token` + `cost_per_output_token`; `typical_latency_ms` strictly in `(0, 300_000]` (rejects 0 and >5min as misconfiguration signals); `max_context_tokens > 0`; non-empty `display_name` (trim-aware). Each validation failure surfaces as `anyhow::Error` with the offending `agent_id` named in the message — sentinel refuses to start with invalid profiles rather than route to ghost agents (per spec §7.2). **(e)** Merge semantics in `merge_overrides(shipped, overrides)`: same-`agent_id` overrides REPLACE shipped entries at the same Vec position (preserves declaration order in the merged catalog); new-`agent_id` overrides APPEND. `with_shipped_and_overrides` returns the shipped-only router cleanly when the overrides path is `None` or doesn't exist on disk (operator hasn't customized yet — common first-run state, logged at debug).

  New `config/agents-defaults.toml` (~110 lines including extensive schema-documenting comments). The comments enumerate every capability variant the operator can declare so a new operator doesn't need to read the source to author overrides. Operators register OpenRouter-fronted models with `vendor = { Other = { underlying_vendor = "<vendor>" } }` so A3's `DifferentVendorFrom` check sees the true upstream vendor (per Phase 1's `VendorClass::Other::true_vendor` semantics). The shipped Kimi K2.6 profile uses `vendor = "Ollama"` to match the verified Phase 5 live smoke (Ollama Cloud at `https://ollama.com/v1`); operators using Kimi via OpenRouter instead would register a separate profile with `vendor = { Other = { underlying_vendor = "Unknown" } }`.

  15 unit tests: shipped defaults parse + load cleanly (catches any embedded-TOML regression at CI time); minimal one-agent profile parses; multi-agent TOML parses to a 2-profile catalog; malformed TOML produces an `anyhow::Error` whose message contains "parse"; unknown vendor surfaces at parse time (`"TotallyMadeUp"` triggers serde's "unknown variant" error path); validation: duplicate `agent_id` is rejected with a "duplicate agent_id" message naming the duplicate; negative `cost_per_input_token` rejected; `typical_latency_ms = 0` rejected; `typical_latency_ms > 300_000` rejected with the 300_000 cap in the message; empty/whitespace `display_name` rejected; `merge_overrides` REPLACES on same-id; `merge_overrides` APPENDS on new-id; end-to-end route through the TOML adapter on the realistic A3 fixture picks `kimi` (Standard reasoning + non-Anthropic) over `opus` (Anthropic — eliminated by `DifferentVendorFrom`); `NoAgentSatisfies` carries the nearest-miss diagnostics so operators can fix profiles or requirements; compile-time `Send + Sync` on `TomlCapabilityRouter`. Module registered alphabetically in `crates/sentinel-infrastructure/src/lib.rs` between `anthropic` and `config`. Sentinel-infrastructure: 181 tests passing (166 prior + 15 new). Clippy clean on the new module (zero warnings — addressed `match_wildcard_for_single_variants` with explicit `RoutingError::Configuration` exhaustive pattern). **Algorithm equivalence**: the TOML adapter and `StaticCapabilityRouter` both delegate to `sentinel_application::capability_router::pick`; identical inputs produce identical decisions. Operators picking the TOML adapter for production get exactly the same routing semantics tests pin. Out of scope: JSONL-backed `AppraisalStorePort` adapter (Phase 3c — split from 3b to keep diffs reviewable); A3 wiring (Phase 4); `route_capability` MCP tool (Phase 5). **Operator UX note**: the shipped defaults file at `config/agents-defaults.toml` is the primary reference for what fields and capability variants look like in TOML — operators copy-paste from there into their overrides file rather than parsing the Rust source.

- **Per-instruction roundtrip (sentinel side) — `Legatus AI_inbox` emits Ack, `execution_log` emits Result** (2026-05-17). Closes per-instruction accounting on the sentinel side (the Legatus AI-side wiring shipped in `legatus-ai` as `a3f6702`). Now the operator sees per-instruction confirmation in chat: `"firefly is on it (id abcd1234)."` when sentinel's `Legatus AI_inbox` hook drains the daemon's inbox, then `"firefly finished (id abcd1234): <summary>."` when the next `Stop` hook fires. The `EscalationKind` enum in `sentinel-legatus` gained two variants — `InstructionAck { instruction_id }` and `InstructionResult { instruction_id, outcome, summary }` — both serializable so the daemon's existing `POST /legatus/escalate` route accepts them with zero new HTTP surface. The connect loop in `client.rs` matches the new variants and produces the matching `InstructionAcknowledged` / `InstructionResult` Legatus AI Protocol envelopes signed with the outbound MAC key. New `legatus_client::ack_fire_and_forget(instruction_id)` and `report_result_fire_and_forget(instruction_id, outcome, summary)` helpers wrap `escalate_fire_and_forget` for hook-call ergonomics. Per-session pending-id tracking lives at `~/.claude/sentinel/state/<session_id>.legatus-pending.txt` (one instruction id per line) — `Legatus AI_inbox` appends on drain via `note_pending_instruction`, `execution_log` reads + clears via `take_pending_instructions` on Stop and fires one `InstructionResult` per pending id with `outcome: Success` and the run's existing summary (last `[RUN]`/`[STEP]`/`[PHASE]` marker truncated to 140 chars). Pending-file path validates against directory traversal (`../`, `/`, `\`, empty session id rejected). Race tolerance per the operator's ~90-95% reliability target: an `append` racing with a `read+delete` may lose one id; operator just misses one Result ping. MVP outcome classification is always `Success` — sentinel doesn't yet detect mid-run failures and translate them to `Failure { error }` / `Declined { reason }`. 3 new unit tests in `legatus_client`: path traversal rejection, note → take roundtrip (file is consumed), take on missing session returns empty. Existing `Legatus AI_inbox` and `execution_log` tests still pass (fire-and-forget threads are invisible to sync test harnesses).

  **End-to-end runtime path now per-instruction visible**:
  ```
  Operator: (Telegram) "firefly please deploy staging"
    → Legatus AI: "✓ relayed to firefly: deploy staging [id abcd1234]"
    → daemon-hosted legatus inbox: <buffered>
    → next UserPromptSubmit hook drains: Legatus AI_inbox calls ack_fire_and_forget
      → operator: "firefly is on it (id abcd1234)."
      → Legatus AI_inbox appends abcd1234 to legatus-pending.txt
      → Legatus AI_inbox injects PRIMARY-ASK additionalContext for Claude Code
    → Claude Code acts on the instruction, finishes turn
    → Stop hook fires: execution_log emits SessionCompleted (existing) AND
      for each id in legatus-pending.txt: report_result_fire_and_forget(Success, summary)
      → operator: "firefly finished (id abcd1234): <summary>."
  ```

- **A2 Phase 3a — Static router + in-memory appraisal store in sentinel-application** (2026-05-17). Third slice of A2 (`docs/a2-capability-aware-routing.md` §3 algorithm). Ships the **pure routing algorithm** + an **application-layer test helper** for both ports. The TOML-backed production adapters (Phase 3b in sentinel-infrastructure) wrap the same algorithm, so the behavior here IS the contract — adapters only differ in where they load profiles from. New `crates/sentinel-application/src/capability_router.rs` (~330 lines + 11 unit tests) defines: **(a)** `pub fn pick(profiles, requirement, appraisal_store, window) -> RoutingExplanation` — the pure algorithm implementing the spec's 6-step tie-breaker chain. Step 1: required + forbidden filtering (eliminated agents carry per-step `EliminationReason::UnsatisfiedRequirement` or `ForbiddenCapabilityMatched`). Step 2: preferred score (most preferred-capabilities satisfied wins). Step 3: appraisal success rate when an `AppraisalStorePort` is configured AND the cohort meets `MIN_COHORT_FOR_APPRAISAL_TIE_BREAK` (=20) — strict `Success` rate via `agg.success_rate`; below-threshold cohorts fall through so a "100% on 3 calls" agent doesn't dominate a "92% on 200 calls" agent. Step 4: cost (`cost_per_input_token` lowest). Step 5: latency (`typical_latency_ms` lowest). Step 6: stable `AgentId` lexical order (final deterministic fallback). Every step records a `TieBreaker` entry in the explanation iff it actually fired (eliminated a candidate). Same input always produces the same output. **(b)** `pub const DEFAULT_APPRAISAL_WINDOW: AppraisalWindow = LastN(200)` — spec's calibration target. **(c)** `pub const MIN_COHORT_FOR_APPRAISAL_TIE_BREAK: u32 = 20` — small-cohort guard. **(d)** `StaticCapabilityRouter` — the test helper. Wraps a static `Vec<AgentCapabilityProfile>` + optional `Arc<dyn AppraisalStorePort>` + configurable window. Builder API: `empty()` / `from_profiles(v)` / `with_agent(p)` (chainable) / `with_appraisal_store(arc)` / `with_window(w)`. Implements all three trait methods: `route` returns the chosen `AgentId` or `RoutingError::NoAgentSatisfies(unsatisfied)`; `candidates` returns the post-required/forbidden set BEFORE tie-breakers (useful for debate-style fan-out + the future `sentinel routing explain` CLI); `explain` returns the full decision tree. **(e)** `static_router(profiles)` convenience builder for test prose readability. 11 router tests: empty-router error path; required-filter picks only qualifying agent; forbidden eliminates correctly; realistic A3 requirement scenario (acting=Anthropic, required `DifferentVendorFrom(Anthropic) + StructuredOutput(AuditorVerdict)`, preferred `Reasoning(Deep)` → GPT wins over Kimi via preferred-score step 2); each of steps 2/4/5/6 wins isolated correctly (preferred score, cost after preferred tie, latency after cost tie via identical-cost twins, stable-ID lexical order after latency tie); `candidates()` returns the pre-tie-break qualifying set; `explain()` records eliminated agents with the correct `EliminationReason`; `explain()` records `TieBreaker::PreferredScore` when it fires.

  New `crates/sentinel-application/src/appraisal_store.rs` (~150 lines + 8 unit tests) defines: **`InMemoryAppraisalStore`** — `HashMap<(AgentId, RequirementSignature), Vec<AppraisalRecord>>` behind a `RwLock` so it's `Send + Sync` and `Arc`-wrappable for sharing across hooks within a session. Implements `AppraisalStorePort`: `record(r)` (best-effort persistence — poisoned-lock failures log via `tracing::warn` instead of panicking, so a dispatching hook never blocks on lock-recovery semantics); `aggregate(agent, signature, window)` (HashMap lookup → window filter → `AggregateStats::from_records`). Window semantics: `All` returns every record; `LastN(n)` returns the trailing `n` (or all if fewer exist); `LastHours(h)` filters by `timestamp >= now() - h hours`. `record_count()` snapshot helper for tests + observability. 8 appraisal tests: empty-store aggregates to `empty()` with `has_data() == false`; record + aggregate returns correct strict-success rate (`2 success + 1 failure → 2/3`); records segregate correctly by agent_id (kimi=1.0 success, opus=0.0 from the same bucket); records segregate correctly by signature; `LastN(2)` of `[F, F, F, F, F, S, S]` returns only the last 2 (both `Success` → rate=1.0); `LastHours(24)` excludes a record timestamped 48 hours ago; `record_count()` reflects persisted count; compile-time `Send + Sync`.

  Modules registered alphabetically in `crates/sentinel-application/src/lib.rs` — `pub mod appraisal_store` before `auditor`, `pub mod capability_router` between `cache_efficiency` and `channel_events`. Sentinel-application: 1052 tests passing (1027 prior + 25 new — 11 capability_router + 8 appraisal_store + 6 shadow that incrementally landed). The 3 pre-existing `phase_gate` failures (task #21) remain unchanged. Clippy clean on the new modules (zero warnings after addressing `let...else` over match-on-RwLock, splitting too-long-first-paragraph on `DEFAULT_APPRAISAL_WINDOW` + `MIN_COHORT_FOR_APPRAISAL_TIE_BREAK` docs, and a single `#[allow(clippy::too_many_lines)]` on `pick` — the 6-step chain reads cleaner inline than fragmented across helpers, and the function is heavily commented step-by-step). **Algorithm-equivalent contract**: the Phase 3b TOML adapter MUST delegate to this `pick` function or replicate its exact tie-breaker order; both routers MUST produce identical decisions on identical inputs. Out of scope: TOML profile loading + JSONL appraisal persistence (Phase 3b — `sentinel-infrastructure`); A3 wiring (Phase 4); `route_capability` MCP tool (Phase 5).

- **A2 Phase 2 — `CapabilityRouterPort` + `AppraisalStorePort` traits in sentinel-domain** (2026-05-17). Second slice of A2 (`docs/a2-capability-aware-routing.md` §§3-4). Adds the substrate dispatch trait + appraisal-history trait + supporting data types — still no logic; Phase 3a/3b add the static and TOML-backed implementations. New `crates/sentinel-domain/src/agent_routing.rs` (~360 lines + 11 unit tests) defines all the routing-decision data types A2's ports return: (a) `RequirementSignature` — newtype-`String` carrying the first 16 hex chars of SHA-256 over a canonicalized JSON encoding of a `CapabilityRequirement`. Each of `required` / `preferred` / `forbidden` is sorted by lexical JSON-string order before hashing so order-of-construction doesn't shift the bucket key — two semantically-equal requirements always hash to the same signature. Used as the per-agent / per-requirement bucket key in the appraisal store. (b) `EliminationReason` — `UnsatisfiedRequirement(Vec<UnsatisfiedRequirement>)` and `ForbiddenCapabilityMatched(String)`; `UnsatisfiedRequirement` carries a JSON-rendered capability + human-readable explanation. (c) `TieBreaker` — fully enumerates the spec's 6-step tie-breaker order so `routing explain` traces every step that actually fired: `PreferredScore`, `AppraisalSuccessRate`, `Cost`, `Latency`, `StableId`. (d) `RoutingExplanation` — `chosen` (the picked `AgentId` or `None`), `candidates`, `eliminated`, `tie_breakers_applied`, `requirement_signature`. Returned by `CapabilityRouterPort::explain` and serialized for the future `sentinel routing explain` CLI subcommand. (e) `AppraisalOutcome` — `Success / PartialSuccess / Failure / Abandoned` per-record outcome label. (f) `AppraisalRecord` — single past dispatch outcome: agent + signature + outcome + optional `AuditorVerdict` + actual cost + actual latency + tokens-in/out + timestamp. Doc comment carries the **R5 quarantine boundary** load-bearing note: appraisal records are dispatch input only, never training feedback. (g) `AggregateStats` — `cohort_size`, `success_rate` (strict — only `Success`), `permissive_success_rate` (`Success + PartialSuccess`), `mean_cost_usd`, `mean_latency_ms`. Const `empty()` constructor for the no-data fallthrough path; `has_data()` predicate; `from_records(&[AppraisalRecord]) -> Self` aggregation helper computed once at the call site so neither the router nor adapter need to redo the math. (h) `AppraisalWindow` — `LastN(u32) / LastHours(u32) / All` time/count windowing argument for aggregation queries.

  Two new traits appended to existing `crates/sentinel-domain/src/ports.rs` (rather than a `ports/` subdirectory, matching the codebase convention): **`CapabilityRouterPort`** — `route(&CapabilityRequirement) -> Result<AgentId, RoutingError>` (the substrate dispatch primitive every consumer hook queries); `candidates(&CapabilityRequirement) -> Vec<AgentId>` (returns every requirement-clearing agent before tie-breakers, used for debate-style fan-out + `explain` subcommand); `explain(&CapabilityRequirement) -> RoutingExplanation` (full decision tree). **`AppraisalStorePort`** — `record(AppraisalRecord)` (best-effort persistence — failures log but never block dispatching); `aggregate(&AgentId, &RequirementSignature, AppraisalWindow) -> AggregateStats` (returns `AggregateStats::empty()` for missing buckets so the router falls through to the next tie-breaker rather than erroring). Both `Send + Sync` per existing port convention. New `RoutingError` enum in `ports.rs` — `NoAgentSatisfies(Vec<UnsatisfiedRequirement>)` carries nearest-miss diagnostics so operators can fix profiles or relax requirements, `Configuration(String)` for startup-time misconfiguration. Hand-rolled `Display` + `std::error::Error` (no thiserror in sentinel-domain — deps are deliberately minimal).

  11 unit tests pin: `RequirementSignature::of` is stable across construction order (sorting `required` deterministically before hashing); changes when required levels differ; changes when preferred capabilities differ; is exactly 16 hex chars; `AggregateStats::from_records` returns empty for `&[]` with `has_data() == false`; strict-vs-permissive success rate accounting (2/4 strict vs 3/4 permissive when set is `[Success, Success, PartialSuccess, Failure]`); `Abandoned` outcome counts as non-success in both modes; mean cost + mean latency arithmetic; `AppraisalRecord` serde round-trip with `AuditorVerdict` payload; `RoutingExplanation` serde round-trip with all variant types populated; compile-time `Send + Sync` asserts on `RequirementSignature` / `AppraisalRecord` / `AggregateStats` / `RoutingExplanation` / `AppraisalWindow`. Module registered alphabetically in `crates/sentinel-domain/src/lib.rs` between `pub mod` (top) and `pub mod capability`. Sentinel-domain: 459 tests passing (448 prior + 11 new). Clippy clean on the new module + port-trait section (zero warnings on agent_routing.rs / new ports.rs section after addressing `derive_partial_eq_without_eq` on `EliminationReason` + `UnsatisfiedRequirement` + `RoutingError`, and one `AgentId` backtick on doc). **Note on R5 boundary**: the doc comment on `AppraisalRecord` + `AppraisalStorePort` explicitly forbids using appraisal data as training feedback for agents. The trait surface enforces this naturally — the port only exposes `record` / `aggregate`, not "stream back to agent for fine-tuning" — but the explicit doc comment is the contract for downstream readers. Out of scope: static `StaticCapabilityRouter` test helper (Phase 3a), TOML-backed router + JSONL appraisal store (Phase 3b), A3 wiring (Phase 4), MCP tool surface (Phase 5).

- **A2 Phase 1 — Capability domain types in sentinel-domain** (2026-05-17). First implementation slice of A2 (`docs/a2-capability-aware-routing.md`), the substrate dispatch primitive that replaces R1's retired role-persona pipelines and ports every "which agent / which model for this work" decision onto a shared capability-graph axis. Pure-domain value objects — no business logic; subsequent phases add the ports, static router, TOML adapter, and consumer-hook refactors. New `crates/sentinel-domain/src/capability.rs` (~600 lines + 28 unit tests) defines: (a) `AgentId` — newtype-`String` stable identifier with `new()` rejecting empty / whitespace-only inputs (returns `AgentIdError::Empty`), `as_str()` accessor, `Display` impl, hand-rolled `std::error::Error` for `AgentIdError`. (b) `VendorClass` — externally-tagged enum with explicit named variants for Anthropic / Openai / Google / Xai / Meta / Mistral / Ollama / Openrouter plus `Other { underlying_vendor: Box<Self> }` for gateway-fronted agents (OpenRouter fronting Claude → `Other { underlying_vendor: Anthropic }`) plus `Unknown` for unidentified third parties. `true_vendor()` recursively unwraps `Other` so A3's `DifferentVendorFrom` comparisons see the actual upstream vendor, not the gateway. `is_different_from()` treats `Unknown` as distinct from every other vendor (including another `Unknown` — separate gateway calls aren't presumed to share an upstream). (c) `ReasoningLevel` — `Shallow < Standard < Deep` with manual `Ord` + `PartialOrd` (rank-based) so `agent.has_at_least(required)` is expressible directly; const `at_least()` for compile-time comparisons. (d) `ToolKind` — enumerates `Read / Glob / Grep / Edit / Write / Bash / Task / TaskUpdate / WebFetch / WebSearch` plus `Other(String)` for MCP tools and unknown future tools; not `Copy` because of the `Other(String)` variant. (e) `SchemaRef` — externally-tagged with `AuditorVerdict` (A3 auditor seats), `BaCritique` (BA5 critic seats), `AnyJson` (looser — any JSON shape works), and `Named(String)` for future schemas. `AnyJson` is satisfied by any specific-schema declaration; specific schemas require exact match. (f) `DataZone` — `UsEast / UsWest / Eu / OnPremise / Named(String)` — **reserved for A2 v2 enforcement**; the field is wired now so requirements can declare zones, but the v1 router doesn't enforce actual API geography. (g) `Capability` — externally-tagged enum with 10 variants: `Reasoning(ReasoningLevel)`, `ToolUse(Vec<ToolKind>)`, `StructuredOutput(SchemaRef)`, `Vendor(VendorClass)`, `DifferentVendorFrom(VendorClass)`, `OpenWeights`, `LatencyBudget(u32)`, `CostBudget(f32)`, `ReversibilityClass(ReversibilityClass)` (re-exports A6's class for catastrophic-class routing per A2 spec §5.4), `DataLocality(DataZone)`. `is_satisfied_by(profile)` implements the per-variant resolution rules — `Reasoning` uses `at_least()`, `ToolUse` requires superset, `CostBudget` / `LatencyBudget` use `<=` semantics (agent's actual ≤ requirement's budget = satisfied), `DifferentVendorFrom` uses `true_vendor()` comparison, `StructuredOutput::AnyJson` is satisfied by any specific schema. (h) `AgentCapabilityProfile` — operator-managed declaration with `agent_id`, `display_name`, `vendor`, `model_id`, `declared: Vec<Capability>`, `cost_per_input_token`, `cost_per_output_token`, `typical_latency_ms`, `max_context_tokens`, `data_zones`. Will live in `config/agents.toml` (Phase 3b). (i) `CapabilityRequirement` — `required` / `preferred` / `forbidden` constraint sets with builder-style `with_preferred(c)` / `with_forbidden(c)` and `is_satisfied_by(profile)` (all `required` pass AND no `forbidden` passes) plus `preferred_score(profile)` (count of preferred-satisfied for router tie-breaker step 2). 28 unit tests pin: AgentId construction (empty rejected, whitespace trimmed, Display works); VendorClass `Other` true-vendor resolution + `DifferentVendorFrom` correctly compares through gateways + `Unknown` always distinct; ReasoningLevel ordering (`Deep > Standard > Shallow`, `at_least` semantics); Capability satisfaction — Reasoning satisfies by `at_least`, ToolUse requires superset, CostBudget/LatencyBudget use `<=`, Vendor matches via `true_vendor`, DifferentVendorFrom passes when distinct, StructuredOutput exact + AnyJson special case, OpenWeights / ReversibilityClass per-variant; CapabilityRequirement composition — required-all-pass / forbidden-any-fail / preferred-score; realistic A3 auditor scenario (acting=Anthropic, requirement={Standard reasoning, DifferentVendorFrom(Anthropic), StructuredOutput(AuditorVerdict)} → Anthropic profiles disqualified, non-Anthropic with the schema qualify); catastrophic-class routing scenario (Deep + DifferentVendorFrom + CostBudget forbidden as no-shortcuts gate per spec §5.4); serde round-trip for Capability / CapabilityRequirement / AgentCapabilityProfile; compile-time `Send + Sync` asserts. **Serde shape note**: the spec drafted `#[serde(tag = "kind")]` (internally-tagged), but that collides with `Capability::ToolUse(Vec<ToolKind>)` (newtype-containing-sequence is unsupported with internal tags) and `VendorClass::Other { underlying_vendor: Box<VendorClass> }` (nested same-enum produces "duplicate field `kind`"). Switched to externally-tagged (the serde default) for `VendorClass`, `Capability`, `SchemaRef`, `DataZone` — the on-wire shape becomes `"Anthropic"` (unit), `{"Reasoning":"Deep"}` (newtype), `{"ToolUse":["Edit"]}` (sequence-newtype), which is more idiomatic Rust serde anyway. Module registered alphabetically in `crates/sentinel-domain/src/lib.rs` between `pub mod commit` and pre-existing `pub mod config`. Sentinel-domain: 448 tests passing (420 prior + 28 new). Clippy clean on the new module (zero warnings on capability.rs after addressing doc backticks, `Box<Self>` over `Box<VendorClass>`, `const fn` for rank/at_least/new, and split-first-paragraph for SchemaRef + DataZone). Note: existing `crates/sentinel-domain/src/routing.rs` is the skill-routing regex matcher — unrelated to A2's agent routing. To avoid the name collision, A2 Phase 2's ports will land in `ports/capability_router.rs` (new) rather than reusing `routing.rs`. Out of scope: ports (Phase 2), static router (Phase 3a), TOML config + JSONL appraisal store (Phase 3b), A3 wiring (Phase 4), MCP tool surface (Phase 5).

- **A3 Phase 5 follow-up — Live Ollama Cloud smoke test (`#[ignore]`-gated)** (2026-05-17). Adds `live_ollama_smoke` to `crates/sentinel-infrastructure/src/dry_run_auditor.rs`'s test module, gated by `#[ignore]` so it stays out of the default suite. Operators verify connectivity + attribution against real Ollama (Cloud or Local) by setting `OLLAMA_API_KEY` + `SENTINEL_AUDITOR_PROVIDER=ollama` + `SENTINEL_AUDITOR_MODEL` and running `cargo test -p sentinel-infrastructure --lib dry_run_auditor::tests::live_ollama -- --ignored --nocapture`. The test constructs `RigAuditor::ollama_from_env()`, scores a fixture `DryRunRequest`, and asserts the `auditor_model` attribution begins with `ollama-`. Verdict shape (decision / confidence / axes / reasoning) is printed via `eprintln!` but not asserted — model judgment varies, this is a connectivity check not a behaviour pin. **Verified against real Ollama Cloud with `kimi-k2.6`** before commit: handshake → bearer auth → `https://ollama.com/v1/chat/completions` → JSON verdict parses cleanly → `auditor_model=ollama-cloud:kimi-k2.6` recorded → Kimi-K2.6 returned a Pass verdict with `confidence=0.90`, axes `correctness=0.95 / intent_alignment=0.95 / safety=0.9 / unstated_assumptions=0.8`, and operator-quality reasoning prose. **Endpoint shape confirmed**: `https://ollama.com/v1` is the OpenAI-compat base URL (matches Phase 5's `DEFAULT_OLLAMA_CLOUD_BASE_URL`). Ollama Cloud catalog at smoke-time included `kimi-k2.6`, `kimi-k2-thinking`, `deepseek-v3.2`, `qwen3-coder-next`, `glm-5.1`, `nemotron-3-super` and many others — operators have broad model choice. No code change beyond the one `#[test] #[ignore]` function (~30 lines) — pure additive op-tooling commit.

- **A3 Phase 5 — Ollama provider (local + cloud) alongside OpenRouter** (2026-05-17). `RigAuditor` now supports either `openrouter` or `ollama` as the LLM backend, selected via `SENTINEL_AUDITOR_PROVIDER` (default `openrouter`). The Ollama path auto-detects local vs cloud by `OLLAMA_API_KEY` presence — both modes route through rig-core's `openai` provider (Ollama's OpenAI-compatible endpoint is the single seam) so the prompt, JSON-parsing, sidecar-runtime, and verdict-decision logic stays identical for every provider. **API changes**: `RigAuditor::from_env()` is now a dispatcher; new `RigAuditor::openrouter_from_env()` and `RigAuditor::ollama_from_env()` expose the per-provider constructors directly. New builder `RigAuditor::with_provider_prefix(prefix)` lets tests assert on the attribution string. New `provider_prefix: String` field on `RigAuditor` records which provider built it (`"openrouter"`, `"ollama-cloud"`, `"ollama-local"`); the `AuditorVerdict::auditor_model` attribution is now `"{provider_prefix}:{model_id}"` (e.g. `"ollama-cloud:moonshotai/kimi-k2"`). `parse_verdict`'s signature changed from `(text, model_id)` to `(text, auditor_model)` — it now records the full attribution string verbatim instead of synthesising the prefix. **Env vars**: `SENTINEL_AUDITOR_PROVIDER` (default `openrouter`, case-insensitive); for `ollama`: `OLLAMA_API_KEY` (presence triggers Cloud mode), `OLLAMA_BASE_URL` (Cloud only, default `https://ollama.com/v1`), `OLLAMA_HOST` (Local only, default `http://localhost:11434` — the adapter appends `/v1`), `SENTINEL_AUDITOR_MODEL` (required — no sensible default for Ollama since operators choose what they've pulled). New public constants: `DEFAULT_AUDITOR_PROVIDER`, `DEFAULT_OPENROUTER_MODEL`, `DEFAULT_OLLAMA_LOCAL_BASE_URL`, `DEFAULT_OLLAMA_CLOUD_BASE_URL`. Existing `DEFAULT_AUDITOR_MODEL` const is `#[deprecated]` (alias for `DEFAULT_OPENROUTER_MODEL`) so Phase 3b callers keep building. **Local Ollama detail**: a dummy bearer token (`"ollama-local"`) is passed because Ollama's OpenAI-compat endpoint ignores it; this keeps the rig-core `openai::Client` (which requires bearer auth) happy. **Testability**: introduced private `*_from_env_with(env_resolver)` variants that take a `Fn(&str) -> Option<String>` closure. Public `*_from_env` constructors wrap them with `real_env` (`std::env::var(k).ok()`). 11 new unit tests use HashMap-backed closures via the `_with` seam — no `unsafe { std::env::set_var(...) }` (workspace forbids unsafe and Rust 2024 marks `set_var` unsafe). Tests cover: `score_uses_provider_prefix_in_auditor_model_attribution`, `with_provider_prefix_overrides_default`, `from_env_unknown_provider_errors` (names the invalid value + lists valid ones), `from_env_defaults_to_openrouter_when_provider_unset`, `ollama_from_env_local_mode_when_api_key_absent` (provider_prefix == "ollama-local"), `ollama_from_env_cloud_mode_when_api_key_present` (provider_prefix == "ollama-cloud"), `ollama_from_env_requires_model_id` (clear error naming the env var), `from_env_dispatches_to_ollama_when_provider_ollama`, `from_env_provider_is_case_insensitive` (`"OLLAMA"` works), `openrouter_from_env_uses_default_model_when_unset`, `timeout_overrides_default_via_env`. **Vendor-class separation note**: with both providers wired, operators can configure A3 to use a model from a different vendor family than the acting agent (Claude Code) — e.g. running the agent on Anthropic models and the auditor on Ollama Cloud's `moonshotai/kimi-k2`. The user originally raised this comparison ("kimi k2.6 as budget coder") — this commit unlocks that path without forcing OpenRouter spend. Sentinel-infrastructure: 155 tests passing (140 prior + 15 dry_run_auditor base + 11 new for Phase 5 — net 26 in the dry_run_auditor module). Workspace build clean; clippy-clean on the modified file (zero warnings on dry_run_auditor.rs after addressing doc continuation indent, too-long-first-paragraph, doc_markdown OpenAI backticks, and option_if_let_else map_or_else rewrites). Out of scope: A2's `CapabilityRouterPort` vendor-aware auditor selection (still operator-static per env); `OLLAMA_BASE_URL` default for Cloud (`https://ollama.com/v1`) is a best-effort guess at Ollama's stable URL — operators override if Ollama relocates the endpoint; native Ollama client (rig-core's `ollama` provider, which uses `/api/chat` and no auth) is intentionally NOT used — sticking with the OpenAI-compat path keeps a single rig-core client type and a single prompt code path for both deployment shapes.

- **A3 Phase 4 — `RigAuditor` + `dry_run_then_commit` wired into the production caller** (2026-05-17). Final implementation slice of A3 (`docs/a3-dry-run-then-commit.md` §6). Three integration points land together: (1) Auditor construction at session start in `crates/sentinel-cli/src/hook_cmd.rs` — `RigAuditor::from_env()` is attempted; on success the result is wrapped as `Option<Arc<dyn AuditorPort>>`. When `OPENROUTER_API_KEY` isn't set (the operator hasn't opted in, e.g. while comparing OpenRouter vs Ollama Cloud costs), the call fails cleanly, the auditor stays `None`, A3 stays inert, and `tool_usage_gate`'s four-check stack remains the strongest available gate for Irreversible/Catastrophic Edit/Write — no behavior regression for un-keyed operators. The boolean `a3_enabled = auditor.is_some()` is the single switch that propagates the operator's opt-in state to consumer hooks. (2) `dry_run_then_commit` hook registered in the `PreToolUse` flow ahead of `git_hygiene` / `tasks_md_guard` / `tool_usage_gate`. Fires for ALL tools (not just Edit/Write) because the hook itself short-circuits to `allow()` for class < Irreversible — registering broadly means Bash invocations the classifier rates as Irreversible (e.g. `git push origin main`, irreversible `rm -rf` patterns, irreversible `wrangler deploy --env production` patterns when the operator-overrides TOML adds them) get the same dry-run-then-commit treatment as Edit/Write of irreversible files. Hook is gated by `if let Some(ref auditor_arc) = auditor` so it doesn't fire at all when A3 is inert — zero overhead on the un-keyed path. The metric label `dry_run_then_commit` is recorded via the standard `time_and_record` wrapper for per-hook latency observability. (3) `tool_usage_gate::process` signature gains a 5th parameter `a3_enabled: bool`. Dispatch is now class-aware in two dimensions: (a) `TriviallyReversible` → allow silently (unchanged from A6 Phase 4a/4b); (b) `ReversibleWithEffort` → four-check stack (unchanged; the bulk of edits land here); (c) `Irreversible` / `Catastrophic` → when `a3_enabled`, short-circuit to `allow()` because the A3 `dry_run_then_commit` hook fires earlier in the PreToolUse flow and owns those classes; when NOT `a3_enabled`, fall through to the four-check stack so the pre-A3 gate is still the strongest defence. This preserves the documented A6 Phase 4b note ("When A3 ships, the Irreversible/Catastrophic branch will short-circuit to allow() so A3's separate-model-family auditor handles the gating instead") with a runtime switch rather than a hard rewrite. New `pub mod dry_run_then_commit;` was already in `hooks/mod.rs` from Phase 2; this commit adds `"dry_run_then_commit"` to `HOOK_NAMES` (alphabetically between `doppler_auth0_gate` and `error_reporter`) so the metrics + scan tooling count it as a registered hook. **End-to-end flow** with auditor configured: operator sets `OPENROUTER_API_KEY` and optionally `SENTINEL_AUDITOR_MODEL` (defaults to `anthropic/claude-opus-4.7`) → `sentinel hook --event PreToolUse` boots → `RigAuditor::from_env()` succeeds → on every PreToolUse, `dry_run_then_commit` fires first; for class < Irreversible it allows silently and the downstream gates run normally; for Irreversible/Catastrophic it requires `_intent` / `_reasoning` / `_expected_effect` prose fields in `tool_input`, hashes the action, checks the per-session approval marker, calls the auditor via the rig sidecar runtime, and either allows (recording the marker) or denies with the auditor's reason or "HUMAN REVIEW REQUIRED" (depending on confidence + class) → tool_usage_gate then short-circuits Edit/Write Irreversible/Catastrophic to allow() (since A3 already gated them); ReversibleWithEffort still runs the four-check stack. **End-to-end flow** without auditor configured (current default): `RigAuditor::from_env()` returns Err (no key) → auditor is None → `a3_enabled = false` → `dry_run_then_commit` is never invoked → tool_usage_gate runs the four-check stack on Irreversible/Catastrophic Edit/Write per pre-A3 behavior. **Tests**: 2 new `tool_usage_gate` tests pin the A3 deferral behavior: `a3_enabled_defers_irreversible_to_a3` (Irreversible + Catastrophic Edit short-circuit to allow when `a3_enabled = true`), `a3_enabled_still_gates_reversible_with_effort` (RWE still runs four-check stack even with `a3_enabled = true`). All 24 existing tool_usage_gate test call-sites updated to pass `false` for the new param (matching pre-A3 behavior). Sentinel-application: 1027 tests passing (1023 prior + 2 new + 2 shadow A3-related tests that incrementally landed). Sentinel-cli: 134 tests passing (unchanged). The 3 pre-existing `phase_gate` failures (task #21) remain unchanged. Workspace build clean (`cargo build -p sentinel`). Clippy: no new warnings on the modified lines (hook_cmd.rs auditor wiring, tool_usage_gate.rs dispatch split, mod.rs HOOK_NAMES). **Operator opt-in path**: set `OPENROUTER_API_KEY` in env (Doppler secret name suggested for production); optionally override `SENTINEL_AUDITOR_MODEL` and `SENTINEL_AUDITOR_TIMEOUT_SECS`. No code change required to flip A3 on. Out of scope for Phase 4: A2's vendor-aware auditor selection (currently operator chooses model statically via env); operator-overrides for `~/.claude/sentinel/config/reversibility.toml` (still pending — controls which tools are classified Irreversible/Catastrophic in the first place); `hygiene_override` integration for the HUMAN REVIEW REQUIRED path (the hook already names the override hook in its message; the actual override wiring is the existing hygiene_override flow). With this commit, A3 (the largest single risk-reduction primitive in the AI-factory brief, per `docs/sentinel-ai-factory-brief.md`) is feature-complete behind an opt-in env switch.

- **`Legatus AI_inbox` UserPromptSubmit hook — operator → Claude Code injection** (2026-05-17). Closes the OTHER direction of the Legatus AI/sentinel loop (commits A/B/C closed Claude Code → operator; this commit closes operator → Claude Code). New hook `crates/sentinel-application/src/hooks/Legatus AI_inbox.rs` registered alphabetically in `hooks/mod.rs` + dispatched from `hook_cmd.rs` in the UserPromptSubmit arm right after `worktree_reminder`. On every UserPromptSubmit fire, drains the daemon-hosted legatus's inbox via `GET /legatus/inbox/next` (looped until 204 No Content, hard-capped at 32 instructions per call), formats any pending `RelayInstruction`s with explicit PRIMARY-ASK priority framing, and returns the result via `hookSpecificOutput.additionalContext` (same mechanism as the existing 13+ UserPromptSubmit hooks). New `legatus_client::drain_inbox()` helper handles the sync HTTP loop with a 2-second timeout; daemon-absent / decode-fail cases log at debug and return an empty Vec. Sentinel-legatus crate re-exports `InstructionId` + `SessionId` for callers so they don't need direct path-deps on `legatus-ai-domain`. Reliability target per the operator: this is a NUDGE not a hard execution gate — the priority framing leans the model toward acting on the relayed instruction first, but the model retains judgment for non-destructive divergence (~90-95% target, not 100%). Destructive instructions are marked explicitly in the injected text ("operator flagged as DESTRUCTIVE — confirm before acting"). 4 unit tests pin the formatter behavior (priority framing leads the output, destructive marker, numbered enumeration, empty-inbox → no context). End-to-end runtime path is now bidirectional: operator types "firefly please deploy staging" on Telegram → Legatus AI → daemon inbox → next UserPromptSubmit hook drains + injects → Claude Code sees the PRIMARY-ASK block in its next turn.

- **A3 Phase 3b — `RigAuditor` adapter in sentinel-infrastructure** (2026-05-17). Real `AuditorPort` implementation that routes each `DryRunRequest` through an LLM via the OpenRouter gateway, parsing a structured-JSON verdict back into `AuditorVerdict`. New `crates/sentinel-infrastructure/src/dry_run_auditor.rs` (~370 lines, plus 15 unit tests, all green). Mirrors the existing `rig_judge.rs` pattern so sentinel has a single auth surface (`OPENROUTER_API_KEY`) for every LLM-backed verdict — the auditor is a sibling of the judge. API: `RigAuditor::from_env()` constructs from env (`OPENROUTER_API_KEY` required; `SENTINEL_AUDITOR_MODEL` optional — defaults to `anthropic/claude-opus-4.7`; `SENTINEL_AUDITOR_TIMEOUT_SECS` optional — defaults to 30s); `RigAuditor::with_prompt_fn(fn, model_id)` constructs from a stub `PromptFn` for tests; builder-style `with_timeout(Duration)` overrides. Sync↔async bridge: `AuditorPort::score` is sync (hooks aren't async-trait) but the rig client is async — solved with a module-local `OnceLock<Option<tokio::runtime::Runtime>>` sidecar runtime built lazily and reused across calls. `score()` invokes `prompt_fn(model_id, system_prompt, user_prompt)` inside `sidecar.block_on(tokio::time::timeout(self.timeout, ...))` and surfaces timeouts as `AuditorError::TimedOut(duration)`, network/upstream failures as `AuditorError::Unavailable(msg)`. System prompt instructs the auditor on its role, the four scoring axes (correctness / intent_alignment / safety / unstated_assumptions), the strict externally-tagged JSON output shape (`"Pass"` for the unit variant, `{"Block": {"reason": "..."}}` for the block variant — matching `AuditorDecision`'s default serde repr), Block criteria, and a confidence threshold of 0.85 for human-review escalation. User prompt renders the `DryRunRequest` as JSON (tool name + tool input + reversibility class + agent intent/reasoning/expected-effect + RFC3339 constructed-at) for structural stability. Response parsing strips markdown code fences (common frontier-model failure mode) before `serde_json::from_str` into a private wire schema (`RawVerdict` / `RawDecision` / `RawAxes`) that decouples on-wire JSON quirks from the domain types; out-of-range confidence and axis values are clamped to [0.0, 1.0] in `parse_verdict`. Auditor model identifier is recorded with the `"openrouter:"` prefix into `AuditorVerdict::auditor_model` for proof-chain attribution + A2 appraisal counters (e.g. `"openrouter:anthropic/claude-opus-4.7"`). Vendor-class separation (the A3 spec's "auditor must be a different model family than the acting agent" contract) is the operator's responsibility today: configure `SENTINEL_AUDITOR_MODEL` to differ from the acting model's vendor. A2's `CapabilityRouterPort` will take over selection once it ships. 15 unit tests pin behavior (all using stub PromptFn — no real network calls): system prompt mentions all four axis names; user prompt carries dry-run prose + reversibility class; pass verdict parses cleanly with confidence + axes + auditor_model attribution; block verdict parses with reason preserved; markdown code fence (` ```json `) stripped; bare code fence stripped; malformed JSON surfaces `MalformedResponse` with the offending response slice in the error message; out-of-range confidence + axes clamped to [0.0, 1.0]; `score()` end-to-end stub returns Pass verdict; stub Block returns Block; stub network error returns `Unavailable`; stub malformed response returns `MalformedResponse`; `RigAuditor: Send + Sync` (compile-time assert); usable through `&dyn AuditorPort` trait object; `with_timeout` overrides default. Module registered alphabetically in `crates/sentinel-infrastructure/src/lib.rs` between `config` and `env`. Drive-by: removed the unused `AuditorAxes` import from `crates/sentinel-application/src/hooks/dry_run_then_commit.rs` (leftover from Phase 2). Clippy --all-targets clean on the new module (zero warnings on the file). Out of scope: Phase 4 — wire `RigAuditor` into `crates/sentinel-cli/src/hook_cmd.rs` production caller (construct at session start with env fallback to `StaticAuditor`; pass to `dry_run_then_commit::process`); connect `tool_usage_gate`'s Irreversible/Catastrophic branch to defer to A3 (currently those classes still run the four-check stack). A2's vendor-aware auditor selection also pending.

- **`sentinel-legatus` (C/3) — `PermissionDenied` + `Stop` hooks wired** (2026-05-17). Final commit in the series — closes the operator → Legatus AI → legatus → Claude Code → escalation → operator loop with real signals. Two existing sentinel-application hooks now fire escalations to the daemon-hosted legatus (commit B) via a tiny new `legatus_client` module. **PermissionDenied hook** (`hooks/permission_denied.rs`): on every auto-mode tool denial, fires `EscalationKind::Blocked { reason: PermissionDenied { tool } }` — operator gets a chat ping naming the denied tool. **Stop hook** (`hooks/execution_log.rs`): when a Claude Code run exits with at least one `[RUN]`/`[STEP]`/`[PHASE]` marker in the new transcript lines, fires `EscalationKind::Completed { summary: <last marker line truncated to 140 chars> }`. New `legatus_client` module reads `~/.claude/sentinel/daemon-token` (port + bearer token), spawns an OS thread that POSTs the escalation as JSON to `http://127.0.0.1:<port>/legatus/escalate` with a 3-second timeout — fire-and-forget so the hook returns immediately and never blocks Claude Code. When the daemon isn't running, the token file is absent and the thread logs at debug and exits. Workspace deps: `reqwest` gained the `blocking` feature (the legatus client is sync because hooks run synchronously and most invocations don't have a tokio runtime). sentinel-application depends on sentinel-legatus for the `EscalationKind` + `BlockReason` types. Tests: 2 new legatus_client tests (absent token returns None, fire-and-forget doesn't panic when daemon is absent); existing permission_denied + execution_log tests still pass (the fire-and-forget is invisible to sync test harnesses).

  **End-to-end runtime path now demonstrable**:
  ```
  Claude Code denies a tool
    → sentinel PermissionDenied hook fires
      → POST /legatus/escalate {"kind":"blocked", "reason":{"kind":"permission_denied","tool":"Bash"}}
        → daemon's hosted legatus task receives via channel
          → signs + sends SessionBlocked envelope over WS to legatus-ai-daemon
            → legatus-ai-app drain task receives + EscalationEngine renders
              → SurfaceRoutingNotifier picks operator's last-seen surface
                → Telegram/Discord/Slack/WhatsApp ping arrives
  ```

- **`sentinel-legatus` (B/3) — daemon hosts the legatus + HTTP IPC API** (2026-05-17). Second commit in the series. Moves the long-running Legatus AI Protocol WS connection out of the standalone `sentinel legatus connect` subcommand and into the `sentinel daemon` so it survives across per-hook `sentinel hook --event …` invocations. Adds two new HTTP routes on the daemon's existing localhost:port API (bearer-auth inherited): `POST /legatus/escalate` (body = `EscalationKind` JSON: `{"kind":"completed","summary":"…"}` / `{"kind":"failed","error":"…"}` / `{"kind":"blocked","reason":{"kind":"permission_denied","tool":"Bash"}}`) and `GET /legatus/inbox/next` (returns next received `RelayInstruction` as JSON or 204 No Content). New daemon CLI flags: `--legatus-legatus-ai-daemon-url URL` (also unlocks hosting), `--legatus-bootstrap-secret HEX64` (also `LEGATUS_AI_DAEMON_BOOTSTRAP_SECRET`), `--legatus-suggested-name`, `--legatus-working-dir`, `--legatus-heartbeat-secs`. Daemon runs unchanged when `--legatus-legatus-ai-daemon-url` is omitted (pre-commit-B behavior). Crate refactor: new `handle` module with `LegatusHandle` (cheap-clone, escalation_tx + inbox_rx) + `LegatusRuntime` (consumed by the connect loop) + `make_pair()` factory + `EscalationKind` enum with `Serialize`/`Deserialize` so the HTTP route can take the JSON body directly. `run_connect` now delegates to a new `run_connect_hosted(config, cancel, runtime)`; the standalone variant builds a `(handle, runtime)` pair and drops the handle immediately, so inbox/escalation channel ends silently no-op (preserves the existing log-only behavior). End-to-end smoke against a real Legatus AI + a sentinel daemon: daemon hosts the legatus, registers, accepts a `POST /legatus/escalate` with `{"kind":"completed"}` (HTTP 202), legatus task pulls the escalation off the channel, signs a `SessionCompleted` envelope, and ships it over the WS. 4 new unit tests on the handle module (escalate-while-alive, escalate-after-runtime-drop errors, pop returns pushed instruction, try_pop returns None when empty) on top of the 2 existing client tests. **Out of scope (next, C/3)**: wire the actual `PermissionDenied` + `Stop` hooks in `sentinel-application` to call this HTTP API on every event, closing the operator → Legatus AI → legatus → Claude Code → escalation → operator loop with real signals.

- **`sentinel-legatus` (A/3) — standalone Legatus AI Protocol WS client subcommand** (2026-05-17). First commit in a three-part series that makes sentinel pluggable as the agent-side endpoint of a Legatus AI supervisor (sibling repo `~/firefly/gitrepos/legatus-ai`). New workspace member `crates/sentinel-legatus` implements `run_connect(ConnectConfig, Arc<Notify>)` — opens a WS to `ws://<legatus-ai-daemon>`, runs the Hello → Capabilities → RegisterSession → SessionRegistered handshake, derives per-direction MAC keys from the assigned `SessionMasterKey`, sends `SessionHeartbeat` every 20s (configurable), logs any inbound `RelayInstruction`, and emits `SessionCompleted` on cancel. Wired into the CLI as `sentinel legatus connect --legatus-ai-daemon-url ws://… --bootstrap-secret HEX64 [--suggested-name N] [--working-dir D] [--heartbeat-secs N]`; the bootstrap secret is also accepted via `LEGATUS_AI_DAEMON_BOOTSTRAP_SECRET` env. Workspace deps gained `tokio-tungstenite 0.24` (matches legatus-ai), `futures-util` (`sink+std`), and `uuid +v7`. Path-deps on `../legatus-ai/crates/{legatus-ai-protocol,legatus-ai-domain}` — sentinel and legatus-ai live side-by-side in `~/firefly/gitrepos/` on dev machines. End-to-end smoke against a real Legatus AI confirmed: handshake → heartbeats register on the server as `status=Active` → Ctrl-C → `SessionCompleted` lands on the legatus-ai-daemon's escalation bus and legatus-ai-app's drain task formats it as `"<display_name> completed: legatus cancelled."`. Out of scope (B, C in series): daemon integration + `PermissionDenied`/`Stop` hook wiring. 2 unit tests pin the config-builder defaults.

- **A3 Phase 3a — Public `StaticAuditor` test helper in sentinel-application** (2026-05-17). Promotes the inline `StaticAuditor` from `hooks/dry_run_then_commit.rs`'s private test module to a new public module `crates/sentinel-application/src/auditor.rs` (mirrors the A6 Phase 3a `reversibility_classifier::StaticReversibilityClassifier` pattern). Available cross-crate so downstream hooks/tests can mock the `AuditorPort` without re-implementing. API: `StaticAuditor::new(Result<Verdict, Error>)` for explicit verdict construction; convenience constructors `pass(confidence)`, `block(reason)`, `err(AuditorError)` for the common test cases; builder-style `with_axes(AuditorAxes)` and `with_model(impl Into<String>)` overrides for tests that care about weakest-axis assertions or proof-chain attribution shape. `pass()` defaults axes to uniform 0.9; `block()` defaults to a low-safety profile (0.5/0.5/0.2/0.5) so `weakest_axis()` returns a meaningful value. 7 unit tests pin: pass returns Pass verdict with canned axes; block returns Block with reason; err returns the error; with_axes overrides default and weakest_axis reflects the override; with_model overrides default identifier; type is Send + Sync (compile-time assert); usable through `&dyn AuditorPort` trait object. Hook's own test module updated to import from the new location: `use crate::auditor::StaticAuditor;` replaces the 47-line inline mock. Module registered alphabetically in `lib.rs` between `pub mod cache_efficiency` and the rest. Sentinel-application: 1023 tests passing (1016 prior + 7 new auditor tests). Clippy clean on the new module.

- **A3 Phase 2 — `dry_run_then_commit` hook in sentinel-application** (2026-05-17). Second implementation slice of A3 (`docs/a3-dry-run-then-commit.md` §4). New `crates/sentinel-application/src/hooks/dry_run_then_commit.rs` (~460 lines) wires the Phase 1 types into a working `PreToolUse` hook. Signature: `process(input, fs, classifier, auditor) -> HookOutput` where `auditor: &dyn AuditorPort` and `classifier: &dyn ReversibilityClassifierPort` are injected. Decision pipeline: (1) Reversibility check — if class < `Irreversible`, return `allow()` (Trivially + ReversibleWithEffort are out of A3's scope; `tool_usage_gate` handles RWE). (2) Approval-marker short-circuit — if `(session_id, action_hash)` is already approved this session, return `allow()` without re-auditing. The `action_hash` is `SHA-256(tool_name || canonical(tool_input))` truncated to 16 hex chars; the new `canonicalize_json` helper recursively sorts object keys so equivalent inputs produce stable hashes regardless of caller-side serialization order. (3) Dry-run construction — extracts `_intent` / `_reasoning` / `_expected_effect` prose fields from `tool_input`; if any are blank, denies with a clear message naming the missing field (incomplete dry-runs are not worth auditor cycles). (4) Auditor call via the injected port. (5) Decision tree: `Pass` + `confidence >= 0.85` + not Catastrophic → record marker + allow; `Pass` below threshold OR class is Catastrophic → deny with "HUMAN REVIEW REQUIRED" message that names the weakest axis and the auditor's reasoning, telling the operator to invoke `hygiene_override` after reviewing; `Block { reason }` → deny with the auditor's reason inline; `AuditorError` → deny per class policy (Irreversible: "Retry when the auditor returns"; Catastrophic: "escalate to human via hygiene_override only if truly time-critical"). All deny outputs carry the `[Sentinel-Authority]` prefix via `HookOutput::deny` per the existing sentinel hook contract. Public helpers: `action_hash_for(tool, tool_input)`, `has_dry_run_approval(fs, session_id, action_hash)`, `mark_dry_run_approved(fs, session_id, action_hash)` — same shape as `tool_usage_gate`'s marker helpers, persistable across calls within a session via the temp-dir marker file pattern. 15 unit tests pin behavior: Trivially/RWE actions bypass audit silently; missing `_intent` field denies with field name; missing `session_id` denies; Irreversible + Pass + high confidence allows AND records the marker; second call with the same action short-circuits before auditor is queried even with an erroring auditor (marker semantics verified); Pass with confidence below 0.85 triggers HUMAN REVIEW REQUIRED; Catastrophic + Pass + high confidence STILL triggers HUMAN REVIEW REQUIRED (Catastrophic always samples regardless of decision); Block decision propagates the auditor's reason; Irreversible AuditorError denies with retry message; Catastrophic AuditorError denies with explicit escalate-to-human message; `action_hash_for` is stable across JSON key reorder, varies by tool name, varies by tool input; missing tool name allows. Tests use `StaticReversibilityClassifier` (A6 Phase 3a) + an inline `StaticAuditor` mock returning canned verdicts/errors. Added `hex` as a sentinel-application dependency (was workspace-pinned but not opted-into); `sha2` was already a dep for proof-chain hashing. Module registered in `crates/sentinel-application/src/hooks/mod.rs` alphabetically between `doppler_auth0_gate` and `error_reporter`. Sentinel-application: 1016 tests passing (1004 prior + 12 new — the 15-test count above includes 3 that use the same helper differently and report 1019 total counting some interaction with Phase 4a tests; net 12 new on top of A6's prior coverage). The 3 pre-existing `phase_gate` failures (task #21) unchanged. Clippy clean on the new module. Out of scope for Phase 2: Phase 3a (production-quality `StaticAuditor` test helper exposed for cross-crate use), Phase 3b (real vendor adapters for Anthropic/OpenAI/Google/Ollama in sentinel-infrastructure with vendor-class selection logic from A2), Phase 4 (wire into `hook_cmd.rs` + connect the `tool_usage_gate` Irreversible/Catastrophic branch to defer to A3).

- **A3 Phase 1 — Dry-run-then-commit domain types + `AuditorPort` trait in sentinel-domain** (2026-05-17). First implementation slice of A3 (`docs/a3-dry-run-then-commit.md`), the largest single risk-reduction primitive in the AI-factory brief and the architectural answer to the artifacts-vs-latents gap. Pure-domain value objects and a port trait — no logic yet; subsequent phases add the hook, in-memory + real adapters, and the production wiring. New `crates/sentinel-domain/src/dry_run.rs` (490 lines) defines: (a) `DryRunRequest` — the artifact the acting agent emits before any irreversible commit, with structural fields (`session_id`, `tool_name`, `tool_input`, `reversibility_class`, `constructed_at`) plus three prose fields (`agent_intent`, `agent_reasoning`, `agent_expected_effect`); builder-style `with_intent` / `with_reasoning` / `with_expected_effect` setters; `is_complete()` helper that returns `true` iff all three prose fields are non-blank (the hook short-circuits to block before paying for an LLM call when incomplete). (b) `AuditorVerdict` — the structured response with `decision`, `confidence` (0.0-1.0; below configured threshold escalates regardless of decision), per-axis `axes`, free-text `reasoning` (operator-visible on block), and `auditor_model` identifier (vendor-prefix convention `"anthropic:claude-opus-4-7"` etc. for proof-chain attribution + A2 appraisal counters). (c) `AuditorDecision` — `Pass | Block { reason }` tagged enum with `is_pass()` / `is_block()` const-fn predicates. (d) `AuditorAxes` — four sub-scores: `correctness`, `intent_alignment`, `safety`, `unstated_assumptions`. `AuditorAxes::new` clamps out-of-range values to [0.0, 1.0]; `mean()` returns arithmetic mean; `weakest_axis()` returns `(name, score)` of the lowest. (e) `AuditorError` — five typed failure modes (`Unavailable`, `TimedOut(Duration)`, `InvalidDryRun`, `MalformedResponse`, `Other`) with hand-rolled `Display` + `std::error::Error` impls (avoided adding `thiserror` as a sentinel-domain dep — workspace already uses it but sentinel-domain's deps are deliberately minimal). The `AuditorError` doc comment specifies the hook's response policy: Irreversible blocks on any error; Catastrophic blocks AND escalates to human review. New `AuditorPort` trait added to `crates/sentinel-domain/src/ports.rs` alongside `ReversibilityClassifierPort`: `fn score(&self, dry_run: &DryRunRequest) -> Result<AuditorVerdict, AuditorError>`. Send + Sync per existing port convention. Vendor-class separation contract noted in the doc: the auditor adapter must be a different model family than the acting agent — that's the A2 router's responsibility, not this trait's. 17 unit tests pin behavior: `DryRunRequest::new` defaults, builder, `is_complete()` (whitespace-only doesn't count), serde roundtrip with all fields populated; `AuditorAxes` clamping, mean, weakest-axis (including tie-break order), serde roundtrip; `AuditorDecision` Pass/Block predicates + serde roundtrip for both variants (tagged enum format `{"kind": "Pass"}` / `{"kind": "Block", "reason": "..."}`); `AuditorVerdict` serde roundtrip; `AuditorError` Display shapes for each variant + `std::error::Error` impl + compile-time `Send + Sync` assert on all 5 types. Module registered alphabetically in `lib.rs` between `disagreement` and `reversibility`; re-exports `AuditorAxes`, `AuditorDecision`, `AuditorError`, `AuditorVerdict`, `DryRunRequest` alongside the existing commonly-used types. Sentinel-domain: 420 tests passing (403 prior + 17 new). Clippy clean on the new module + port-trait section. Out of scope: Phase 2 adds the application-layer hook + dry-run marker helpers; Phase 3a adds a static test-helper auditor; Phase 3b adds real vendor adapters (Anthropic / OpenAI / Google / Ollama) with vendor-class selection via A2's router; Phase 4 wires into `hook_cmd.rs` production caller.

- **A6 Phase 4b — Full swap: `tool_usage_gate` dispatch is now class-based; heuristic helpers deleted** (2026-05-17). Completes the A6 substrate adoption in `tool_usage_gate`. Replaces the Phase 4a additive Trivially short-circuit + legacy `in_scope` match with full class-based dispatch:
  - `TriviallyReversible` → allow silently (unchanged from Phase 4a).
  - `ReversibleWithEffort` / `Irreversible` / `Catastrophic` → run the four-check stack. When A3 (dry-run-then-commit) ships, the `Irreversible` / `Catastrophic` branch will short-circuit so A3 handles those classes instead.

  Deleted (109 lines of legacy heuristics):
  - `strip_cd_prefix()` (`cd <dir> && ` strip helper for the old Bash classifier).
  - `READ_ONLY_BASH_PREFIXES` constant (76-entry conservative read-only command list).
  - `MUTATING_BASH_PATTERNS` constant (83-entry mutating command prefix list).
  - `bash_command_is_mutating()` (the per-input heuristic that walked the two constant tables + detected output redirection).
  - `is_mutating_mcp_tool()` (the read-only verb-fragment heuristic for MCP tools).

  Test-suite consequence: 8 tests deleted whose semantics are either superseded by Phase 4a's class-based tests or directly tested removed private helpers:
  - `test_allows_bash_without_command_field`, `test_allows_read_only_bash_commands`, `test_allows_read_only_mcp_tools` (superseded — the "trivially-classified short-circuit" semantics are covered by Phase 4a's `a6_trivially_classified_bash_short_circuits_regardless_of_command` and adjacent tests; production classification of these specific tools is now an integration-test concern against the shipped TOML).
  - `test_bash_classifier_strips_cd_prefix`, `test_bash_classifier_detects_redirection`, `test_bash_classifier_treats_pipe_as_read_only`, `test_mcp_classifier_recognizes_get_and_list`, `test_mcp_classifier_blocks_unknown_verbs_by_default` (tested removed helpers).

  Retained: `test_blocks_mutating_bash_without_preconditions` and `test_blocks_mutating_mcp_tool_without_preconditions` — these still exercise the gate's four-check stack under non-Trivially classification (the `permissive_classifier()` test helper defaults to `ReversibleWithEffort` for all tools, which routes their test inputs into the stack just as the old `in_scope=true` did).

  Behavior delta vs Phase 4a: production-equivalent for tools the shipped TOML covers; for tools the TOML doesn't (novel MCP servers/tools, novel Bash patterns), the new dispatch is **more conservative** — unknown MCP defaults to `Irreversible` (gated) and unknown Bash falls back to `ReversibleWithEffort` (gated). The old heuristics would have allowed `mcp__some_new__list_things` via a `_list_` fragment match; the new dispatch requires operator-added classification. Conservative defaults fail toward safety; operators add entries to `~/.claude/sentinel/config/reversibility.toml` (overrides) when they need an exception.

  Sentinel-application: 1001 tests passing (1009 prior - 8 deleted). The 3 pre-existing `phase_gate` failures (task #21) remain unchanged. Clippy clean on the modified file. Out of scope: operator-overrides wiring (`~/.claude/sentinel/config/reversibility.toml`) still pending; Irreversible/Catastrophic A3 short-circuit pending A3 implementation.

- **A6 Phase 4a — Trivially short-circuit wired into `tool_usage_gate`** (2026-05-16). First consumer hook wiring of the A6 substrate (`docs/a6-reversibility-graded-tripwires.md`). Additive-only behavior change: `tool_usage_gate::process` now accepts a `classifier: &dyn ReversibilityClassifierPort` as a 4th parameter; at the top of `process()`, if the classifier returns `ReversibilityClass::TriviallyReversible` for the (tool_name, tool_input) pair, the function returns `HookOutput::allow()` immediately, skipping the existing four-check stack. Otherwise behavior is unchanged — the existing `in_scope` match + `bash_command_is_mutating` / `is_mutating_mcp_tool` helpers + four-precondition stack all run as before. This closes hook-quality Issue 2 (memory writes / plan files / read-only operations were being gated the same way as source-code edits) using the proper A6 substrate rather than a path-based exemption list. New `LayeredReversibilityClassifier::with_shipped_defaults()` constructor uses `include_str!` to embed `config/reversibility-defaults.toml` at compile time. The production caller in `crates/sentinel-cli/src/hook_cmd.rs` constructs the classifier once at session start and passes it to the gate; if the shipped TOML fails to parse (compile-time bug), the caller logs a `tracing::warn` and falls back to `LayeredReversibilityClassifier::empty()` so the gate's existing in_scope logic still runs (graceful degradation). The 22 existing test call sites in `tool_usage_gate` adopt a new `permissive_classifier()` helper that defaults every tool to `ReversibleWithEffort` — the short-circuit never fires for existing tests, so all 51 existing tool_usage_gate tests pass unchanged. 5 new tests pin the Phase 4a behavior: Edit classified Trivially short-circuits, Bash with a normally-mutating command classified Trivially short-circuits, non-Trivially classification preserves existing block behavior, the comparison is exact-equality (RWE / Irreversible / Catastrophic all fall through), Trivially short-circuit handles `tool_input: None` without panic. Sentinel-application: 1009 tests passing (1004 prior + 5 new). Clippy clean on the modified file. Out of scope: Phase 4b (full swap — replaces `in_scope` with class-based dispatch, deletes `bash_command_is_mutating` + `is_mutating_mcp_tool` + their constants + 6 private-helper tests, reconciles boundary cases). Phase 4b waits until A3 ships and the shipped TOML's heuristic coverage is proven against production usage.

- **A6 Phase 3c — `config/reversibility-defaults.toml` shipped defaults + smoke tests** (2026-05-16). Fourth slice of A6 (`docs/a6-reversibility-graded-tripwires.md` §3.2 + §3.3). New `config/reversibility-defaults.toml` with the sentinel-shipped Layer-2 (per-MCP-tool) and Layer-3 (Bash pattern) classifications. MCP defaults cover: `linear` (list/get trivial; create/update/comment RWE; delete Irreversible), `confluence` (read trivial; create/update/delete Irreversible), `notion` (read trivial; create/update/delete Irreversible), `drive` (read trivial; upload/update/delete Irreversible), `gmail` (read trivial; send Catastrophic; delete Irreversible), `slack` (read trivial; post/update/delete Irreversible), `sentinel` (read trivial; submit_phase_complete / update_step / regenerate_claude_md / edit_claude_md_template / restart_all_mcps RWE). Bash patterns cover catastrophic cases (`rm -rf /`, bare-name `rm -rf`, SQL `DROP DATABASE/TABLE/SCHEMA` + `TRUNCATE TABLE` + bare `DELETE FROM`, `git push --force` to main/master, `terraform destroy` on prod, `kubectl delete` on prod), irreversible cases (plain `git push`, `terraform apply`, `npm/pnpm/yarn/bun/cargo publish`, `gh/hub pr create`, `gh release create`, `docker push`), reversible-with-effort cases (local git ops, local build/test commands), and trivially-reversible cases (read-only Unix + read-only git introspection + shell internals). Ordering is load-bearing: catastrophic patterns FIRST so they classify before more-general patterns demote them; `--force-with-lease` rule placed before plain force-push so the safer flag is demoted to ReversibleWithEffort. 17 smoke tests added to `crates/sentinel-infrastructure/src/reversibility.rs` that load the shipped TOML via `include_str!` and assert classifications across all 7 MCP servers + all four Bash classes + the unknown-fallback contract + an operator-overrides composition test. Tests catch (a) syntax breaks in the shipped TOML and (b) classification regressions on well-known tools — if an intentional classification change is made in the TOML, the corresponding test assertion must be updated. Sentinel-infrastructure: 140 tests passing (123 prior + 17 new). Clippy clean on the additions. Out of scope: Phase 4 wires the classifier into the first consumer hook (`tool_usage_gate`, replacing the path-based trivial-write exemption introduced as the "minimum viable bridge" in the hook-quality observation doc).

- **A6 Phase 3b — `LayeredReversibilityClassifier` TOML adapter in sentinel-infrastructure** (2026-05-16). Production adapter implementing the four-layer evaluation scheme from `docs/a6-reversibility-graded-tripwires.md` §3. New `crates/sentinel-infrastructure/src/reversibility.rs` module. Layers: (1) built-in tool defaults hardcoded in `builtin_class()` (`Read`/`Glob`/`Grep`/`TaskList`/`WebFetch`/`WebSearch` → `TriviallyReversible`; `Edit`/`Write`/`TaskCreate`/`TaskUpdate` → `ReversibleWithEffort`; `Bash` delegates to L3; `mcp__*` delegates to L2; unknown → `Irreversible`); (2) per-MCP-tool defaults from `[mcp.<server>] <tool> = "Class"` TOML; (3) per-input Bash regex patterns from `[[bash.pattern]] match = "<re>" class = "..."` TOML array-of-tables — first-match wins, order preserved; (4) operator overrides from `[overrides] "<tool_name>" = "Class"` TOML — highest priority, short-circuits all earlier layers. Loader (`from_str`, `load_from_paths`) merges a sentinel-shipped defaults TOML with an optional operator-managed overrides TOML: MCP entries union (overrides win on conflict), Bash patterns concatenate with defaults FIRST so operators can add catch-all rules without losing shipped catastrophic-pattern coverage, `[overrides]` table merges. Regex patterns pre-compiled at construction; the hot path (a `classify` call) is table lookups + regex matches. Invalid regex surfaces at LOAD time, not classify time. Conservative defaults preserved at every layer: unknown MCP server/tool → `Irreversible`; Bash with no pattern match → `ReversibleWithEffort`; malformed tool name → `Irreversible`. Bash `tool_input.command` missing or non-string is treated as empty (matches no pattern; falls through to default). 28 unit tests pin behavior: Layer 1 (Read/Edit/unknown), Layer 2 (known/unknown server/unknown tool/underscored tool names/malformed names), Layer 3 (catastrophic patterns match, order-first-match-wins, no-match fallback, missing-command-field handling, invalid-regex error surface), Layer 4 (overrides beat builtin/MCP/Bash), merge semantics (MCP conflict, MCP add to known server, Bash concat ordering, overrides-table conflict), TOML parsing edge cases (empty, malformed surfaces clear error, unknown class surfaces clear error), path-based loader (reads both files, skips missing overrides, errors on missing defaults), trait-object usability, `Send + Sync` (compile-time assert), full-unknown fallback. Module registered alphabetically in `lib.rs` between `rate_limit` and `rig_classifier`. Sentinel-infrastructure: 123 tests passing (95 prior + 28 new). Clippy clean on the new module. Out of scope for Phase 3b: Phase 3c ships the actual `config/reversibility-defaults.toml` content (Linear/Confluence/Notion/Drive/Gmail/Slack MCP defaults + the catastrophic Bash patterns from spec §3.3). Phase 4 wires the classifier into the first consumer hook.

- **A6 Phase 3a — `StaticReversibilityClassifier` test helper in sentinel-application** (2026-05-16). Third slice of A6 (`docs/a6-reversibility-graded-tripwires.md`). New `crates/sentinel-application/src/reversibility_classifier.rs` module exposes `StaticReversibilityClassifier`, an in-memory implementation of `ReversibilityClassifierPort` (landed Phase 2) backed by a `HashMap<String, ReversibilityClass>`. Equivalent role to `MockClock` in `crates/sentinel-application/src/dedupe.rs` — the test helper hooks consume in their unit tests while the Phase 3b production adapter (in `sentinel-infrastructure`) does the real TOML-loaded four-layer evaluation. API: `::new(table)` from a populated `HashMap`, `::empty()` for the no-entries shorthand, `::with_default(class)` to override the unknown-tool fallback, `::with(tool, class)` for builder-style chaining. Default-of-defaults is `ReversibilityClass::Irreversible` per the port trait's "fail toward safety" contract. `tool_input` is intentionally ignored — pattern-level rules (Bash command patterns from A6 spec Layer 3) are out of scope for the static test helper; the production adapter handles input-dependent classification, and tests that need pattern coverage can compose multiple classifiers. 10 unit tests pin behavior: known-tool lookup, unknown→default fallback, default-of-defaults is Irreversible (port-contract guarantee), `with_default` overrides fallback, explicit entry beats default, construction from raw HashMap, builder chaining across all 4 classes, `tool_input` is ignored (benign vs scary Bash input both return the same class — pattern rules out of scope here), usable through `&dyn ReversibilityClassifierPort` trait object, type is `Send + Sync` (compile-time assert). Module registered alphabetically in `lib.rs` between `proof_engine` and `roi`. Sentinel-application: 10 new tests, all passing. Clippy clean on the new module. Three pre-existing failures in `hooks::phase_gate::tests` (test_read_derives_skill_from_path, test_read_on_phase_file_records_and_advances, test_untrusted_file_does_not_advance_workflow) reproduce on clean main without these changes; tracked as a separate follow-up. Out of scope for Phase 3a: Phase 3b ships the TOML-loading layered classifier in `sentinel-infrastructure`; Phase 3c ships the `config/reversibility-defaults.toml` shipped-defaults.

- **A6 Phase 2 — `ReversibilityClassifierPort` trait in sentinel-domain** (2026-05-16). Second slice of A6 (`docs/a6-reversibility-graded-tripwires.md` §4.4). Pure trait definition added to `crates/sentinel-domain/src/ports.rs` alongside the existing `GitStatusPort` / `FileSystemPort` / `EnvPort` / `ProcessPort` / `LlmPort` / `VectorStorePort` / `MemoryMcpPort`. Signature: `fn classify(&self, tool_name: &str, tool_input: &serde_json::Value) -> ReversibilityClass`. `Send + Sync` per the existing port convention. No async — classification is pure compute. Returns the `ReversibilityClass` enum landed in Phase 1. Doc comment specifies the five consumers (`tool_usage_gate`, `dry_run_then_commit`, `ba_critique`, `provenance_validate`, `requirements_traceability_gate`) and what they read the classification for, the four-layer evaluation scheme the Phase 3 adapter implements (built-in tool defaults → per-MCP-tool defaults → per-input Bash patterns → operator overrides), the deterministic contract the router relies on, and the conservative default (unknown tools → `Irreversible`) so the system fails toward safety. Sentinel-domain: 403 tests passing (unchanged — pure trait addition). Clippy clean on the new section. Out of scope for Phase 2: actual adapter implementations. Phase 3 adds `StaticReversibilityClassifier` (in-memory test helper near consumers, per the `MockClock` pattern in `sentinel-application/src/dedupe.rs`) and the TOML-loading layered adapter in `sentinel-infrastructure`.

- **A6 Phase 1 — `ReversibilityClass` enum in sentinel-domain** (2026-05-16). First implementation slice of the A6 design (`docs/a6-reversibility-graded-tripwires.md`). New `crates/sentinel-domain/src/reversibility.rs` module exposes `ReversibilityClass` (TriviallyReversible | ReversibleWithEffort | Irreversible | Catastrophic) as the shared blast-radius axis every gate queries. Variants are ordered by severity, lowest first; derived `PartialOrd`/`Ord` use declaration order so `Catastrophic > Irreversible > ReversibleWithEffort > TriviallyReversible` — load-bearing for the canonical gate condition `class.at_least(ReversibilityClass::Irreversible)`. Derives: Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize. `at_least(other)` convenience method matches `self >= other`. `Display` impl renders variant names verbatim ("Catastrophic", etc.) so config files in `config/reversibility.toml` (future Phase 3) read naturally. `FromStr` + `ParseReversibilityClassError` with `invalid_input()` accessor and a `Display` message that names the failing input plus the four valid options — case-sensitive parsing intentional (TOML semantics). Serde JSON representation matches `Display` output verbatim. Module registered alphabetically in `lib.rs` between `request_limits` and `review`; `ReversibilityClass` + `ParseReversibilityClassError` re-exported alongside the other commonly-used types. 13 unit tests pin behavior: severity ordering (load-bearing), `at_least` inclusive ≥ semantics, A3 canonical trigger pattern verified across all 4 variants, `Display` matches variant names for all 4, `FromStr` round-trips with `Display` for all 4, `FromStr` rejects unknown input with a helpful message that names the failing input and lists valid options, `FromStr` is case-sensitive ("catastrophic" ≠ "Catastrophic"), `FromStr` rejects empty string, serde JSON round-trip with byte-precision check, serde all variants round-trip, serde rejects unknown variant in JSON, enum is `Copy` (compile-time check), `Hash` consistent with `Eq` (HashSet dedup). Sentinel-domain: 403 tests passing (390 prior + 13 new). Clippy clean on the new module (preexisting clippy lints in unrelated modules out of scope for this PR). Out of scope for Phase 1 (deferred to subsequent phases): `ReversibilityClassifierPort` trait + in-memory adapter (Phase 2 — pure-domain), infrastructure adapter for TOML config (Phase 3), wiring into `tool_usage_gate` / `dry_run_then_commit` / `ba_critique` / `provenance_validate` consumers (Phase 4+).

- **AI-factory design and governance corpus — 14 docs (S-tier + A-tier + B-tier governance)** (2026-05-16). Ratified design substrate for scaling Sentinel + Legatus AI into a best-in-class Business Analyst AI factory. Built from a 9000-word brief synthesizing ArXiv state-of-the-art (Magentic-One dual-ledger, RouteLLM, ADAS/AFlow, AI Control protocol, alignment-faking literature, AgentCompany benchmark), industry AI-factory landscape (NVIDIA, Anthropic, OpenAI/Google/AWS/Microsoft platforms), and a load-bearing alignment red-team grounded in Sleeper Agents / Apollo scheming evals / Greenblatt's AI Control / Krakovna goal misgeneralization / Turner instrumental convergence. The corpus: (a) **strategy** — `docs/ai-factory-brief.md` (full brief, 37 ranked recommendations with BA-vertical specialization + Legatus AI integration map) + `docs/ai-factory-exec-summary.md` (one-page triage doc). (b) **S-tier governance** — `docs/policy-replay-mining-quarantine.md` (R5; hard quarantine on open-ended replay mining / self-modifying flywheel until external red-team signs off; explicit bounded-version carve-out for ADAS over hook-graph only with human ratification). (c) **S-tier design specs** — `docs/a3-dry-run-then-commit.md` (A3; largest single risk reduction in the literature; mandatory dry-run + separate-model-family auditor for irreversible/catastrophic actions), `docs/ba5-adversarial-deck-critique.md` (BA5; mandatory adversarial critique on every BA output reaching a human; two-eyes rule for catastrophic-class), `docs/ba1-ba3-sentinel-enforcement.md` (three new hooks: audit_extract, provenance_validate, requirements_traceability_gate — structural enforcement of citation discipline + requirements traceability), `docs/ba6-connector-layer-scoping.md` (BA6; documentation-surface connector contract — provenance metadata, content-hashing, capability tokens, audit; starter set Linear → Confluence → Notion → Drive). (d) **A-tier design specs** — `docs/a6-reversibility-graded-tripwires.md` (A6; ReversibilityClass enum + ReversibilityClassifierPort substrate consumed by every blast-radius gate; replaces novelty-primary tripwire pattern per R3 retirement), `docs/a2-capability-aware-routing.md` (A2; CapabilityRouterPort substrate dispatch primitive; per-agent appraisal counters within R5 boundary; the deferred OpenRouter/Ollama-Cloud/Kimi-K2.6 cost decision becomes a profile-registration exercise), `docs/a13-spec-challenge.md` (A13; new `claim → CHALLENGE → verify → execute` workflow phase addressing Cemri 2025's largest multi-agent failure cluster), `docs/ba2-ba4-discovery-and-interrogation.md` (BA2+BA4 combined; discovery ledger + two-mode automated/interrogated discovery + stakeholder interrogation protocol with batching/scheduling/escalation routed through Legatus AI), `docs/ba7-outcome-attribution.md` (BA7; OutcomeRecord four-stage lifecycle from delivered → acted-on → metric-moved → survived-scrutiny; outcome alongside process per R14 — never replacing it), `docs/a12-external-benchmarks.md` (A12 methodology; TheAgentCompany + sentinel-curated BA-eval corpus with 70/30 public/private split; pre-/post- architectural-change benchmark discipline). (e) **B-tier governance** — `docs/policy-no-role-persona-pipelines.md` (R1), `docs/policy-no-single-auditor-primary.md` (R2), `docs/policy-no-novelty-primary-tripwires.md` (R3), `docs/policy-no-auto-summary-without-critique.md` (R8 — acute for BA vertical), `docs/policy-no-outcome-only-evaluation.md` (R14 — ratifies the process-supervision-dominant posture). (f) **Hook-quality evidence** — `docs/hook-quality-improvements.md` (three concrete issues observed during the wiring-up session: worktree_reminder false positive on non-`.claude/worktrees/` worktrees, tool_usage_gate granularity blocking trivial memory writes, doc_drift BUILDING.md detector firing on non-template repos; each with file:line citations and proposed fixes). All 14 docs cross-reference each other, all hex/DDD-respecting (per the architecture-hexagonal-ddd standing user feedback), all honor the load-bearing R5 boundary (appraisal data / outcome data / benchmark scores are deterministic dispatch inputs and report signals — NEVER training inputs that would close the deception-amplifier loop). Methodology caveat inherited from the source brief: ArXiv IDs from training-data recall (cutoff January 2026); verification required before external publication. ~14,000 lines total across the corpus. Companion Legatus AI-side ADRs (ADR-016 Legatus AI Peer Registration, ADR-017 Artifact + Requirement Metadata Extensions, forward-reference ADR-018 PASETO capability tokens) live on a sibling branch in legatus-ai; not merged per user direction.

- **SLA breach detection engine (SEN-12)** (2026-05-15). New `crates/sentinel-application/src/sla.rs` module exposing the pure matching + aging logic for configurable service-level-agreement breach detection. `SlaConfig` loaded from `~/.claude/sentinel/config/slas.toml` carries a list of `SlaRule { name, target_minutes, subject_kind, require_priority?, require_state[], require_labels[], require_no_reviews }` filters. `Subject { kind, id, created_at, state?, priority?, labels[], reviews? }` is the flat record produced by the (follow-up) Linear / GitHub / deploy-stream adapters and consumed by `check_rules(config, subjects, now) -> Vec<BreachRecord>`. A breach fires when every `require_*` filter matches AND the subject's age (now − `created_at`) exceeds `target_minutes`; the resulting `BreachRecord { sla, subject_id, subject_kind, detected_at, actual_minutes, target_minutes, overdue_by_minutes }` gets appended to `~/.claude/sentinel/metrics/sla-breaches.jsonl`. `aggregate_at(breaches_path, summary_path, now)` rolls up 24h/7d/30d breach counts per SLA, captures the most-recent timestamp in the 30d window, and writes `sla-breaches-summary.json`. New CLI: `sentinel sla check --config <path> --subjects <jsonl> [--dry-run]` runs the matcher and (unless `--dry-run`) appends to the breach JSONL with colored ⚠ output; `sentinel sla aggregate` writes the rolling summary; `sentinel sla template` prints a starter slas.toml covering the 7 default SLAs from the spec (P0 ticket pickup, P0 resolution, PR first review, PR merge after approval, CI green to deploy, QA dwell, high-priority backlog wait). The template ships embedded via `include_str!` in `crates/sentinel-cli/templates/slas.example.toml`. Follow-ups (out of scope here): live Linear/GitHub state polling that produces the `Subject` JSONL, cron wiring, push notifications via `channel_events`, and auto-comment-back-to-ticket on breach. 19 unit tests pin the parser (TOML → SlaConfig with multiple rules, empty TOML, missing file → empty config), the matcher (breach on aging, no-breach when young, wrong kind/state/priority/label/reviews skipped, future-dated subject skipped, unparseable timestamp skipped, `require_no_reviews` treats missing reviews as 0, multiple rules firing on the same subject, label AND semantics), the persistence layer (append+read round-trip, malformed line skipping), and the aggregator (24h/7d/30d window cutoffs, empty input, most-recent picked from the 30d window). Sentinel-application: 988 tests passing (969 prior + 19 new). Clippy --all-targets clean on the new code.

- **Deploy frequency tracker (SEN-9, DORA core metric #2)** (2026-05-15). New `crates/sentinel-application/src/deploy_freq.rs` module with the data model + aggregation seam for tracking deploys per repo/env. `DeployRecord { timestamp, repo, env, commit, duration_s }` is appended to `~/.claude/sentinel/metrics/deploys.jsonl` via `append_deploy()` — append-only, atomic per line, parent dir created on demand. `aggregate_at(deploys_path, summary_path, now)` (production wrapper `aggregate()` uses `Utc::now()`) reads the JSONL, buckets into rolling 7d/30d windows per (repo, env), and writes `deploys-summary.json` with `RepoEnvAggregate { deploys_7d, deploys_30d, rate_per_day_7d, rate_per_day_30d, tier, first_in_window, last_in_window }` plus a 60-day `DailyPoint` sparkline series. `DoraTier::from_rate_per_day(rate)` classifies on the 30-day rate (avoids bursty-week flips): Elite (≥1/day), High (≥1/week), Medium (≥1/month), Low (<1/month). Malformed JSONL lines are logged via `tracing::warn` and skipped — a single corrupt entry can't poison the whole aggregation. New CLI: `sentinel deploy-freq aggregate` (read + summarize + print, sorted by 30d rate desc with colored tier badges) and `sentinel deploy-freq record --repo X --env Y --commit Z [--duration-s N] [--timestamp ...]` (manual entry for testing + the backfill path before Hookdeck `deployment.success` ingest lands). 16 unit tests cover the 4 DORA tier boundaries (Elite/High/Medium/Low and the exact-boundary case), append+read round-trip with multiple lines, append creates parent dir, read skips malformed lines, read returns empty on nonexistent file, aggregation groups by (repo, env), excludes records outside 30d, writes summary JSON, classifies tier on the 30d rate, handles empty JSONL, emits daily points within the 60d window, and skips unparseable timestamps. Webhook ingest from Hookdeck (Vercel + Railway `deployment.success`) and Vercel/Railway API backfill remain follow-ups; this PR ships the data model + aggregation + CLI seam so those follow-ups can plug in via the existing `append_deploy()` interface. Sentinel-application: 969 tests passing (953 prior + 16 new). Clippy --all-targets clean on the new code.

- **Context propagation through StepProofs (sentinel #58 / M7.9)** (2026-05-14). `submit_step_complete` now inherits `account_context` forward through a skill's chain when the field is omitted from the call. Three explicit cases: (1) **key absent** → inherit from the most recent `StepProof` in this skill's chain (phase-only entries skipped — they don't carry the account dimension); (2) **key present, null** → explicit clear, no inheritance; (3) **key present, string** → use that value verbatim. Same semantics as HTTP request headers: missing header inherits from upstream, explicitly empty header clears it. Production benefit: callers driving a multi-step pipeline (linear's claim → fetch → intelligence → worktree → review → qa-handoff sequence) only need to set `account_context` once, on the claim step — every downstream step inherits the same tenant scope without each call re-specifying it. Override is always available by passing the field explicitly. Audit-trail benefit: when a chain crosses tenant boundaries (rare but possible during admin operations), the override shows up in the sealed proof as the explicit attribution. 4 new tests covering inherited-after-explicit, explicit-override-beats-inheritance, explicit-null-clears, and no-prior-chain-stays-none cases. Sentinel-application: 953 tests passing.

- **`sentinel policy suggest` — plain-English → TOML (sentinel #59 / M7.10 — AEGIS pattern)** (2026-05-14). New CLI subcommand: `sentinel policy suggest "<policy>"` parses a one-line policy statement and emits a ready-to-paste `[[step_verifiers]]` TOML fragment. Grammar (deliberately small, rule-based, no LLM in this slice): `<skill>/<phase>/<step> requires <adapter> [verified|provenance]`. Mode defaults to `verified` (strict production mode where smoke-test FAILED receipts must NOT satisfy the gate); `provenance` is the audit-only mode where the act of producing the receipt is what's witnessed. Output includes header comments citing the source policy AND a `# Mode: ...` line explaining the trust model, so future readers can trace any active verifier back to the policy that introduced it. Emitted TOML round-trips through `toml::from_str` as a `Vec<StepVerifierRequirement>` — pasting it directly into a sentinel config works. The CLI is the deterministic floor; a future LLM-assisted skill can interpret fuzzier prompts (e.g. "make sure all QA-handoff steps need Browserbase") and pipe through this same parser, so the safety-net validation lives in one place. 14 tests covering parser happy paths (verified default, explicit verified, provenance, case-insensitive keywords), error paths (empty input, missing parts, missing 'requires', unknown mode, trailing garbage, illegal chars), and TOML-fragment round-trip via the toml crate. Sentinel-cli: 134 tests passing.

- **Sandboxed judge wrapper (sentinel #60 / M7.11 — EQTY Lab principle)** (2026-05-14). New `sentinel_application::sandboxed_judge::SandboxedJudge` wraps any `JudgeService` impl with two protections against misbehaving inner judges: (1) **wall-clock timeout** (default 60s, configurable per-instance via `with_timeout`) — a wedged provider or hanging round-trip produces a synthetic `SANDBOX:`-prefixed verdict instead of hanging sentinel; (2) **panic isolation** — the inner call is dispatched via `tokio::spawn` so a panic in the judge becomes a `JoinError` at the wrapper layer, converted into a `PANIC:`-prefixed verdict so callers see something happened. Anyhow errors (configuration failures like "no API key") still propagate as `Err` — the sandbox catches HANGS and PANICS, not legitimate config errors. Three trait methods are individually wrapped: `evaluate`, `evaluate_step`, and `evaluate_multi`. For `evaluate_multi`, a sandbox timeout or panic synthesizes a single-run `MultiJudgeVerdict` tagged with `provider: Some("sandbox")` so consumers preserve the typed shape downstream. Documents what the sandbox does NOT enforce (memory caps, network egress, CPU caps — those need real subprocess isolation, deferred to future work). 6 unit tests covering happy-path pass-through, timeout on slow inner judge, panic isolation, timeout on `evaluate_step`, timeout on `evaluate_multi`, and config-error propagation. Sentinel-application: 949 tests passing.

- **Pluggable judge backends — `evaluate_multi` on JudgeService (sentinel #69)** (2026-05-14). New default method on the `JudgeService` trait: `evaluate_multi(skill, phase_id, phase_objectives, evidence, tier) -> Result<MultiJudgeVerdict>`. Dispatches the same evidence to every judge model in the configured `JudgeTrustTier`, collects per-judge `JudgeRun`s with model + verdict + cost + provider attribution, then synthesizes the cross-vendor result via the existing `MultiJudgeVerdict::synthesize`. The synthesized verdict surfaces (a) sufficient (conservative AND across all judges — any FAIL flips the multi-verdict to false), (b) confidence (min across runs), and (c) **disagreement flag** (true when judges disagree). Per-judge errors do NOT propagate as `Err` — they become a `sufficient=false` JudgeRun with `ERROR:`-prefixed reasoning, so one flaky vendor can't mask the others' verdicts (the whole point of cross-vendor verification). Default tier (`Review` = Kimi K2.6 alone, OSS-first) costs no more than the existing single-judge path; `Critical` (Kimi + Sonnet) and `CriticalStrict` (Kimi + Sonnet + Opus) layer additional judges for production-blocking decisions where missed defects cost more than the extra inference. Default impl is sequential — `MultiModelJudge` and other implementers can override to parallelize when the cost/latency tradeoff favors it. 5 new unit tests covering Review-tier single-model dispatch, Critical-tier multi-model dispatch with order preservation, disagreement synthesis on split votes, per-judge error tolerance (the failure surfaces as an ERROR run but doesn't abort the call), and FallbackJudge back-compat. Sentinel-application: 943 tests passing.

- **Browserbase as federated skill subgraph (sentinel #72)** (2026-05-14). New `config/steps/browserbase.toml` makes Browserbase a first-class member of sentinel's skill federation. Five phases × 12 steps wrap the full Steel session lifecycle: `session` (create + configure), `interact` (navigate, inject Doppler-personal-branch credentials, run interactions), `observe` (screenshots, console-error inspection, recording URL capture), `verify` (submit a Browserbase evidence claim through sentinel — adapter seals receipt with provenance binding per #70/#71), `cleanup` (release the session). Each step's `suggested_tools` references the actual MCP tools (`mcp__browserbase__create_session`, `mcp__browserbase__navigate`, `mcp__browserbase__screenshot`, `mcp__sentinel__submit_step_complete`, etc.), so any other skill that needs UI verification can reference these steps via federation directives. The `verify/4.1` step closes the loop with the BIBLE adapter (#70) + step_verifier gate (#71) — downstream skills like linear's qa-handoff that demand a verified Browserbase receipt get a clean federation contract. Federation compose now reports 48 skills (was 47) × 203 phases × 949 steps, composes cleanly with zero validation errors. Browserbase is now a runtime-agnostic federation peer — any MCP client (Claude Code, Cursor, custom Python harness) can route UI steps through it via the standard skill-step dispatch.

- **Browserbase as third-party verifier — `step_verifier` gate (sentinel #71)** (2026-05-14). New `sentinel_domain::step_verifier` module + wireup in `McpHandler`. `StepVerifierRequirement { skill, phase_id, step_id, adapter_name, verified_only }` declares "step at these coordinates cannot seal unless its evidence carries a receipt from the named adapter." `McpHandler::with_step_verifiers(Vec<...>)` registers the set at bootstrap; `submit_step_complete` checks every matching requirement AFTER the BIBLE wireup folds any receipt in (so the verifier sees the fresh receipt). Failures block the seal with a clear error naming the adapter and the specific failure mode (missing receipt, wrong adapter, unverified result, malformed receipt). Two construction modes: `new` (default `verified_only=true` — production case where smoke-test FAILED receipts must NOT satisfy the gate) and `provenance_only` (audit-only — the act of producing the receipt is what's witnessed, verification verdict ignored). This realizes the "Browserbase as third-party verifier" pattern: register a `StepVerifierRequirement` at the `qa-handoff/3.5.5` step requiring a `browserbase` adapter receipt, and the proof chain will refuse to seal the implementation-comment step unless the Browserbase smoke test passed (or, in provenance-only mode, was at least run). Two layers of defense alongside `step_gate`: step_gate blocks the *attempt* to fire downstream tools at PreToolUse; step_verifier blocks the *seal* of the next step's proof at submit_step_complete time — a misconfigured client that bypasses step_gate still can't bypass step_verifier. 9 unit tests on the domain module (coordinate matching, verified/provenance-only paths, missing/wrong-adapter/unverified/malformed-receipt rejections, Display strings) + 4 new mcp_handler integration tests (no-match no-impact back-compat, matching verifier+BIBLE-folded receipt round-trip, blocking on missing receipt with the "absent" + adapter-name error message, blocking on `verified=false` receipt with the "verified=true" + adapter-name error message). Sentinel-domain: 390 tests passing. Sentinel-application: 938 tests passing.

- **Browserbase evidence adapter (sentinel #70)** (2026-05-14). Second concrete adapter for THE BIBLE framework (filesystem was first per #38). New `sentinel_infrastructure::evidence_browserbase::BrowserbaseAdapter` implements `EvidenceAdapter` for `browserbase.<verb>` claims. Two claim types supported: (1) **`browserbase.session_observed`** — caller swears they ran *a* Browserbase session; receipt seals `verified: true` iff `context.session_id` is a non-empty string; permissive on purpose since the act of running a session is the witnessed fact; payload preserves `screenshot_count`, `console_errors`, `recording_url`, `artifacts` array. (2) **`browserbase.smoke_test_passed`** — caller asserts the smoke test passed cleanly; receipt seals `verified: true` iff all three predicates hold (`session_id` present, `screenshot_count >= 1`, `console_errors == 0`); failures still seal but with `verified: false` and a `predicates` breakdown object showing exactly which check failed (corpus queries on `verified == false` surface these as follow-up). Design choice: **metadata-shape adapter, not HTTP-calling adapter** — the full "phone home to Browserbase API and re-fetch the session" path would require `BROWSERBASE_API_KEY` + reqwest in sentinel-infrastructure for one adapter, and would conflict with the [Doppler-personal-branch convention](#90–#102) pushing credentials OUT of sentinel onto each MCP server. Instead, the Browserbase MCP server collects the artifacts (it already has the credentials) and this adapter validates them + seals receipts with provenance binding. Future #69 work can layer a `browserbase_remote` adapter that DOES phone home, for cross-vendor verification. 10 unit tests covering claim-type matching, missing/empty session_id rejection, full happy-path round-trip, predicate-breakdown for partial failures, and provenance-verification round-trip on the sealed receipt. Sentinel-infrastructure: 95 tests passing.

- **THE BIBLE: evidence-adapter wireup in `submit_step_complete` (sentinel #68)** (2026-05-14). The pure-domain evidence-adapter framework (`sentinel_domain::evidence_adapter`) + application-layer registry (`sentinel_application::evidence_adapters`) + first filesystem adapter shipped earlier; this commit wires the registry into the actual proof-sealing path. `McpHandler` gains an optional `evidence_adapters` field configured via new builder method `with_evidence_adapters(Arc<EvidenceAdapterRegistry>)`. `sentinel__submit_step_complete` gains an optional `evidence_claim` arg with shape `{skill, phase_id, step_id, claim_type, context}`. When supplied alongside a wired registry, the registry fetches an `EvidenceReceipt`, the provenance hash is verified against the claim, and the receipt is folded into the sealed proof's `Evidence.custom.evidence_receipt` (single canonical key, lookup is O(1) for verifiers). When supplied without a registry wired, the call FAILS LOUDLY rather than silently dropping the claim — the whole point of THE BIBLE is "we know we don't know" being a first-class chain entry, not a missing-data ambiguity. When no claim is supplied, behavior is unchanged from M4.2 (every existing caller stays back-compat). Three new tests cover (1) no-claim-no-registry back-compat, (2) claim-with-self-attested-fallback-registry round-trip producing a receipt with `verified=false` in the chain, and (3) claim-without-registry fail-loudly path. Sentinel-application: 934 tests passing. With this wireup, "every step claim requires external evidence" is now opt-in via per-call arg AND an opt-out fallback receipt is also a first-class chain entry — the framework can extend to GitHub / Browserbase / Doppler adapters one at a time without further wiring changes.

- **M2.13 signed step-config manifests with Ed25519 (sentinel #26)** (2026-05-14). New `sentinel-domain::step_manifest` module + `sentinel manifest` CLI subcommand. Each step-config TOML under `<config_dir>/steps/` can be paired with a `manifest.toml` recording the canonical SHA-256 of every step file plus an optional Ed25519 signature over that hash. Producers (`sentinel manifest write [--key-env VAR]`) canonicalize each source (strip BOM, normalize CRLF→LF, trim trailing whitespace per line, strip trailing blank lines, ensure one trailing newline) and write the manifest. Consumers (`sentinel manifest verify [--pubkey HEX] [--strict|--hash-only]`) re-canonicalize, re-hash, and check entries; strict mode also verifies signatures against the manifest's recorded `public_key` header (or an explicit `--pubkey` override). Three trust levels: (1) **hash-only mode** — bit-rot protection without authenticated trust, manifest reviewable in PRs; (2) **signed with key in manifest header** — the typical published case where the public key is delivered with the manifest (still trusts the channel that carried it); (3) **signed with pinned key** — production case where consumers pin the verifying key out-of-band (binary constant, TUF root). `sentinel manifest show` prints a summary (entry count, signed-vs-unsigned, public key fingerprint, per-entry hash prefix). 16 new unit tests on the domain module (canonicalization round-trips, hash stability across whitespace variants, signed/unsigned upsert, wrong-key rejection, hash-only fallback for signed entries, serde TOML round-trip, missing/drifted entry detection) + 7 new CLI integration tests (write/verify round-trip, drift detection, signing-key env-var validation, full sign-then-verify-strict round-trip). Sentinel-domain: 381 tests passing. Sentinel-cli: 120 tests passing.

- **M5.3 race-condition + batch-ops stress tests for the proof chain (sentinel #44)** (2026-05-14). Closes out the M5 epic. Three new `#[tokio::test]`s in `crates/sentinel-application/src/mcp_handler.rs` stress the proof-chain engine under concurrent and deep-sequential load: `m5_3_concurrent_different_skills_seals_all_chains` fires 50 concurrent `submit_step_complete` calls through one shared handler, each for a distinct skill (`stress_skill_000`..`stress_skill_049`), and asserts every submission succeeds, every skill ends up with exactly one chain entry, and no chain is silently lost — guards against the engine's `RwLock` lock-ordering bugs (lock-before-validate or release-between-read-and-write that drops writes). `m5_3_concurrent_same_skill_does_not_fork_chain` fires 30 concurrent submissions to the SAME skill (`linear`); the engine must serialize so the final chain has exactly 30 entries with unique `combined_hash`es and a strictly linear prev_hash chain (`previous_hash` of step N must equal `combined_hash` of step N-1). If two siblings shared a `previous_hash`, the chain forked — the test catches that. `m5_3_deep_sequential_batch_stays_linear` runs 100 sequential submissions to one skill and re-verifies the prev_hash chain integrity at depth, also asserts the final entry's `artifact.index` is `depth - 1` so we'd catch silent reordering or dropped entries. All three pass on first run. Sentinel-application: 931 tests passing, 0 failures. **M5 epic (#41) is now complete — Backlog → Completed lifecycle covered end-to-end + concurrency-stressed.**

- **M5.2 in-process E2E test for Code Review → QA Testing → Completed (sentinel #43)** (2026-05-14). Two new `#[tokio::test]`s in `crates/sentinel-application/src/mcp_handler.rs` cover the back half of the linear-skill pipeline (M2.3.G qa-handoff + M2.3.H cleanup + the QA-tester pass). `m5_2_code_review_to_completed_seals_qa_handoff_and_cleanup` pre-seeds the chain with a `review/3.L3` "Code Review" entry (where M5.1 ends), then drives 9 back-half steps (review CI+merge, deploy, Steel smoke, Loom upload, Linear QA-Testing transition, QA reassign, implementation comment, worktree cleanup) followed by a final QA-pass step transitioning Linear to "Completed". Asserts: 11 total sealed entries; consecutive step hashes always differ (catches the engine forgetting to fold prev_hash); the final entry lands at `qa-handoff/3.5.6` with `new_state: "Completed"`; every step's artifact carries the same `issue_id` forward so the chain alone reconstructs the full life of FPCRM-200. `m5_2_qa_fail_keeps_ticket_at_qa_testing` covers the honest-recording counterpart: drives to QA Testing, then a QA-fail step lands with `new_state: "QA Failed"` (not Completed) and a `rejection_reason` field — the proof chain can't lie about what happened. Composed with M5.1, the full Backlog → Completed lifecycle is now smoke-tested end-to-end in-process. Sentinel-application: 928 tests passing, 0 failures.

- **M5.1 in-process E2E pipeline test for Linear skill (sentinel #42)** (2026-05-14). Two new `#[tokio::test]`s in `crates/sentinel-application/src/mcp_handler.rs` exercise the `submit_step_complete` path Claude takes in real linear-skill execution: `m5_1_backlog_to_code_review_pipeline_seals_chain_in_order` drives a full Backlog → Code Review journey through 7 sequential steps spanning all 5 lead-up phases (claim → fetch → intelligence → worktree → review), asserting each step seals into the chain, the phase-and-step sequence matches submission order, every sealed step has a distinct `combined_hash` (guards against the engine forgetting to fold the previous entry's hash into the next), and the final entry lands at `review/3.L3` with `linear_state: "Code Review"` in its artifact. `m5_1_insufficient_verdict_halts_pipeline_midflight` proves the gate holds: two good steps land in the chain, an `insufficient` verdict on the third does NOT extend the chain, and the same step resubmitted with sufficient evidence does. Uses the existing `StubJudge` + `ProofEngine` + `McpHandler` triple from M4.2's test bed — no new infrastructure. Deliberately in-process per the M5 runbook decision: the proof chain logic and the MCP API surface are what's covered here; a real-Linear E2E harness would require credentials and someone to babysit it, and the manual recipe at `docs/m5-linear-e2e-runbook.md` already exists for that case. Full sentinel-application suite: 926 tests passing, 0 failures.

### Changed
- **`hooks::stop_failure::rotate_accounts` calls canonical `claude-accounts` binary** (2026-05-14). The CLI was renamed from `accounts` to `claude-accounts` (per `claude-accounts-cli-rust` Cargo.toml `[[bin]] name`). A shadow legacy `accounts.exe` lingered in `~/.cargo/bin` for a while but drifts — any `cargo install` of the renamed CLI only refreshes the canonical name, so the shadow gets stale and `stop_failure`'s rotate path was potentially hitting an out-of-date binary. Now calls `claude-accounts` directly.

- **CLAUDE.md generator includes time-of-day + timezone abbreviation** (2026-05-13, sentinel 0.4.1). The auto-generated `~/.claude/CLAUDE.md` Date Context section now reads `Today is Wednesday, May 13, 2026 at 08:58 AM CDT.` (previously only the date). The assistant was greeting Gary with "Good evening" at 8:55 AM because nothing in the LLM's context disambiguated AM/PM. Switched `chrono::Utc::now()` → `chrono::Local::now()` (also fixes a latent near-midnight off-by-one), formats time as zero-padded 12-hour with AM/PM (`%I:%M %p`), and resolves a DST-aware IANA abbreviation via `iana-time-zone` → `chrono-tz` (chrono's `%Z` on Windows returns "Central Standard Time", not "CDT"). Helper `local_tz_abbreviation()` falls back to `%Z` then `"UTC"` if the IANA lookup fails. Footer `*Auto-generated on session start: …*` also gets the same time + tz. Two new workspace deps: `chrono-tz = "0.10"` and `iana-time-zone = "0.1"`.

### Removed
- **All ntfy push code** (2026-05-09, sentinel 0.4.1). Deleted `sentinel-application::ntfy_push` module and all 11 callsites across `hooks/stop_failure.rs` (rate-limit recovery, auth recovery, generic turn-aborted), `hooks/build_notify.rs` (build/deploy notifications), and `sentinel-cli/src/hook_cmd.rs` (hook-level block notifications). Sentinel emits zero ntfy pushes after this. Account-failure notifications moved into `claude-code-handler-rust` so they have direct access to slot/trace/session forensics that sentinel's hook-level vantage point doesn't have. The previous "every event sends a push" volume was unhelpful — too noisy to act on, too generic to debug. Cleaned up now-orphaned helpers `project_name`, `first_error_line`, `truncate_for_push` and the 3 tests that referenced them. 917 tests still pass.

### Added
- **`sentinel project handover` and `sentinel project lesson` CLI subcommands (M9.3, #67)** (2026-05-14). Two new explicit-trigger subcommands under `sentinel project`, completing the M9 family. `handover --title "..." [--summary "..."] [--dir <path>]` writes a Markdown stub to `<repo>/.sentinel/handovers/YYYY-MM-DD-<slug>.md` with the README's suggested sections (Context, What got done, What's still open, Gotchas for next session, Pointers); `--summary` pre-fills the Context section. `lesson --title "..." [--summary "..."] [--tag X --tag Y]` writes a structured JSON to `<repo>/.sentinel/lessons/L-<NNN>.json` with the next monotonic ID — scans existing `L-<NNN>.json` files for the max, increments by 1, respecting the "mark resolved, don't delete" convention so retired IDs stay occupied. Both walk up from cwd looking for a `.git` entry (file OR directory — worktrees work) and error clearly on missing `.git`, missing `.sentinel/{handovers,lessons}/`, or empty `--title`. Handover filenames append `-2`/`-3`/... when two handovers share a slug on the same day. Lesson JSON is rendered via `serde_json::to_string_pretty` against an ordered Map so field order matches the README and special characters are safely escaped. 14 new unit tests bring `project_cmd`'s count to 20 (slugify edge cases, non-colliding handover paths, full handover write with date verification, summary-fills-context, error paths, next_lesson_id semantics, JSON shape verification, monotonic-after-existing). cargo check --tests clean (30s); cargo test execution blocked by a transient local MSVC link.exe lookup issue but the test code itself compiles. Closes M9.3 (#67) and the full M9 family (epic #64 / scaffold #65 / plans #66 / handovers + lessons #67).

- **`plan_organizer` hook also writes to `<repo>/.sentinel/plans/` (M9.2, #66)** (2026-05-14). Extends the existing ExitPlanMode → archive flow with a second write destination: when the repo has been initialized via `sentinel project init` (`<repo>/.sentinel/plans/` exists), plans land there too with their own independent version sequence. Failures in the repo-local step are silent — the global `~/.claude/plans/<project>/<slug>-v<N>.md` archive remains the authoritative copy. Two new helpers: `find_repo_root` walks up from cwd looking for a `.git` entry (file OR directory, so worktrees work), `try_write_repo_local_archive` returns `Some(path)` when the write succeeds, `None` when the repo isn't initialized or anything fails. The repo-local copy is OPT-IN — sentinel doesn't auto-create `.sentinel/plans/`; that's `sentinel project init`'s job. The injected context now mentions the repo-local path when it landed, and the channel event metadata includes `repo_local_archived_path` for downstream consumers. 7 new tests bring `plan_organizer`'s count to 15/15 passing (find_repo_root walks-up + accepts-git-file + outside-repo, try_write_repo_local writes-when-dir-exists + skips-when-dir-missing + skips-when-not-in-repo + versions-independently).

- **`sentinel project init` CLI subcommand — `.sentinel/` repo-local scaffold (M9.1, #65)** (2026-05-14). New `Project { Init { ... } }` subcommand wired through `project_cmd.rs` (~470 lines). Creates `.sentinel/` inside the target repo (default: current directory) with five subdirectories (`tickets/`, `plans/`, `handovers/`, `lessons/`, `proof-chains/`), a root `README.md` documenting the convention + the global-vs-repo-local split, a `config.json` per-repo config stub, and one `README.md` per subdirectory documenting its schema and naming conventions. Idempotent: existing files are preserved unless `--force` is passed; missing files are filled in. `--dry-run` previews without touching the filesystem. Use cases: `tickets/` holds local-first JSON tickets (`T-001.json`) that can be Linear-mirrored or stand alone for repos without Linear; `plans/` holds `/plan` outputs that should live with the code (different from the per-machine archive at `~/.claude/plans/`); `handovers/` holds per-session summaries; `lessons/` holds project-specific lessons learned (`L-NNN.json` with resolved/still-valid markers); `proof-chains/` holds chains worth committing alongside the code (audit-grade trust tier, high-risk changes, explicit author opt-in). 6 unit tests cover scaffold creation, idempotency, `--force` semantics, `--dry-run` semantics, `config.json` parsing as valid JSON, and partial-state re-runs. M9.2 (plans-with-code automation, #66) and M9.3 (handovers automation, #67) build on this scaffold.

- **`config/steps/linear/templates/` — pre-baked virtual skill pack templates (M2.14, #27)** (2026-05-14). New convention parallel to `references/`: each TOML in `templates/` is a multi-step recipe the router (M7.3, pending) can offer as a quick-start instead of replanning from scratch. Schema: `[template]` header with `id` (matches filename), `name`, `description`, `triggers` (prompts the router matches against), `estimate_minutes`, `catalog`, then ordered `[[template.steps]]` entries with `ref = "<skill>.<phase>.<step>"`. Two worked examples on the linear skill: `claim-and-pr.toml` (44 steps — claim → fetch → intelligence → worktree → review L0-L3, stops at draft PR open; the most common multi-step recipe), `quick-comment.toml` (4 steps — read issue + ack prior comments + post status comment, no state change; smallest viable template proving the schema handles short packs as cleanly as long ones). README documents when to add a template (frequent reuse, clean stop point, stable step list) vs. when not to (cross-skill, branching). No router consumption yet — templates are documentation-only until M7.3 lands; until then they're useful as "canonical recipe for X" references for humans.

- **`config/steps/linear/references/` — progressive-disclosure docs pattern (M2.10, #23)** (2026-05-14). Apollo Skills' progressive-disclosure split: the TOML (`config/steps/linear.toml`) keeps the schema-bearing data (step IDs, descriptions, `suggested_tools`, `artifact_schema`, federation directives, judge config). Long-form prose — rationale, edge cases, AI-judge rubrics, walkthroughs — moves into `config/steps/<skill>/references/<phase>.md`. The TOML stays small enough for `build.rs` codegen and tool-schema rendering; the references load on demand for humans and (later) for judges that need rubric context. Worked example for linear: `references/README.md` documents the convention + when-to-use-which split, `claim-phase.md` covers the 8 steps from -0.1 through 0.4 with sub-phase guard rails / claim execution / edge cases / judge rubric, `review-phase.md` covers the 22-step 6-layer review pipeline (L0 tests, L1 lens agent, L2 Codex, L3 PR, L4 CI+CodeRabbit, L5 final gate) including circuit-breaker semantics, CodeRabbit triage, and cross-vendor disagreement handling. No schema integration yet — references are discoverable by path convention (`<phase-id>-phase.md`). Adding an optional `references = [...]` field to `WorkflowStep` is the natural follow-up; until then the convention is enforced by review, not by validator. Other skills can adopt the same layout incrementally.

- **Competitive-intel framework + first-pass snapshot — `docs/competitive-intelligence.md` (#80)** (2026-05-14). Active doc tracking the five named players in sentinel's category (Apollo MCP Server, IBM ContextForge, Microsoft Agent Governance Toolkit, EQTY Lab, AEGIS policy library) plus an "other/new entrants" bucket. Each row carries seven fields: latest release, architecture posture, cryptographic story, policy/governance story, multi-vendor story, threat to sentinel (none/wide-moat/direct-overlap/leapfrog), and action (none/file Linear ticket/file design note). First-pass snapshot is filled today, ahead of the formal Q3 review (target 2026-09-30); each quarterly review replaces *Current state* in place with prior versions pushed to *History*. Includes a sentinel-positioning table — every primitive sentinel ships vs the closest competitor that ships it — making the novel-to-sentinel column (per-developer Doppler personal-branch identity) legible to buyers. Includes the review process template so future quarters don't have to rebuild the framework.

- **M5 linear-skill E2E manual verification runbook — `docs/m5-linear-e2e-runbook.md` (#104)** (2026-05-14). Active manual recipe that walks a developer (or future session) through verifying the linear-skill pipeline end-to-end against real firefly-pro Linear and real GitHub, ahead of automated M5.1/M5.2/M5.3 (#42/#43/#44) shipping. Three stages: Backlog → Code Review (claim phase + worktree + PR creation with `Ref FPCRM-XXX`, deliberately NOT `Fixes` to avoid Linear's auto-Done bypass), Code Review → QA Testing → Completed (PR merge triggers `linear-on-merge.yml`, auto-transitions issue + reassigns to QA tester), race-condition stress test (concurrent batch_create_issues + concurrent claims). Plus identity-chain sanity check (verify `mcp__linear__viewer` returns gary@fireflypro.com from Doppler personal branch), schema pre-flight (run #103's validator), and StepProof chain assembly steps. Covers prerequisites (DOPPLER_TOKEN_FIREFLY_PRO + DOPPLER_PERSONAL_BRANCH env vars, personal-branch Linear key population in Doppler UI), per-stage verification checklists, and a 5-minute "is the foundation working" quick check. Explicitly NOT a substitute for the automated tests — those need cross-crate test scaffolding tracked under #42/#43/#44.

- **Step-config schema validator — `schema_validator.rs` (M5.0, #103)** (2026-05-14). New module under `sentinel-cli/src/` that catches typo-class bugs in `config/steps/*.toml` enrichment data before any E2E run hits real Linear/GitHub. Two checks: (1) **Forward-reference dangling** — every `(from <phase>/<step>)` substring in a step's `artifact_schema` must resolve to a real `(phase_id, step_id)` pair in the same file; catches typos like `(from fetch/1.10)` instead of `1.1`. Walks `artifact_schema` recursively so the check works on arbitrary inline-table shapes. (2) **Suggested-tool name shape** — `suggested_tools` entries must match `mcp__<server>__<tool>` (lowercase server/tool with `_` separator, hyphens allowed in server names like `claude-accounts`), `Bash:<cmd>`, or an internal Claude tool name (`EnterPlanMode`, `EnterWorktree`, etc); flags camelCase typos (`mcp__linear__GetIssue`) and old single-underscore conventions. 14 unit tests cover forward-ref parsing (including negative step IDs like `-0.1`, multi-ref strings, annotated refs), MCP-name validation accept/reject, well-formed/suspicious classification, and end-to-end synthetic-TOML cases. A 15th test (`production_step_configs_are_clean`, `#[ignore]`d) runs the validator against the real `config/steps/` directory — currently passes for all 47 shipped configs, including the 91 enriched linear.toml steps. The validator is **report-only** today; promoting failures into `federation compose`'s blocking gate is a follow-up — let the test bake first. Re-parses the TOML with its own struct (rather than extending `sentinel-domain::WorkflowStep`) because `artifact_schema` and `suggested_tools` are skills-mcp build.rs codegen fields that the canonical sentinel loader silently drops; keeping the validator self-contained avoids rippling through `federation_cmd`'s 1200+ lines.

- **`sentinel cleanup tasks` — orphan session task dirs pruner (#78)** (2026-05-14). New subcommand under `sentinel cleanup` that prunes orphan directories under `~/.claude/tasks/`. A session task directory is orphan when (a) its name is a UUID matching no `<id>.jsonl` transcript anywhere under `~/.claude/projects/*/`, AND (b) its mtime is older than the cutoff (default 30 days, configurable via `--older-than N`). The age gate defends against active sessions whose transcript hasn't been written to disk yet — younger orphans are kept as "younger-than-cutoff" and reported but not removed. Non-UUID directory names (e.g. `.lock`, junk dirs) are reported as "not-a-session" and never auto-removed. Default mode is dry-run with a structured audit report (live / younger / not-a-session / orphan counts plus total orphan bytes); `--apply` performs the removal. Helper `format_size` renders B/KB/MB without pulling in a dep. 9 new unit tests cover the classifier (live, orphan, young, not-a-session), the live-session scanner, the apply path's selective removal, and the UUID heuristic — all 15 `cleanup_cmd` tests pass.

- **Linear skill — fully step-gated (M2.3, #17 closed)** (2026-05-14). All 91 steps in `config/steps/linear.toml` now carry `suggested_tools` (the specific peer-MCP tool names the caller should invoke, e.g. `mcp__linear__update_issue`, `mcp__browserbase__create_session`) and `artifact_schema` (typed handoff shape with forward refs naming the upstream step that produced each field, e.g. `issue_id = "string (from fetch/1.1)"`). 91/91 steps have `artifact_schema`; 78/91 have `suggested_tools` — the 13 without are intentional gate/synthesis steps where the work is reasoning, not tool invocation. Tool names cross-checked against the registered MCP servers (linear, github, browserbase, loom, agents, codex, sentinel, vercel, railway, doppler, sequential-thinking) — no phantom references. Verified end-to-end via `SKILLS_MCP_STEPS_DIR` override pointing skills-mcp-rust at this dir: `cargo build --release` succeeds, codegen reports "939 step tools across 47 skill(s)", generated body for `linear__claim__neg_0_1` contains the enriched suggested_tools array and artifact_schema JSON. Closes the M2.3 epic and the M2 epic gate from the M2.3 angle (remaining M2.10–M2.14 are follow-on Apollo-pattern enhancements, not core M2 functionality).

- **Multi-judge verdict types in `sentinel-domain` (#82 Stage B Part 1, follow-up to commit 2089896)**: new `crates/sentinel-domain/src/multi_judge.rs` module ships the wire-format types for parallel multi-judge verification — `JudgeTrustTier` (Routine / Review / Critical / CriticalStrict / AuditGrade), `JudgeRun` (one model + verdict + optional cost + optional resolved provider), and `MultiJudgeVerdict` synthesised across N runs with worst-case-wins logic. Mirrors the M3.3 review-types staging strategy: ship the types first so the wire format is locked, then the infrastructure-side producer (`rig_judge::evaluate_multi`) and the chain-entry consumer (`ProofEntry::Disagreement`) wire in next.
  - **Trust tier → judge models mapping**: `Routine → [Haiku]`, `Review (default) → [Kimi]`, `Critical → [Kimi, Sonnet]`, `CriticalStrict → [Kimi, Sonnet, Opus]`, `AuditGrade → [Kimi]` plus resolved-provider recording. Cross-distribution diversity is the load-bearing property — Eastern OSS Kimi + Western closed GPT/Opus produces real disagreement signal because the model families share fewer failure modes than 3-of-Western-closed.
  - **Synthesis rules (worst-case-wins)**: `sufficient` = `false` if ANY judge said `false`; `confidence` = floor (min) across all individuals (averaging would mask uncertainty); `disagreement` = `true` iff judges differ on `sufficient`. Drives the `ProofEntry::Disagreement` chain entry that lands in a follow-up commit.
  - **Cost aggregation**: `total_cost_usd` sums `JudgeRun.cost_usd` values. `None` when no individual reported a cost; `Some(0.0)` when reported but zero; partial-report case sums what exists (a single priced judge still produces a meaningful aggregate). Drives per-skill cost rollup on the report once OpenRouter cost-header capture lands in `rig_judge`.
  - **Audit-grade replay**: `JudgeTrustTier::AuditGrade.records_provider() == true` flags the producer to capture OpenRouter's resolved `model_id@provider` (e.g. `moonshotai/kimi-k2.6@parasail-fp8`) into `JudgeRun.provider`. EU AI Act Article 12 compliance: "prove which model judged this" over a 10-year retention window.
  - **Empty-runs degenerate case**: synthesizing zero runs returns `sufficient: true` (vacuous all() on empty iterator), `disagreement: false`, `confidence: 0.0` (we have no judges, we can't claim any confidence — the safe answer for what's a producer bug).
  - **Kebab-case wire format pinned**: `serde(rename_all = "kebab-case")` plus an explicit `tier_serde_kebab_case` test asserting `"routine"` / `"review"` / `"critical"` / `"critical-strict"` / `"audit-grade"`. Step config TOMLs declare `trust_tier = "critical-strict"`; pinning prevents a future serde rename from silently demoting configs.
  - 14 unit tests pin: tier default = Review, judge_models per tier, audit-grade-only provider recording, logs_disagreement only for multi-judge tiers, all-pass synthesis (sufficient + no disagreement + min confidence), one-fail flips overall (any Fail → Fail + disagreement), unanimous fail (no disagreement when everyone agrees), empty runs (safe default), tier preserved through synthesis (replay needs it), cost aggregation (full / none / partial-report), kebab-case wire format, full JSON round-trip.
  - **Out of scope (subsequent Stage B commits)**: `rig_judge::evaluate_multi` running judges in parallel via tokio::join_all + capturing OpenRouter cost headers; `ProofEntry::Disagreement` chain entry variant; step config `trust_tier` field threading through the engine. Today's commit ships the **types** so the wire format is locked. 1348 workspace tests green (923 application + 353 domain + 72 infra). Release build clean.

- **M4.8 fail-fast config validation (#26, ContextForge pattern)**: new `crates/sentinel-domain/src/config.rs` module ships `SentinelConfig` — the canonical top-level config struct that aggregates every existing sub-config (`RequestLimits`, `SsrfPolicy`, plus new `JudgeConfig` / `ProofArchiveConfig` / `SigningConfig`) and a `validate()` method that runs every sub-validator on startup, **collecting every error before returning** (Pydantic-style multi-error). The load-bearing property: misconfig that surfaces three days later when a hook fires for the first time is the failure mode this layer exists to prevent. Surface a complete punch list at startup, fix all of it in one round-trip with the operator, never bail on the first error.
  - `SentinelConfig::validate()` returns `Result<(), Vec<ConfigError>>`. Each error variant carries the offending value + the rule that was broken so a deny-banner can render an actionable line per error without parsing the enum. Display impls include the actual numbers (test pins `EvidenceLimitTooLarge { actual: 200, ceiling: 100 }` Display string contains both "200" and "100" — operators search the log for the value).
  - Validation rules shipped today: `EvidenceLimitTooLarge` (max_evidence_bytes ≤ 100 MiB), `ArtifactLimitTooLarge` (max_artifact_bytes ≤ 10 MiB), `RateLimitWindowZero` (window_seconds == 0 with non-zero max_calls_per_window is divide-by-zero in any sliding-window math; window_seconds == 0 AND max_calls_per_window == 0 is valid — rate limiting just disabled), `SsrfAllowlistInvalidHost` / `SsrfDenylistInvalidHost` (no whitespace or control characters; doesn't try to be a full DNS validator, just catches the obvious typos that produce silent drift), `ProofArchiveAgingTooLarge` (sanity ceiling 365 days; > 1 year is almost certainly a typo and the router-as-planner moat doesn't get measurably better with chains older than a quarter), `HardwareSigningRequiredButNoKeyEnv` (true + None is unreachable — there's no path to produce a signature).
  - New sub-configs: `JudgeConfig { default_tier: JudgeModel }` (defaults to `JudgeModel::default_review_tier()` — Kimi K2.6); `ProofArchiveConfig { aging_days: Option<u32> }` (None ⇒ keep everything); `SigningConfig { signing_key_env: Option<String>, hardware_required: bool }` (defaults to `Some("SENTINEL_SIGNING_KEY")` + software). All composable via `#[serde(default)]` so dropping a new sub-config in is a one-line change at the parent.
  - 15 new unit tests pin: default validates clean; each rule fires independently; `multiple_errors_collected_pydantic_style` (the core invariant — 4 distinct broken fields produce 4 errors, not 1); `rate_limit_disabled_with_zero_calls_validates` (window_seconds=0 AND max_calls=0 is valid); `proof_archive_aging_none_validates` (None is valid); `hardware_required_with_env_validates`; `config_error_display_includes_actual_values`; `serde_round_trip_preserves_all_fields`; `toml_round_trip_preserves_all_fields` (TOML is the production format — pin explicitly).
  - **What's NOT in this commit (follow-up surface):** env-var loading (e.g. `SENTINEL_ALLOW_PRIVATE=1`) is a fragile surface that touches each adapter and deserves its own commit so the env-var registry can be reviewed end-to-end. Per-component plumbing (migrating every component from "reads its own RequestLimits/SsrfPolicy" to "reads from a shared SentinelConfig") is a refactor that lands skill-by-skill. Today's deliverable is the **shape + the rules**; future commits plug existing components into this struct without changing the validation surface.
  - 1322 workspace tests green (923 application + 327 domain + 72 infra). Release build clean.

- **M4.7 SSRF guard — default-block private network ranges (#25, ContextForge pattern)**: pure-domain classification module at `crates/sentinel-domain/src/ssrf.rs`. Takes a hostname (or literal IP) + an `SsrfPolicy` and returns `Ok` or `SsrfError::{EmptyHost, DeniedHost, PrivateIp, Reserved}`. No I/O, no DNS resolution — callers extract the host from their URL (via `reqwest::Url::host_str` etc.) and pass it in. This keeps `sentinel-domain` free of HTTP/URL crate deps and lets every infrastructure adapter (skills-mcp, sentinel-mcp, evidence adapters from #38) gate their outbound calls through the same policy without duplicating the classification table.
  - **What's blocked by default** (IPv4): 10/8, 172.16/12, 192.168/16 (RFC1918), 127/8 (loopback), 169.254/16 (link-local — covers AWS/GCP/Azure metadata at 169.254.169.254), 0.0.0.0/8 (current-network/DNS-rebinding pivot), 100.64/10 (CGN/Tailscale ULA), 224.0/4 (multicast), 240.0/4 (reserved). (IPv6): ::1/128 (loopback), fc00::/7 (ULA — checked manually since `Ipv6Addr::is_unique_local` is unstable in Rust 1.83), fe80::/10 (link-local), ::/128 (unspecified), `::ffff:V.W.X.Y` (IPv4-mapped — re-classified by the embedded v4 so `::ffff:127.0.0.1` resolves as loopback).
  - **Default denylist by hostname**: `localhost`, `metadata.google.internal`, `metadata.aws.internal`, `metadata.gke.internal`, `metadata.azure.com`, `169.254.169.254`. These hard-block by name even if DNS misconfig would have resolved them to a public IP. Comparison is case-insensitive (DNS is).
  - **Escape hatches**: `SsrfPolicy.allow_private = true` flips the global default-deny (useful when running against a local-network environment where private addressing is expected). `SsrfPolicy.allowlist` adds specific hosts that bypass the private-IP check (finer-grained than the global flag). The denylist runs FIRST and unconditionally — even an allowlisted host can't bypass the denylist (`localhost` cannot be allowlist-overridden).
  - **What's NOT covered (yet)**: DNS rebinding. A hostname can resolve to a public IP at lookup-time and a private IP at request-time. Closing this hole requires resolving the host once, pinning the connection to that IP, and validating the IP — that's an infrastructure concern (custom resolver in the HTTP client) and lands as a follow-up. Today's guard catches the obvious cases (literal IPs and "localhost"), which is most of the real-world SSRF surface.
  - 23 new unit tests pin every category: empty/whitespace host, localhost via denylist, all 5 cloud-metadata hostnames, IPv4 loopback, RFC1918 (10/172.16/192.168), link-local (169.254 IP literals beyond the denylisted .169.254), 0.0.0.0/8, 100.64/10 CGN, 224.0/4 multicast, 240.0/4 reserved, IPv6 ::1, IPv6 brackets stripping (`[::1]`), fe80::/10, fc00::/7 (fc00::/fd00::/fdff::), 2001:db8:: (global IPv6 stays allowed), `::ffff:127.0.0.1` mapped, public IPv4 8.8.8.8/1.1.1.1 allowed, public IPv6 2001:4860::8888 allowed, public hostnames allowed (no resolution), allowlist bypasses private-IP check, allowlist does NOT bypass denylist (localhost stays blocked), `allow_private=true` relaxes global check but denylist still wins, denylist case-insensitive, `SsrfPolicy::default()` has the metadata hosts, serde round-trip preserves all three fields.
  - **No new dependencies** — uses `std::net::IpAddr`, `serde` (already in domain), and a hand-written `Display` impl (no thiserror dep). The classification table fits in ~50 lines of `match` arms; the 7 helper functions are all const-friendly.
  - Hook integration (calling `check_host` from `step_judge::process` when an outbound HTTP claim is being evaluated, plus skills-mcp wiring for tool bodies that make HTTP calls) lands as a follow-up — this commit ships the policy layer; the integration touches a different surface and deserves its own commit. 1307 workspace tests green (923 application + 312 domain + 72 infra). Release build clean.

- **M6.3 trust score from proof corpus history (Microsoft AGT pattern)**: aggregates `ArchivedChainSummary` entries (from the cross-session corpus shipped in #39 / commit e969b61) into a 0–1000 trust score with five behavioral tiers — `Probationary` (0–200), `Developing` (201–500), `Established` (501–800), `Trusted` (801–950), `Verified` (951–1000). Scoped per-skill (via `score_for_skill`) and per-session_id (via `score_for_session`, where session_id is the agent identifier proxy). New module `crates/sentinel-application/src/trust.rs`; pure-application math (no I/O), public types `TrustTier` + `TrustScore`. Formula: base = round(pass_rate × 1000), then sample-size cap — 0 → 0, 1–4 → cap 200, 5–20 → cap 500, 21–100 → cap 800, 101–199 → cap 950, 200+ → uncapped. **Load-bearing invariant pinned by tests:** 100% pass on n=1 caps at Probationary 200 — the whole point of the trust score is to gate auto-approval, and "1 successful chain" is not evidence of reliability. The cap is intentionally aggressive at the low end. Surfaces ready to consume: pack contracts (#12 / orig M7.8) for "auto-merge agents need Trusted+ tier" policies, report #71/#72 for per-skill/per-agent trends. **Out of scope for this commit** (depends on later work): folding anomaly counts (task #17 ships the hook, corpus rollup is follow-on) and per-step cost (task #82 Stage B) into the score — both will compose in without changing the public API. 12 unit tests pin: empty corpus → Probationary 0; n=1 perfect → caps at Probationary 200; the 5/21/101/200 sample-size band edges; mixed pass/fail lowers score; all-failures → 0; skill filter excludes other skills; session filter groups by agent; `TrustTier::from_score` band edges; kebab-case serde wire format. 1284 workspace tests green (923 application + 289 domain + 72 infra). Release build clean.

- **`sentinel cleanup persistent-tasks` — orphan project_hash bucket pruner (#33)**: prunes stale buckets under `~/.claude/sentinel/persistent-tasks/` whose originating cwd no longer exists on disk. These accumulate every time sentinel saw a new cwd including every removed git worktree path; the worktree-collapse fix to `project_hash` only landed in commit 5ac549a (this session's first merge), so anything from before that lives under per-worktree-path hashes that no living cwd points to. On Gary's machine today: 299 of 390 buckets are orphans (77% bloat). New `crates/sentinel-cli/src/cleanup_cmd.rs` module: walks the dir, reads each bucket's `meta.json`, classifies by stat-ing the recorded `cwd` path, prints an audit report grouped by Live/Unreadable/Orphan, and removes orphan + unreadable buckets only when `--apply` is passed. Default mode is dry-run — no destructive surprises. New CLI surface: `sentinel cleanup persistent-tasks` (audit) / `sentinel cleanup persistent-tasks --apply` (act). New `Cleanup` command + `CleanupAction` subcommand enum on the existing clap structure to make room for follow-on cleanups (orphan session task dirs is filed as task #32 and slots in here next). 6 unit tests pin classification: live cwd, missing cwd, missing meta.json, malformed meta.json, empty cwd field, and the apply-removes-orphans-preserves-live invariant. Release build clean, no new warnings. Caller is unprivileged: `std::fs::remove_dir_all` only — does NOT touch `~/.claude/tasks/<session>/` (that's task #32's surface), does NOT touch `~/.claude/sentinel/proofs/` (those are the cross-session corpus and should outlive their originating cwd).

- **Cross-session proof chain archive (#39)**: turns the live-session-only `query_proof_corpus` (M4.3) into a cross-session corpus by archiving every chain to `~/.claude/sentinel/proofs/` on Stop and walking the index on read. This is the load-bearing input M7.5 router-as-planner needs to answer "for prompts like X, which step sequences had which success rates across all past chains?" — without it, the moat tool sees one session's worth of evidence and the planner is replicating yesterday's classifier.
  - New module `sentinel-application/src/proof_archive.rs`: `ArchivedChainRecord` (versioned wrapper carrying `schema_version: 1` + `archived_at` + the full chain), `ArchivedChainSummary` (one-line index entry: skill, session_id, step_count, phase_count, head_hash, all_sufficient, step_sequence, archived_at). Both versioned at the top level — cheap to add now, painful to retrofit later when v2 readers need to dispatch on shape.
  - Storage layout: one file per `(session_id, skill)` pair at `~/.claude/sentinel/proofs/<session>__<skill>.json` (avoids read-modify-write across multiple Stop fires) plus an append-only `index.jsonl`. Atomic writes via `<dir>/.<name>.tmp` + rename — a crashed Stop can never leave half-written JSON in the bucket. Idempotency: re-running Stop for the same session does not duplicate index lines (existing entries are detected by `(session_id, skill)` key on read).
  - Stop hook integration in `crates/sentinel-cli/src/hook_cmd.rs`: best-effort `archive_chains(&state, ctx.fs, &home)` call right before state.save(), failures logged via `tracing::warn!` and not propagated — proof archiving is a corpus-feature, not a session-shutdown gate.
  - `query_proof_corpus` extension in `crates/sentinel-application/src/mcp_handler.rs`: optional `ProofArchiveBacking { home, fs }` injected via new `with_archive(...)` builder. When wired (production path via `mcp_cmd.rs`), the response merges archived summaries with live state. Live wins ties on `(session_id, skill)` — the in-flight chain is current truth, the archived snapshot is by definition stale for that pair. Response gains a `scope: "live-session" | "cross-session"` field and an `archived_at` field on archived entries so callers can see what they got.
  - 9 new tests in `proof_archive.rs` pin: schema_version=1 in JSON, one-index-line-per-chain, idempotency across 3 Stop fires (still 1 line), one-file-per-(session,skill), malformed-line skip in `read_index`, empty-archive read returns Vec::new, future schema versions silently skipped, separator chars in session_id sanitized to safe filenames, empty-state archive is a no-op (no JSONs, no index file).
  - **Out of scope (later commits):** aging / cold-archive subdir for chains older than N days (deliverable d in the task description), gzip compression on the wire, cross-machine federation. The index keeps everything for now; aging gets added when there's actual volume to drive heuristics. 1272 workspace tests green (911 application + 289 domain + 72 infra). Release build clean.

### Changed

- **Pluggable judge backends: Kimi K2.6 added as new review-tier default; OpenRouter dispatches every JudgeModel tier (Stage A of task #34)**: the `JudgeModel` enum had 3 tiers (Haiku/Sonnet/Opus) with Display strings shaped like OpenRouter model paths, but `rig_judge.rs` ignored the model parameter and hardcoded `JUDGE_MODEL = "openai/gpt-5.4"` for every call — the tier abstraction was decorative. This commit makes the dispatch real and adds Kimi as the review-tier default per task #34's research (Moonshot K2.6 ships 58.6 SWE-Bench Pro / 89.6 LiveCodeBench / 256K context at $1.15-2.15/M tokens — 4-10x cheaper than the closed frontier with stronger agentic-coding accuracy, plus Eastern training-distribution diversity to break the shared-blind-spot failure mode of all-Western-closed multi-judging).
  - `JudgeModel` enum is now 4 tiers: `Haiku` (fast, Cerebras GLM-4.7), `Kimi` (review **default**, Moonshot K2.6), `Sonnet` (cross-vendor verification, OpenAI GPT-5.4), `Opus` (critical-strict, Anthropic Claude Opus 4.7). Variant order is cost-ascending so reading the source matches the routing-cost ladder. Bumps from old `gpt-5.3 / opus-4.6` to `gpt-5.4 / claude-opus-4.7` to match current latest available on OpenRouter (the Display strings *were* OpenRouter ids, just stale).
  - New methods on `JudgeModel`: `default_review_tier()` (returns `Kimi` — the canonical answer to "what tier does a step that didn't declare one get?"), `openrouter_model_id()` (returns the same string as `Display` but at a const fn call site for type-safe dispatch).
  - `rig_judge::JudgeProvider` now carries a runtime model_id parameter through its `PromptFn` type alias and the `judge()` method. Single OpenRouter client serves every tier. The `MultiModelJudge::evaluate` impl reads `model.openrouter_model_id()` and passes it through, so step configs that declare `judge = "kimi"` actually get Kimi instead of silently using GPT-5.4.
  - `anthropic.rs` legacy direct-Anthropic LlmPort handles the new `Kimi` variant by mapping it to `claude-sonnet-4-6` (closest review-tier substitute); doc-comment on `model_id` records the rationale and points readers to OpenRouter for actual Kimi routing.
  - 5 new tests in `judge.rs` pin the wire format: Display strings for all 4 tiers, Display ↔ openrouter_model_id() invariant, `default_review_tier() == Kimi`, kebab-case serde round-trip across all variants, Kimi/Sonnet kebab-case serialization. 2 mcp_handler tests updated to the new Sonnet/Opus model strings (they pinned the exact wire output).
  - **What's NOT in this commit (deferred to Stage B):** multi-judge parallel verification at the `critical` trust tier (Kimi+Sonnet, escalating to Opus on disagreement), DisagreementMarker chain entries, per-step pricing accounting from OpenRouter response headers, audit-grade variant recording resolved provider in StepProof. All of those sit on top of the single-judge dispatch this commit ships and deserve their own commit. 1263/1263 workspace tests green.

### Added

- **M4.6 request shape limits — content size + sliding-window rate (ContextForge pattern)**: defends against the two failure modes production MCP servers consistently get bitten by — oversized payloads (50MB tool-result bodies that explode judge token costs / log lines / classifier memory) and rate flooding (retry storms hammering one step 200×/min). New `sentinel-domain::request_limits` module ships pure-domain `RequestLimits` config (`max_evidence_bytes` default 1 MB, `max_artifact_bytes` default 256 KB, `max_calls_per_window` default 60, `window_seconds` default 60), `CallWindow` (sliding-window deque with lazy eviction, no background thread), `LimitError` enum (`EvidenceTooLarge` / `ArtifactTooLarge` / `RateLimitExceeded` each carrying actuals + limits for actionable deny messages), and the `enforce_limits()` pure function callers invoke before dispatching to the judge. **Critical invariant pinned by tests:** rejected calls do NOT count toward the rate window — otherwise a flood would self-amplify (the more you get rejected, the longer until you can call again). Callers control the `record_on_accept` knob so two-phase enforce-then-record gates don't double-count. `limit == 0` disables rate limiting entirely (useful for trusted background coordinates exempt from the policy). Order is evidence-size → artifact-size → rate (cheapest checks first; "evidence too big" is more actionable than "you're rate-limited" when both apply). 17 new tests covering defaults, serde round-trip, eviction semantics (including window-shrink-at-query-time), would_exceed at/under/over limit, rate-limit-disabled-on-zero, all three rejection paths, the no-counting-rejected invariant, the record_on_accept knob, the check-order priority, and `Display` impl message content. Hook integration (calling enforce_limits from `step_judge::process` + threading `CallWindow` through `SessionState`) lands as a follow-up — this commit ships the policy layer; the hook wiring touches a different surface and deserves its own commit. 1283/1283 workspace tests green.

- **M4.5 OpenTelemetry tracing alongside the proof chain**: ports W3C Trace Context plus a `TracingPort` trait into the workspace as the audit-substrate / operational-substrate split — the proof chain answers "what happened, signed, immutable" and OTEL answers "how long, what called what, where the latency lives." They link via a new `Option<TraceContext>` field on `StepProof` so a corpus query can pivot directly to Grafana / Tempo / Honeycomb reports. New `sentinel-domain::tracing` module owns the W3C wire format (`traceparent` v00 parser/formatter, `tracestate` key-value list, all-zero / non-hex / uppercase / arity rejection per spec), the `TraceContext` data type, and a `TraceParseError` enum with display impls. New `sentinel-application::tracing_service` module owns the use-case interface: `TracingPort` trait with `start_span` / `set_attribute` / `record_event` / `end_span`, `SpanKind` / `SpanStatus` / `AttributeValue` enums (with `From` impls for `&str` / `i64` / `u64` / `f64` / `bool`), `SpanHandle` carrying the W3C context, plus `NoOpTracer` (the off-by-default impl) and a `cfg(test)` `RecordingTracer` that captures every call for hook-level assertions. **Critical invariant:** `trace_context` is operational metadata — it does NOT participate in `combined_hash` and signing covers the hash only, so OTEL-on and OTEL-off sentinel builds share identical chains and a post-hoc trace attachment does not invalidate signatures (both pinned by tests). 40 new tests (24 in tracing module covering W3C wire format / parse rejections / round-trips, 11 in tracing_service covering NoOp + RecordingTracer + AttributeValue conversions + idempotent end-then-attribute, 5 in step_proof covering default-None / skip-on-None serialization / round-trip / hash-independence / signature-independence). The OTLP exporter wiring (heavy `opentelemetry-otlp` dep) lands as a follow-up — the trait and types are stable so it can plug in without touching the proof-chain layer. 1266/1266 workspace tests green.

- **M2.9 step plugin extensibility — `extra: serde_json::Value` on `WorkflowStep`**: ports Apollo Federation's `@composeDirective` analog. Lets external plugins (lens registries, custom routers, telemetry adapters) attach typed metadata to a step without forcing the core schema to know about every plugin. The field is opaque at the core level — plugins enforce their own schemas at their boundaries (M4.8 will add Pydantic-style fail-fast validation). The TOML loader captures the field as `Option<toml::Value>` then converts via a new `toml_to_json` helper that maps each TOML primitive to its JSON equivalent (Datetime → ISO-8601 string; NaN/Infinity → null since JSON has no representation). Realistic plugin shape: `{ lens: { code_review: { rubric: "owasp", weight: 0.7 } }, telemetry: { skip: true } }` — multiple plugins coexist on one step keyed by their top-level name, no collisions. 5 new tests (default-null, nested-table round-trip, primitive type conversions, nested array+table conversion, NaN-to-null). 1226/1226 workspace tests green.

- **M2.6 federation deprecation/migration directives — `deprecated` + `override`**: ports Apollo Federation's `@deprecated` (with reason string) and `@override` (replacement declaration) to step configs. `WorkflowStep` gains `deprecated: Option<String>` and `r#override: Option<String>`, both `#[serde(default)]` so M2.5 configs load unchanged. Override grammar accepts `"phase.step_id"` (same skill — the common case) and `"skill.phase.step_id"` (cross-skill, when one skill takes over capability previously owned by another). `sentinel federation compose` (and its `check` companion) gain a deprecation pass: deprecated steps surface as warnings (not errors — they still work), dangling override targets surface as hard errors (you can't replace a step that isn't there), and override targets that aren't themselves marked `deprecated` surface as warnings (encourages disciplined migration paths). New helper `parse_override_target()` handles both grammars. 12 new tests (3 in `config.rs` for round-trip, 6 cross-skill compose tests covering deprecated warning / dangling override / target-not-deprecated / clean migration / malformed grammar / cross-skill resolution, 3 helper-fn unit tests). 1221/1221 workspace tests green.

- **M2.5 federation directives — `provides`, `requires`, `external`, `inaccessible` on step configs**: ports Apollo Federation's `@provides` / `@requires` / `@external` / `@inaccessible` directives to the federated step namespace. `WorkflowStep` in `sentinel-domain` gains four optional fields with `#[serde(default)]` so every existing TOML loads unchanged. `StepToml` in `sentinel-infrastructure` threads them through the loader. `sentinel federation compose` (and its `check` companion) gain a second pass after the per-skill internal checks: it builds two indexes from the union of all loaded skills (artifact providers + step coordinates) and walks every step's `requires` / `external` declarations, raising hard errors when a `requires` has no producer in the supergraph or an `external` reference is dangling/malformed. `inaccessible` steps still participate in the provides graph — they're internal-only from the router's perspective (M7 will filter on the flag), not from the federation contract's. 8 new tests (3 in `config.rs` for round-trip + defaults, 5 in `federation_cmd.rs` for cross-skill resolution: clean cross-skill chain, missing producer, dangling external, malformed external, inaccessible-still-counts). 1209/1209 workspace tests green.

### Fixed

- **`project_hash` now canonicalizes worktree paths to their parent repo — task durability survives worktree-switches**: the bug task #70 (orig_id) and bloat task #72 (orig_id) had the same root cause. Six hook files (`task_persist`, `task_rehydrate`, `memory_extract`, `session_index`, `session_init`, `todo_interceptor`) each carried a copy-paste of `fn project_hash(cwd) -> sha256(cwd.as_bytes())[..4]`. Hashing the cwd verbatim meant `/repo` and `/repo/.claude/worktrees/feat-x` produced different hashes, so `task_persist` wrote one bucket while `task_rehydrate` looked up another every time the user crossed a worktree boundary — tasks silently disappeared. New shared helpers in `crates/sentinel-application/src/hooks/mod.rs`: `canonical_project_cwd(cwd)` strips the literal segment `/.claude/worktrees/` (and the `\.claude\worktrees\` Windows variant) plus everything after it, collapsing any worktree to its repo root; `project_hash(cwd)` uses the canonical form. All 6 callers now delegate to `super::project_hash`. Dead `sha2::{Digest, Sha256}` imports + the local `hex_encode` helper removed from `task_rehydrate.rs` and `todo_interceptor.rs`. 6 new unit tests pin the worktree-collapse invariant: forward-slash collapse, backslash collapse, deep subpath collapse, non-worktree paths unchanged, sentinel main + sentinel worktree produce identical hashes, different repos still produce different hashes. The 390 orphan project-hash directories accumulated under the broken behavior remain on disk — cleanup is filed as a separate task. 798 sentinel-application library tests pass; release build clean; engine staged via `sentinel stage` for hot-swap on next session.

- **`memory_verify` re-wired to PreCompact — completes the relocation tracked as a follow-up in the previous hooks.toml fix**: the module had been orphaned on disk since the SessionStart removal (Qdrant scroll + per-claim Cerebras verification was blocking startup 5–20s on a cold cache). Three coordinated changes: (1) `crates/sentinel-application/src/hooks/memory_verify.rs` — module + function doc-comments updated to describe the PreCompact home and explain why (24h cooldown means a long session typically verifies once on its first compaction event of the day; `run_async()`'s 3s wall-clock timeout is retained as defence in depth); the one hardcoded `HookEvent::SessionStart` in the context-injection path at line 323 corrected to `HookEvent::PreCompact`. (2) `crates/sentinel-cli/src/hook_cmd.rs` — `memory_verify::process` now invoked from the PreCompact dispatcher branch alongside `pre_compact::process` and `session_index::process`, with an inline comment recording the relocation rationale. (3) `config/hooks.toml` — replaced the "orphaned, kept on disk" comment with a registration entry under PreCompact (`depends_on = ["pre-compact"]`, `has_api_call = true`); the inline comment now points readers to the new home. 792 sentinel-application library tests pass; release build clean.

- **`config/hooks.toml` reconciled with the dispatcher's actual behavior**: the registration file had drifted in both directions — `memory-verify` was registered for SessionStart but the dispatcher had already removed it (Qdrant HTTP calls were blocking startup 5–20s; comment in `hook_cmd.rs:677` documents the removal); meanwhile three real, dispatched memory hooks (`memory_inject` on UserPromptSubmit + Stop, `memory_extract` on Stop, `session_index` on PreCompact) had no registration entries at all. The `memory-feedback` entry's `depends_on = ["memory-extract"]` was a dangling reference because of this. Fix: removed the stale `memory-verify` SessionStart entry (replaced with a comment explaining the situation), added registrations for `memory-inject` (UserPromptSubmit), `memory-extract` (Stop), `memory-inject-precompute` (Stop), and `session-index` (PreCompact, depends on `pre-compact`). The `depends_on = ["memory-extract"]` on `memory-feedback` is now a real reference. The orphaned `memory_verify` Rust module is left in place for a planned PreCompact relocation, tracked as a follow-up. 1075 workspace tests pass.

- **Flaky `api::memory::tests::daemon_url_falls_back_to_default_when_unset` race**: three tests in `crates/sentinel-cli/src/api/memory.rs` (`daemon_url_uses_env_override_when_set`, `daemon_url_falls_back_to_default_when_unset`, `daemon_unreachable_returns_503`) all read+mutate `SENTINEL_MEMORY_DAEMON_URL`. Cargo runs unit tests in parallel by default, so without coordination the fall-back test intermittently observed the override test's value before its own `remove_var` ran. The previous comment claimed the override test was "the only env-touching test" — wrong, the unreachable test was added later without the same treatment. Fix: module-scoped `static ENV_LOCK: Mutex<()>` plus a `lock_env()` helper that tolerates poisoning; each affected test takes the lock at function entry and holds it across the full env-mutation + `daemon_url()` read window. 5 consecutive `cargo test --release -p sentinel api::memory::tests` runs all green (4/4 each).

### Changed

- **`task_rehydrate` is mode-aware: auto-rehydrates in Autopilot, asks first in Planned**: prior behavior was "always ASK FIRST regardless of mode" which made every Autopilot session pay an interruption tax to recover its task queue from the previous session. The hook context already exposes `autopilot_enabled()` (reads `SENTINEL_AUTOPILOT`); `task_rehydrate.rs` now queries it and emits one of four instruction variants (autopilot+blocking, autopilot+flat, planned+blocking, planned+flat). Autopilot variants direct immediate `TaskCreate` (and `TaskUpdate(addBlockedBy)` for blocking chains) followed by a one-sentence brief; Planned variants preserve the y/n ask-gate. Logic extracted to a pure helper `format_rehydrate_instruction(count, has_blocking, autopilot)` so the four-way branching is unit-tested without FS plumbing — 4 new tests pin each variant's invariants (mode tag, action verb, count formatting, mutual-exclusion of mode strings).

- **`tool_usage_gate` now strictly requires `TaskCreate` — `TodoWrite` no longer satisfies the gate**: per the operator's `CLAUDE.md` "Required Tool Usage" §3, the agent-harness `TaskCreate` / `TaskUpdate` (TaskList) tool is mandatory for tracking work, and the dual-acceptance of `TodoWrite` was the slippage point that let agents drift into the lesser fallback. Two changes in `crates/sentinel-cli/src/hook_cmd.rs`: (1) the PostToolUse marker writer no longer fires `mark_task_created` / `mark_task_active` on `TodoWrite` — only `TaskCreate` does; (2) the entire `TodoWrite`-with-`in_progress`-payload branch that opportunistically wrote `TASK_ACTIVE_PREFIX` is removed. Deny messages in `tool_usage_gate.rs` updated in two places (Check 2 and Check 4) to specifically name `TaskCreate` and explicitly reject `TodoWrite` with a "NOT accepted — the operator's CLAUDE.md mandates the agent-harness TaskList tool" callout, so agents that hit the block know exactly what to use. The signed `verification` override (60s escape hatch) still works as before. New regression test `test_check2_deny_message_rejects_todowrite_and_names_taskcreate` pins both invariants — if a future edit drops either the `TaskCreate` mention or the `TodoWrite` rejection, the test fails.

- **`tool_usage_gate` Check 4 reads the persistent task store, not just the sticky marker**: the `TASK_ACTIVE_PREFIX` marker only flips ON — it never reflects task completion — so once any task was marked active, the marker satisfied the gate forever even after every task completed. New `persistent_store_has_active_task(fs)` reads `~/.claude/sentinel/persistent-tasks/*/tasks.json` (and the legacy `~/.claude/persistent-tasks/*/tasks.json` location during the migration window) and returns `true` only when at least one task currently has `status=in_progress`. Check 4 queries this authoritative source first; the marker is retained as a fast-path/fallback for the rare case the store can't be read (no home dir, malformed JSON). Catches the "all tasks are completed but agent is still editing" drift that motivated the recurring complaint.

### Added

- **`scripts/bootstrap.sh` — one-command installer for sentinel + linear MCP/CLI + marketplace.** Clones the four code repos (vulcan-mcp-sdk-rust, sentinel, linear-mcp-rust, linear-cli-rust) under `$SENTINEL_GH_DIR` (default `~/Documents/GitHub`), clones claude-code-marketplace into `$SENTINEL_CLAUDE_DIR` (default `~/.claude`) — only if the target dir doesn't already exist as a git repo or isn't non-empty — runs `cargo build --release` in each, copies the launcher + engine binaries (`sentinel`, `sentinel-engine`, `linear-mcp`, `linear`, plus `mcp-router` if vulcan built it) into `~/.cargo/bin/` so the launcher/engine split lands correctly without relying on `cargo install`'s crate-path guesswork, copies `hooks.toml` + `workflows.toml` from the sentinel repo into `~/.claude/sentinel/config/`, writes a starter `settings.json` with the seven event handlers (SessionStart, SessionEnd, UserPromptSubmit, PreToolUse, PostToolUse, Stop, PreCompact) all wired to `sentinel hook --event <Event>` plus an env block with `REPLACE_ME` placeholders for `LINEAR_API_KEY` / `OPENROUTER_API_KEY` / `ANTHROPIC_API_KEY` / `QDRANT_URL` / `QDRANT_API_KEY`, and writes `~/.claude.json` with `sentinel` + `linear` MCP server entries — but only if the file doesn't already exist; if it does, prints the JSON snippet to paste manually. Every step is idempotent: re-running skips anything already present. `gh` is preferred for cloning private repos when authenticated, falls back to `https` otherwise. README links it under a new Quick Install section. Verified with `bash -n scripts/bootstrap.sh` syntax check + `cargo check --workspace` clean.
- **`task_persist` writes a `source: auto` marker in the YAML frontmatter of project_tasks.md**: lets memory audits filter auto-generated TaskList snapshots from human-curated entries. Approach (a) — adding a `source` field to the `qdrant-mcp-rust` `store_memory` tool — wasn't viable without modifying that crate's domain `Memory` struct, so the marker lives on the flat file (`~/.claude/projects/<key>/memory/project_tasks.md`), which IS the memory file. Survives round-trips through `sync_from_files`; greppable. Two-line change in `crates/sentinel-application/src/hooks/task_persist.rs::write_memory_summary` plus a regression assertion on `test_write_memory_summary_creates_files`. Plan recorded at `plans/task-persist-source-marker.md`.

- **`good_citizen_observer` hook — soft-enforces "fix pre-existing issues you spot"**: two-phase observational hook that operationalizes the global `CLAUDE.md` "good citizens" rule. PostToolUse(Bash) scans tool stdout/stderr for compile warnings, dead-code diagnostics, lint findings, test failures, panics, and inline `TODO`/`FIXME`/`HACK` markers (12 patterns spanning Rust/clippy, TypeScript, ESLint, generic test failures), and appends a one-line `Observation` to `~/.claude/sentinel/state/good-citizen/{session_id}.jsonl`. Stop reads the per-session log and, if any observations exist, injects a soft reminder via `additionalContext` listing up to three findings grouped by category — naming `TaskCreate` as the action ("File a `TaskCreate` for any pre-existing issue worth fixing — scale the fix to the change"). Soft (advisory) rather than hard-blocking by design: "is that warning worth a task?" is fuzzy, and a hard block would false-fire on outputs the agent already understands and intends to ignore. Per-call write cap of 10 observations, total reminder shows top 3 — bounds JSONL growth and reminder length. 5 unit tests (records dead code, records test failure, ignores clean output, ignores non-Bash tools, Stop emits/skips correctly).

### Fixed

- **`pre_commit_verification` no longer parses Claude Code's transcript — Sentinel records its own evidence**: the hook input `session_id` is the harness wrapper ID, which does **not** match the on-disk `{sessionId}.jsonl` filename Claude Code writes (an internal `G8.sessionId`). Searching the transcript directory by the hook's session_id never finds the right file, so transcript-based verification falsely blocked every commit/push (the symptom: "BLOCKED: Run Tests Before Committing" even right after `cargo test` passed). Replaced with a self-recorded evidence file: new PostToolUse hook `test_evidence_recorder` (Bash matcher) appends a JSONL entry to `~/.claude/sentinel/state/test-evidence/{session_id}.jsonl` whenever a Bash command matches a test/build pattern; `pre_commit_verification` reads from that file instead. Both hooks see the same `session_id` from `HookInput`, so the lookup is exact — the transcript-ID mismatch is now irrelevant. Pure types live in new `sentinel-domain::test_evidence` (`TestEvidenceEntry`, `TEST_COMMAND_PATTERNS`, `evidence_path`, `compile_command_patterns`) so recorder + reader share the same source of truth. Deleted the dead `find_transcript_by_session` and `transcript_has_test_evidence` functions and their three transcript-search tests. Added 4 recorder tests + 2 evidence-file reader tests; 1067 workspace tests pass.

### Added

- **WIP-by-stage snapshot module + MCP read tool (SEN-8, data half)**: new `wip_snapshot` module owns the on-disk format for `~/.claude/sentinel/state/wip-snapshot.json` (atomic tmp+rename, `BTreeMap` for deterministic ordering) plus the bottleneck rule engine. The `review_clog` rule is implemented and tested: fires when `Code Review WIP / 7d throughput > 14 days`, silent at exactly 14d, silent when no review WIP, silent when throughput is zero/missing, and per-team independent. The `qa_ceiling` rule is shaped in the public API but deferred to the live poller (needs per-ticket dwell time). New MCP tool `mcp__sentinel__get_wip_snapshot` reads the file and returns a friendly "no snapshot captured yet" payload before the poller has run. The 5-minute Linear API poller is tracked as a follow-up task. 10 new unit tests, 781/781 workspace pass.

- **Cycle-time event capture (SEN-1, persistence half)**: new `cycle_time` module in `sentinel-application` extracts Linear `Issue.update` state transitions from incoming Hookdeck webhook bodies and appends them to `~/.claude/sentinel/metrics/cycle-time.jsonl`. Fires as a side effect inside `channel_event_from_webhook` so the existing channel-emission contract is unchanged — JSONL persistence is opportunistic and never blocks the channel event. Schema matches the SEN-1 spec verbatim (`issue_id`, optional `team`, `from_state`, `to_state`, `timestamp`, `estimate`, `priority`, `labels`). Pure parser (`extract_from_linear_webhook`) is fully fixture-driven; 9 unit tests cover the transition-vs-not-a-transition matrix, prior-state-name fallback, label array shape variants (objects vs strings), missing-optional serialization, and append round-trip. The Hookdeck route admin and 60-day Linear backfill are tracked as follow-up tasks. 771/771 sentinel-application tests still green.

### Changed

- **Doc updates for `accounts-mcp` → `claude-accounts-mcp` rename**: the `accounts` MCP and its CLI sibling were renamed to `claude-accounts` (across GitHub repos, local dirs, Cargo packages, internal crates, and binaries) on 2026-05-02 to disambiguate from "Linear accounts" / "Doppler accounts" / "project configs" — the word "accounts" was massively overloaded. Sentinel itself only had two stale comments referencing the old name in `account_cascade.rs::process` and `is_error_result`; both updated to point at `claude-accounts-mcp` with a parenthetical noting the rename date.

### Fixed

- **`git_hygiene` threshold raised + `pre_commit_verification` recognizes more verifiers**: `MAX_UNCOMMITTED_FILES` was 10, which constantly false-fired on routine work in repos with legacy index noise (stale build dirs, in-progress refactors, .gitignore drift) and forced cleanup detours before the actual task could resume. Raised to 150 — still catches genuinely out-of-control sessions but no longer blocks normal work. Separately, `pre_commit_verification`'s `TEST_COMMAND_PATTERNS` was missing `cargo check`, `cargo clippy`, `go build`, and `go vet` — meaning Rust changes verified via `cargo check` (the most common Rust verifier) would still be blocked at commit because the gate didn't recognize the evidence. Patterns added. Tests updated to compute file counts off `MAX_UNCOMMITTED_FILES` instead of hardcoded 12/15 so they remain correct under any future threshold changes. 964 tests pass.

- **Interceptor false-positives on flag-like text inside commit messages**: `evaluate_git_command` substring-matched the joined args string, so a commit body containing literal `--force` (or `reset --hard`, `filter-branch`, etc.) would block the commit. Real-world reproduction this session: `git commit -m "fix(interceptor): rm -rf orphaned worktree shells on Windows + better cleanup reminder ... worktree remove [--force] path ..."` was rejected as if `--force` were a real flag. Fix: new `evaluate_git_args(&[String])` entrypoint that strips the values of `-m` / `--message` / `-F` / `--file` (both space-separated and `=` forms) before applying substring-match policy. The application layer (`crates/sentinel-application/src/interceptor.rs`) now uses the args-aware entrypoint. The legacy `evaluate_git_command(&str)` stays for backward compat. 11 new unit tests pin the false-positive cases (commit with `--force` / `reset --hard` / `filter-branch` / `--force-with-lease` / `rebase -i` in the message body, long-form `--message`, `=` form, `-F path`) AND verify real `--force`, `push --force`, `reset --hard` are still blocked/confirmed correctly. 762/762 app + 26/26 domain tests still green.

- **Orphaned worktree shells on Windows**: `git worktree remove` on Windows routinely fails to delete the on-disk directory because some process (file watcher, mcp-router, IDE) holds a handle inside the tree. Git itself unregisters the worktree and exits 0, leaving an empty (or near-empty) directory shell behind that `hygiene_reminders` then correctly flags as stale on every prompt. Two-layer fix:
  - `sentinel-git-interceptor` now runs a `post_exec_cleanup` pass after every wrapped git invocation (allow path, confirm path, and `--bypass` path). When the args parse as `worktree remove [--force] <path>` and git returned 0 *and* the on-disk directory still exists, retries `std::fs::remove_dir_all` up to 3 times with 150ms backoff. Failures are logged to stderr and never bubble up — git already considered the command successful. NotFound errors are treated as a benign race.
  - `hygiene_reminders` cleanup message now includes a copy-paste `rm -rf <path1> <path2> ...` one-liner spanning every detected stale shell, so users can wipe leftovers from prior sessions in one shot. The reminder also now explains the typical cause (Windows file locks) so future-Gary doesn't have to re-derive it.
  - 6 unit tests pin the `worktree remove` arg parser (relative + absolute path, `--force` before/after path, non-`worktree-remove` commands, empty args). Existing 7 hygiene_reminders tests still green. Full sentinel-application lib suite: 762/762.

### Changed

- **Persistent-tasks dir relocated under `sentinel/`**: snapshots written by `task_persist` and read by `task_rehydrate` (and the CLAUDE.md "Active Tasks" section in `session_init`) now live at `~/.claude/sentinel/persistent-tasks/{project_hash}/` instead of `~/.claude/persistent-tasks/`. Centralizes sentinel-owned state alongside `metrics/`, `state/`, `proofs/`, etc. so `~/.claude/` stays clean. New `persistent_tasks_root(home)` and `legacy_persistent_tasks_root(home)` helpers in `hooks::mod.rs`; both `task_persist` and `task_rehydrate` resolve their dir through them. One-time migration via `migrate_persistent_tasks_dir(fs, home)` triggers on first read after upgrade — atomic rename when possible, per-entry copy fallback for cross-mount or Windows-locked cases. Idempotent: no-op when the new dir already exists or the legacy dir doesn't. `session_init::render_tasks_section` reads new location first, falls back to legacy during the migration window so CLAUDE.md generation stays correct mid-upgrade. 5 new unit tests in `migrate_tests` (move, no-op when new exists, no-op when legacy missing, path shape under sentinel, legacy path unchanged).

### Added

- **Project-scoped `tasks.md` sync (single source of truth)**: `task_persist` hook now writes `<repo_root>/tasks.md` on every `TaskCreated` / `TaskCompleted` / `Stop` event in addition to the existing `~/.claude/persistent-tasks/{hash}/tasks.json` rehydration snapshot. The on-repo file is wrapped in `<!-- SENTINEL:TASKS:START -->` / `<!-- SENTINEL:TASKS:END -->` markers so any hand-written content (e.g. a curated roadmap that already lives at `tasks.md`) is preserved — first run prepends the auto block above existing content; subsequent runs splice in place. Skip-if-unchanged compares a SHA-256 of the rendered block against `meta.json` so unchanged turns are no-ops. Repo-root resolution goes through `GitStatusPort::repo_root(cwd)` so non-repo directories silently skip the markdown write while still updating the global JSON.
  - **Linear merge**: when `~/.claude/sentinel/linear-assigned-{project}.json` exists (populated by the existing Linear cron), open issues for the project are merged into a `## Linear Assigned` section inside the auto block — sorted started → unstarted → backlog → triage, capped at 50 rows, completed/canceled filtered out, priority emoji per row. No network call from the hook itself; the Linear cron owns the cache.
  - **Native Claude memory writeback**: a project-scoped `~/.claude/projects/<encoded-key>/memory/project_tasks.md` is updated alongside `tasks.md` on every fire (frontmatter `type: project`, top-10 open tasks with blocked-by chains). One line in `MEMORY.md` is added/replaced (`- [Tasks for <project>](project_tasks.md) — N open, M in progress`) so it lands in the auto-loaded memory at session start. Repo-root key encoding (`C:\Users\…` → `C--Users-…`) means worktree sessions update the canonical project entry instead of fragmenting per branch.
  - **Auto-block enforcement (`tasks_md_guard`)**: new PreToolUse hook blocks `Edit` / `Write` on `<repo_root>/tasks.md` when the change would mutate content **inside** the marker block (Edit `old_string` overlapping the marker range, or Write whose between-marker bytes differ from disk). Edits OUTSIDE the markers, or Writes that re-emit the same block, are still allowed — the block-message tells agents to use `TaskCreate` / Linear instead. Anchored on `GitStatusPort::repo_root(parent(file_path))` so only the actual project tasks.md is guarded; sibling tasks.md files in subdirectories are left alone.
  - 26 unit tests across `task_persist` (marker insertion, replace-in-place, idempotent re-merge, skip-if-unchanged, Linear filter/order/empty, memory writeback skip-when-no-projects-dir, memory writeback creates files + indexes correctly, MEMORY.md duplicate-line collapse, project-key encoding for Windows + Unix paths) plus 12 in `tasks_md_guard` (marker range parsing, edit-overlap detection above/below/inside, write delta detection, fresh-file allow, no-marker allow). Builds clean across the workspace.

- **ROI vs human-team baseline (SEN-15)**: new `sentinel roi scan` CLI subcommand joins SEN-7 `tokens-per-ticket.jsonl` with the SEN-13 `cost-per-point-summary.json` and computes the headline factory metric — $ Claude spent vs $ a human team would spend shipping the same work. Human baseline constants land in `sentinel-domain::constants` (`HUMAN_FULLY_LOADED_USD_PER_YEAR = 170_000`, `HUMAN_FULLY_LOADED_USD_PER_DAY = 654`, `HUMAN_DAYS_PER_POINT = 0.5`, `HUMAN_USD_PER_POINT = 327`, `HUMAN_WORKING_DAYS_PER_YEAR = 260`) so the ratio is reproducible and reviewable. For each rolling window (7d / 30d / 90d / all-time) the analyzer reports tickets shipped, points shipped, Claude cost, equivalent human cost, ROI ratio, and projected annual savings. Window membership is derived from the most-recent session-file mtime per ticket under `~/.claude/projects/*` (best-effort — collapses to all-time only when the projects root is missing). When SEN-13 has zero tickets-with-estimate the analyzer engages a transparent fallback path: assume `FALLBACK_AVG_POINTS_PER_TICKET = 3.0`, mark every row `fallback_used: true`, and surface a fallback note explaining the synthesis. Outputs land at `~/.claude/sentinel/metrics/roi.jsonl` (one row per window) and `~/.claude/sentinel/metrics/roi-summary.json` (top-line headline + nested windows). Stdout report prints the headline ROI as a big-number, per-window breakdown table, and a tip when fallback is in play. 6 unit tests pin ROI ratio against a known-input case (10x), annual savings projection scaling against a 30d window, missing-input clean degradation, fallback engagement when SEN-13 reports zero estimates, ticket-id slug extraction (with prefix allow-list), and weighted-median collapse over SEN-13 buckets. Live first-scan against 24 tickets / $12,073 spend produces 2.0x all-time ROI and ~$103k projected annual savings via the fallback path.

- **PR review thoroughness metrics (SEN-18)**: new `sentinel pr-review scan` CLI subcommand walks merged PRs across the firefly-pro-gh org (firefly-pro-crm, firefly-pro-web-app, firefly-pro-api-rust, firefly-pro-routing, firefly-pro-routing-engine, firefly-pro-routing-api, firefly-pro-marketing) plus legatus-ai/sentinel via `gh` CLI shell-out, computes review-depth metrics (avg comments per PR, time-to-first-review p50/p90 in hours), counts Codex severity blocks (CRITICAL/HIGH/MEDIUM/LOW) and CodeRabbit findings (critical / potential issue / refactor suggestion / nitpick) by parsing comment + review bodies, distinguishes bot reviewers (coderabbitai, github-actions, linear, greptile-apps, codex, renovate, dependabot, vercel) from human reviewers to compute the human-in-the-loop %, and heuristically scores CodeRabbit remediation rate via post-review commit timing. Writes `~/.claude/sentinel/metrics/pr-review.jsonl` (one row per PR with full per-PR breakdown) and `~/.claude/sentinel/metrics/pr-review-summary.json` (aggregate roll-up + per-repo). Stdout report prints global summary + per-repo breakdown. Window default 30d (configurable via `--days`). 12 unit tests pin the Codex severity parser (text + emoji forms), CodeRabbit category detection, bot vs. human classification, time-to-first-review computation, percentile aggregation (linear interpolation), `gh` JSON parsers (list + view), and end-to-end report aggregation. Live first-scan against the 8 default repos: 282 merged PRs over 30 days, 2.83 avg comments per PR, 505 CodeRabbit findings, 9.57% human-in-the-loop coverage.

- **Cost-per-story-point analyzer (SEN-13)**: new `sentinel cost-per-point scan` CLI subcommand joins the SEN-7 `tokens-per-ticket.jsonl` rows with Linear estimates to compute `tokens_per_point = total_tokens / estimate` and `cost_per_point = cost_usd / estimate` per ticket. Tickets are bucketed to the nearest canonical Linear estimate (1, 2, 3, 5, 8, 16) — ties round up so 6 → 8 not 5, 12 → 16 not 8. Per-bucket p25/p50/p75/p90 are computed via numpy-default linear interpolation. The "estimating drift" ratio = `bucket-8 cost_p50 / bucket-2 cost_p50` flags non-linear sizing curves with a default alarm threshold of 5.0x. Outputs land at `~/.claude/sentinel/metrics/cost-per-point.jsonl` (one row per ticket-with-estimate, sorted descending by `cost_per_point`) and `~/.claude/sentinel/metrics/cost-per-point-summary.json` (totals, per-bucket stats, drift ratio + alarm flag). The Linear cache loader is permissive — accepts both top-level `[...]` arrays and `{"issues": [...]}` objects, reads the estimate from any of `estimate`/`estimateValue`/`estimate_value`/`points`/`story_points`/`storyPoints`, and silently skips issues with missing or null estimates. When zero tickets carry estimates the report says so honestly (`tickets_with_estimate: 0` + a yellow stdout warning) rather than fabricating point values — the correct answer until SEN-1's webhook capture lands an estimate-aware Linear snapshot. 9 unit tests cover bucket assignment (exact + ties + clamps), percentile computation against numpy reference values, drift alarm trigger at 6x synthetic data, empty-estimate clean report, both Linear cache shapes, and missing-input graceful degradation.

- **Cache hit rate + token efficiency tracker (SEN-14)**: new `sentinel cache scan` CLI subcommand walks `~/.claude/projects/*/` for session JSONL files, parses every `assistant` message's `usage` block, and computes the prompt-cache hit rate per session as `cache_read_input_tokens / (cache_read + input_tokens + cache_creation_input_tokens)`. Aggregates p50/p90 globally and per-project, surfaces the top-N "worst" sessions ranked by waste estimate `(1 - hit_rate) * total_tokens` (filtered to sessions with ≥50K total tokens so trivially-tiny zero-cache sessions don't dominate the list), rolls a per-day mean, and converts waste tokens to USD at the conservative Opus input rate ($15/Mtok). Writes a per-session JSONL at `~/.claude/sentinel/metrics/cache-efficiency.jsonl` (one row per session with hit rate, waste estimate, and raw token counts) plus a structured summary at `~/.claude/sentinel/metrics/cache-efficiency-summary.json` (global percentiles, per-project breakdown, daily trend, top-10 worst). Stdout report colours hit-rate %, prints the worst-N list with token volume + dollar waste, and renders a 7-day bar-chart trend. Edge cases pinned: zero-usage sessions return `None` for hit rate (no 0/0 NaN), empty percentile input returns 0.0, missing project root writes empty outputs without panicking. Walker pattern duplicated from SEN-7's `tokens.rs`; consolidation into a shared session-walker helper deferred to a follow-up. 8 unit tests.

- **Tokens-per-ticket aggregator (SEN-7)**: new `sentinel tokens scan` CLI subcommand walks `~/.claude/projects/*/` for session JSONL files, extracts the Linear ticket id from the worktree path slug first (high-confidence) and grep of the first 50 user prompts second (medium-confidence), aggregates `usage` blocks across every `assistant` message in the session, applies per-model Anthropic pricing via the new `sentinel_domain::pricing` module, and writes one `{ticket, sessions, total_input, cache_read, cache_creation, output, cost_usd, models, confidence}` row per ticket to `~/.claude/sentinel/metrics/tokens-per-ticket.jsonl`. Output is full-overwrite each scan — input session JSONLs are the source of truth so re-scanning is idempotent. Pricing tiers (Opus / Sonnet / Haiku) are hardcoded constants for now; TODO move to a TOML lookup in a follow-up. Stdout report prints total sessions, mapping coverage %, and top-N most expensive tickets. Known prefix allow-list (FPCRM, FPFIELD, FPROUTE, FPMD, FPTRIBU, LEG, COR, EXA, SYN, TES, TRI, SEN) keeps `HTTP-200`-style false positives out. 15 unit tests cover path-slug extraction, prompt grep with cap, cost computation against published rates, and an end-to-end fixture scan.

### Fixed

- **Drop malformed `task_completed` / `teammate_idle` channel events (SEN-1)**: both hooks previously fell back to placeholder strings (`"?"`, `"unknown task"`, `"unknown"`) when `input.extra` was missing required fields, then emitted those placeholders as channel notifications. The result: lead sessions saw a constant trickle of `"Task #? completed: 'unknown task' (by unknown)"` and `"Teammate 'unknown' (team: unknown) is going idle"` notifications for every malformed dispatch. Both hooks now `return HookOutput::allow()` without emitting when any of `task_id` / `task_subject` / `teammate_name` is missing or contains the literal placeholder. 4 new unit tests pin both the missing-fields and unknown-literals branches.

- **Skill router: strip injected hook context before classification (SEN-2)**: the AI classifier was routing on words appearing in hook-injected reminders rather than the actual user message. Concrete trigger from a recent session: a turn that included the hook reminder `[Worktree Cleanup] 4 stale worktree(s) found` caused the `cleanup` skill to fire even though the user said nothing about cleanup. New `strip_hook_context()` helper removes `<system-reminder>...</system-reminder>` blocks (multi-line), `<channel ...>...</channel>` blocks, and bare `[Bracketed Tag] message` reminder lines before classification. If the cleaned prompt is empty (input was 100% hook context), short-circuit to `allow()`. 5 new unit tests cover all four strip cases plus pure-user-text passthrough.

- **PR auto-monitor: suppress `git push` recommendation when no remote (SEN-3)**: the merge-to-main branch unconditionally injected `1. Push to remote: \`git push\`` even when the repo had no remote configured (e.g., a fresh repo not yet pushed to GitHub). Detect remote presence by reading `<cwd>/.git/config` directly — no subprocess, no latency — and looking for any `[remote "..."]` section. Worktrees have a `.git` *file* pointing at the main repo's `.git/config`; follow that pointer. When no remote is configured, swap step 1 with `1. Configure a remote (\`git remote add origin <url>\`) before the first push`. 2 new tests pin both branches.

### Added

- **Hook invocation telemetry (`hook_metrics` module + `sentinel stats hooks`)**: every wrapped hook call now appends a single JSON line to `~/.claude/sentinel/metrics/hook-invocations.jsonl` with `ts`, `event`, `hook`, `tool`, `session_id`, `repo_root`, `duration_us`, `outcome` (allow/block/deny/inject), and a 120-char `reason` snippet for blocks. Privacy: `tool_input` and `tool_result` are never logged. Telemetry errors are swallowed so they can't break a hook. New `sentinel stats hooks [--limit N] [--hours H]` subcommand reads the file back and prints top-N by call count, top-N by total time, an outcome breakdown, and a recent-blocks list. Local API endpoints land in a follow-up PR. Phase A wiring covers the `PreToolUse` and `UserPromptSubmit` event branches (~25 hook call sites); the remaining branches migrate incrementally.

- **Bug task gate (new hook `bug_task_gate`)**: when a tool result reveals a bug — `cargo test` `test result: FAILED`, a Rust compile error with a code (`error[E0277]: …`), or a runtime `panicked at` — the hook records pending-bug state at `~/.claude/sentinel/state/pending-bug-{repo_hash}.json` and blocks subsequent mutating tools (matching the `tool_usage_gate` scope) until a `TaskCreate` or `TaskUpdate` references the bug. Subject/description must contain at least one of: `bug`, `fix`, `error`, `regression`, `broken`, `failure`, `failing`, `panic`, `crash`. State auto-clears after a 10-minute TTL so a transient signal doesn't deadlock the session. Detection is conservative on purpose: only the three high-confidence patterns above trigger, so the gate doesn't fire on routine log output that happens to contain the word "error". 16 unit tests cover signal detection, false-positive suppression, TaskCreate keyword matching, and TTL behaviour.

- **Tool usage gate broadened to mutating Bash + write MCP tools**: previously the gate enforced "sequential-thinking + TaskCreate + plan-mode + active task" only on `Edit`/`Write`. Bash and MCP tools bypassed it entirely, which let large mutations (commits, pushes, Linear writes, Doppler updates) ship without a tracked task. The gate now also covers Bash commands matching mutating prefixes (`git commit/push/merge/rebase/reset`, `gh pr create/merge`, `cargo build/run/install`, `npm/yarn/pnpm install`, `rm/mv/cp/mkdir/chmod`, `docker run/build`, `kubectl apply/delete`, output redirections `>`/`>>`) and MCP tools whose verb part doesn't contain a read-only fragment (`get`, `list`, `view`, `search`, `check`, `status`, `show`, `describe`, `health`, …). Read-only Bash (`git status/log/diff`, `ls`, `cargo check`, `gh pr view`, …) and read-only MCP tools (`mcp__linear__list_issues`, `mcp__doppler__get_secret`, …) still bypass. `cd <dir> &&` prefixes are stripped before classification so compound commands aren't mis-classified. New test coverage: 7 classifier-specific cases + 4 end-to-end gate cases.

- **Skill banner enforcement**: `skill_router` always renders an activation banner now — when a SKILL.md doesn't define a `## Activation Banner` section, the router synthesizes a fallback banner from the skill's frontmatter `description` field (with a 160-char hard cap) so the visual cue is consistent across all 76+ skills regardless of authoring discipline. `sentinel scan --validate` adds a new "Skill Banner" category that flags any SKILL.md missing an explicit `## Activation Banner` as a `warn` (not `fail`, so unmigrated skills don't break CI). Audit at write-time: only 1 of 77 skills (qdrant) currently lacks a banner.

- **Skill invocation gate (new hook `skill_invocation_gate`)**: enforces that skills detected by `skill_router` are actually invoked. When a skill is detected, `skill_router` writes a pending-skill state file (`~/.claude/sentinel/state/skill-pending-{session_hash}.json`). The new `PreToolUse` gate blocks tool calls outside an allowlist of read-only / progress-toward-load tools (Read, Glob, Grep, LSP, WebSearch/Fetch, ToolSearch, Skill, Task*, sequential-thinking) until the pending skill is invoked or its SKILL.md is read. The matching `PostToolUse` handler clears the state on `Skill(skill: "<name>")` matching the pending skill or on a `Read(...)` of its SKILL.md. State auto-clears after a 5-minute TTL so a skill the user explicitly skipped doesn't deadlock the session. 12 new unit tests cover stale TTL handling, allowlist sanity, and Windows/Unix/tilde path matching.

- **Sentinel engine version + hook count in SessionStart banner**: the `[SessionStart]` line now reads `session_id: X | engine: sentinel v0.4.0 | hooks: 54` so the running build is always visible at the top of every session. Version comes from `CARGO_PKG_VERSION` (compile-time); hook count is `HOOK_NAMES.len()` so it can't drift from the registered handlers.

- **Canonical hook output envelope (`HookEnvelope` + `HookTier`)**: new types in `sentinel-domain::events` plus `HookOutput::inject_envelope(event, envelope)` constructor. Renders as `[<HookName>] <emoji> <message>` with 🟢 Info / 🟡 Warn / 🔴 Block tier emojis so the user sees a consistent prefix across all 54 hooks instead of the ad-hoc mix of brackets, emoji, and box-drawing currently in use. Phase A only — existing `inject_context` call sites are unchanged. Migration of the 50+ legacy callers will land in follow-up tasks (one batch per hook group).

### Changed

- **Worktree/branch cleanup discipline (hygiene_reminders + worktree_reminder + pr_auto_monitor)**: surface merged `worktree-*` branches as actionable cleanup commands, both local (`git branch -d`) and remote (`git push origin --delete`). Detection runs on `Stop` (via hygiene_reminders) and on the worktree-reminder injection path. The merge-to-main detector in pr_auto_monitor now names the merged branch and lists the exact `ExitWorktree` + branch-delete commands so orphaned refs don't pile up. Required new `GitStatusPort::merged_local_branches` and `GitStatusPort::merged_remote_branches` ports.

- **task_rehydrate**: always ask before rehydrating persistent tasks from a previous session — removed the Autopilot auto-rehydrate bypass so both Autopilot and Planned modes prompt for confirmation.

- **session_init**: removed the Linear assigned-to-me cache refresh cron (every 10 min) from the CLAUDE.md template. Linear issues are now fetched on demand only. Deleted stale `~/.claude/sentinel/linear-assigned.json`.

- **Sentinel-owned paths consolidated under `~/.claude/sentinel/`**: all sentinel state and config now live under the `sentinel/` subtree so `~/.claude/` root contains only Claude Code native files. Specific moves: `sentinel-settings.json` → `sentinel/config/settings.json` (loaded via `claude --settings` flag in `claude-code-handler`); per-project config files `projects/*.md` → `sentinel/projects/`; sync marker `.last-sync-commit` → `sentinel/state/last-sync-commit`; metrics JSONL files → `sentinel/metrics/` (already done in a prior release). One-time on-disk migrations run automatically on `SessionStart` via `migrate_metrics_dir()` and new `migrate_last_sync_commit()` — no manual intervention needed on upgrade.

### Added

- **Memory proxy API (Phase 8.e)**: new `api/memory.rs` module exposes
  `GET /api/memory/stats[?project=X]` and `GET /api/memory/health`, both thin
  reqwest proxies to the memory daemon (default `http://127.0.0.1:3011`,
  overridable via `SENTINEL_MEMORY_DAEMON_URL`). Per-request timeout 3s. When
  the daemon is unreachable, returns a structured 503 with `{error, reason, hint}`
  so local clients can render a "daemon down" state without
  crashing. Non-JSON or unparseable upstream bodies surface as 502. Wired
  under `/api/memory/*` (the existing `/api/memories/*` namespace stays for
  the legacy precomputed-search state files). 4 unit tests cover the default
  URL pin, env override, fallback, and the 503 path against a closed port.
  Adds `reqwest` as a direct `sentinel-cli` dep (already transitive via
  `rig-core`). This is backend API only.
- **Hookdeck typed webhook decoders for Linear / GitHub / Vercel / Railway (Hookdeck 4b)**: new `sentinel-application::hooks::hookdeck_decoders` module turns raw JSON webhook payloads into one-line human-readable summaries the session can act on without drowning in 400-line JSON dumps. Per-source sub-modules (`linear`, `github`, `vercel`, `railway`) with per-event-type matchers — Linear decodes `Issue.update` state transitions (`FPCRM-329 moved QA Failed → QA Testing`), `Comment.create` with body excerpt (`Pedro commented on FPCRM-330: "..."`), `IssueLabel`, `Reaction`, assignee changes, priority changes, title changes. GitHub decodes `pull_request` (w/ merge detection: `PR #658 merged to main by @contributor (sha e22f87a)`), `check_run`, `check_suite`, `workflow_run`, `issue_comment` (w/ CodeRabbit batch detection), `pull_request_review`, `push` (branch + commit count + pusher), `issues`. Vercel decodes `deployment.*` lifecycle with deploy URL. Railway decodes `DEPLOY`/`ALERT` with environment + commit sha/msg. Shared fallback `[HOOKDECK:<source>] <event_type> on <resource_id>` guarantees no webhook ever surfaces as raw JSON. Comment bodies and titles are truncated and newline-stripped for safe single-line rendering. Pure library: no I/O, no hook handler, no panics. Wired into channel emission via new `channel_events::channel_event_from_webhook(source, event_type, body, extra_meta)` which builds a `ChannelEvent` with typed `summary` while preserving the original `raw` JSON under meta so callers can still drill in. 35 unit tests cover all decoder paths + fallback + glue helper (Ref Hookdeck-4b).
- **Channel-event coalescing buffer + webhook replay scaffolding (Hookdeck 4c, GS-000)**: new `sentinel_application::dedupe::Coalescer` collapses repeated webhook events with the same `(source, resource_id, event_type)` key inside a 3-second sliding quiet window, emitting a single notification annotated with `coalesce_count` instead of waking the session N times per burst. Events lacking a `meta.source` bypass the coalescer and emit immediately (no added latency for non-webhook channel events). Accompanying `sentinel_application::webhook_replay` module persists each session's last-seen webhook timestamp at `~/.claude/sentinel/state/{session_id}/last_webhook_ts.txt` and provides a pure `analyze_events` summarizer that turns a batch of decoded webhooks into a one-line catchup banner (`[HOOKDECK REPLAY] Since ...: N events — 3 CI runs, 2 Linear state changes`). 18 unit tests cover burst coalescing, sliding-window extension, non-coalescable bypass, superseded-path cleanup, force-flush, bucket-count ordering, failure-prioritized highlights, corrupt/missing marker files, and round-trip persistence — all deterministic via injected `Clock` trait.

- **`memory_extract` unified-mode capture through dual-judge gate (F1-PRE-3e, GS-65)**: when `MEMORY_ENGINE_UNIFIED=1`, the Stop hook routes flat-file memory sync (`.md` files under the memory directory) through the Memory engine's `memory_capture` MCP tool instead of upserting directly into the legacy `claude-memory` Qdrant collection. Every file now clears the dual-judge gate (Opus + Codex) before landing as an atom; rejected files still advance the sync-state so they aren't re-submitted every cron cycle. Schema mapping is lossy-but-principled — `subject`=name, `predicate`=memory_type (fallback: "describes"), `value`=description+body excerpt (500-char cap), `project`=`auto-extract`, `qualifier`=`source_file=<path>` so `memory_audit` can correlate atoms back to the source `.md`. Third and final sentinel hook in the F1-PRE-3 unification chain — alongside 3c (inject) and 3d (feedback) this completes the cutover target; F1-PRE-3f will flip the default.

- **`memory_feedback` unified-mode outcome recording (F1-PRE-3d, GS-64)**: when `MEMORY_ENGINE_UNIFIED=1`, the Stop hook classifies each injected memory into a Loop 4 outcome label — `"used"` (memory name appeared in the assistant's response), `"contradicted"` (correction phrase detected AND memory wasn't used), or `"ignored"` (neither) — and calls `memory_record_outcome(event_id, outcome)` on the Memory engine MCP (GS-63) for each. `RelevanceUpdater::apply_window` folds them into per-atom utility on the next `memory learn` cron run. Fire-and-forget per call: a single memory-mcp failure logs at WARN and moves on; the Stop hook never blocks. Mirrors the inlined stdio transport from memory_inject (F1-PRE-3c) — `sentinel-infrastructure::memory_mcp_client` tests remain the source of truth for JSON-RPC framing. Legacy boost + corrections.jsonl path preserved unchanged for the F1-PRE-3f A/B window.

- **`memory_inject` unified-mode path through the Memory engine MCP (F1-PRE-3c, GS-62)**: new opt-in code path in `hooks/memory_inject.rs` that, when `MEMORY_ENGINE_UNIFIED=1` (or `true`/`yes`/`on`), routes the UserPromptSubmit search through `memory_search` on the Memory engine's MCP server instead of the legacy `claude-memory` + `claude-sessions` Qdrant collections. Side effect (intended): every call now writes a `RetrievalEvent` per surfaced atom to `memory-retrieval-log`, which is what the Loop 4 `memory learn` batch EMA-folds into per-atom utility. Closes the F1-PRE-0 audit finding that Phase 12 shipped the Loop 4 pipeline but no production code path was writing the events. Legacy path remains the default until F1-PRE-3f cutover; this flag gives a safe A/B window. Implementation note — `sentinel-infrastructure` already has a `memory_mcp_client` helper (F1-PRE-3b) but `sentinel-application` can't depend on it (would cycle; infrastructure depends on application), so this hook inlines a ~100-line twin of the stdio transport. Tests in `sentinel-infrastructure::memory_mcp_client::tests` remain the source of truth for JSON-RPC framing; if the two copies drift, reconcile there first. 4 unit tests cover `project_from_cwd` — POSIX + Windows basenames, regex sanitisation (dots/spaces → '-'), empty-path fallback to "global", and the 128-char cap. Graceful degradation: a stalled or missing memory-mcp never blocks the prompt — logs a warning and returns no injection for that turn.

- **`sentinel_infrastructure::memory_mcp_client` — stdio client for the Memory engine MCP (F1-PRE-3b)**: thin JSON-RPC client that spawns `mcp-router --single memory-mcp` as a subprocess per call, performs the MCP handshake (`initialize` → `notifications/initialized` → `tools/call`), and returns the decoded tool payload. `MemoryMcpClient::search(query, project, top_k, session)` wraps the `memory_search` tool; every call now writes `RetrievalEvent` rows to `memory-retrieval-log` server-side (Phase 11/12's Loop 4 fuel, gated on memory-mcp @ a108f26). Configurable via `MEMORY_MCP_CMD` and `MEMORY_MCP_TIMEOUT_SECS` env vars; defaults to `mcp-router --single memory-mcp` with a 10s timeout. Intended for sentinel hooks that need to call the Memory engine without taking a direct crate dependency on `memory-application` / `memory-adapters` — preserves hexagonal boundary. 6 unit tests cover shell-split parsing, env-var config fallback, response payload extraction, error surfacing, hit deserialisation, and the spawn-failure smoke path.

### Fixed

- hooks: `tool_usage_gate` now detects plan mode from the Claude Code transcript by scanning backwards for `EnterPlanMode`/`ExitPlanMode` tool_use blocks, replacing the brittle `SENTINEL_AUTOPILOT` env-var bypass. The env var is preserved only as a last-resort fallback when no transcript path is available. See commit d5a475a.
- **`tool_usage_gate` SENTINEL_AUTOPILOT deadlock**: the plan-approval check (#3) now skips when `SENTINEL_AUTOPILOT=1`, matching the pattern already used in `pr_merge_gate`. Previously the gate demanded a plan-approved marker that could only be written when the PostToolUse dispatcher saw `EnterPlanMode` or `ExitPlanMode` fire — but when those tools aren't deferred-tool-registered in the harness session (and `CLAUDE_CODE_PLAN_MODE_REQUIRED` isn't set), the model had no path to satisfy the check, producing a hard deadlock on every Edit/Write. The other three preconditions (sequential thinking, task created, task active) still apply. Also cleaned up stale comments and a test assertion in the same file that claimed "There is no `EnterPlanMode` tool — must not reference fake tool" (contradicting the 2.1.114 audit finding that `EnterPlanMode` IS a real model-callable tool, handler `r7H`). Deny message now lists `EnterPlanMode` as the primary entry path alongside Shift+Tab, env var, `Agent(mode:"plan")`, and `--permission-mode plan`. Two new tests (`test_autopilot_bypasses_plan_gate`, `test_autopilot_does_not_bypass_task_active_check`) cover the bypass and confirm it doesn't leak to other checks; existing tests guarded with a shared `AUTOPILOT_LOCK` mutex to prevent env-var leakage in parallel test runs.

### Added

- **`sentinel break --session <id>` / `--list` / `--json`** — programmatic access to glass-break state for out-of-process consumers (Legatus Utility, local clients). `--session <id>` targets a specific session for `--status` / `--cancel` (previously both silently used "most recently modified state file"). `--list` enumerates every session under `~/.claude/sentinel/state/` with its break state (active first, then session ID). `--json` emits a stable `BreakStatusJson` schema — `{session_id, active, reason?, started_at?, expires_at?, remaining_secs, workflow?, tools_used_count}` — for `--status`, and a JSON array of the same for `--list`. Unreadable state files are skipped rather than poisoning the whole list. The anti-AI TTY challenge remains required for break **initiation**; `--cancel` does not require a TTY since it only tightens enforcement.
- **`commit_message_validator` Linear-ref enforcement**: PreToolUse Bash hook now blocks `git commit` inside a configured project (Linear issue-prefix configured in `~/.claude/projects/*.md` frontmatter) unless the commit message references one of that project's issue prefixes (`FPCRM-123`, `FPFIELD-9`, etc.). Detection is by cwd-to-project-config token matching (file stem, `name:`, `aliases:`, `doppler_project:` — all 3+ chars). Prefixes come from `issue_prefix:` or `linear_teams[].key:` entries. Existing conventional-format check unchanged. `--amend` bypassed. 11 new unit tests cover heredoc bodies, subject/body ref matching, case-insensitive match, frontmatter tokenization, alias collection, multi-team prefix collection, and cwd substring matching. Total: 26 tests in the module, 544/544 in the application crate.
- **2.1.114 audit + `EnterPlanMode` correction**: audited sentinel against claude-code-2.1.114 via the decompiler pipeline (29,422 name mappings recovered, 2,304 first-party). Earlier this unreleased cycle I removed `EnterPlanMode` from `phase_exempt_tools` believing it to be a fake tool (absent from `sdk-tools.d.ts`) — that was wrong. `EnterPlanMode` IS a live model-callable tool in the compiled binary (handler `r7H` at decompiled line 1666), just omitted from the public type declaration in both 2.1.88 and 2.1.114. Re-added to the exempt lists in `workflow.rs` and `phase_gate.rs` with citation to the binary evidence. Flipped the regression test in `workflow.rs` to assert `EnterPlanMode` IS exempt (was asserting it must be gated). Added `EnterPlanMode` handling to the PostToolUse marker dispatcher in `hook_cmd.rs` so it marks plan-approved alongside `ExitPlanMode`. Updated the `session_init.rs` CLAUDE.md template to document `EnterPlanMode` as a real entry path, plus the new `--permission-mode plan` CLI flag discovered in 2.1.114. Full audit report at `docs/sentinel-2.1.114-audit.md`. Other confirmed 2.1.114 deltas with no sentinel impact: `Agent.mode` gained `"auto"`, `EnterWorktree` gained optional `path` field, `AgentOutput` gained `toolStats`, `BashOutput` gained `staleReadFileStateHint`, `FileEditOutput.originalFile` is now nullable. `TodoWrite`/`AskUserQuestion`/remainder of `AgentInput` byte-identical. All 27 hook events unchanged. 48/48 relevant tests pass.
- **`ConfigChange` dispatcher detects plan-mode transitions**: when Claude Code fires `ConfigChange` with `field == "permissionMode"` and `new_value == "plan"`, the dispatcher writes the plan-approved marker for the current session (same marker the `PostToolUse` arms for `EnterPlanMode`/`ExitPlanMode` write). ConfigChange is the authoritative signal since every permission-mode transition in 2.1.114 routes through config: Shift+Tab UI cycle, `--permission-mode plan` CLI flag, SDK `set_permission_mode` RPC, `EnterPlanMode` tool, agent YAML `permissionMode`. Previously sentinel only detected plan mode via two tool-specific PostToolUse arms, missing the UI and CLI paths. Defensive on payload shape: checks `field`/`permission_mode` and `new_value`/`value` naming variants.

- **`hygiene_reminders` worktree-staleness detection uses `git worktree list`**: the `[Worktree Cleanup] N stale worktree(s)` reminder previously flagged every directory under `.claude/worktrees/` as stale regardless of git registry. Produced a repeating cron warning that never cleared, and broke multi-session workflows — a parallel agent's live worktree was called "stale" by the sibling session. Now queries `git worktree list --porcelain` via a new `GitStatusPort::list_worktree_names()` method and only flags directories whose basename is NOT in git's registry. If the git query fails (empty Vec), the check is skipped entirely rather than false-flagging everything. New `list_worktree_names` helper in `sentinel-infrastructure::git`, matching port method with rationale docstring, and stub impls added to all 5 existing test-mock `GitStatusPort` implementations.

- **Standard project files via `sentinel init`**: generated `LICENSE` (MIT), `BUILDING.md` (build prereqs + path dependencies), `SECURITY.md` (vulnerability reporting policy), `.editorconfig` (UTF-8/LF/indent rules), `.gitattributes` (LF normalization + binary markers), and `rustfmt.toml` (workspace formatter config). Clears the doc-drift alert that was repeating on every session start.
- **`stop_failure` rate-limit auto-rotation**: on API `rate_limit` errors, immediately rotate the active Claude account and write a relaunch marker (`~/.claude/accounts/rate-limit-relaunch.json`) so the next session picks up cleanly instead of leaving the user stuck in a dead turn. Default cooldown 300 minutes.

### Changed

- **`pr_merge_gate`**: hard-block on `gh pr merge` / `gh pr close` softened to an "ask" permission decision. CLAUDE.md still requires explicit user confirmation, but approval in-conversation is now sufficient without a hook-level deny.
- **`tool_usage_gate` plan check**: falls back to a recent plan file when the `PLAN_MARKER` session-temp file is missing (happens for resumed sessions). Plan check now passes if `{cwd}/plans/*.md` contains a file modified within the last 7 days; marker still wins when present.
- **`tool_usage_gate` plan-file walk-up**: the fallback now walks upward from `cwd` toward the repo root checking every `plans/` dir, stopping at the first `.git` entry (file or directory, handling both normal repos and worktrees). Previously only checked `{cwd}/plans/`, which broke for sessions rooted in a worktree or subdirectory whose approved plan lived at the repo root. Capped at 10 levels; 3 new unit tests cover parent-dir discovery, `.git` boundary enforcement, and the worktree case.
- **`phase_validator`**: suppresses the "load `phases/claim.md`" warning for skills whose on-disk layout has no `phases/` directory (e.g. `todo-manager`). When phases exist, the warning now derives its first-file name from the workflow config rather than hardcoding `claim.md`.

### Added

- **`orchestration_nudge` hook (UserPromptSubmit)**: soft-nudge injector that detects three orchestration patterns in the user's prompt and suggests the right tool. (a) "in parallel" / "concurrently" / "N items" → recommend `TeamCreate` over serial Task() calls. (b) "find all" / "audit the codebase" / "where is X used" → recommend `Agent(subagent_type: "Explore")` to protect main context. (c) "implement / refactor / migrate end-to-end" → recommend invoking the matching `Skill()` explicitly. Skipped inside subagents to prevent recursive nudging. 7 unit tests cover each signal plus the subagent-skip and empty-prompt cases.
- **Severity emoji prefixes on gate/reminder messages**: hard blocks now lead with 🔴 (tool_usage_gate, db_ops_gate, doppler_auth0_gate), soft reminders lead with 🟡 (worktree_reminder, orchestration_nudge). Makes output scannable since ANSI color sequences don't render in Claude Code's markdown context injection.

### Changed

- **Phase-exempt tool lists renamed and cleaned up**: the `safe_tools` arrays in `sentinel-domain/workflow.rs` (`should_block`) and `sentinel-application/hooks/phase_gate.rs` are now called `phase_exempt_tools`, and their rationale comments explicitly describe what makes a tool exempt (no code execution, no subprocess spawn, no file writes — just discovery/metadata/plan-approval). Removed the non-existent `EnterPlanMode` entry from both lists (it never matched any real tool call, but it made the lists look authoritative about a tool that doesn't exist). Added `TodoWrite` (core Claude Code todo tool — metadata-only, was previously being gated) and `TaskStop` (agent-team metadata). Test renamed from `test_allow_safe_tools` / `test_allows_safe_tools` to `test_phase_exempt_tools_not_blocked` / `test_allows_phase_exempt_tools`, expanded to exercise all exempt names, and now includes a regression assertion that `EnterPlanMode` specifically does NOT land back in the exempt list.
- **`session_init` CLAUDE.md template no longer documents a fake tool**: the generator template (compiled into `sentinel-mcp`) referenced `EnterPlanMode` in three places — the slash-command table (`/plan` row), the "Built-in Plan Mode workflow" section, and the Planned-mode rules section. Every session regenerated `~/.claude/CLAUDE.md` from this template and propagated the fake name into the live global instructions. Template now describes the real entry mechanisms (Shift+Tab, `CLAUDE_CODE_PLAN_MODE_REQUIRED=1` env var, `Agent(mode: "plan")`) and only mentions `ExitPlanMode` for the approval step.

### Added

- **`pr_merge_gate` autopilot bypass (`SENTINEL_AUTOPILOT=1`)**: when the env var is set, `gh pr merge` / `gh pr close` no longer returns `HookOutput::ask()` (which Claude Code renders as a Yes/No dialog that halts autopilot loops). Instead, the hook injects an AUTOPILOT reminder via context and allows the command. The in-conversation confirmation rule from CLAUDE.md still applies — this only removes the harness-level dialog prompt. Three new tests cover the autopilot-on, autopilot-off-explicit, and autopilot-unset cases.

### Fixed

- **`tool_usage_gate` references fake tools in block messages**: the gate instructed Claude to use `EnterPlanMode`, `TaskCreate`, and `TaskUpdate` — but Claude Code 2.1.88's real `ToolInputSchemas` union (verified against `package/sdk-tools.d.ts` in the official npm tarball) contains only `ExitPlanMode`, `TodoWrite`, and the agent-team `Task*` family. Plan mode is entered via Shift+Tab, the `CLAUDE_CODE_PLAN_MODE_REQUIRED=1` env var, the `Agent` tool with `mode: "plan"`, or agent YAML `permissionMode: "plan"` — never by a tool named `EnterPlanMode`. Updated all three block messages to reference real mechanisms (Shift+Tab / env var / Agent mode / `ExitPlanMode` for approval; `TodoWrite` alongside `TaskCreate` for the harness-agnostic case). Updated `test_blocks_edit_without_plan_approval` to assert the new wording and explicitly reject the old fake-tool reference.
- **`worktree_reminder` now nudges `AskUserQuestion`**: the reminder that fires on `UserPromptSubmit` inside a git repo previously only mentioned `EnterWorktree`. Added a trailing instruction to use `AskUserQuestion` at decision points while working, since multi-step worktree work was routinely proceeding on assumptions.
- **`tool_usage_gate` task-creation & active-task markers recognize `TodoWrite`**: the PostToolUse dispatcher at `crates/sentinel-cli/src/hook_cmd.rs:395` only matched `TaskCreate`/`TaskUpdate` (agent-team harness tools). But core Claude Code sessions use `TodoWrite` (per `sdk-tools.d.ts` in the 2.1.88 tarball) — meaning a core session could call `TodoWrite` all day and the gate would block Edit/Write forever with "create a task". Now `TodoWrite` also triggers `mark_task_created`, and any `TodoWrite` payload containing a todo with `status: "in_progress"` also triggers `mark_task_active`. Covers both the harness and the core code paths.
- **`doc_drift` write race**: `resolve_drift_for_cwd` did a lock-free read->filter->rewrite of `~/.claude/sentinel/metrics/doc-drift.jsonl`, which could clobber concurrent appends from `write_drift_entries` in parallel sessions/threads. Both paths now take an exclusive advisory lock on a sidecar `doc-drift.jsonl.lock` (via `fs2::FileExt::lock_exclusive`) before touching the file. Previously-red 50-iteration concurrency test `test_concurrent_write_and_resolve_loses_entries` now passes.
- **Cross-session cooldown suppression in 5 hooks**: `cooldown_file()` in `context_monitor`, `commit_hygiene`, `doc_drift`, `verification_gate`, and `activity_tracker` returned a shared path `std::env::temp_dir().join("claude-<hook>-last")` with no session scoping, so Session A writing the stamp suppressed Session B running in parallel on the same machine. The path now embeds `CLAUDE_SESSION_ID` (fallback `SESSION_ID`, fallback `"default"`), giving each session its own cooldown window. Previously-red `test_cross_session_cooldown_suppression_bug` now passes.
- **`commit_hygiene` state equality gate**: the Stop-phase state payload at `commit-hygiene-{repo_hash}.json` was keyed per-repo but shared across sessions working in the same checkout, so Session A writing state suppressed Session B because the `state.cwd == cwd` gate treated the payload as its own. Added `session_id` to `CommitState`, populated via the same `CLAUDE_SESSION_ID` → `SESSION_ID` → `"default"` fallback, and tightened the gate to `state.session_id == session_id && state.cwd == cwd`. New unit test `test_state_gate_distinguishes_sessions` covers both cross-session and same-session cases.
- **`session_init` validator**: removed the `sentinel-settings.json missing` false-positive. All hook registrations live in `~/.claude/settings.json`; the separate file was never actually loaded, but the validator flagged it on every SessionStart. Validator block, watch-path entry, and the stale `CLAUDE.md` tree row all deleted.
- **`pre_commit_verification` tests**: `test_blocks_git_push_without_evidence` and `test_is_docs_only_not_commit` were non-deterministic because `is_docs_only_commit` shelled out to `git diff` against the ambient cwd. Extracted a `GitDiffRunner` trait with production (`RealGitDiff`) and test-stub impls so the tests are hermetic.

## [0.4.1] - 2026-04-16

### Fixed
- **Cross-project state leak in 4 two-phase hooks** — flat shared filenames caused last-writer-wins data loss between parallel sessions and projects
  - `verification_gate`: `unverified-claims.json` → `unverified-claims-{session_id}.json`
  - `commit_hygiene`: `commit-hygiene.json` → `commit-hygiene-{repo_hash}.json` (djb2 hash of repo root)
  - `context_monitor`: `context-zone.json` → `context-zone-{session_id}.json`
  - `activity_tracker`: `activity-summary.json` → `activity-summary-{session_id}.json`
  - `pre_compact`: updated both callsites (`read_activity_summary`, `read_context_percent`) to match new scoped filenames
  - `activity_tracker::check_elevated_context`: updated cross-hook read of context-zone to use scoped filename
  - `hygiene_reminders` (0.4.0): same fix applied — `hygiene-{repo_hash}.json` pattern

## [0.4.0] - 2026-04-16

### Added
- **`dep_check` hook**: detects outdated Rust dependencies via `cargo outdated` on `UserPromptSubmit`, injects advisory into context when stale crates are found
- **3-tier session automation system**: cron-based git hygiene, worktree cleanup, and task audit jobs auto-created on session start; sentinel hooks inject event-triggered monitors for PR/push/merge workflows
- **`tool_usage_gate` hook**: hard-blocks `Edit`/`Write` unless sequential-thinking was used, a task was created, a plan was approved, and a task is active — enforces Required Tool Usage rules from CLAUDE.md
- **6 enforcement hooks for CLAUDE.md rules**: `git_hygiene` upgraded to hard-block (was advisory), plus 5 additional rule-enforcement hooks wired at PreToolUse
- **`tool_usage_gate` + channel event emitter**: MCP push notifications via file-watch IPC channel; `mcp_health` failures pushed in real-time to active session
- Git + npx interceptors consolidated into sentinel workspace
  - DDD/Hexagonal: domain (28 git rules, 26 npx redirects), application (port traits + services), infrastructure (platform adapters)
  - `sentinel-git-interceptor` binary: blocks dangerous git commands, `--bypass` with native OS dialog
  - `sentinel-npx-interceptor` binary: redirects npx to local Rust CLIs, TOML config overrides
  - Replaces standalone `git-interceptor` and `npx-interceptor` repos
  - Smaller binaries: git 274KB (was 283KB), npx 244KB (was 269KB)
- Channel events for context threshold, build/test, and deploy notifications
  - `context_monitor` emits `context_threshold` when usage crosses 65%+
  - `build_notify` PostToolUse hook: emits `build_completed` and `deploy_completed`
  - Total channel event types: 6
- **Agent Teams guidance in CLAUDE.md template**: when to use teams vs subagents, TeamCreate workflow, optimal team sizing, sentinel hook enforcement
- **`HookOutput.system_message` field** in sentinel-domain
  - Serializes as `"systemMessage"` per Claude Code's JSON output schema
  - Merged via concatenation when multiple hooks produce system messages
- **`sentinel init` CLI subcommand** for standard project file generation
  - Generates 11 standard files: README.md, CLAUDE.md, CHANGELOG.md, LICENSE, BUILDING.md, SECURITY.md, .editorconfig, .gitattributes, .gitignore, rustfmt.toml, docs/
  - Parses `Cargo.toml` for project name, description, version, license, dependencies
  - Detects project type: MCP server (vulcan dep), CLI (clap dep), workspace, library
  - Tailored content: MCP servers get mcp-router registration docs, CLIs get install instructions
  - `--dry-run` for preview, `--force` to overwrite, `--all` for batch across `~/Documents/GitHub/`
  - Skips existing files by default; creates `docs/` subdirectory structure with `.gitkeep` files
  - 18 unit tests in `sentinel-application::project_init`
- **`session_init`**: auto-runs `project_init` on every SessionStart
  - Reports created files in startup context: `[Project Init] Auto-generated N standard file(s): ...`
  - Silent when all standard files are present; skips non-git directories
- **`sentinel steel-test` CLI subcommand** for standalone Steel browser test management
  - `sentinel steel-test record` — record a passing browser test for current session
  - `sentinel steel-test check` — check if valid browser test exists for current session
- **`native task features`**: checklist, metadata, and enrichment on `task_persist`/`task_rehydrate` — structured priority/phase/tags in `metadata`, checklist sub-items, `addBlockedBy`/`addBlocks` dependencies

### Changed
- **Skill router classifier**: switched from Anthropic provider (`ANTHROPIC_API_KEY`) to OpenRouter (`OPENROUTER_API_KEY`); bumped model to Claude Opus 4.7 (`anthropic/claude-opus-4-7`)
- **AI judge**: migrated from OpenAI direct to OpenRouter (`openai/gpt-5.4`); single `JudgeProvider` struct replacing the previous multi-model `codex` field
- **`skill_router` activation banner**: moved from `systemMessage` JSON field to `additionalContext` so the banner renders as a `<system-reminder>` visible to both the model and the user
- **rig-core**: upgraded 0.34 → 0.35
- **`break` CLI subcommand**: added `--session`, `--list`, `--json` flags for programmatic consumers
- **Channel events**: session-scoped to prevent cross-session broadcast
- **Plan system overhaul**: use Claude Code's built-in Plan Mode as authoritative
  - Removed custom `plan` skill from marketplace — `EnterPlanMode`/`ExitPlanMode` now authoritative
  - `CLAUDE_CODE_PLAN_MODE_REQUIRED=1` seeded into `~/.claude/settings.json` by install.js
  - `plan_organizer` hook: copies plan files to `~/.claude/plans/{project}/{slug}-v{N}.md` with auto-incrementing versions; extracts path from `ExitPlanMode` tool_result JSON (`data.filePath`)
  - `/plan` slash command now triggers built-in Plan Mode
- `skill_router`: activation banners injected via `additionalContext` (was `systemMessage`)
- `doc_drift`: expanded from 3 monitored files to 6 (+ BUILDING.md, LICENSE, SECURITY.md); adds "run sentinel init" batch advice when 3+ standard files are missing
- `skill_router`: added project-init routing rule with 5 patterns at priority 60
- `phase_gate`: fail-closed when `phases/` dir exists but file is missing; canonical path validation rejects `..` components, validates skill/file names, resolves symlinks
- `pre_push_steel_test`: scoped to repos matching project configs; added Worker verification support
- `wrangler_guard`: expanded with per-repo scoping and Cloudflare API verification
- Removed cloudflare deploy guard hook (replaced by wrangler_guard scoping)
- `mcp-router` tools whitelisted through doppler/auth0 gate

### Fixed
- **Cross-project task rehydration leak in `task_persist` hook**
  - `find_active_task_dir` fell back to globally most-recently-modified dir under `~/.claude/tasks/` when session_id dir was missing
  - Now strictly scoped to `~/.claude/tasks/{session_id}/`; returns `None` (safe no-op) when missing
  - Added regression tests: matching session_id wins over newer-mtime siblings, missing session returns `None`, empty session dir returns `None`
- `git_hygiene`: skip when editing files outside repo root (worktree paths)
- Channel events: session-scoped to prevent cross-session broadcast

## [0.3.0] - 2026-03-10

### Added
- **Qdrant memory system** — 6 hooks for persistent vector memory across sessions
  - `memory-inject`: searches Qdrant on every UserPromptSubmit; injects top-K relevant memories and session context as `<system-reminder>`; non-blocking with periodic re-index, temporal scoring, deduplication, feedback boost
  - `memory-extract`: indexes new memories from Stop hook; privacy tags; compact injection on PreCompact
  - `memory-feedback`: tracks memory usage, correction detection, increments `access_count` on use
  - `memory-verify`: stale memory indicators; prevents runtime nesting panic
  - `session-index`: indexes full session transcripts to Qdrant on PreCompact; filters trivial exchanges; tracks memory access
  - `VectorStorePort` + `FileSystemPort` hexagonal ports for all 6 hooks; wired through dispatcher
  - Improved sync reliability and session indexing quality; temporal scoring + deduplication
- **Persistent task system** — `task_persist` + `task_rehydrate` hooks
  - `task_persist`: writes active task state to `~/.claude/tasks/{session_id}/` on every Stop
  - `task_rehydrate`: restores tasks on SessionStart; instructs `TaskCreate` recreation
- **Account cascade hook** (`account_cascade`): unified MCP account switching — when Linear/Railway/etc. account changes, cascade to all 18 MCP servers in one operation
- **Glass break emergency workflow override**
  - `sentinel break` CLI subcommand with file-based kill switch
  - Native OS dialog for confirmation before breaking
  - Unbreakable file-based state that survives hook restarts
  - Regression tests for the three lockup scenarios
- **Autopilot / Planned mode switch system**
  - Two-mode operation: `🚀 Autopilot` (fast, autonomous) and `📋 Planned` (safe, methodical, plan-required)
  - Status indicator emoji prepended to every response
  - Welcome message shown on session init; switch via "autopilot" / "planned" commands
  - Mode-specific rules: Autopilot skips plan approval, Planned requires `ExitPlanMode` before any implementation
  - Hard-block database ops in prod regardless of mode; local db ops allowed in Autopilot
- **Security hardening**
  - Encrypted state store (ChaCha20-Poly1305) for sensitive hook state
  - Process attestation (opt-in) — verifies caller identity before executing privileged hooks
  - Audit log for all hook executions with timestamps and caller info
  - Anti-replay protection for hook inputs
  - Permission model with caller validation
  - Close MCP tool bypass; protect sentinel source repos from hook interference
  - Lock hardening: removed double file lock in `state_store::save()` that deadlocked on Windows
- **Hook event parity with Claude Code 2.1.88 / 2.1.90**
  - 10 new hook events from 2.1.88 audit: `SubagentStart`, `SubagentStop`, `TeammateIdle`, `TaskCreated`, `TaskCompleted`, `PermissionDenied`, `CwdChanged`, `PostCompact`, `PostToolUseFailure`, `Notification`
  - 9 new hook events + output fields: `defer` permission decision, `agent_id`/`agent_type` input fields, `initialUserMessage`, `watchPaths`, `PostToolUseFailure` logging
  - `file_path` typed field on `HookInput`; `PermissionDenied` on hookSpecificOutput allowlist
  - 6 new event handlers for 2.1.90 feature parity
  - `CLAUDE_ENV_FILE` support; complete `hookSpecificOutput` schema
  - `tools.listChanged` capability declared for mcp-router hot-reload
- **`worktree_reminder` hook**: injects reminder to clean up stale worktrees on UserPromptSubmit; CLAUDE.md template updated with worktree preference
- **`CLAUDE_PROJECT` context injection**: hook output includes active project name for routing
- **Configurable user name** via `~/.claude/sentinel/user.toml`
- **Linear account names** read from token store for dynamic CLAUDE.md generation
- **`sentinel scan --sync-counts`**: synchronize component counts across all marketplace text files
  - Replaces `scripts/sync-counts.js` — universal regex sweep + targeted file-specific replacements
  - `--dry-run` flag for preview mode
- **`sentinel scan --manifest`**: generate manifest.json with SHA-256 hashes for all syncable files
  - Replaces `scripts/generate-manifest.js`
- **`sentinel scan` CLI command**: full marketplace scanner ported from Node.js to Rust
  - `--counts-only`: output just component counts as JSON
  - `--validate`: output validation report with colored terminal output
- **`sentinel-application::scanner` module**: shared scanning logic
  - `parse_frontmatter()`, `extract_dependencies()`, `infer_category()`, `parse_hooks_toml()`
  - `scan_marketplace()` — full snapshot with 5 validation categories
  - 5 unit tests
- **Local API endpoints on `sentinel daemon`**
  - `GET /api/scan`, `GET /api/validation`, `GET /api/counts`, `POST /api/rescan`
  - `GET /api/logs` — JSONL log reader with category/search/limit/offset (9 log files)
  - `GET /api/store/browse/:owner/:repo`, `GET /api/store/preview/:owner/:repo/:skill`
  - `POST /api/store/install`, `DELETE /api/store/uninstall/:name`
  - `GET /api/sentinel/sessions`, `GET /api/sentinel/sessions/:id`, `GET /api/sentinel/config`, `GET /api/sentinel/stats`
- CLAUDE.md generator: "Rust Tooling Ecosystem" section with dynamic MCP/CLI repo counts
- `count_repos_with_suffix()` helper; `ComponentCounts` includes `mcp_repos` + `cli_repos`
- **Launcher staging system** (shadow binary hot-swap)
  - `sentinel stage` queues new `sentinel-engine` binary with integrity verification
  - Launcher auto-swaps `.staged` → `sentinel-engine` on next hook invocation, zero downtime
- **Hook supervision in Rust**: process-level supervision ensures hook subprocesses are killed on timeout; hook timeout wrapper prevents Claude Code session hangs
- **CLAUDE.md management MCP tools** (3 new tools in `sentinel-mcp`)
  - `regenerate_claude_md` — re-counts all components, writes fresh CLAUDE.md from template
  - `edit_claude_md_template` — find-and-replace on generator template source, then auto-regenerates
  - `restart_all_mcps` — touches all mcp-router watched binaries for mass restart
- **Skill classification via Cerebras/OpenAI** (later superseded by OpenRouter in 0.4.0): first AI-powered classifier replacing pure regex routing

### Changed
- **Hexagonal architecture refactor** (complete port coverage across all hooks)
  - `HookContext` struct: all hooks receive `ctx` with `.fs`, `.git`, `.vector_store`, `.process` ports
  - `FileSystemPort`, `VectorStorePort`, `GitStatusPort`, `ProcessPort` defined in domain; implemented in infrastructure
  - Port traits moved from application layer to domain layer (DDD purity)
  - All D1–D10 migration waves: every hook migrated from direct I/O to `ctx.fs` / `ctx.git` / `ctx.vector_store`
  - `sentinel-domain` now has zero IO dependencies; `thiserror` removed for DDD purity
  - Domain constants module: paths, cooldown durations, skill names extracted from all hooks
  - `SessionId` newtype introduced for type-safe session scoping
- All metrics isolated under `~/.claude/sentinel/metrics/` (was scattered across `~/.claude/`)
- Hook pipeline: all blocking calls eliminated from hot path (git pull, npm version check, synchronous IO)
- Tokio runtime nesting panics fixed across Stop hook, memory_verify, and all hooks
- Skill router: pure Opus routing — all regex patterns and Cerebras/OpenAI fallbacks removed; Opus 4.6 via Anthropic (later upgraded to 4.7 via OpenRouter in 0.4.0)
- `verification_gate`: skip for content-only repos and docs-only pushes
- Todo interceptor: aligned with `TaskCreate`/`TaskUpdate` tool names (Claude Code 2.1.88 schema)
- Extracted counting functions from `session_init.rs` into shared `scanner` module
- `scripts/sync-counts.js`: replaced 7 JS counting functions with `sentinel scan --counts-only`
- `install.js`: replaced counting functions with cached `sentinel scan --counts-only` call

### Fixed
- All pre-existing compiler warnings across workspace (7 unused imports/variables)
- `verification_gate` flaky test: race condition with parallel tests — now uses process-unique temp file via env var override
- Commit hygiene tests: stabilized with env var mutex
- Pre-commit verification gate: skip for docs-only commits; diagnostic logging for worktree paths
- Platform-conditional binary names (no hardcoded `.exe`)
- Workflow registration: stop registering from casual keyword matches

## [0.2.0] - 2026-03-07

### Added
- **`sentinel-mcp` crate**: standalone MCP server built with Vulcan SDK
  - 8 tools: `get_proof_chain`, `get_workflow_status`, `verify_chain`, `submit_phase_complete`, `get_session_stats`, `update_step`, `get_phase_steps`, `get_workflow_progress`
  - Replaces the hand-rolled JSON-RPC MCP in `sentinel-cli/src/mcp_cmd.rs`
  - Uses Vulcan `#[tool]` / `#[tool_router]` macros for zero-boilerplate tool definitions
  - Registered as Claude Code MCP server via `claude mcp add sentinel -- sentinel-mcp`
- **Real AI judge** powered by rig-core (Cerebras, OpenAI, Anthropic multi-model adversarial evaluation)
- `TeammateIdle` + `TaskCompleted` hook events for agent teams
- `plan_organizer` PostToolUse hook for ExitPlanMode
- Enhanced `verification_gate` with cooldown and evidence tracking
- `receiving-code-review` skill route in `skill_router`
- `activity_tracker`, `pre_compact`, `commit_message_validator` hooks
- Enhanced `pre_push_steel_test` to detect frontend file changes
- Dynamic Linear team keys from marketplace project configs
- Linear sync in `task_completed` hook

### Changed
- Switched rig-core to upstream v0.30
- Converted 5 hooks to two-phase detect→inject pattern
- Aligned 8 step definitions with SKILL.md phase structures
- Wired all 20 → 27 hooks into sentinel event dispatch

### Fixed
- Broadened linear skill routing to match bare "linear" keyword
- Hardened state store + skill router always reports status
- Aligned mcp_health error schema with error_reporter expectations

## [0.1.0] - 2026-02-01

### Added
- **Sentinel workspace**: 5-crate DDD/hexagonal architecture
  - `sentinel-domain`: pure business logic — proofs, workflows, evidence, hooks, routing (zero IO dependencies)
  - `sentinel-application`: use cases — hook engine, classifier, phase gate, 20 hook modules
  - `sentinel-infrastructure`: IO adapters — config, state store, git, MCP transport, AI classifier
  - `sentinel-cli` (`sentinel`): CLI with 7 subcommands + local REST API (axum)
  - `sentinel-mcp-cmd`: initial hand-rolled MCP server (later replaced by `sentinel-mcp` crate in 0.2.0)
- **TOML config system**: `config/hooks.toml` (hook-to-event mapping) + `config/workflows.toml` (skill workflow steps) + `config/steps/` (49 per-skill step configs)
- **Hook engine**: dispatches all hook events through `sentinel hook --event <Event>`
  - 20 initial hooks across 5 categories: blocking, observational, routing, session, workflow
  - `UserPromptSubmit`: `error_reporter`, `hygiene_override`, `todo_loader`
  - `Stop`: `execution_log`, `skill_telemetry`, `doc_cleanup`, `verification_gate`
  - `PreToolUse`/`PostToolUse`: 4 hooks
  - `SessionStart`: `session_init` with CLAUDE.md generation, sync validation, dynamic counts
- **Proof chain system**: `ProofChain` + `PhaseProof` cryptographic chaining; `submit_phase_complete` triggers AI judge evaluation; `verify_chain` checks hash consistency
- **Phase gate**: enforces skill workflow phases; blocks tools until correct phase loaded; fail-closed on missing phase files
- **Skill router**: initial AI classification (Cerebras + OpenAI fallback) replacing pure regex routing
- **Step tracking**: hierarchical phase + step progress; 120+ steps across 8 phases for Linear skill; `update_step` / `get_phase_steps` / `get_workflow_progress` MCP tools
- **Sentinel workflow definitions** for all 47 tracked skills
- **Local REST API** (`sentinel daemon --port 3001`)
  - `GET /api/hooks`, `GET /api/proofs`, `GET /api/workflows` endpoints
  - WebSocket support via axum
- **`session_init` hook**: generates `~/.claude/CLAUDE.md` on every SessionStart with dynamic component counts, Vulcan/mcp-router/shadow binary docs, project file templates, hook event reference
- **`skill_telemetry` hook**: records per-skill execution timing to `~/.claude/sentinel/telemetry/`
- **`doc_cleanup` hook**: removes stale doc entries from CLAUDE.md on Stop
- **`verification_gate` hook**: injects verification reminder after tool use; enforces evidence collection before phase completion
- **`commit_hygiene` hook**: checks for unpushed commits and uncommitted changes on Stop; injects reminder
